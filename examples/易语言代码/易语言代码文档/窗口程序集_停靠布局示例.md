.版本 2

.程序集 窗口程序集_窗口_停靠布局示例
.程序集变量 窗口句柄, 整数型
.程序集变量 顶部标签, 整数型
.程序集变量 底部标签, 整数型
.程序集变量 左侧列表, 整数型
.程序集变量 中间编辑框, 整数型

.子程序 _窗口_停靠布局示例_创建完毕

窗口句柄 ＝ 创建Emoji窗口 ("停靠布局示例 - 上下左中", 700, 500)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ "\icon.ico")

' 设置停靠布局，间距5像素
设置布局管理器 (窗口句柄, #LAYOUT_DOCK, 0, 0, 5)
设置布局内边距 (窗口句柄, 5, 5, 5, 5)

' 创建顶部标题栏（停靠到顶部）
顶部标签 ＝ 创建标签_辅助 (窗口句柄, 0, 0, 690, 40, "停靠布局演示 - 拖动窗口改变大小观察效果", #COLOR_BG_WHITE, #COLOR_PRIMARY)

' 创建底部状态栏（停靠到底部）
底部标签 ＝ 创建标签_辅助 (窗口句柄, 0, 0, 690, 30, "状态栏：就绪", #COLOR_TEXT_REGULAR, #COLOR_BG_LIGHT)

' 创建左侧列表框（停靠到左侧）
左侧列表 ＝ 创建列表框_辅助 (窗口句柄, 0, 0, 180, 400, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 假)
添加列表项_辅助 (左侧列表, "菜单项 1")
添加列表项_辅助 (左侧列表, "菜单项 2")
添加列表项_辅助 (左侧列表, "菜单项 3")
添加列表项_辅助 (左侧列表, "菜单项 4")
添加列表项_辅助 (左侧列表, "菜单项 5")

' 创建中间编辑框（填充剩余空间）
中间编辑框 ＝ 创建编辑框_辅助 (窗口句柄, 0, 0, 400, 300, "这是中间内容区域，会自动填充剩余空间。" ＋ #换行符 ＋ "拖动窗口边缘改变大小，各区域会自动调整。", #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, , , , , , , 真, 假, 假, 真, 假)

' 添加控件到布局（注意：停靠布局中添加顺序很重要！）
' 先添加的控件先占据空间
添加控件到布局 (窗口句柄, 顶部标签)
添加控件到布局 (窗口句柄, 底部标签)
添加控件到布局 (窗口句柄, 左侧列表)
添加控件到布局 (窗口句柄, 中间编辑框)

' 设置停靠属性
设置控件布局属性 (顶部标签, 0, 0, 0, 0, #DOCK_TOP, 假, 假)
设置控件布局属性 (底部标签, 0, 0, 0, 0, #DOCK_BOTTOM, 假, 假)
设置控件布局属性 (左侧列表, 0, 0, 0, 0, #DOCK_LEFT, 假, 假)
设置控件布局属性 (中间编辑框, 0, 0, 0, 0, #DOCK_FILL, 假, 假)

' 立即应用布局
更新布局 (窗口句柄)

运行消息循环 ()


.子程序 _启动窗口_将被销毁

销毁窗口 (窗口句柄)
