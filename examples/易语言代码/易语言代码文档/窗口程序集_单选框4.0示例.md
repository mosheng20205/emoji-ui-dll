.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 窗口句柄, 整数型
.程序集变量 单选框_默认, 整数型
.程序集变量 单选框_边框, 整数型
.程序集变量 单选框_按钮, 整数型
.程序集变量 单选框_禁用演示, 整数型
.程序集变量 单选框_动态演示, 整数型
.程序集变量 标签_说明, 整数型
.程序集变量 标签_读取结果, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 按钮_读取全部, 整数型
.程序集变量 按钮_读取字体, 整数型
.程序集变量 按钮_切换字体, 整数型
.程序集变量 按钮_切换文本, 整数型
.程序集变量 按钮_选中默认, 整数型
.程序集变量 按钮_选中边框, 整数型
.程序集变量 按钮_选中按钮, 整数型
.程序集变量 按钮_动态默认, 整数型
.程序集变量 按钮_动态边框, 整数型
.程序集变量 按钮_动态按钮, 整数型
.程序集变量 按钮_切换启用, 整数型
.程序集变量 按钮_切换显示, 整数型
.程序集变量 按钮_移动位置, 整数型
.程序集变量 按钮_切换配色, 整数型
.程序集变量 按钮_恢复默认, 整数型
.程序集变量 动态文本已切换, 逻辑型
.程序集变量 动态字体为雅黑, 逻辑型
.程序集变量 当前为暖色方案, 逻辑型
.程序集变量 动态初始X, 整数型
.程序集变量 动态初始Y, 整数型
.程序集变量 动态初始宽度, 整数型
.程序集变量 动态初始高度, 整数型

.子程序 _启动子程序, 整数型
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 图标字节集, 字节集

窗口句柄 ＝ 创建Emoji窗口 (“单选框4.0完整示例”, 100, 100, 1460, 860)
图标字节集 ＝ #图片_icon
设置窗口图标_字节集 (窗口句柄, 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))
字体字节集 ＝ 编码_Ansi到Utf8 (“Microsoft YaHei UI”)

文本字节集 ＝ 编码_Ansi到Utf8 (“本页只演示 RadioButton。左侧展示默认 / Border / Button 三种样式，右侧展示可动态修改的属性与读取结果。”)
标签_说明 ＝ 创建标签 (窗口句柄, 24, 20, 1380, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 假)

emoji字节集 ＝ { 240, 159, 133, 176, 239, 184, 143 }
文本字节集 ＝ 组合前缀文本_辅助 (emoji字节集, “默认样式”)
单选框_默认 ＝ 创建单选按钮 (窗口句柄, 56, 92, 220, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 101, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 假, 假, 假)
emoji字节集 ＝ { 240, 159, 133, 177, 239, 184, 143 }
文本字节集 ＝ 组合前缀文本_辅助 (emoji字节集, “Border 样式”)
单选框_边框 ＝ 创建单选按钮 (窗口句柄, 56, 142, 220, 38, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 101, 假, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 假, 假, 假)
emoji字节集 ＝ { 240, 159, 133, 178, 239, 184, 143 }
文本字节集 ＝ 组合前缀文本_辅助 (emoji字节集, “Button 样式”)
单选框_按钮 ＝ 创建单选按钮 (窗口句柄, 56, 194, 220, 40, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 101, 假, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 假, 假, 假)

emoji字节集 ＝ { 240, 159, 148, 146 }
文本字节集 ＝ 组合前缀文本_辅助 (emoji字节集, “禁用演示”)
单选框_禁用演示 ＝ 创建单选按钮 (窗口句柄, 360, 92, 240, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 202, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 假, 假, 假)
emoji字节集 ＝ { 240, 159, 167, 170 }
文本字节集 ＝ 组合前缀文本_辅助 (emoji字节集, “动态样式 / 文本 / 颜色”)
单选框_动态演示 ＝ 创建单选按钮 (窗口句柄, 360, 144, 280, 40, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 202, 假, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 假, 假, 假)

设置单选按钮样式 (单选框_边框, 1)
设置单选按钮样式 (单选框_按钮, 2)
设置单选按钮样式 (单选框_动态演示, 1)
设置单选按钮圆点颜色 (单选框_默认, 到ARGB (255, 64, 158, 255))
设置单选按钮圆点颜色 (单选框_边框, 到ARGB (255, 230, 162, 60))
设置单选按钮圆点颜色 (单选框_按钮, 到ARGB (255, 103, 194, 58))
设置单选按钮圆点颜色 (单选框_禁用演示, 到ARGB (255, 144, 147, 153))
设置单选按钮圆点颜色 (单选框_动态演示, 到ARGB (255, 142, 68, 173))
启用单选按钮 (单选框_禁用演示, 假)

设置单选按钮回调 (单选框_默认, &单选按钮回调)
设置单选按钮回调 (单选框_边框, &单选按钮回调)
设置单选按钮回调 (单选框_按钮, &单选按钮回调)
设置单选按钮回调 (单选框_禁用演示, &单选按钮回调)
设置单选按钮回调 (单选框_动态演示, &单选按钮回调)

.如果真 (获取单选按钮位置 (单选框_动态演示, 动态初始X, 动态初始Y, 动态初始宽度, 动态初始高度) ≠ 0)
    动态初始X ＝ 360
    动态初始Y ＝ 174
    动态初始宽度 ＝ 280
    动态初始高度 ＝ 40

.如果真结束

文本字节集 ＝ 编码_Ansi到Utf8 (“读取结果会显示在这里。建议先点击'读取全部'，再用右侧按钮逐项测试属性变化。”)
标签_读取结果 ＝ 创建标签 (窗口句柄, 24, 276, 1388, 400, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“RadioButton 页状态将在这里更新。”)
标签_状态 ＝ 创建标签 (窗口句柄, 24, 700, 1388, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 假)

emoji字节集 ＝ { 240, 159, 147, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“读取全部”)
按钮_读取全部 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 720, 92, 132, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 240, 159, 148, 164 }
文本字节集 ＝ 编码_Ansi到Utf8 (“读取字体”)
按钮_读取字体 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 868, 92, 132, 38, 到ARGB (255, 144, 147, 153))

emoji字节集 ＝ { 240, 159, 150, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换字体”)
按钮_切换字体 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1016, 92, 132, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 226, 156, 143, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换文本”)
按钮_切换文本 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1164, 92, 132, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 226, 145, 160 }
文本字节集 ＝ 编码_Ansi到Utf8 (“选中默认”)
按钮_选中默认 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 720, 144, 132, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 226, 145, 161 }
文本字节集 ＝ 编码_Ansi到Utf8 (“选中边框”)
按钮_选中边框 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 868, 144, 132, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 226, 145, 162 }
文本字节集 ＝ 编码_Ansi到Utf8 (“选中按钮”)
按钮_选中按钮 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1016, 144, 132, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 65 }
文本字节集 ＝ 编码_Ansi到Utf8 (“动态默认”)
按钮_动态默认 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1164, 144, 132, 38, 到ARGB (255, 144, 147, 153))

emoji字节集 ＝ { 66 }
文本字节集 ＝ 编码_Ansi到Utf8 (“动态边框”)
按钮_动态边框 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 720, 196, 132, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 67 }
文本字节集 ＝ 编码_Ansi到Utf8 (“动态按钮”)
按钮_动态按钮 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 868, 196, 132, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 240, 159, 148, 146 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换启用”)
按钮_切换启用 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1016, 196, 132, 38, 到ARGB (255, 245, 108, 108))

emoji字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换显示”)
按钮_切换显示 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1164, 196, 132, 38, 到ARGB (255, 144, 147, 153))

emoji字节集 ＝ { 240, 159, 147, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“移动位置”)
按钮_移动位置 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 720, 248, 132, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 240, 159, 142, 168 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换配色”)
按钮_切换配色 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 868, 248, 132, 38, 到ARGB (255, 245, 108, 108))

emoji字节集 ＝ { 240, 159, 148, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“恢复默认”)
按钮_恢复默认 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1016, 248, 280, 38, 到ARGB (255, 103, 194, 58))

动态文本已切换 ＝ 假
动态字体为雅黑 ＝ 真
当前为暖色方案 ＝ 假

设置按钮点击回调 (&按钮点击回调)
刷新单选框信息 (“RadioButton 页已加载，可直接测试全部样式与全部属性读写。”)
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 单选按钮回调, , , stdcall
.参数 句柄, 整数型
.参数 分组ID, 整数型
.参数 选中, 逻辑型
.局部变量 提示文本, 文本型

提示文本 ＝ “Radio 回调：组=” ＋ 到文本 (分组ID) ＋ “ 句柄=” ＋ 到文本 (句柄) ＋ “ 选中=” ＋ 到文本 (选中)
刷新单选框信息 (提示文本)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 当前样式, 整数型
.局部变量 当前启用, 整数型
.局部变量 当前可视, 整数型
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型
.局部变量 emoji字节集, 字节集

.判断开始 (按钮ID ＝ 按钮_读取全部)
    刷新单选框信息 (“已读取全部 RadioButton 属性。”)

.判断 (按钮ID ＝ 按钮_读取字体)
    刷新单选框信息 (“动态单选框字体：” ＋ 获取单选框字体说明_辅助 (单选框_动态演示))

.判断 (按钮ID ＝ 按钮_切换字体)
    动态字体为雅黑 ＝ 取反 (动态字体为雅黑)
    .如果 (动态字体为雅黑)
        文本字节集 ＝ 编码_Ansi到Utf8 (“Microsoft YaHei UI”)
        设置单选按钮字体 (单选框_动态演示, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 13, 0, 0, 0)
        刷新单选框信息 (“动态单选框字体已切回 Microsoft YaHei UI，13px，常规。”)
    .否则
        文本字节集 ＝ 编码_Ansi到Utf8 (“SimSun”)
        设置单选按钮字体 (单选框_动态演示, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 16, 1, 0, 1)
        刷新单选框信息 (“动态单选框字体已切到 SimSun，16px，粗体+下划线。”)
    .如果结束

.判断 (按钮ID ＝ 按钮_切换文本)
    动态文本已切换 ＝ 取反 (动态文本已切换)
    emoji字节集 ＝ { 240, 159, 167, 170 }
    .如果 (动态文本已切换)
        文本字节集 ＝ 组合前缀文本_辅助 (emoji字节集, “动态样式 / 文本 已切换”)
    .否则
        文本字节集 ＝ 组合前缀文本_辅助 (emoji字节集, “动态样式 / 文本 / 颜色”)
    .如果结束
    设置单选按钮文本 (单选框_动态演示, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    刷新单选框信息 (“动态单选框文本已更新。”)

.判断 (按钮ID ＝ 按钮_选中默认)
    设置单选按钮状态 (单选框_默认, 真)
    刷新单选框信息 (“已选中默认样式单选框。”)

.判断 (按钮ID ＝ 按钮_选中边框)
    设置单选按钮状态 (单选框_边框, 真)
    刷新单选框信息 (“已选中 Border 样式单选框。”)

.判断 (按钮ID ＝ 按钮_选中按钮)
    设置单选按钮状态 (单选框_按钮, 真)
    刷新单选框信息 (“已选中 Button 样式单选框。”)

.判断 (按钮ID ＝ 按钮_动态默认)
    设置单选按钮样式 (单选框_动态演示, 0)
    刷新单选框信息 (“动态单选框已切到默认样式。”)

.判断 (按钮ID ＝ 按钮_动态边框)
    设置单选按钮样式 (单选框_动态演示, 1)
    刷新单选框信息 (“动态单选框已切到 Border 样式。”)

.判断 (按钮ID ＝ 按钮_动态按钮)
    设置单选按钮样式 (单选框_动态演示, 2)
    刷新单选框信息 (“动态单选框已切到 Button 样式。”)

.判断 (按钮ID ＝ 按钮_切换启用)
    当前启用 ＝ 获取单选按钮启用状态 (单选框_禁用演示)
    启用单选按钮 (单选框_禁用演示, 当前启用 ＝ 0)
    .如果 (当前启用 ＝ 0)
        刷新单选框信息 (“禁用演示单选框已重新启用。”)
    .否则
        刷新单选框信息 (“禁用演示单选框已被禁用。”)
    .如果结束

.判断 (按钮ID ＝ 按钮_切换显示)
    当前可视 ＝ 获取单选按钮可视状态 (单选框_动态演示)
    显示单选按钮 (单选框_动态演示, 当前可视 ＝ 0)
    .如果 (当前可视 ＝ 0)
        刷新单选框信息 (“动态单选框已显示。”)
    .否则
        刷新单选框信息 (“动态单选框已隐藏。”)
    .如果结束

.判断 (按钮ID ＝ 按钮_移动位置)
    .如果 (获取单选按钮位置 (单选框_动态演示, X, Y, 宽度, 高度) ＝ 0)
        X ＝ X ＋ 48
        .如果 (X ＞ 520)
            X ＝ 360
        .否则

        .如果结束
        设置单选按钮位置 (单选框_动态演示, X, Y, 宽度, 高度)
        刷新单选框信息 (“动态单选框位置已更新，新的 X=” ＋ 到文本 (X))
    .否则
        刷新单选框信息 (“读取动态单选框位置失败。”)
    .如果结束

.判断 (按钮ID ＝ 按钮_切换配色)
    当前为暖色方案 ＝ 取反 (当前为暖色方案)
    .如果 (当前为暖色方案)
        设置单选按钮颜色 (单选框_默认, 到ARGB (255, 97, 64, 37), 到ARGB (255, 255, 251, 245))
        设置单选按钮颜色 (单选框_边框, 到ARGB (255, 97, 64, 37), 到ARGB (255, 255, 251, 245))
        设置单选按钮颜色 (单选框_按钮, 到ARGB (255, 97, 64, 37), 到ARGB (255, 255, 243, 224))
        设置单选按钮颜色 (单选框_禁用演示, 到ARGB (255, 97, 64, 37), 到ARGB (255, 255, 251, 245))
        设置单选按钮颜色 (单选框_动态演示, 到ARGB (255, 97, 64, 37), 到ARGB (255, 255, 251, 245))
        设置单选按钮圆点颜色 (单选框_默认, 到ARGB (255, 230, 162, 60))
        设置单选按钮圆点颜色 (单选框_边框, 到ARGB (255, 245, 108, 108))
        设置单选按钮圆点颜色 (单选框_按钮, 到ARGB (255, 230, 162, 60))
        设置单选按钮圆点颜色 (单选框_禁用演示, 到ARGB (255, 144, 147, 153))
        设置单选按钮圆点颜色 (单选框_动态演示, 到ARGB (255, 245, 108, 108))
        刷新单选框信息 (“已切换到暖色配色方案。”)
    .否则
        设置单选按钮颜色 (单选框_默认, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
        设置单选按钮颜色 (单选框_边框, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
        设置单选按钮颜色 (单选框_按钮, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
        设置单选按钮颜色 (单选框_禁用演示, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
        设置单选按钮颜色 (单选框_动态演示, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
        设置单选按钮圆点颜色 (单选框_默认, 到ARGB (255, 64, 158, 255))
        设置单选按钮圆点颜色 (单选框_边框, 到ARGB (255, 230, 162, 60))
        设置单选按钮圆点颜色 (单选框_按钮, 到ARGB (255, 103, 194, 58))
        设置单选按钮圆点颜色 (单选框_禁用演示, 到ARGB (255, 144, 147, 153))
        设置单选按钮圆点颜色 (单选框_动态演示, 到ARGB (255, 142, 68, 173))
        刷新单选框信息 (“已切回默认蓝绿配色方案。”)
    .如果结束

.判断 (按钮ID ＝ 按钮_恢复默认)
    emoji字节集 ＝ { 240, 159, 167, 170 }
    文本字节集 ＝ 组合前缀文本_辅助 (emoji字节集, “动态样式 / 文本 / 颜色”)
    设置单选按钮文本 (单选框_动态演示, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    文本字节集 ＝ 编码_Ansi到Utf8 (“Microsoft YaHei UI”)
    设置单选按钮字体 (单选框_动态演示, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 13, 0, 0, 0)
    设置单选按钮状态 (单选框_默认, 真)
    设置单选按钮状态 (单选框_禁用演示, 真)
    设置单选按钮样式 (单选框_默认, 0)
    设置单选按钮样式 (单选框_边框, 1)
    设置单选按钮样式 (单选框_按钮, 2)
    设置单选按钮样式 (单选框_动态演示, 1)
    设置单选按钮颜色 (单选框_默认, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
    设置单选按钮颜色 (单选框_边框, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
    设置单选按钮颜色 (单选框_按钮, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
    设置单选按钮颜色 (单选框_禁用演示, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
    设置单选按钮颜色 (单选框_动态演示, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
    设置单选按钮圆点颜色 (单选框_默认, 到ARGB (255, 64, 158, 255))
    设置单选按钮圆点颜色 (单选框_边框, 到ARGB (255, 230, 162, 60))
    设置单选按钮圆点颜色 (单选框_按钮, 到ARGB (255, 103, 194, 58))
    设置单选按钮圆点颜色 (单选框_禁用演示, 到ARGB (255, 144, 147, 153))
    设置单选按钮圆点颜色 (单选框_动态演示, 到ARGB (255, 142, 68, 173))
    启用单选按钮 (单选框_禁用演示, 假)
    显示单选按钮 (单选框_动态演示, 真)
    设置单选按钮位置 (单选框_动态演示, 动态初始X, 动态初始Y, 动态初始宽度, 动态初始高度)
    动态文本已切换 ＝ 假
    动态字体为雅黑 ＝ 真
    当前为暖色方案 ＝ 假
    刷新单选框信息 (“RadioButton 示例已恢复默认状态。”)

.默认

.判断结束


.子程序 刷新单选框信息, , , 把所有单选框属性读取到状态标签
.参数 提示文本, 文本型
.局部变量 总字节集, 字节集

总字节集 ＝ 编码_Ansi到Utf8 (“操作提示：” ＋ 提示文本 ＋ #换行符 ＋ #换行符)
总字节集 ＝ 总字节集 ＋ 读取单选框摘要_字节集 (“默认”, 单选框_默认, 101) ＋ 编码_Ansi到Utf8 (#换行符 ＋ #换行符)
总字节集 ＝ 总字节集 ＋ 读取单选框摘要_字节集 (“边框”, 单选框_边框, 101) ＋ 编码_Ansi到Utf8 (#换行符 ＋ #换行符)
总字节集 ＝ 总字节集 ＋ 读取单选框摘要_字节集 (“按钮”, 单选框_按钮, 101) ＋ 编码_Ansi到Utf8 (#换行符 ＋ #换行符)
总字节集 ＝ 总字节集 ＋ 读取单选框摘要_字节集 (“禁用演示”, 单选框_禁用演示, 202) ＋ 编码_Ansi到Utf8 (#换行符 ＋ #换行符)
总字节集 ＝ 总字节集 ＋ 读取单选框摘要_字节集 (“动态演示”, 单选框_动态演示, 202)

写标签_字节集 (标签_读取结果, 总字节集)
写标签 (标签_状态, 提示文本)

.子程序 读取单选框摘要_字节集, 字节集
.参数 名称, 文本型
.参数 句柄, 整数型
.参数 分组ID, 整数型
.局部变量 前景色, 整数型
.局部变量 背景色, 整数型
.局部变量 圆点颜色, 整数型
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型
.局部变量 选中值, 逻辑型
.局部变量 样式值, 整数型
.局部变量 可视值, 整数型
.局部变量 启用值, 整数型
.局部变量 样式名, 文本型
.局部变量 结果字节集, 字节集

选中值 ＝ 获取单选按钮状态 (句柄)
样式值 ＝ 获取单选按钮样式 (句柄)
可视值 ＝ 获取单选按钮可视状态 (句柄)
启用值 ＝ 获取单选按钮启用状态 (句柄)
获取单选按钮颜色 (句柄, 前景色, 背景色)
获取单选按钮圆点颜色 (句柄, 圆点颜色)
获取单选按钮位置 (句柄, X, Y, 宽度, 高度)
样式名 ＝ 取单选框样式名 (样式值)

结果字节集 ＝ 编码_Ansi到Utf8 (“【” ＋ 名称 ＋ “】group=” ＋ 到文本 (分组ID) ＋ “ checked=” ＋ 到文本 (选中值) ＋ “ style=” ＋ 样式名)
结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “文本：”)
结果字节集 ＝ 结果字节集 ＋ 获取单选框文本字节集_辅助 (句柄)
结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “字体：” ＋ 获取单选框字体说明_辅助 (句柄))
结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “前景色=” ＋ 到文本 (前景色) ＋ “  背景色=” ＋ 到文本 (背景色) ＋ “  圆点色=” ＋ 到文本 (圆点颜色))
结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “位置：X=” ＋ 到文本 (X) ＋ “ Y=” ＋ 到文本 (Y) ＋ “ W=” ＋ 到文本 (宽度) ＋ “ H=” ＋ 到文本 (高度) ＋ “  可视=” ＋ 到文本 (可视值) ＋ “  启用=” ＋ 到文本 (启用值))
返回 (结果字节集)

.子程序 获取单选框文本字节集_辅助, 字节集
.参数 单选框句柄, 整数型
.局部变量 实际长度, 整数型
.局部变量 缓冲区, 字节集

实际长度 ＝ 获取单选按钮文本 (单选框句柄, 0, 0)
.如果 (实际长度 ＞ 0)
    缓冲区 ＝ 取空白字节集 (实际长度 ＋ 1)
    实际长度 ＝ 获取单选按钮文本 (单选框句柄, 取变量数据地址 (缓冲区), 实际长度 ＋ 1)
    返回 (取字节集左边 (缓冲区, 实际长度))
.否则

.如果结束
返回 ({  })

.子程序 获取单选框字体说明_辅助, 文本型
.参数 单选框句柄, 整数型
.局部变量 字体长度, 整数型
.局部变量 字体缓冲区, 字节集
.局部变量 字体大小, 整数型
.局部变量 粗体, 整数型
.局部变量 斜体, 整数型
.局部变量 下划线, 整数型
.局部变量 字体名, 文本型

字体长度 ＝ 获取单选按钮字体 (单选框句柄, 0, 0, 字体大小, 粗体, 斜体, 下划线)
.如果 (字体长度 ＞ 0)
    字体缓冲区 ＝ 取空白字节集 (字体长度)
    获取单选按钮字体 (单选框句柄, 取变量数据地址 (字体缓冲区), 字体长度, 字体大小, 粗体, 斜体, 下划线)
    字体名 ＝ 编码_Utf8到Ansi (取字节集左边 (字体缓冲区, 字体长度))
    返回 (字体名 ＋ “, ” ＋ 到文本 (字体大小) ＋ “px, 粗体=” ＋ 到文本 (粗体) ＋ “, 斜体=” ＋ 到文本 (斜体) ＋ “, 下划线=” ＋ 到文本 (下划线))
.否则

.如果结束
返回 (“读取失败”)

.子程序 取单选框样式名, 文本型
.参数 样式值, 整数型

.判断开始 (样式值 ＝ 0)
    返回 (“默认”)
.判断 (样式值 ＝ 1)
    返回 (“边框”)
.判断 (样式值 ＝ 2)
    返回 (“按钮”)
.默认
    返回 (“未知”)
.判断结束


.子程序 组合前缀文本_辅助, 字节集
.参数 前缀字节集, 字节集
.参数 文本, 文本型

返回 (前缀字节集 ＋ 编码_Ansi到Utf8 (“ ” ＋ 文本))

.子程序 写标签, , , 把 ANSI 文本写入 UTF-8 标签
.参数 标签句柄, 整数型
.参数 文本, 文本型
.局部变量 文本字节集, 字节集

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
设置标签文本 (标签句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.子程序 写标签_字节集, , , 直接写入 UTF-8 字节集标签
.参数 标签句柄, 整数型
.参数 文本字节集, 字节集

设置标签文本 (标签句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
