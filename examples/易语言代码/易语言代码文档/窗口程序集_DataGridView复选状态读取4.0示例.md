.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_DataGridView复选状态读取4_0示例

.程序集变量 窗口句柄, 整数型
.程序集变量 表格句柄, 整数型
.程序集变量 状态标签, 整数型
.程序集变量 按钮_读取状态, 整数型
.程序集变量 按钮_切换状态, 整数型

.子程序 _启动子程序, 整数型
.局部变量 行索引, 整数型
.局部变量 emoji字节集, 字节集

窗口句柄 ＝ 创建Emoji窗口 ("DataGridView复选状态读取4.0示例", 80, 40, 1080, 760)
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 20, 1020, 560, 假, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))

DG_添加复选框列 (表格句柄, "启用", 100)
DG_添加文本列 (表格句柄, "功能", 220)
DG_添加文本列 (表格句柄, "说明", 660)

行索引 ＝ 表格_添加行 (表格句柄)
表格_设置单元格复选状态 (表格句柄, 行索引, 0, 真)
DG_设置文本 (表格句柄, 行索引, 1, "消息提醒")
DG_设置文本 (表格句柄, 行索引, 2, "用于演示 表格_获取单元格复选状态。")

行索引 ＝ 表格_添加行 (表格句柄)
表格_设置单元格复选状态 (表格句柄, 行索引, 0, 假)
DG_设置文本 (表格句柄, 行索引, 1, "自动保存")
DG_设置文本 (表格句柄, 行索引, 2, "先选中某一行，再点击按钮读取或切换。")

表格_设置冻结首行 (表格句柄, 真)
表格_设置选择模式 (表格句柄, #DGSEL_ROW)

emoji字节集 ＝ { 240, 159, 147, 150 }  ' 📖
按钮_读取状态 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "读取选中行状态", 20, 600, 190, 44, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 148, 129 }  ' 🔁
按钮_切换状态 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "切换选中行状态", 226, 600, 190, 44, 到ARGB (255, 103, 194, 58))

状态标签 ＝ 创建标签_辅助 (窗口句柄, 20, 660, 1020, 56, "状态：请先选中一行。", 到ARGB (255, 48, 49, 51), 到ARGB (255, 244, 247, 250), "Microsoft YaHei UI", 11, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 行, 整数型
.局部变量 当前状态, 逻辑型

行 ＝ 表格_获取选中行 (表格句柄)
.如果 (行 ＝ -1)
    行 ＝ 0
    表格_设置选中单元格 (表格句柄, 0, 1)
.如果结束

.如果真 (按钮ID ＝ 按钮_读取状态)
    当前状态 ＝ 表格_获取单元格复选状态 (表格句柄, 行, 0)
    写状态 ("状态：第 " ＋ 到文本 (行 ＋ 1) ＋ " 行的复选状态=" ＋ 选择文本 (当前状态, "真", "假") ＋ "。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_切换状态)
    当前状态 ＝ 表格_获取单元格复选状态 (表格句柄, 行, 0)
    表格_设置单元格复选状态 (表格句柄, 行, 0, 取反 (当前状态))
    表格_刷新 (表格句柄)
    写状态 ("状态：第 " ＋ 到文本 (行 ＋ 1) ＋ " 行已切换为 " ＋ 选择文本 (取反 (当前状态), "真", "假") ＋ "。")
.如果真结束

.子程序 选择文本, 文本型
.参数 条件, 逻辑型
.参数 真文本, 文本型
.参数 假文本, 文本型
.如果 (条件)
    返回 (真文本)
.否则
    返回 (假文本)
.如果结束

.子程序 DG_添加复选框列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加复选框列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_添加文本列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加文本列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_设置文本
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置单元格文本 (表格, 行, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 写状态
.参数 文本, 文本型
设置标签文本_UTF8 (状态标签, 文本)
