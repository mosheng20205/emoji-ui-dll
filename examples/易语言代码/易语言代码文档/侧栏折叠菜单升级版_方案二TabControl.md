.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 树形框句柄, 整数型
.程序集变量 Tab_主, 整数型
.程序集变量 节点_商户中心, 整数型
.程序集变量 节点_订单管理, 整数型
.程序集变量 节点_订单_全部订单, 整数型
.程序集变量 节点_订单_售后单, 整数型
.程序集变量 节点_通道管理, 整数型
.程序集变量 节点_通道_账号管理, 整数型
.程序集变量 节点_通道_轮询池, 整数型
.程序集变量 节点_通道_支付设置, 整数型
.程序集变量 节点_财务中心, 整数型
.程序集变量 节点_财务_对账明细, 整数型
.程序集变量 节点_财务_结算记录, 整数型
.程序集变量 节点_API, 整数型
.程序集变量 节点_API_演示子项, 整数型
.程序集变量 节点_账号管理, 整数型
.程序集变量 节点_账号_个人中心, 整数型
.程序集变量 待切换树节点ID, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 文本, 字节集
.局部变量 图标, 字节集
.局部变量 字体名, 字节集
.局部变量 内容句柄, 整数型

文本 ＝ 编码_Ansi到Utf8 (“侧栏+Tab切换”)
主窗口句柄 ＝ 创建Emoji窗口_字节集 (取变量数据地址 (文本), 取字节集长度 (文本), -1, -1, 1500, 600)
.如果真 (主窗口句柄 ＝ 0)
    信息框 (“创建主窗口失败”, 0, , )
    返回 (0)
.如果真结束

树形框句柄 ＝ 创建树形框 (主窗口句柄, 10, 40, 320, 520, 4294967295, 4281348403, 0)
.如果真 (树形框句柄 ＝ 0)
    信息框 (“创建树形框失败”, 0, , )
    返回 (0)
.如果真结束

清空树形框 (树形框句柄)
调用子程序 (&_演示侧栏样式, , , )
设置树形框侧栏模式 (树形框句柄, 真)

' ---------- 树 ----------
文本 ＝ 编码_Ansi到Utf8 (“商户中心”)
图标 ＝ { 240, 159, 143, 162 }
节点_商户中心 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“订单管理”)
图标 ＝ { 240, 159, 147, 166 }
节点_订单管理 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“全部订单”)
节点_订单_全部订单 ＝ 添加子节点 (树形框句柄, 节点_订单管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“售后单”)
节点_订单_售后单 ＝ 添加子节点 (树形框句柄, 节点_订单管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“通道管理”)
图标 ＝ { 240, 159, 142, 175 }
节点_通道管理 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“账号管理”)
节点_通道_账号管理 ＝ 添加子节点 (树形框句柄, 节点_通道管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“轮询池”)
节点_通道_轮询池 ＝ 添加子节点 (树形框句柄, 节点_通道管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“支付设置”)
节点_通道_支付设置 ＝ 添加子节点 (树形框句柄, 节点_通道管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“财务中心”)
图标 ＝ { 240, 159, 146, 176 }
节点_财务中心 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“对账明细”)
节点_财务_对账明细 ＝ 添加子节点 (树形框句柄, 节点_财务中心, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“结算记录”)
节点_财务_结算记录 ＝ 添加子节点 (树形框句柄, 节点_财务中心, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“API”)
图标 ＝ { 240, 159, 146, 187 }
节点_API ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“演示子项”)
节点_API_演示子项 ＝ 添加子节点 (树形框句柄, 节点_API, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“账号管理”)
图标 ＝ { 240, 159, 145, 164 }
节点_账号管理 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“个人中心”)
节点_账号_个人中心 ＝ 添加子节点 (树形框句柄, 节点_账号管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

展开树节点 (树形框句柄, 节点_通道管理)
展开树节点 (树形框句柄, 节点_账号管理)
折叠树节点 (树形框句柄, 节点_订单管理)
折叠树节点 (树形框句柄, 节点_财务中心)
折叠树节点 (树形框句柄, 节点_API)

' ---------- Tab：340,40,540,500 ----------
Tab_主 ＝ 创建TabControl (主窗口句柄, 340, 40, 1140, 500)
文本 ＝ 编码_Ansi到Utf8 (“商户”)
添加Tab页 (Tab_主, 取变量数据地址 (文本), 取字节集长度 (文本), 0)
文本 ＝ 编码_Ansi到Utf8 (“订单”)
添加Tab页 (Tab_主, 取变量数据地址 (文本), 取字节集长度 (文本), 0)
文本 ＝ 编码_Ansi到Utf8 (“通道”)
添加Tab页 (Tab_主, 取变量数据地址 (文本), 取字节集长度 (文本), 0)
文本 ＝ 编码_Ansi到Utf8 (“财务”)
添加Tab页 (Tab_主, 取变量数据地址 (文本), 取字节集长度 (文本), 0)
文本 ＝ 编码_Ansi到Utf8 (“API”)
添加Tab页 (Tab_主, 取变量数据地址 (文本), 取字节集长度 (文本), 0)
文本 ＝ 编码_Ansi到Utf8 (“账号”)
添加Tab页 (Tab_主, 取变量数据地址 (文本), 取字节集长度 (文本), 0)

字体名 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
内容句柄 ＝ 获取Tab内容窗口 (Tab_主, 0)
文本 ＝ 编码_Ansi到Utf8 (“商户中心内容页：可放仪表盘、公告等。”)
创建标签 (内容句柄, 16, 16, 480, 80, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
内容句柄 ＝ 获取Tab内容窗口 (Tab_主, 1)
文本 ＝ 编码_Ansi到Utf8 (“订单管理内容页：列表与操作。”)
创建标签 (内容句柄, 16, 16, 480, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
内容句柄 ＝ 获取Tab内容窗口 (Tab_主, 2)
文本 ＝ 编码_Ansi到Utf8 (“通道管理内容页。”)
创建标签 (内容句柄, 16, 16, 480, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
内容句柄 ＝ 获取Tab内容窗口 (Tab_主, 3)
文本 ＝ 编码_Ansi到Utf8 (“财务中心内容页。”)
创建标签 (内容句柄, 16, 16, 480, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
内容句柄 ＝ 获取Tab内容窗口 (Tab_主, 4)
文本 ＝ 编码_Ansi到Utf8 (“API 文档与密钥管理。”)
创建标签 (内容句柄, 16, 16, 480, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
内容句柄 ＝ 获取Tab内容窗口 (Tab_主, 5)
文本 ＝ 编码_Ansi到Utf8 (“账号与个人设置内容页。”)
创建标签 (内容句柄, 16, 16, 480, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)

更新TabControl布局 (Tab_主)
设置选中树节点 (树形框句柄, 节点_API)
调用子程序 (&_按节点切换Tab, 节点_API, , )

设置树形框回调 (树形框句柄, 1, &回调_树选中)
设置树形框回调 (树形框句柄, 2, &回调_树展开)
设置树形框回调 (树形框句柄, 3, &回调_树折叠)

设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

' 若工程已有「到ARGB」子程序（如辅助程序集），请删除下面整段「.子程序 到ARGB …」避免重名。

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 _演示侧栏样式

设置树形框行高 (树形框句柄, 38)
设置树形框行间距 (树形框句柄, 2)
设置树形框文字色 (树形框句柄, 4281348403)
设置树形框选中背景色 (树形框句柄, 859870950)
设置树形框选中前景色 (树形框句柄, 4282423014)
设置树形框悬停背景色 (树形框句柄, 4294309882)

.子程序 _按节点切换Tab
.参数 节点ID, 整数型
.局部变量 idx, 整数型

' 这里只按“已保存的节点ID”切 Tab，不再查询父节点。
' 如果你的工程里还写着「取树节点父节点 (树形框句柄, 某变量)」，请删掉那段旧逻辑。
' 切换到Tab (Tab_主, idx)

.判断开始 (节点ID ＝ 节点_商户中心)
    idx ＝ 0
.判断 (节点ID ＝ 节点_订单管理)
    idx ＝ 1
.判断 (节点ID ＝ 节点_订单_全部订单)
    idx ＝ 1
.判断 (节点ID ＝ 节点_订单_售后单)
    idx ＝ 1
.判断 (节点ID ＝ 节点_通道管理)
    idx ＝ 2
.判断 (节点ID ＝ 节点_通道_账号管理)
    idx ＝ 2
.判断 (节点ID ＝ 节点_通道_轮询池)
    idx ＝ 2
.判断 (节点ID ＝ 节点_通道_支付设置)
    idx ＝ 2
.判断 (节点ID ＝ 节点_财务中心)
    idx ＝ 3
.判断 (节点ID ＝ 节点_财务_对账明细)
    idx ＝ 3
.判断 (节点ID ＝ 节点_财务_结算记录)
    idx ＝ 3
.判断 (节点ID ＝ 节点_API)
    idx ＝ 4
.判断 (节点ID ＝ 节点_API_演示子项)
    idx ＝ 4
.判断 (节点ID ＝ 节点_账号管理)
    idx ＝ 5
.判断 (节点ID ＝ 节点_账号_个人中心)
    idx ＝ 5
.默认
    idx ＝ 0
.判断结束
调试输出 (“[切Tab] idx=” ＋ 到文本 (idx))
切换到Tab (Tab_主, idx)


.子程序 _时钟_树选中延迟_周期事件

' 时钟_树选中延迟.禁止 ＝ 真
调用子程序 (&_按节点切换Tab, 待切换树节点ID, , )
调试输出 (“[选中] id=” ＋ 到文本 (待切换树节点ID))

.子程序 回调_树选中, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型

.判断开始 (节点ID ＝ 节点_商户中心)
    切换到Tab (Tab_主, 0)
.判断 (节点ID ＝ 节点_订单管理)
    切换到Tab (Tab_主, 1)
.判断 (节点ID ＝ 节点_订单_全部订单)
    切换到Tab (Tab_主, 1)
.判断 (节点ID ＝ 节点_订单_售后单)
    切换到Tab (Tab_主, 1)
.判断 (节点ID ＝ 节点_通道管理)
    切换到Tab (Tab_主, 2)
.判断 (节点ID ＝ 节点_通道_账号管理)
    切换到Tab (Tab_主, 2)
.判断 (节点ID ＝ 节点_通道_轮询池)
    切换到Tab (Tab_主, 2)
.判断 (节点ID ＝ 节点_通道_支付设置)
    切换到Tab (Tab_主, 2)
.判断 (节点ID ＝ 节点_财务中心)
    切换到Tab (Tab_主, 3)
.判断 (节点ID ＝ 节点_财务_对账明细)
    切换到Tab (Tab_主, 3)
.判断 (节点ID ＝ 节点_财务_结算记录)
    切换到Tab (Tab_主, 3)
.判断 (节点ID ＝ 节点_API)
    切换到Tab (Tab_主, 4)
.判断 (节点ID ＝ 节点_API_演示子项)
    切换到Tab (Tab_主, 4)
.判断 (节点ID ＝ 节点_账号管理)
    切换到Tab (Tab_主, 5)
.判断 (节点ID ＝ 节点_账号_个人中心)
    切换到Tab (Tab_主, 5)
.默认
    切换到Tab (Tab_主, 0)
.判断结束


.子程序 回调_树展开, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型



.子程序 回调_树折叠, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型


