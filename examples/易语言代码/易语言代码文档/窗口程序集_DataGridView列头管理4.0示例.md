.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_DataGridView列头管理4_0示例

.程序集变量 窗口句柄, 整数型
.程序集变量 表格句柄, 整数型
.程序集变量 状态标签, 整数型
.程序集变量 按钮_改第一列, 整数型
.程序集变量 按钮_恢复标题, 整数型
.程序集变量 按钮_读取标题, 整数型

.子程序 _启动子程序, 整数型
.局部变量 行索引, 整数型
.局部变量 emoji字节集, 字节集

窗口句柄 ＝ 创建Emoji窗口 ("DataGridView列头管理4.0示例", 80, 40, 1100, 760)
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 20, 1040, 560, 假, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))

DG_添加文本列 (表格句柄, "编号", 120)
DG_添加文本列 (表格句柄, "姓名", 180)
DG_添加文本列 (表格句柄, "城市", 180)
DG_添加文本列 (表格句柄, "说明", 500)

行索引 ＝ 表格_添加行 (表格句柄)
DG_设置文本 (表格句柄, 行索引, 0, "1001")
DG_设置文本 (表格句柄, 行索引, 1, "张三")
DG_设置文本 (表格句柄, 行索引, 2, "深圳")
DG_设置文本 (表格句柄, 行索引, 3, "使用 表格_设置列头文本 / 表格_获取列头文本。")

表格_设置冻结首行 (表格句柄, 真)

emoji字节集 ＝ { 226, 156, 143 }  ' ✏
按钮_改第一列 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "改第一列标题", 20, 600, 180, 44, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 148, 129 }  ' 🔁
按钮_恢复标题 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "恢复默认标题", 216, 600, 180, 44, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 240, 159, 147, 150 }  ' 📖
按钮_读取标题 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "读取所有标题", 412, 600, 180, 44, 到ARGB (255, 230, 162, 60))

状态标签 ＝ 创建标签_辅助 (窗口句柄, 20, 660, 1040, 56, "状态：准备读取和修改列头。", 到ARGB (255, 48, 49, 51), 到ARGB (255, 244, 247, 250), "Microsoft YaHei UI", 11, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型

.如果真 (按钮ID ＝ 按钮_改第一列)
    DG_设置列头 (表格句柄, 0, "工号")
    表格_刷新 (表格句柄)
    写状态 ("状态：已把第 1 列标题改为 “工号”。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_恢复标题)
    DG_设置列头 (表格句柄, 0, "编号")
    DG_设置列头 (表格句柄, 1, "姓名")
    DG_设置列头 (表格句柄, 2, "城市")
    DG_设置列头 (表格句柄, 3, "说明")
    表格_刷新 (表格句柄)
    写状态 ("状态：已恢复全部列头文本。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_读取标题)
    写状态 ("状态：标题列表 => " ＋ DG_取列头 (表格句柄, 0) ＋ " | " ＋ DG_取列头 (表格句柄, 1) ＋ " | " ＋ DG_取列头 (表格句柄, 2) ＋ " | " ＋ DG_取列头 (表格句柄, 3))
.如果真结束

.子程序 DG_添加文本列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加文本列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_设置文本
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置单元格文本 (表格, 行, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 DG_设置列头
.参数 表格, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置列头文本 (表格, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 DG_取列头, 文本型
.参数 表格, 整数型
.参数 列, 整数型
.局部变量 缓冲区, 字节集
.局部变量 长度, 整数型
长度 ＝ 表格_获取列头文本 (表格, 列, 0, 0)
.如果真 (长度 > 0)
    缓冲区 ＝ 取空白字节集 (长度 ＋ 1)
    长度 ＝ 表格_获取列头文本 (表格, 列, 取变量数据地址 (缓冲区), 长度 ＋ 1)
    返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 长度)))
.如果真结束
返回 ("")

.子程序 写状态
.参数 文本, 文本型
设置标签文本_UTF8 (状态标签, 文本)
