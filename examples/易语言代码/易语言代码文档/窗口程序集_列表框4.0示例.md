' 列表框完整示例 - 演示所有列表框命令
' 包含：创建、添加、删除、清空、选中、获取文本、修改文本、获取数量
'       启用/禁用、显示/隐藏、设置位置、回调、滚动条效果
.版本 2
.支持库 spec

.程序集 窗口程序集_列表框完整示例

.程序集变量 窗口句柄, 整数型
.程序集变量 列表框句柄, 整数型
.程序集变量 状态标签, 整数型
.程序集变量 计数器, 整数型
.程序集变量 按钮_添加项目, 整数型
.程序集变量 按钮_批量添加, 整数型
.程序集变量 按钮_删除选中, 整数型
.程序集变量 按钮_清空列表, 整数型
.程序集变量 按钮_获取数量, 整数型
.程序集变量 按钮_获取文本, 整数型
.程序集变量 按钮_修改文本, 整数型
.程序集变量 按钮_设置选中, 整数型
.程序集变量 按钮_禁用切换, 整数型
.程序集变量 按钮_显示切换, 整数型
.程序集变量 按钮_调整位置, 整数型
.程序集变量 列表框已禁用, 逻辑型
.程序集变量 列表框已隐藏, 逻辑型
.程序集变量 位置已调整, 逻辑型

.子程序 _窗口_列表框完整示例_创建完毕
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 标题字节集, 字节集
.局部变量 字体字节集, 字节集

' 创建窗口（使用字节集版本支持emoji标题）
' "📋 列表框完整示例"
标题字节集 ＝ { 240, 159, 147, 139, 32 } ＋ 编码_Ansi到Utf8 ("列表框4.0示例")
窗口句柄 ＝ 创建Emoji窗口_字节集 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 750, 600)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ "\icon.ico")

' ===== 创建列表框 =====
' 创建列表框 (父窗口, X, Y, 宽, 高, 多选模式, 前景色, 背景色)
列表框句柄 ＝ 创建列表框 (窗口句柄, 20, 20, 380, 400, 假, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE)

' ===== 添加初始项目（演示emoji和多语言） =====
' 使用完整版命令添加纯文本项目
文本字节集 ＝ 编码_Ansi到Utf8 ("第1项 - 普通文本")
添加列表框项 (列表框句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 ("第2项 - 中文测试")
添加列表框项 (列表框句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 ("第3项 - English Text")
添加列表框项 (列表框句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

' 使用字节集方式添加emoji项目
' "😀 笑脸表情"
添加列表项_字节集 (列表框句柄, { 240, 159, 152, 128, 32 } ＋ 编码_Ansi到Utf8 ("笑脸表情"))
' "🎯 靶心目标"
添加列表项_字节集 (列表框句柄, { 240, 159, 142, 175, 32 } ＋ 编码_Ansi到Utf8 ("靶心目标"))
' "📁 文件夹"
添加列表项_字节集 (列表框句柄, { 240, 159, 147, 129, 32 } ＋ 编码_Ansi到Utf8 ("文件夹"))
' "🇯🇵 東京 Tokyo"
添加列表项_字节集 (列表框句柄, { 240, 159, 135, 175, 240, 159, 135, 181, 32, 230, 157, 177, 228, 186, 172, 32, 84, 111, 107, 121, 111 })
' "🇰🇷 서울 Seoul"
添加列表项_字节集 (列表框句柄, { 240, 159, 135, 176, 240, 159, 135, 183, 32, 236, 132, 156, 236, 154, 184, 32, 83, 101, 111, 117, 108 })

计数器 ＝ 9

' ===== 设置列表框回调 =====
设置列表框回调 (列表框句柄, &列表框选中回调)

' ===== 创建操作按钮 =====
' 第一列按钮（基础操作）
emoji字节集 ＝ { 226, 158, 149 }  ' ➕
文本字节集 ＝ 编码_Ansi到Utf8 ("添加项目")
按钮_添加项目 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 420, 20, 145, 36, #COLOR_PRIMARY)

emoji字节集 ＝ { 240, 159, 147, 166 }  ' 📦 (批量)
文本字节集 ＝ 编码_Ansi到Utf8 ("批量添加30项")
按钮_批量添加 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 580, 20, 145, 36, #COLOR_PRIMARY)

emoji字节集 ＝ { 226, 157, 140 }  ' ❌
文本字节集 ＝ 编码_Ansi到Utf8 ("删除选中项")
按钮_删除选中 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 420, 66, 145, 36, #COLOR_DANGER)

emoji字节集 ＝ { 240, 159, 151, 145 }  ' 🗑
文本字节集 ＝ 编码_Ansi到Utf8 ("清空列表")
按钮_清空列表 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 580, 66, 145, 36, #COLOR_DANGER)

' 第二列按钮（查询操作）
emoji字节集 ＝ { 240, 159, 148, 138 }  ' 📊
文本字节集 ＝ 编码_Ansi到Utf8 ("获取数量")
按钮_获取数量 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 420, 120, 145, 36, #COLOR_SUCCESS)

emoji字节集 ＝ { 240, 159, 147, 132 }  ' 📄
文本字节集 ＝ 编码_Ansi到Utf8 ("获取选中文本")
按钮_获取文本 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 580, 120, 145, 36, #COLOR_SUCCESS)

' 第三列按钮（修改操作）
emoji字节集 ＝ { 226, 156, 143 }  ' ✏
文本字节集 ＝ 编码_Ansi到Utf8 ("修改选中文本")
按钮_修改文本 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 420, 174, 145, 36, #COLOR_WARNING)

emoji字节集 ＝ { 240, 159, 142, 175 }  ' 🎯
文本字节集 ＝ 编码_Ansi到Utf8 ("选中第3项")
按钮_设置选中 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 580, 174, 145, 36, #COLOR_WARNING)

' 第四列按钮（控件状态）
emoji字节集 ＝ { 240, 159, 148, 146 }  ' 🔒
文本字节集 ＝ 编码_Ansi到Utf8 ("禁用/启用")
按钮_禁用切换 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 420, 228, 145, 36, #COLOR_INFO)

emoji字节集 ＝ { 240, 159, 145, 129 }  ' 👁
文本字节集 ＝ 编码_Ansi到Utf8 ("显示/隐藏")
按钮_显示切换 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 580, 228, 145, 36, #COLOR_INFO)

emoji字节集 ＝ { 240, 159, 147, 144 }  ' 📐
文本字节集 ＝ 编码_Ansi到Utf8 ("调整位置")
按钮_调整位置 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 420, 282, 305, 36, #COLOR_INFO)

' ===== 创建状态标签 =====
字体字节集 ＝ 编码_Ansi到Utf8 ("Microsoft YaHei UI")
文本字节集 ＝ 编码_Ansi到Utf8 ("列表框完整示例 - 点击按钮测试各项功能，批量添加可触发滚动条")
状态标签 ＝ 创建标签 (窗口句柄, 20, 440, 700, 130, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_PRIMARY, #COLOR_BG_LIGHT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 假, 假, 假, 0, 真)

' 设置按钮回调
设置按钮点击回调 (&按钮点击回调)

列表框已禁用 ＝ 假
列表框已隐藏 ＝ 假
位置已调整 ＝ 假

运行消息循环 ()


.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 选中索引, 整数型
.局部变量 数量, 整数型
.局部变量 i, 整数型
.局部变量 缓冲区, 字节集
.局部变量 长度, 整数型
.局部变量 新文本字节集, 字节集
.局部变量 结果字节集, 字节集

' ===== 添加项目 =====
.如果真 (按钮ID ＝ 按钮_添加项目)
    计数器 ＝ 计数器 ＋ 1
    文本字节集 ＝ 编码_Ansi到Utf8 ("新增项目 #" ＋ 到文本 (计数器))
    添加列表框项 (列表框句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    更新状态 ("已添加: 新增项目 #" ＋ 到文本 (计数器))
.如果真结束

' ===== 批量添加30项（触发滚动条） =====
.如果真 (按钮ID ＝ 按钮_批量添加)
    .计次循环首 (30, i)
        计数器 ＝ 计数器 ＋ 1
        文本字节集 ＝ 编码_Ansi到Utf8 ("批量项目 #" ＋ 到文本 (计数器))
        添加列表框项 (列表框句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    .计次循环尾 ()
    数量 ＝ 获取列表项数量 (列表框句柄)
    更新状态 ("已批量添加30项，当前共 " ＋ 到文本 (数量) ＋ " 项，滚动条已自动显示")
.如果真结束

' ===== 删除选中项 =====
.如果真 (按钮ID ＝ 按钮_删除选中)
    选中索引 ＝ 获取选中项索引 (列表框句柄)
    .如果 (选中索引 ≠ -1)
        移除列表项 (列表框句柄, 选中索引)
        更新状态 ("已删除索引 " ＋ 到文本 (选中索引) ＋ " 的项目")
    .否则
        更新状态 ("请先选中一个项目再删除")
    .如果结束
.如果真结束

' ===== 清空列表 =====
.如果真 (按钮ID ＝ 按钮_清空列表)
    清空列表框 (列表框句柄)
    计数器 ＝ 0
    更新状态 ("列表已清空")
.如果真结束

' ===== 获取数量 =====
.如果真 (按钮ID ＝ 按钮_获取数量)
    数量 ＝ 获取列表项数量 (列表框句柄)
    更新状态 ("当前列表项数量: " ＋ 到文本 (数量))
.如果真结束

' ===== 获取选中项文本（两次调用模式） =====
.如果真 (按钮ID ＝ 按钮_获取文本)
    选中索引 ＝ 获取选中项索引 (列表框句柄)
    .如果 (选中索引 ≠ -1)
        ' 第一次调用：获取文本长度
        长度 ＝ 获取列表项文本 (列表框句柄, 选中索引, 0, 0)
        .如果真 (长度 ＞ 0)
            ' 第二次调用：获取实际文本
            缓冲区 ＝ 取空白字节集 (长度 ＋ 1)
            获取列表项文本 (列表框句柄, 选中索引, 取变量数据地址 (缓冲区), 长度 ＋ 1)
            ' 全程UTF-8字节集拼接，不用到文本()转换
            结果字节集 ＝ 编码_Ansi到Utf8 ("索引[") ＋ 编码_Ansi到Utf8 (到文本 (选中索引)) ＋ 编码_Ansi到Utf8 ("] 文本: ") ＋ 缓冲区 ＋ { 10 } ＋ 编码_Ansi到Utf8 ("字节长度: " ＋ 到文本 (长度))
            设置标签文本 (状态标签, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
        .如果真结束
    .否则
        更新状态 ("请先选中一个项目")
    .如果结束
.如果真结束

' ===== 修改选中项文本（SetListItemText） =====
.如果真 (按钮ID ＝ 按钮_修改文本)
    选中索引 ＝ 获取选中项索引 (列表框句柄)
    .如果 (选中索引 ≠ -1)
        ' 构建新文本（带emoji）
        ' "✅ 已修改-索引X"
        新文本字节集 ＝ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("已修改-索引" ＋ 到文本 (选中索引))
        设置列表项文本 (列表框句柄, 选中索引, 取变量数据地址 (新文本字节集), 取字节集长度 (新文本字节集))
        更新状态 ("已修改索引 " ＋ 到文本 (选中索引) ＋ " 的文本")
    .否则
        更新状态 ("请先选中一个项目再修改")
    .如果结束
.如果真结束

' ===== 设置选中项索引 =====
.如果真 (按钮ID ＝ 按钮_设置选中)
    数量 ＝ 获取列表项数量 (列表框句柄)
    .如果 (数量 ＞ 2)
        设置选中项索引 (列表框句柄, 2)
        更新状态 ("已设置选中第3项（索引2）")
    .否则
        更新状态 ("列表项不足3项，无法选中")
    .如果结束
.如果真结束

' ===== 禁用/启用切换 =====
.如果真 (按钮ID ＝ 按钮_禁用切换)
    列表框已禁用 ＝ 取反 (列表框已禁用)
    启用列表框 (列表框句柄, 取反 (列表框已禁用))
    .如果 (列表框已禁用)
        更新状态 ("列表框已禁用")
    .否则
        更新状态 ("列表框已启用")
    .如果结束
.如果真结束

' ===== 显示/隐藏切换 =====
.如果真 (按钮ID ＝ 按钮_显示切换)
    列表框已隐藏 ＝ 取反 (列表框已隐藏)
    显示列表框 (列表框句柄, 取反 (列表框已隐藏))
    .如果 (列表框已隐藏)
        更新状态 ("列表框已隐藏（再次点击恢复）")
    .否则
        更新状态 ("列表框已显示")
    .如果结束
.如果真结束

' ===== 调整位置 =====
.如果真 (按钮ID ＝ 按钮_调整位置)
    位置已调整 ＝ 取反 (位置已调整)
    .如果 (位置已调整)
        设置列表框位置 (列表框句柄, 50, 50, 350, 360)
        更新状态 ("列表框位置已调整为 (50,50,350,360)")
    .否则
        设置列表框位置 (列表框句柄, 20, 20, 380, 400)
        更新状态 ("列表框位置已恢复为 (20,20,380,400)")
    .如果结束
.如果真结束


.子程序 列表框选中回调, , , stdcall
.参数 列表框, 整数型
.参数 索引, 整数型
.局部变量 缓冲区, 字节集
.局部变量 长度, 整数型
.局部变量 结果字节集, 字节集

' 获取列表项文本（两次调用模式）
长度 ＝ 获取列表项文本 (列表框, 索引, 0, 0)
.如果真 (长度 ＞ 0)
    缓冲区 ＝ 取空白字节集 (长度 ＋ 1)
    获取列表项文本 (列表框, 索引, 取变量数据地址 (缓冲区), 长度 ＋ 1)
    ' 全程UTF-8字节集拼接显示
    ' "选中: " ＋ 缓冲区内容 ＋ 换行 ＋ "索引: X, 字节长度: Y"
    结果字节集 ＝ 编码_Ansi到Utf8 ("选中: ") ＋ 缓冲区 ＋ { 10 } ＋ 编码_Ansi到Utf8 ("索引: " ＋ 到文本 (索引) ＋ ", 字节长度: " ＋ 到文本 (长度))
    设置标签文本 (状态标签, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
.如果真结束


.子程序 更新状态
.参数 消息, 文本型
.局部变量 消息字节集, 字节集

消息字节集 ＝ 编码_Ansi到Utf8 (消息)
设置标签文本 (状态标签, 取变量数据地址 (消息字节集), 取字节集长度 (消息字节集))


.子程序 添加列表项_字节集, 整数型
.参数 列表框, 整数型
.参数 UTF8字节集, 字节集

返回 (添加列表项 (列表框, 取变量数据地址 (UTF8字节集), 取字节集长度 (UTF8字节集)))
