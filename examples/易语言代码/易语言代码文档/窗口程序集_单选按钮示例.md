.版本 2

.程序集 窗口程序集_窗口_单选按钮示例
.程序集变量 窗口句柄, 整数型
.程序集变量 单选按钮1, 整数型
.程序集变量 单选按钮2, 整数型
.程序集变量 单选按钮3, 整数型
.程序集变量 单选按钮4, 整数型
.程序集变量 单选按钮5, 整数型
.程序集变量 单选按钮6, 整数型
.程序集变量 标签1, 整数型

.子程序 _窗口_单选按钮示例_创建完毕

窗口句柄 ＝ 创建Emoji窗口 (“单选按钮示例 - Element UI风格”, 500, 400)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ “\icon.ico”)

' 创建第一组单选按钮（性别选择）
创建标签_辅助 (窗口句柄, 20, 20, 200, 30, “性别选择：”, #黑色, #白色, “Microsoft YaHei UI”, 12, 假, 假, 假, , 假)

单选按钮1 ＝ 创建单选按钮_辅助 (窗口句柄, 20, 50, 100, 30, “男”, 1, 真, #黑色, #白色)
单选按钮2 ＝ 创建单选按钮_辅助 (窗口句柄, 130, 50, 100, 30, “女”, 1, 假, #黑色, #白色)
单选按钮3 ＝ 创建单选按钮_辅助 (窗口句柄, 240, 50, 100, 30, “保密”, 1, 假, #黑色, #白色)

' 设置第一组回调
设置单选按钮回调 (单选按钮1, &单选按钮回调)
设置单选按钮回调 (单选按钮2, &单选按钮回调)
设置单选按钮回调 (单选按钮3, &单选按钮回调)

' 创建第二组单选按钮（会员等级）
创建标签_辅助 (窗口句柄, 20, 100, 200, 30, “会员等级：”, #黑色, #白色, , , , , , , )

单选按钮4 ＝ 创建单选按钮_辅助 (窗口句柄, 20, 130, 120, 30, “普通会员”, 2, 真, #黑色, #白色)
单选按钮5 ＝ 创建单选按钮_辅助 (窗口句柄, 150, 130, 120, 30, “VIP会员”, 2, 假, #黑色, #白色)
单选按钮6 ＝ 创建单选按钮_辅助 (窗口句柄, 280, 130, 120, 30, “SVIP会员”, 2, 假, #黑色, #白色)

' 设置第二组回调
设置单选按钮回调 (单选按钮4, &单选按钮回调)
设置单选按钮回调 (单选按钮5, &单选按钮回调)
设置单选按钮回调 (单选按钮6, &单选按钮回调)

' 创建状态显示标签
标签1 ＝ 创建标签_辅助 (窗口句柄, 20, 200, 460, 150, “当前选择：” ＋ #换行符 ＋ “性别：男” ＋ #换行符 ＋ “会员等级：普通会员”, #黑色, #COLOR_TEXT_PLACEHOLDER, , , , , , , 真)

运行消息循环 ()


.子程序 _启动窗口_将被销毁

销毁窗口 (窗口句柄)

.子程序 单选按钮回调, , , 单选按钮状态改变回调
.参数 句柄, 整数型
.参数 分组ID, 整数型
.参数 选中, 逻辑型
.局部变量 性别文本, 文本型
.局部变量 等级文本, 文本型

.如果真 (分组ID ＝ 1)
    ' 性别选择组
    .如果真 (句柄 ＝ 单选按钮1)
        性别文本 ＝ “男”
    .如果真结束
    .如果真 (句柄 ＝ 单选按钮2)
        性别文本 ＝ “女”
    .如果真结束
    .如果真 (句柄 ＝ 单选按钮3)
        性别文本 ＝ “保密”
    .如果真结束

    ' 获取当前会员等级
    .如果真 (获取单选按钮状态 (单选按钮4))
        等级文本 ＝ “普通会员”
    .如果真结束
    .如果真 (获取单选按钮状态 (单选按钮5))
        等级文本 ＝ “VIP会员”
    .如果真结束
    .如果真 (获取单选按钮状态 (单选按钮6))
        等级文本 ＝ “SVIP会员”
    .如果真结束

    设置标签文本_辅助 (标签1, “当前选择：” ＋ #换行符 ＋ “性别：” ＋ 性别文本 ＋ #换行符 ＋ “会员等级：” ＋ 等级文本)
.如果真结束

.如果真 (分组ID ＝ 2)
    ' 会员等级选择组
    .如果真 (句柄 ＝ 单选按钮4)
        等级文本 ＝ “普通会员”
    .如果真结束
    .如果真 (句柄 ＝ 单选按钮5)
        等级文本 ＝ “VIP会员”
    .如果真结束
    .如果真 (句柄 ＝ 单选按钮6)
        等级文本 ＝ “SVIP会员”
    .如果真结束

    ' 获取当前性别
    .如果真 (获取单选按钮状态 (单选按钮1))
        性别文本 ＝ “男”
    .如果真结束
    .如果真 (获取单选按钮状态 (单选按钮2))
        性别文本 ＝ “女”
    .如果真结束
    .如果真 (获取单选按钮状态 (单选按钮3))
        性别文本 ＝ “保密”
    .如果真结束

    设置标签文本_辅助 (标签1, “当前选择：” ＋ #换行符 ＋ “性别：” ＋ 性别文本 ＋ #换行符 ＋ “会员等级：” ＋ 等级文本)
.如果真结束

