.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_组合框示例
.程序集变量 主窗口句柄, 整数型
.程序集变量 组合框1, 整数型
.程序集变量 组合框2, 整数型
.程序集变量 标签_选中项, 整数型
.程序集变量 标签_编辑框文本, 整数型
.程序集变量 按钮_获取选中, 整数型
.程序集变量 按钮_设置选中, 整数型
.程序集变量 按钮_添加项, 整数型
.程序集变量 按钮_清空, 整数型
.程序集变量 编辑框_新项, 整数型

.子程序 _窗口_组合框示例_创建完毕
.局部变量 空字节集, 字节集
.局部变量 图标_对勾, 字节集
.局部变量 图标_垃圾桶, 字节集
.局部变量 图标_加号, 字节集

主窗口句柄 ＝ 创建Emoji窗口 (“组合框示例”, 800, 600)
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ “\icon.ico”)

' 准备按钮图标字节集（UTF-8编码）
图标_对勾 ＝ { 226, 156, 133 }  ' ?
图标_垃圾桶 ＝ { 240, 159, 151, 145, 239, 184, 143 }  ' ???
图标_加号 ＝ { 226, 158, 149 }  ' ?

' 创建标题标签（使用更明显的颜色 - 主题蓝色，字体加粗）
创建标签_辅助 (主窗口句柄, 20, 20, 760, 40, “组合框控件示例”, #COLOR_PRIMARY, #COLOR_TRANSPARENT, “Microsoft YaHei UI”, 18, 真, 假, 假, 1, 假)

' 创建可编辑组合框（使用更明显的标签颜色 - 主题蓝色，字体加粗）
创建标签_辅助 (主窗口句柄, 20, 80, 200, 30, “可编辑组合框：”, #COLOR_PRIMARY, #COLOR_TRANSPARENT, “Microsoft YaHei UI”, 14, 真, 假, 假, 0, 假)
组合框1 ＝ 创建组合框_辅助 (主窗口句柄, 20, 110, 300, 35, 假, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 40, “Microsoft YaHei UI”, 16, 假, 假, 假)
设置组合框回调 (组合框1, &组合框1_选中回调)

' 添加初始项目
添加组合框项_辅助 (组合框1, “北京”)
添加组合框项_辅助 (组合框1, “上海”)
添加组合框项_辅助 (组合框1, “广州”)
添加组合框项_辅助 (组合框1, “深圳”)
添加组合框项_辅助 (组合框1, “杭州”)

' 创建只读组合框（使用更明显的标签颜色 - 主题蓝色，字体加粗）
创建标签_辅助 (主窗口句柄, 20, 170, 200, 30, “只读组合框：”, #COLOR_PRIMARY, #COLOR_TRANSPARENT, “Microsoft YaHei UI”, 14, 真, 假, 假, 0, 假)
组合框2 ＝ 创建组合框_辅助 (主窗口句柄, 20, 200, 300, 35, 真, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 40, “Microsoft YaHei UI”, 16, 假, 假, 假)
设置组合框回调 (组合框2, &组合框2_选中回调)

' 添加初始项目
添加组合框项_辅助 (组合框2, “选项一”)
添加组合框项_辅助 (组合框2, “选项二”)
添加组合框项_辅助 (组合框2, “选项三”)
添加组合框项_辅助 (组合框2, “选项四”)
添加组合框项_辅助 (组合框2, “选项五”)

' 设置默认选中
设置组合框选中索引 (组合框2, 0)

' 创建状态显示标签
标签_选中项 ＝ 创建标签_辅助 (主窗口句柄, 20, 260, 760, 30, “当前选中：无”, #COLOR_TEXT_PRIMARY, #COLOR_BG_BASE, “”, 12, 假, 假, 假, 0, 假)
标签_编辑框文本 ＝ 创建标签_辅助 (主窗口句柄, 20, 300, 760, 30, “编辑框文本：”, #COLOR_TEXT_PRIMARY, #COLOR_BG_BASE, “”, 12, 假, 假, 假, 0, 假)

' 创建操作按钮（使用字节集传递Emoji图标）
按钮_获取选中 ＝ 创建Emoji按钮_字节集辅助 (主窗口句柄, 图标_对勾, “获取选中项”, 20, 360, 150, 40, #COLOR_PRIMARY)
按钮_设置选中 ＝ 创建Emoji按钮_字节集辅助 (主窗口句柄, 图标_对勾, “设置选中项2”, 180, 360, 150, 40, #COLOR_SUCCESS)
按钮_清空 ＝ 创建Emoji按钮_字节集辅助 (主窗口句柄, 图标_垃圾桶, “清空组合框”, 340, 360, 150, 40, #COLOR_DANGER)

' 创建添加项功能（使用更明显的标签颜色 - 主题蓝色，字体加粗）
创建标签_辅助 (主窗口句柄, 20, 420, 200, 30, “添加新项目（支持彩色Emoji）：”, #COLOR_PRIMARY, #COLOR_TRANSPARENT, “Microsoft YaHei UI”, 14, 真, 假, 假, 0, 假)
空字节集 ＝ {  }
' ? 使用彩色Emoji编辑框（RichEdit控件）替代标准编辑框
编辑框_新项 ＝ 创建D2D彩色Emoji编辑框_辅助 (主窗口句柄, 20, 450, 300, 35, “”, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, “Microsoft YaHei UI”, 14, 假, 假, 假, 0, 假, 假, 假, 真, 真)
按钮_添加项 ＝ 创建Emoji按钮_字节集辅助 (主窗口句柄, 图标_加号, “添加到组合框1”, 330, 450, 160, 35, #COLOR_WARNING)

' 设置按钮回调
设置按钮点击回调 (&按钮点击回调)

运行消息循环 ()

.子程序 组合框1_选中回调, , , stdcall
.参数 组合框句柄, 整数型
.参数 索引, 整数型
.局部变量 文本, 文本型

.如果 (索引 ≥ 0)
    文本 ＝ 获取组合框项文本_辅助 (组合框句柄, 索引)
    设置标签文本_辅助 (标签_选中项, “当前选中：索引 ” ＋ 到文本 (索引) ＋ “ - ” ＋ 文本)

.否则
    设置标签文本_辅助 (标签_选中项, “当前选中：无”)
.如果结束


' 更新编辑框文本显示
文本 ＝ 获取组合框文本_辅助 (组合框句柄)
设置标签文本_辅助 (标签_编辑框文本, “编辑框文本：” ＋ 文本)

.子程序 组合框2_选中回调, , , stdcall
.参数 组合框句柄, 整数型
.参数 索引, 整数型
.局部变量 文本, 文本型

.如果 (索引 ≥ 0)
    文本 ＝ 获取组合框项文本_辅助 (组合框句柄, 索引)
    设置标签文本_辅助 (标签_选中项, “当前选中：索引 ” ＋ 到文本 (索引) ＋ “ - ” ＋ 文本)

.否则
    设置标签文本_辅助 (标签_选中项, “当前选中：无”)
.如果结束


.子程序 按钮点击回调, , , stdcall
.参数 按钮句柄, 整数型
.局部变量 索引, 整数型
.局部变量 文本, 文本型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型
.局部变量 空白字节集, 字节集
.局部变量 标题字节集, 字节集
.局部变量 消息字节集, 字节集
.局部变量 图标字节集, 字节集

.判断开始 (按钮句柄 ＝ 按钮_获取选中)
    索引 ＝ 获取组合框选中索引 (组合框1)
    .如果 (索引 ≥ 0)
        文本 ＝ 获取组合框项文本_辅助 (组合框1, 索引)
        标题字节集 ＝ 编码_Ansi到Utf8 (“获取选中项”)
        消息字节集 ＝ 编码_Ansi到Utf8 (“选中项索引：” ＋ 到文本 (索引) ＋ #换行符 ＋ “选中项文本：” ＋ 文本)
        图标字节集 ＝ { 226, 156, 133 }  ' ?
        信息框_辅助 (主窗口句柄, 标题字节集, 消息字节集, 图标字节集)

    .否则
        标题字节集 ＝ 编码_Ansi到Utf8 (“获取选中项”)
        消息字节集 ＝ 编码_Ansi到Utf8 (“未选中任何项”)
        图标字节集 ＝ { 226, 132, 185, 239, 184, 143 }  ' ??
        信息框_辅助 (主窗口句柄, 标题字节集, 消息字节集, 图标字节集)
    .如果结束


.判断 (按钮句柄 ＝ 按钮_设置选中)
    设置组合框选中索引 (组合框1, 2)
    标题字节集 ＝ 编码_Ansi到Utf8 (“设置选中项”)
    消息字节集 ＝ 编码_Ansi到Utf8 (“已设置组合框1选中项为索引2”)
    图标字节集 ＝ { 226, 156, 133 }  ' ?
    信息框_辅助 (主窗口句柄, 标题字节集, 消息字节集, 图标字节集)
.判断 (按钮句柄 ＝ 按钮_清空)
    清空组合框 (组合框1)
    设置标签文本_辅助 (标签_选中项, “当前选中：无”)
    设置标签文本_辅助 (标签_编辑框文本, “编辑框文本：”)
    标题字节集 ＝ 编码_Ansi到Utf8 (“清空组合框”)
    消息字节集 ＝ { 229, 183, 178, 230, 184, 133, 231, 169, 186, 231, 187, 132, 229, 144, 136, 230, 161, 134, 49, 32, 240, 159, 151, 145, 239, 184, 143 }  ' 已清空组合框1 ???
    图标字节集 ＝ { 226, 156, 133 }  ' ?
    信息框_辅助 (主窗口句柄, 标题字节集, 消息字节集, 图标字节集)
.判断 (按钮句柄 ＝ 按钮_添加项)
    缓冲区 ＝ 取空白字节集 (1024)
    实际长度 ＝ 获取D2D编辑框文本 (编辑框_新项, 取变量数据地址 (缓冲区), 1024)
    .如果 (实际长度 ＞ 0)
        ' ?? 关键修复：直接使用UTF-8字节集添加项，不转换为ANSI
        ' 这样可以保留emoji等Unicode字符
        添加组合框项 (组合框1, 取变量数据地址 (缓冲区), 实际长度)

        ' 使用UTF-8消息框显示，支持emoji
        标题字节集 ＝ 编码_Ansi到Utf8 (“添加项目”)
        ' 消息 = "已添加项目：" + 用户输入的文本 + " ?"
        消息字节集 ＝ 编码_Ansi到Utf8 (“已添加项目：”)
        消息字节集 ＝ 消息字节集 ＋ 取字节集左边 (缓冲区, 实际长度)
        消息字节集 ＝ 消息字节集 ＋ { 32, 226, 156, 133 }  ' 空格 + ?
        图标字节集 ＝ { 226, 158, 149 }  ' ?
        信息框_辅助 (主窗口句柄, 标题字节集, 消息字节集, 图标字节集)

        ' 清空编辑框
        空白字节集 ＝ {  }
        设置编辑框文本 (编辑框_新项, 取变量数据地址 (空白字节集), 0)

    .否则
        标题字节集 ＝ 编码_Ansi到Utf8 (“添加项目”)
        消息字节集 ＝ 编码_Ansi到Utf8 (“请输入项目文本”)
        图标字节集 ＝ { 226, 154, 160, 239, 184, 143 }  ' ??
        信息框_辅助 (主窗口句柄, 标题字节集, 消息字节集, 图标字节集)
    .如果结束


.默认

.判断结束

