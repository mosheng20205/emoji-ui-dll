.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄
.程序集变量 进度条1, 整数型
.程序集变量 进度条2, 整数型
.程序集变量 进度条3, 整数型
.程序集变量 按钮_增加, 整数型
.程序集变量 按钮_减少, 整数型
.程序集变量 按钮_不确定, 整数型
.程序集变量 标签_值, 整数型
.程序集变量 当前值, 整数型
.程序集变量 是否不确定模式, 逻辑型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集

标题字节集 ＝ { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 (“进度条示例 - Element UI 风格”)

主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 600, 500, 16382715)
.如果真 (主窗口句柄 ＝ 0)
    返回 (0)
.如果真结束
调试输出 (取程序名称 (1), “主窗口句柄”, 主窗口句柄)
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ “\icon.ico”)


' 创建标题标签
创建标签_辅助 (主窗口句柄, 20, 20, 560, 30, “进度条控件示例”, #白色, #COLOR_TRANSPARENT, “Microsoft YaHei UI”, 16, 真, 假, 假, 1, 假)

' 创建进度条1 - 普通模式，显示百分比
进度条1 ＝ 创建进度条 (主窗口句柄, 20, 70, 560, 30, 0, #COLOR_PRIMARY, #COLOR_BORDER_LIGHTER, 真, #COLOR_TEXT_PRIMARY)

' 创建进度条2 - 普通模式，不显示百分比
进度条2 ＝ 创建进度条 (主窗口句柄, 20, 120, 560, 20, 50, #COLOR_SUCCESS, #COLOR_BORDER_LIGHTER, 假, #COLOR_TEXT_PRIMARY)

' 创建进度条3 - 不确定模式
进度条3 ＝ 创建进度条 (主窗口句柄, 20, 160, 560, 30, 0, #COLOR_WARNING, #COLOR_BORDER_LIGHTER, 假, #COLOR_TEXT_PRIMARY)
设置进度条不确定模式 (进度条3, 真)

' 创建说明标签
创建标签_辅助 (主窗口句柄, 20, 210, 560, 25, “进度条1：显示百分比文本”, #COLOR_TEXT_REGULAR, #COLOR_TRANSPARENT, “Microsoft YaHei UI”, 12, 假, 假, 假, 0, 假)
创建标签_辅助 (主窗口句柄, 20, 235, 560, 25, “进度条2：不显示文本，初始值50%”, #COLOR_TEXT_REGULAR, #COLOR_TRANSPARENT, “Microsoft YaHei UI”, 12, 假, 假, 假, 0, 假)
创建标签_辅助 (主窗口句柄, 20, 260, 560, 25, “进度条3：不确定模式（循环动画）”, #COLOR_TEXT_REGULAR, #COLOR_TRANSPARENT, “Microsoft YaHei UI”, 12, 假, 假, 假, 0, 假)

' 创建控制按钮
emoji字节集 ＝ { 226, 158, 149 }
文本字节集 ＝ 编码_Ansi到Utf8 (“增加进度”)
按钮_增加 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 310, 120, 40, #COLOR_PRIMARY)
调试输出 (取程序名称 (1), “按钮_增加”, 按钮_增加)

emoji字节集 ＝ { 226, 158, 150 }
文本字节集 ＝ 编码_Ansi到Utf8 (“减少进度”)
按钮_减少 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 160, 310, 120, 40, #COLOR_DANGER)
调试输出 (取程序名称 (1), “按钮_减少”, 按钮_减少)

emoji字节集 ＝ { 240, 159, 148, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换模式”)
按钮_不确定 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 300, 310, 120, 40, #COLOR_WARNING)
调试输出 (取程序名称 (1), “按钮_不确定”, 按钮_不确定)

' 创建当前值显示标签  #303133 #透明色
标签_值 ＝ 创建标签_辅助 (主窗口句柄, 20, 370, 560, 30, “当前进度：0%”, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, “Microsoft YaHei UI”, 16, 真, 假, 假, 1, 假)
' 设置按钮回调
设置按钮点击回调 (&进度条示例_按钮点击回调)
当前值 ＝ 0
是否不确定模式 ＝ 假


设置消息循环主窗口 (主窗口句柄)  ' 不设置主窗口的消息循环，那么关闭子窗口会连主窗口一起关闭
运行消息循环 ()


返回 (0)  ' 可以根据您的需要返回任意数值

.子程序 进度条示例_按钮点击回调, , , stdcall
.参数 按钮ID, 整数型

调试输出 (取程序名称 (1), 按钮ID)
.如果真 (按钮ID ＝ 按钮_增加)
    当前值 ＝ 当前值 ＋ 10
    .如果真 (当前值 ＞ 100)
        当前值 ＝ 100
    .如果真结束

    设置进度条值 (进度条1, 当前值)
    设置进度条值 (进度条2, 当前值)
    更新值显示 ()
.如果真结束
.如果真 (按钮ID ＝ 按钮_减少)
    当前值 ＝ 当前值 － 10
    .如果真 (当前值 ＜ 0)
        当前值 ＝ 0
    .如果真结束

    设置进度条值 (进度条1, 当前值)
    设置进度条值 (进度条2, 当前值)
    更新值显示 ()

.如果真结束
.如果真 (按钮ID ＝ 按钮_不确定)
    ' 切换进度条1的模式

    .如果 (是否不确定模式 ＝ 假)
        ' 当前是确定模式，切换到不确定模式
        设置进度条不确定模式 (进度条1, 真)
        设置标签文本_UTF8 (标签_值, “当前模式：不确定模式（循环动画）”)
        是否不确定模式 ＝ 真

    .否则
        ' 当前是不确定模式，切换到确定模式
        设置进度条不确定模式 (进度条1, 假)
        设置进度条值 (进度条1, 当前值)
        是否不确定模式 ＝ 假
        更新值显示 ()

    .如果结束

.如果真结束


.子程序 进度条回调, , , stdcall
.参数 进度条句柄, 整数型
.参数 值, 整数型

' 进度条值改变时的回调
调试输出 (“进度条值改变：” ＋ 到文本 (值) ＋ “%”)

.子程序 更新值显示
.局部变量 文本, 文本型

文本 ＝ “当前进度：” ＋ 到文本 (当前值) ＋ “%”

.如果真 (当前值 ＝ 100)
    文本 ＝ 文本 ＋ “ 完成！”
.如果真结束

设置标签文本_UTF8 (标签_值, 文本)
