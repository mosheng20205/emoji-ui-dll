.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_DataGridView表头样式4_0示例

.程序集变量 窗口句柄, 整数型
.程序集变量 表格句柄, 整数型
.程序集变量 状态标签, 整数型
.程序集变量 按钮_Plain, 整数型
.程序集变量 按钮_Dark, 整数型
.程序集变量 按钮_Bordered, 整数型
.程序集变量 按钮_切换多行, 整数型
.程序集变量 表头多行已启用, 逻辑型

.子程序 _启动子程序, 整数型
.局部变量 行索引, 整数型
.局部变量 emoji字节集, 字节集

窗口句柄 ＝ 创建Emoji窗口 ("DataGridView表头样式4.0示例", 80, 40, 1140, 780)
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 20, 1080, 560, 假, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))

DG_添加文本列 (表格句柄, "第一列", 160)
DG_添加文本列 (表格句柄, "第二列", 220)
DG_添加文本列 (表格句柄, "第三列", 260)
DG_添加文本列 (表格句柄, "说明", 360)

行索引 ＝ 表格_添加行 (表格句柄)
DG_设置文本 (表格句柄, 行索引, 0, "A-01")
DG_设置文本 (表格句柄, 行索引, 1, "示例数据")
DG_设置文本 (表格句柄, 行索引, 2, "切换表头样式后观察边框和背景。")
DG_设置文本 (表格句柄, 行索引, 3, "多行表头需配合更高的列头高度。")

表格_设置冻结首行 (表格句柄, 真)
表格_设置表头样式 (表格句柄, 0)
表格_设置表头多行 (表格句柄, 假)
表格_设置列头高度 (表格句柄, 42)
表头多行已启用 ＝ 假

按钮_Plain ＝ 创建Emoji按钮_辅助 (窗口句柄, "1", "Plain", 20, 600, 120, 44, 到ARGB (255, 144, 147, 153))
按钮_Dark ＝ 创建Emoji按钮_辅助 (窗口句柄, "2", "Dark", 156, 600, 120, 44, 到ARGB (255, 64, 158, 255))
按钮_Bordered ＝ 创建Emoji按钮_辅助 (窗口句柄, "3", "Bordered", 292, 600, 140, 44, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 226, 134, 149 }  ' ↕
按钮_切换多行 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "切换多行表头", 448, 600, 180, 44, 到ARGB (255, 230, 162, 60))

状态标签 ＝ 创建标签_辅助 (窗口句柄, 20, 660, 1080, 56, "状态：当前表头样式为 Plain。", 到ARGB (255, 48, 49, 51), 到ARGB (255, 244, 247, 250), "Microsoft YaHei UI", 11, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型

.如果真 (按钮ID ＝ 按钮_Plain)
    表格_设置表头样式 (表格句柄, 0)
    表格_刷新 (表格句柄)
    写状态 ("状态：已切换到 Plain 表头样式。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_Dark)
    表格_设置表头样式 (表格句柄, 1)
    表格_刷新 (表格句柄)
    写状态 ("状态：已切换到 Dark 表头样式。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_Bordered)
    表格_设置表头样式 (表格句柄, 2)
    表格_刷新 (表格句柄)
    写状态 ("状态：已切换到 Bordered 表头样式。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_切换多行)
    表头多行已启用 ＝ 取反 (表头多行已启用)
    表格_设置表头多行 (表格句柄, 表头多行已启用)
    .如果 (表头多行已启用)
        DG_设置列头 (表格句柄, 0, "第一列" ＋ #换行符 ＋ "编号")
        DG_设置列头 (表格句柄, 1, "第二列" ＋ #换行符 ＋ "名称")
        DG_设置列头 (表格句柄, 2, "第三列" ＋ #换行符 ＋ "详细说明")
        表格_设置列头高度 (表格句柄, 66)
        写状态 ("状态：已启用多行表头。")
    .否则
        DG_设置列头 (表格句柄, 0, "第一列")
        DG_设置列头 (表格句柄, 1, "第二列")
        DG_设置列头 (表格句柄, 2, "第三列")
        表格_设置列头高度 (表格句柄, 42)
        写状态 ("状态：已关闭多行表头。")
    .如果结束
    表格_刷新 (表格句柄)
.如果真结束

.子程序 DG_添加文本列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加文本列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_设置文本
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置单元格文本 (表格, 行, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 DG_设置列头
.参数 表格, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置列头文本 (表格, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 写状态
.参数 文本, 文本型
设置标签文本_UTF8 (状态标签, 文本)
