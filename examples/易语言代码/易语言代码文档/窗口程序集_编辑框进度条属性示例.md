.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 标签_结果, 整数型
.程序集变量 编辑框句柄, 整数型
.程序集变量 进度条句柄, 整数型
.程序集变量 按钮_获取编辑框字体, 整数型
.程序集变量 按钮_获取编辑框颜色, 整数型
.程序集变量 按钮_获取编辑框位置, 整数型
.程序集变量 按钮_获取编辑框对齐, 整数型
.程序集变量 按钮_切换编辑框启用, 整数型
.程序集变量 按钮_切换编辑框可视, 整数型
.程序集变量 按钮_获取进度条颜色, 整数型
.程序集变量 按钮_获取进度条位置, 整数型
.程序集变量 按钮_切换进度条启用, 整数型
.程序集变量 按钮_切换进度条可视, 整数型
.程序集变量 按钮_获取进度条显示文本, 整数型
.程序集变量 编辑框是否启用, 逻辑型
.程序集变量 编辑框是否可见, 逻辑型
.程序集变量 进度条是否启用, 逻辑型
.程序集变量 进度条是否可见, 逻辑型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 utf8字节集, 字节集
.局部变量 按钮文本字节集, 字节集
.局部变量 字体名utf8, 字节集
.局部变量 文本utf8, 字节集

' 创建窗口
utf8字节集 ＝ 编码_Ansi到Utf8 (“编辑框和进度条属性操作示例”)
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 900, 600, 到RGB (70, 130, 180))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ “\icon.ico”)

' 创建结果显示标签
utf8字节集 ＝ 编码_Ansi到Utf8 (“点击下方按钮操作编辑框和进度条属性”)
字体名utf8 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
标签_结果 ＝ 创建标签 (主窗口句柄, 20, 10, 860, 130, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 0, 0, 0), 到ARGB (255, 245, 245, 245), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

' 创建编辑框（微软雅黑 14号 粗体 左对齐）
文本utf8 ＝ 编码_Ansi到Utf8 (“这是测试编辑框内容”)
字体名utf8 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
编辑框句柄 ＝ 创建编辑框 (主窗口句柄, 20, 155, 400, 35, 取变量数据地址 (文本utf8), 取字节集长度 (文本utf8), 到ARGB (255, 0, 0, 0), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 14, 真, 假, 假, 0, 假, 假, 假, 真, 真)

' 创建进度条（蓝色前景 灰色背景 显示文本 初始值50）
进度条句柄 ＝ 创建进度条 (主窗口句柄, 20, 205, 400, 30, 50, 到ARGB (255, 70, 130, 180), 到ARGB (255, 220, 220, 220), 真, 到ARGB (255, 255, 255, 255))

' 初始化状态标志
编辑框是否启用 ＝ 真
编辑框是否可见 ＝ 真
进度条是否启用 ＝ 真
进度条是否可见 ＝ 真

' ===== 编辑框操作按钮 =====

' "?? 获取编辑框字体"  ?? = { 240, 159, 148, 164 }
utf8字节集 ＝ { 240, 159, 148, 164 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取编辑框字体”)
按钮_获取编辑框字体 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 255, 160, 40, 到ARGB (255, 100, 149, 237))

' "?? 获取编辑框颜色"  ?? = { 240, 159, 142, 168 }
utf8字节集 ＝ { 240, 159, 142, 168 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取编辑框颜色”)
按钮_获取编辑框颜色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 195, 255, 160, 40, 到ARGB (255, 100, 149, 237))

' "?? 获取编辑框位置"  ?? = { 240, 159, 147, 144 }
utf8字节集 ＝ { 240, 159, 147, 144 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取编辑框位置”)
按钮_获取编辑框位置 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 370, 255, 160, 40, 到ARGB (255, 100, 149, 237))

' "? 获取对齐方式"  ? = { 226, 134, 148 }
utf8字节集 ＝ { 226, 134, 148 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取对齐方式”)
按钮_获取编辑框对齐 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 545, 255, 160, 40, 到ARGB (255, 100, 149, 237))

' "? 切换编辑框启用"  ? = { 226, 154, 161 }
utf8字节集 ＝ { 226, 154, 161 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“切换编辑框启用”)
按钮_切换编辑框启用 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 720, 255, 160, 40, 到ARGB (255, 180, 100, 100))

' "?? 切换编辑框可视"  ?? = { 240, 159, 145, 129, 239, 184, 143 }
utf8字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“切换编辑框可视”)
按钮_切换编辑框可视 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 310, 160, 40, 到ARGB (255, 180, 100, 100))

' ===== 进度条操作按钮 =====

' "?? 获取进度条颜色"  ?? = { 240, 159, 142, 168 }
utf8字节集 ＝ { 240, 159, 142, 168 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取进度条颜色”)
按钮_获取进度条颜色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 195, 310, 160, 40, 到ARGB (255, 100, 180, 100))

' "?? 获取进度条位置"  ?? = { 240, 159, 147, 144 }
utf8字节集 ＝ { 240, 159, 147, 144 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取进度条位置”)
按钮_获取进度条位置 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 370, 310, 160, 40, 到ARGB (255, 100, 180, 100))

' "? 切换进度条启用"  ? = { 226, 154, 161 }
utf8字节集 ＝ { 226, 154, 161 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“切换进度条启用”)
按钮_切换进度条启用 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 545, 310, 160, 40, 到ARGB (255, 180, 100, 100))

' "?? 切换进度条可视"  ?? = { 240, 159, 145, 129, 239, 184, 143 }
utf8字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“切换进度条可视”)
按钮_切换进度条可视 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 720, 310, 160, 40, 到ARGB (255, 180, 100, 100))

' "?? 获取显示文本状态"  ?? = { 240, 159, 147, 138 }
utf8字节集 ＝ { 240, 159, 147, 138 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取显示文本状态”)
按钮_获取进度条显示文本 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 365, 160, 40, 到ARGB (255, 100, 180, 100))

' 设置按钮点击回调
设置按钮点击回调 (&按钮点击回调)

' 设置消息循环主窗口
设置消息循环主窗口 (主窗口句柄)

' 运行消息循环
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 结果字节集, 字节集
.局部变量 字体名缓冲区, 字节集
.局部变量 字体名长度, 整数型
.局部变量 字体大小, 整数型
.局部变量 粗体, 整数型
.局部变量 斜体, 整数型
.局部变量 下划线, 整数型
.局部变量 前景色, 整数型
.局部变量 背景色, 整数型
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型
.局部变量 对齐方式, 整数型
.局部变量 状态值, 整数型

.判断开始 (按钮ID ＝ 按钮_获取编辑框字体)  ' 获取编辑框字体信息
    字体名长度 ＝ 获取编辑框字体 (编辑框句柄, 0, 0, 字体大小, 粗体, 斜体, 下划线)
    .如果 (字体名长度 ＞ 0)
        字体名缓冲区 ＝ 取空白字节集 (字体名长度)
        获取编辑框字体 (编辑框句柄, 取变量数据地址 (字体名缓冲区), 字体名长度, 字体大小, 粗体, 斜体, 下划线)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框字体】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“字体名：”) ＋ 字体名缓冲区 ＋ { 10 } ＋ 编码_Ansi到Utf8 (“字体大小：”) ＋ 编码_Ansi到Utf8 (到文本 (字体大小)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“粗体：”) ＋ 编码_Ansi到Utf8 (到文本 (粗体)) ＋ 编码_Ansi到Utf8 (“  斜体：”) ＋ 编码_Ansi到Utf8 (到文本 (斜体)) ＋ 编码_Ansi到Utf8 (“  下划线：”) ＋ 编码_Ansi到Utf8 (到文本 (下划线))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框字体】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“获取失败（返回-1）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取编辑框颜色)  ' 获取编辑框颜色
    .如果 (获取编辑框颜色 (编辑框句柄, 前景色, 背景色) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框颜色】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“前景色(ARGB)：0x”) ＋ 编码_Ansi到Utf8 (到文本 (前景色)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“背景色(ARGB)：0x”) ＋ 编码_Ansi到Utf8 (到文本 (背景色))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框颜色】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“获取失败（返回-1）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取编辑框位置)  ' 获取编辑框位置和大小
    .如果 (获取编辑框位置 (编辑框句柄, X, Y, 宽度, 高度) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框位置大小】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“X坐标：”) ＋ 编码_Ansi到Utf8 (到文本 (X)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“Y坐标：”) ＋ 编码_Ansi到Utf8 (到文本 (Y)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“宽度：”) ＋ 编码_Ansi到Utf8 (到文本 (宽度)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“高度：”) ＋ 编码_Ansi到Utf8 (到文本 (高度))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框位置大小】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“获取失败（返回-1）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取编辑框对齐)  ' 获取编辑框对齐方式
    对齐方式 ＝ 获取编辑框对齐方式 (编辑框句柄)
    .判断开始 (对齐方式 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框对齐方式】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式：左对齐（0）”)
    .判断 (对齐方式 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框对齐方式】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式：居中（1）”)
    .判断 (对齐方式 ＝ 2)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框对齐方式】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式：右对齐（2）”)
    .默认
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取编辑框对齐方式】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“获取失败（返回-1）”)
    .判断结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_切换编辑框启用)  ' 切换编辑框启用/禁用
    .如果 (编辑框是否启用 ＝ 真)
        编辑框是否启用 ＝ 假
    .否则
        编辑框是否启用 ＝ 真
    .如果结束

    启用编辑框 (编辑框句柄, 编辑框是否启用)
    状态值 ＝ 获取编辑框启用状态 (编辑框句柄)
    .如果 (编辑框是否启用 ＝ 真)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【切换编辑框启用状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“已启用编辑框”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：”) ＋ 编码_Ansi到Utf8 (到文本 (状态值)) ＋ 编码_Ansi到Utf8 (“（1=启用）”)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“【切换编辑框启用状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“已禁用编辑框（再次点击可重新启用）”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：”) ＋ 编码_Ansi到Utf8 (到文本 (状态值)) ＋ 编码_Ansi到Utf8 (“（0=禁用）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_切换编辑框可视)  ' 切换编辑框显示/隐藏
    .如果 (编辑框是否可见 ＝ 真)
        编辑框是否可见 ＝ 假
    .否则
        编辑框是否可见 ＝ 真
    .如果结束

    显示编辑框 (编辑框句柄, 编辑框是否可见)
    状态值 ＝ 获取编辑框可视状态 (编辑框句柄)
    .如果 (编辑框是否可见 ＝ 真)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【切换编辑框可视状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“已显示编辑框”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：”) ＋ 编码_Ansi到Utf8 (到文本 (状态值)) ＋ 编码_Ansi到Utf8 (“（1=可见）”)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“【切换编辑框可视状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“已隐藏编辑框（再次点击可重新显示）”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：”) ＋ 编码_Ansi到Utf8 (到文本 (状态值)) ＋ 编码_Ansi到Utf8 (“（0=不可见）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取进度条颜色)  ' 获取进度条颜色
    .如果 (获取进度条颜色 (进度条句柄, 前景色, 背景色) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取进度条颜色】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“前景色(ARGB)：0x”) ＋ 编码_Ansi到Utf8 (到文本 (前景色)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“背景色(ARGB)：0x”) ＋ 编码_Ansi到Utf8 (到文本 (背景色))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取进度条颜色】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“获取失败（返回-1）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取进度条位置)  ' 获取进度条位置和大小
    .如果 (获取进度条位置 (进度条句柄, X, Y, 宽度, 高度) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取进度条位置大小】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“X坐标：”) ＋ 编码_Ansi到Utf8 (到文本 (X)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“Y坐标：”) ＋ 编码_Ansi到Utf8 (到文本 (Y)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“宽度：”) ＋ 编码_Ansi到Utf8 (到文本 (宽度)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“高度：”) ＋ 编码_Ansi到Utf8 (到文本 (高度))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取进度条位置大小】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“获取失败（返回-1）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_切换进度条启用)  ' 切换进度条启用/禁用
    .如果 (进度条是否启用 ＝ 真)
        进度条是否启用 ＝ 假
    .否则
        进度条是否启用 ＝ 真
    .如果结束

    启用进度条 (进度条句柄, 进度条是否启用)
    状态值 ＝ 获取进度条启用状态 (进度条句柄)
    .如果 (进度条是否启用 ＝ 真)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【切换进度条启用状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“已启用进度条”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：”) ＋ 编码_Ansi到Utf8 (到文本 (状态值)) ＋ 编码_Ansi到Utf8 (“（1=启用）”)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“【切换进度条启用状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“已禁用进度条（再次点击可重新启用）”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：”) ＋ 编码_Ansi到Utf8 (到文本 (状态值)) ＋ 编码_Ansi到Utf8 (“（0=禁用）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_切换进度条可视)  ' 切换进度条显示/隐藏
    .如果 (进度条是否可见 ＝ 真)
        进度条是否可见 ＝ 假
    .否则
        进度条是否可见 ＝ 真
    .如果结束
    显示进度条 (进度条句柄, 进度条是否可见)
    状态值 ＝ 获取进度条可视状态 (进度条句柄)
    .如果 (进度条是否可见 ＝ 真)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【切换进度条可视状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“已显示进度条”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：”) ＋ 编码_Ansi到Utf8 (到文本 (状态值)) ＋ 编码_Ansi到Utf8 (“（1=可见）”)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“【切换进度条可视状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“已隐藏进度条（再次点击可重新显示）”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：”) ＋ 编码_Ansi到Utf8 (到文本 (状态值)) ＋ 编码_Ansi到Utf8 (“（0=不可见）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取进度条显示文本)  ' 获取进度条是否显示文本
    状态值 ＝ 获取进度条显示文本状态 (进度条句柄)
    .判断开始 (状态值 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取进度条显示文本状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：显示百分比文本（1）（1=显示，0=不显示，-1=错误）”)
    .判断 (状态值 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取进度条显示文本状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前状态：不显示文本（0）（1=显示，0=不显示，-1=错误）”)
    .默认
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取进度条显示文本状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“获取失败（返回-1）（1=显示，0=不显示，-1=错误）”)
    .判断结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.默认

.判断结束


.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
