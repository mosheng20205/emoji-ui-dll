.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_DataGridView图片列4_0示例

.程序集变量 窗口句柄, 整数型
.程序集变量 表格句柄, 整数型
.程序集变量 状态标签, 整数型
.程序集变量 按钮_加载图片, 整数型
.程序集变量 按钮_清空图片, 整数型

.子程序 _启动子程序, 整数型
.局部变量 行索引, 整数型
.局部变量 emoji字节集, 字节集

窗口句柄 ＝ 创建Emoji窗口 ("DataGridView图片列4.0示例", 80, 40, 1080, 760)
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 20, 1020, 560, 假, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))

DG_添加图片列 (表格句柄, "图标", 90)
DG_添加文本列 (表格句柄, "文件名", 220)
DG_添加文本列 (表格句柄, "说明", 620)

行索引 ＝ 表格_添加行 (表格句柄)
DG_设置文本 (表格句柄, 行索引, 1, "icon.ico")
DG_设置文本 (表格句柄, 行索引, 2, "点击下方按钮，把运行目录中的 icon.ico 加载到图片列。")

行索引 ＝ 表格_添加行 (表格句柄)
DG_设置文本 (表格句柄, 行索引, 1, "icon.ico")
DG_设置文本 (表格句柄, 行索引, 2, "第二行演示批量设置图片；也可调用 表格_清除单元格图片 清空。")

表格_设置冻结首行 (表格句柄, 真)
表格_设置默认行高 (表格句柄, 54)
表格_设置列头高度 (表格句柄, 42)

emoji字节集 ＝ { 240, 159, 150, 188 }  ' 🖼
按钮_加载图片 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "加载图片", 20, 600, 160, 44, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 167, 185 }  ' 🧹
按钮_清空图片 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "清空图片", 196, 600, 160, 44, 到ARGB (255, 245, 108, 108))

状态标签 ＝ 创建标签_辅助 (窗口句柄, 20, 660, 1020, 56, "状态：等待操作。", 到ARGB (255, 48, 49, 51), 到ARGB (255, 244, 247, 250), "Microsoft YaHei UI", 11, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 图片路径, 文本型

图片路径 ＝ 取运行目录 () ＋ "\icon.ico"

.如果真 (按钮ID ＝ 按钮_加载图片)
    .如果真 (DG_从文件设置图片 (表格句柄, 0, 0, 图片路径))
        DG_从文件设置图片 (表格句柄, 1, 0, 图片路径)
        表格_刷新 (表格句柄)
        写状态 ("状态：已从运行目录加载 icon.ico 到前两行图片列。")
    .否则
        写状态 ("状态：加载失败，请确认运行目录下存在 icon.ico。")
    .如果真结束
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_清空图片)
    表格_清除单元格图片 (表格句柄, 0, 0)
    表格_清除单元格图片 (表格句柄, 1, 0)
    表格_刷新 (表格句柄)
    写状态 ("状态：已清空前两行图片单元格。")
.如果真结束

.子程序 DG_添加文本列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加文本列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_添加图片列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加图片列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_设置文本
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置单元格文本 (表格, 行, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 DG_从文件设置图片, 逻辑型
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 路径, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (路径)
返回 (表格_从文件设置单元格图片 (表格, 行, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集)))

.子程序 写状态
.参数 文本, 文本型
设置标签文本_UTF8 (状态标签, 文本)
