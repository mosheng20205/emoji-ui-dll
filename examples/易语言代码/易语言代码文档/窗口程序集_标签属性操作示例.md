.版本 2
.支持库 spec
.支持库 eAPI

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 标签_演示, 整数型
.程序集变量 标签_结果, 整数型
.程序集变量 按钮_获取文本, 整数型
.程序集变量 按钮_获取字体, 整数型
.程序集变量 按钮_获取颜色, 整数型
.程序集变量 按钮_获取位置, 整数型
.程序集变量 按钮_获取对齐, 整数型
.程序集变量 按钮_获取启用状态, 整数型
.程序集变量 按钮_获取可视状态, 整数型
.程序集变量 按钮_获取全部属性, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 utf8字节集, 字节集
.局部变量 按钮文本字节集, 字节集
.局部变量 字体名utf8, 字节集

' 创建窗口（标题需要UTF-8）
utf8字节集 ＝ 编码_Ansi到Utf8 (“标签属性操作示例”)
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 800, 650, 到RGB (70, 130, 180))

' 创建演示用标签（带各种属性）
' "这是演示标签 ?? Hello World 你好世界"
' ?? = { 240, 159, 142, 137 }
utf8字节集 ＝ 编码_Ansi到Utf8 (“这是演示标签 ”) ＋ { 240, 159, 142, 137, 32 } ＋ 编码_Ansi到Utf8 (“Hello World 你好世界”)
字体名utf8 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
标签_演示 ＝ 创建标签 (主窗口句柄, 50, 30, 700, 60, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 255, 255, 255), 到ARGB (255, 70, 130, 180), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 16, 真, 假, 假, 1, 假)

' 创建结果显示标签（中文需要UTF-8）
utf8字节集 ＝ 编码_Ansi到Utf8 (“点击下方按钮查看标签属性”)
字体名utf8 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
标签_结果 ＝ 创建标签 (主窗口句柄, 50, 110, 700, 300, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 0, 0, 0), 到ARGB (255, 245, 245, 245), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

' 创建操作按钮（保存返回的按钮ID到程序集变量）
' "?? 获取文本"  ?? = { 240, 159, 147, 157 }
utf8字节集 ＝ { 240, 159, 147, 157 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取文本”)
按钮_获取文本 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 50, 430, 130, 40, 到ARGB (255, 100, 180, 100))

' "?? 获取字体"  ?? = { 240, 159, 148, 164 }
utf8字节集 ＝ { 240, 159, 148, 164 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取字体”)
按钮_获取字体 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 200, 430, 130, 40, 到ARGB (255, 100, 180, 100))

' "?? 获取颜色"  ?? = { 240, 159, 142, 168 }
utf8字节集 ＝ { 240, 159, 142, 168 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取颜色”)
按钮_获取颜色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 350, 430, 130, 40, 到ARGB (255, 100, 180, 100))

' "?? 获取位置"  ?? = { 240, 159, 147, 144 }
utf8字节集 ＝ { 240, 159, 147, 144 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取位置”)
按钮_获取位置 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 500, 430, 130, 40, 到ARGB (255, 100, 180, 100))

' "?? 获取对齐"  ?? = { 226, 134, 148, 239, 184, 143 }
utf8字节集 ＝ { 226, 134, 148, 239, 184, 143 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取对齐”)
按钮_获取对齐 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 50, 490, 130, 40, 到ARGB (255, 180, 100, 100))

' "? 获取启用状态"  ? = { 226, 156, 133 }
utf8字节集 ＝ { 226, 156, 133 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取启用状态”)
按钮_获取启用状态 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 200, 490, 130, 40, 到ARGB (255, 180, 100, 100))

' "??? 获取可视状态"  ??? = { 240, 159, 145, 129, 239, 184, 143 }
utf8字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取可视状态”)
按钮_获取可视状态 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 350, 490, 130, 40, 到ARGB (255, 180, 100, 100))

' "?? 获取全部属性"  ?? = { 240, 159, 147, 138 }
utf8字节集 ＝ { 240, 159, 147, 138 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 (“获取全部属性”)
按钮_获取全部属性 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 500, 490, 130, 40, 到ARGB (255, 100, 100, 180))

' 设置按钮点击回调
设置按钮点击回调 (&按钮点击回调)

' 设置消息循环主窗口
设置消息循环主窗口 (主窗口句柄)

' 运行消息循环
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 文本长度, 整数型
.局部变量 文本缓冲区, 字节集
.局部变量 结果字节集, 字节集
.局部变量 字体名长度, 整数型
.局部变量 字体名缓冲区, 字节集
.局部变量 字体大小, 整数型
.局部变量 粗体, 整数型
.局部变量 斜体, 整数型
.局部变量 下划线, 整数型
.局部变量 前景色, 整数型
.局部变量 背景色, 整数型
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型
.局部变量 对齐方式, 整数型
.局部变量 启用状态, 整数型
.局部变量 可视状态, 整数型

.判断开始 (按钮ID ＝ 按钮_获取文本)  ' 获取文本
    ' 第一次调用获取所需长度
    文本长度 ＝ 获取标签文本 (标签_演示, 0, 0)
    .如果 (文本长度 ＞ 0)
        ' 第二次调用获取内容（文本缓冲区本身就是UTF-8字节集）
        文本缓冲区 ＝ 取空白字节集 (文本长度)
        获取标签文本 (标签_演示, 取变量数据地址 (文本缓冲区), 文本长度)
        ' 结果全程用UTF-8字节集拼接，直接传给设置标签文本
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签文本】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“文本内容：”) ＋ 文本缓冲区 ＋ { 10 } ＋ 编码_Ansi到Utf8 (“字节长度：”) ＋ 编码_Ansi到Utf8 (到文本 (文本长度))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“获取标签文本失败（返回值：”) ＋ 编码_Ansi到Utf8 (到文本 (文本长度)) ＋ 编码_Ansi到Utf8 (“）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取字体)  ' 获取字体
    ' 第一次调用获取字体名所需长度（缓冲区指针和大小均传0）
    字体名长度 ＝ 获取标签字体 (标签_演示, 0, 0, 字体大小, 粗体, 斜体, 下划线)
    .如果 (字体名长度 ＞ 0)
        ' 第二次调用获取字体名（字体名缓冲区本身就是UTF-8字节集）
        字体名缓冲区 ＝ 取空白字节集 (字体名长度)
        获取标签字体 (标签_演示, 取变量数据地址 (字体名缓冲区), 字体名长度, 字体大小, 粗体, 斜体, 下划线)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签字体】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“字体名：”) ＋ 字体名缓冲区 ＋ { 10 } ＋ 编码_Ansi到Utf8 (“字体大小：”) ＋ 编码_Ansi到Utf8 (到文本 (字体大小)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“粗体：”) ＋ 编码_Ansi到Utf8 (到文本 (粗体)) ＋ 编码_Ansi到Utf8 (“  斜体：”) ＋ 编码_Ansi到Utf8 (到文本 (斜体)) ＋ 编码_Ansi到Utf8 (“  下划线：”) ＋ 编码_Ansi到Utf8 (到文本 (下划线))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“获取标签字体失败（返回值：”) ＋ 编码_Ansi到Utf8 (到文本 (字体名长度)) ＋ 编码_Ansi到Utf8 (“）”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()


.判断 (按钮ID ＝ 按钮_获取颜色)  ' 获取颜色
    .如果 (获取标签颜色 (标签_演示, 前景色, 背景色) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签颜色】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“前景色（ARGB）：”) ＋ 编码_Ansi到Utf8 (到文本 (前景色)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“背景色（ARGB）：”) ＋ 编码_Ansi到Utf8 (到文本 (背景色)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“前景色十六进制：#”) ＋ 编码_Ansi到Utf8 (_十六进制 (前景色)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“背景色十六进制：#”) ＋ 编码_Ansi到Utf8 (_十六进制 (背景色))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“获取标签颜色失败”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()


.判断 (按钮ID ＝ 按钮_获取位置)  ' 获取位置
    .如果 (获取标签位置 (标签_演示, X, Y, 宽度, 高度) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签位置】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“X坐标：”) ＋ 编码_Ansi到Utf8 (到文本 (X)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“Y坐标：”) ＋ 编码_Ansi到Utf8 (到文本 (Y)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“宽度：”) ＋ 编码_Ansi到Utf8 (到文本 (宽度)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“高度：”) ＋ 编码_Ansi到Utf8 (到文本 (高度))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“获取标签位置失败”)
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取对齐)  ' 获取对齐方式
    对齐方式 ＝ 获取标签对齐方式 (标签_演示)
    .判断开始 (对齐方式 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签对齐方式】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式值：0”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式：左对齐”)
    .判断 (对齐方式 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签对齐方式】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式值：1”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式：居中对齐”)
    .判断 (对齐方式 ＝ 2)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签对齐方式】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式值：2”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式：右对齐”)
    .默认
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签对齐方式】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式值：”) ＋ 编码_Ansi到Utf8 (到文本 (对齐方式)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“对齐方式：未知（错误）”)
    .判断结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取启用状态)  ' 获取启用状态
    启用状态 ＝ 获取标签启用状态 (标签_演示)
    .判断开始 (启用状态 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签启用状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“状态：已启用（1）”)
    .判断 (启用状态 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签启用状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“状态：已禁用（0）”)
    .默认
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签启用状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“状态：获取失败（-1）”)
    .判断结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取可视状态)  ' 获取可视状态
    可视状态 ＝ 获取标签可视状态 (标签_演示)
    .判断开始 (可视状态 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签可视状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“状态：可见（1）”)
    .判断 (可视状态 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签可视状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“状态：不可见（0）”)
    .默认
        结果字节集 ＝ 编码_Ansi到Utf8 (“【获取标签可视状态】”) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“状态：获取失败（-1）”)
    .判断结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()


.判断 (按钮ID ＝ 按钮_获取全部属性)  ' 获取全部属性
    结果字节集 ＝ 编码_Ansi到Utf8 (“【标签全部属性汇总】”) ＋ { 10 }
    ' 获取文本
    文本长度 ＝ 获取标签文本 (标签_演示, 0, 0)
    .如果真 (文本长度 ＞ 0)
        文本缓冲区 ＝ 取空白字节集 (文本长度)
        获取标签文本 (标签_演示, 取变量数据地址 (文本缓冲区), 文本长度)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (“文本：”) ＋ 文本缓冲区 ＋ { 10 }
    .如果真结束

    ' 获取字体
    字体名长度 ＝ 获取标签字体 (标签_演示, 0, 0, 字体大小, 粗体, 斜体, 下划线)
    .如果真 (字体名长度 ＞ 0)
        字体名缓冲区 ＝ 取空白字节集 (字体名长度)
        获取标签字体 (标签_演示, 取变量数据地址 (字体名缓冲区), 字体名长度, 字体大小, 粗体, 斜体, 下划线)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (“字体：”) ＋ 字体名缓冲区 ＋ 编码_Ansi到Utf8 (“ ”) ＋ 编码_Ansi到Utf8 (到文本 (字体大小)) ＋ 编码_Ansi到Utf8 (“pt”) ＋ { 10 }
    .如果真结束
    ' 获取颜色
    .如果真 (获取标签颜色 (标签_演示, 前景色, 背景色) ＝ 0)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (“前景色：#”) ＋ 编码_Ansi到Utf8 (_十六进制 (前景色)) ＋ 编码_Ansi到Utf8 (“  背景色：#”) ＋ 编码_Ansi到Utf8 (_十六进制 (背景色)) ＋ { 10 }
    .如果真结束

    ' 获取位置
    .如果真 (获取标签位置 (标签_演示, X, Y, 宽度, 高度) ＝ 0)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (“位置：(”) ＋ 编码_Ansi到Utf8 (到文本 (X)) ＋ 编码_Ansi到Utf8 (“, ”) ＋ 编码_Ansi到Utf8 (到文本 (Y)) ＋ 编码_Ansi到Utf8 (“)  大小：”) ＋ 编码_Ansi到Utf8 (到文本 (宽度)) ＋ 编码_Ansi到Utf8 (“x”) ＋ 编码_Ansi到Utf8 (到文本 (高度)) ＋ { 10 }
    .如果真结束
    ' 获取对齐方式
    对齐方式 ＝ 获取标签对齐方式 (标签_演示)
    .判断开始 (对齐方式 ＝ 0)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (“对齐：左对齐”) ＋ { 10 }
    .判断 (对齐方式 ＝ 1)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (“对齐：居中”) ＋ { 10 }
    .判断 (对齐方式 ＝ 2)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (“对齐：右对齐”) ＋ { 10 }
    .默认
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (“对齐：未知”) ＋ { 10 }
    .判断结束

    ' 获取启用和可视状态
    启用状态 ＝ 获取标签启用状态 (标签_演示)
    可视状态 ＝ 获取标签可视状态 (标签_演示)
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 (“启用：”) ＋ 编码_Ansi到Utf8 (到文本 (启用状态)) ＋ 编码_Ansi到Utf8 (“  可见：”) ＋ 编码_Ansi到Utf8 (到文本 (可视状态))

    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

    返回 ()



.默认

.判断结束


.子程序 _十六进制, 文本型
.参数 数值, 整数型

' 将整数转换为十六进制字符串（8位）
返回 (格式化文本 (“%08X”, 数值))

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 取文本指针, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集

字节集 ＝ 到字节集 (文本)
返回 (取变量数据地址 (字节集))

.子程序 取文本字节数, 整数型
.参数 文本, 文本型

返回 (取字节集长度 (到字节集 (文本)))
