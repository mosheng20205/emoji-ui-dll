.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_DataGridView虚拟模式示例

.程序集变量 窗口句柄, 整数型
.程序集变量 表格句柄, 整数型
.程序集变量 状态标签, 整数型
.程序集变量 按钮_1万行, 整数型
.程序集变量 按钮_5万行, 整数型
.程序集变量 按钮_10万行, 整数型
.程序集变量 按钮_滚动到底, 整数型
.程序集变量 虚拟总行数, 整数型

.子程序 _窗口_DataGridView虚拟模式示例_创建完毕
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集

窗口句柄 ＝ 创建Emoji窗口 ("DataGridView虚拟模式示例 - 大数据量测试", 800, 550)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ "\icon.ico")

' ========== 创建虚拟模式DataGridView ==========
' 虚拟模式=真，隔行变色=真
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 20, 600, 400, 真, 真, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE)

' ========== 添加列 ==========
表格_添加文本列_辅助 (表格句柄, "行号", 80)

文本字节集 ＝ { 240, 159, 134, 148, 32, 73, 68 }  ' 🆔 ID
表格_添加文本列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 100)

文本字节集 ＝ { 240, 159, 145, 164, 32, 229, 167, 147, 229, 144, 141 }  ' 👤 姓名
表格_添加文本列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 130)

文本字节集 ＝ { 240, 159, 147, 167, 32, 233, 130, 174, 231, 174, 177 }  ' 📧 邮箱
表格_添加文本列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 200)

文本字节集 ＝ { 240, 159, 146, 176, 32, 233, 135, 145, 233, 162, 157 }  ' 💰 金额
表格_添加文本列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 100)

' ========== 设置虚拟模式回调 ==========
表格_设置虚拟数据回调 (表格句柄, &虚拟数据回调)

' ========== 默认加载1万行 ==========
虚拟总行数 ＝ 10000
表格_设置虚拟行数 (表格句柄, 虚拟总行数)

' ========== 设置事件回调 ==========
表格_设置单元格点击回调 (表格句柄, &单元格点击回调)
表格_设置选择改变回调 (表格句柄, &选择改变回调)

' ========== 设置冻结首行 ==========
表格_设置冻结首行 (表格句柄, 真)

' ========== 创建操作按钮 ==========
emoji字节集 ＝ { 240, 159, 147, 138 }  ' 📊
文本字节集 ＝ 编码_Ansi到Utf8 ("加载1万行")
按钮_1万行 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 640, 20, 140, 40, #COLOR_PRIMARY)

emoji字节集 ＝ { 240, 159, 154, 128 }  ' 🚀
文本字节集 ＝ 编码_Ansi到Utf8 ("加载5万行")
按钮_5万行 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 640, 70, 140, 40, #COLOR_SUCCESS)

emoji字节集 ＝ { 240, 159, 146, 170 }  ' 💪
文本字节集 ＝ 编码_Ansi到Utf8 ("加载10万行")
按钮_10万行 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 640, 120, 140, 40, #COLOR_DANGER)

emoji字节集 ＝ { 226, 172, 135 }  ' ⬇
文本字节集 ＝ 编码_Ansi到Utf8 ("滚动到底部")
按钮_滚动到底 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 640, 170, 140, 40, #COLOR_INFO)

' ========== 创建状态标签 ==========
状态标签 ＝ 创建标签_辅助 (窗口句柄, 20, 440, 760, 80, "虚拟模式示例 - 仅渲染可见行，支持海量数据。当前: 10000行", #COLOR_TEXT_PRIMARY, #COLOR_BG_LIGHT, "Microsoft YaHei UI", 13, 假, 假, 假, 0, 真)

' 设置按钮回调
设置按钮点击回调 (&按钮点击回调)

运行消息循环 ()


.子程序 虚拟数据回调, 整数型, , stdcall
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 缓冲区指针, 整数型
.参数 缓冲区大小, 整数型
.局部变量 文本, 文本型
.局部变量 字节集, 字节集
.局部变量 实际长度, 整数型

' 根据列索引生成不同数据
.判断开始 (列 ＝ 0)
    ' 行号
    文本 ＝ 到文本 (行 ＋ 1)
.判断 (列 ＝ 1)
    ' ID
    文本 ＝ "ID-" ＋ 到文本 (10000 ＋ 行)
.判断 (列 ＝ 2)
    ' 姓名（循环使用几个名字）
    .判断开始 (行 ％ 5 ＝ 0)
        文本 ＝ "张三"
    .判断 (行 ％ 5 ＝ 1)
        文本 ＝ "李四"
    .判断 (行 ％ 5 ＝ 2)
        文本 ＝ "王五"
    .判断 (行 ％ 5 ＝ 3)
        文本 ＝ "赵六"
    .默认
        文本 ＝ "孙七"
    .判断结束
.判断 (列 ＝ 3)
    ' 邮箱
    文本 ＝ "user" ＋ 到文本 (行) ＋ "@example.com"
.判断 (列 ＝ 4)
    ' 金额
    文本 ＝ 到文本 ((行 ＋ 1) × 13 ％ 9999) ＋ ".00"
.默认
    文本 ＝ ""
.判断结束

' 转换为UTF-8并写入缓冲区
字节集 ＝ 编码_Ansi到Utf8 (文本)
实际长度 ＝ 取字节集长度 (字节集)

.如果真 (实际长度 ＞ 缓冲区大小)
    实际长度 ＝ 缓冲区大小
.如果真结束

.如果真 (实际长度 ＞ 0)
    RtlMoveMemory (缓冲区指针, 取变量数据地址 (字节集), 实际长度)
.如果真结束

返回 (实际长度)


.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型

.如果真 (按钮ID ＝ 按钮_1万行)
    虚拟总行数 ＝ 10000
    表格_设置虚拟行数 (表格句柄, 虚拟总行数)
    表格_刷新 (表格句柄)
    更新状态 ("已加载 10,000 行数据（虚拟模式）")
.如果真结束

.如果真 (按钮ID ＝ 按钮_5万行)
    虚拟总行数 ＝ 50000
    表格_设置虚拟行数 (表格句柄, 虚拟总行数)
    表格_刷新 (表格句柄)
    更新状态 ("已加载 50,000 行数据（虚拟模式）")
.如果真结束

.如果真 (按钮ID ＝ 按钮_10万行)
    虚拟总行数 ＝ 100000
    表格_设置虚拟行数 (表格句柄, 虚拟总行数)
    表格_刷新 (表格句柄)
    更新状态 ("已加载 100,000 行数据（虚拟模式）")
.如果真结束

.如果真 (按钮ID ＝ 按钮_滚动到底)
    ' 选中最后一行，触发滚动
    表格_设置选中单元格 (表格句柄, 虚拟总行数 － 1, 0)
    表格_刷新 (表格句柄)
    更新状态 ("已滚动到第 " ＋ 到文本 (虚拟总行数) ＋ " 行")
.如果真结束


.子程序 单元格点击回调, , , stdcall
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型

更新状态 ("点击: 行=" ＋ 到文本 (行) ＋ " 列=" ＋ 到文本 (列) ＋ " (共" ＋ 到文本 (虚拟总行数) ＋ "行)")


.子程序 选择改变回调, , , stdcall
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型

更新状态 ("选中: 行=" ＋ 到文本 (行) ＋ " 列=" ＋ 到文本 (列) ＋ " | 总行数: " ＋ 到文本 (虚拟总行数))


.子程序 更新状态
.参数 消息, 文本型

设置标签文本_辅助 (状态标签, 消息)
