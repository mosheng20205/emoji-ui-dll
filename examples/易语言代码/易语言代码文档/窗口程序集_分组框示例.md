.版本 2

.程序集 窗口程序集_窗口_分组框示例
.程序集变量 窗口句柄, 整数型
.程序集变量 分组框1, 整数型
.程序集变量 分组框2, 整数型
.程序集变量 单选按钮1, 整数型
.程序集变量 单选按钮2, 整数型
.程序集变量 单选按钮3, 整数型
.程序集变量 复选框1, 整数型
.程序集变量 复选框2, 整数型
.程序集变量 复选框3, 整数型
.程序集变量 标签1, 整数型
.程序集变量 按钮1, 整数型
.程序集变量 按钮2, 整数型
.程序集变量 按钮3, 整数型
.程序集变量 按钮4, 整数型

.子程序 _窗口_分组框示例_创建完毕

窗口句柄 ＝ 创建Emoji窗口 ("分组框示例 - Element UI风格", 600, 550)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ "\icon.ico")

' 创建第一个分组框（性别选择）
分组框1 ＝ 创建分组框_辅助 (窗口句柄, 20, 20, 260, 150, "性别选择", #COLOR_BORDER_BASE, #白色)

' 在分组框1中创建单选按钮
单选按钮1 ＝ 创建单选按钮_辅助 (窗口句柄, 40, 60, 100, 30, "男", 1, 真, #黑色, #白色)
单选按钮2 ＝ 创建单选按钮_辅助 (窗口句柄, 40, 100, 100, 30, "女", 1, 假, #黑色, #白色)
单选按钮3 ＝ 创建单选按钮_辅助 (窗口句柄, 40, 140, 100, 30, "保密", 1, 假, #黑色, #白色)

' 添加单选按钮到分组框1
添加子控件到分组框 (分组框1, 单选按钮1)
添加子控件到分组框 (分组框1, 单选按钮2)
添加子控件到分组框 (分组框1, 单选按钮3)

' 设置单选按钮回调
设置单选按钮回调 (单选按钮1, &单选按钮回调)
设置单选按钮回调 (单选按钮2, &单选按钮回调)
设置单选按钮回调 (单选按钮3, &单选按钮回调)

' 创建第二个分组框（兴趣爱好）
分组框2 ＝ 创建分组框_辅助 (窗口句柄, 300, 20, 280, 150, "兴趣爱好（多选）", #COLOR_BORDER_BASE, #白色)

' 在分组框2中创建复选框
复选框1 ＝ 创建复选框_辅助 (窗口句柄, 320, 60, 120, 30, "编程", 假, #黑色, #白色)
复选框2 ＝ 创建复选框_辅助 (窗口句柄, 320, 100, 120, 30, "音乐", 假, #黑色, #白色)
复选框3 ＝ 创建复选框_辅助 (窗口句柄, 320, 140, 120, 30, "运动", 假, #黑色, #白色)

' 添加复选框到分组框2
添加子控件到分组框 (分组框2, 复选框1)
添加子控件到分组框 (分组框2, 复选框2)
添加子控件到分组框 (分组框2, 复选框3)

' 设置复选框回调
设置复选框回调 (复选框1, &复选框回调)
设置复选框回调 (复选框2, &复选框回调)
设置复选框回调 (复选框3, &复选框回调)

' 创建状态显示标签
标签1 ＝ 创建标签_辅助 (窗口句柄, 20, 200, 560, 250, "当前选择：" ＋ #换行符 ＋ "性别：男" ＋ #换行符 ＋ "兴趣爱好：无", #黑色, #COLOR_TEXT_PLACEHOLDER, , , , , , , 真)

' 创建控制按钮
按钮1 ＝ 创建Emoji按钮_辅助 (窗口句柄, "", "禁用分组框1", 20, 460, 120, 30, #COLOR_DANGER)
按钮2 ＝ 创建Emoji按钮_辅助 (窗口句柄, "", "启用分组框1", 150, 460, 120, 30, #COLOR_SUCCESS)
按钮3 ＝ 创建Emoji按钮_辅助 (窗口句柄, "", "隐藏分组框2", 280, 460, 120, 30, #COLOR_WARNING)
按钮4 ＝ 创建Emoji按钮_辅助 (窗口句柄, "", "显示分组框2", 410, 460, 120, 30, #COLOR_INFO)

' 设置按钮点击回调（全局回调，所有按钮共享）
设置按钮点击回调 (&按钮点击回调)

运行消息循环 ()


.子程序 _启动窗口_将被销毁

销毁窗口 (窗口句柄)

.子程序 单选按钮回调, , , stdcall
.参数 句柄, 整数型
.参数 分组ID, 整数型
.参数 选中, 逻辑型
.局部变量 性别文本, 文本型
.局部变量 兴趣文本, 文本型

调试输出 ("单选按钮回调被触发！句柄=" ＋ 到文本 (句柄) ＋ ", 分组ID=" ＋ 到文本 (分组ID) ＋ ", 选中=" ＋ 到文本 (选中))

.如果真 (选中)
    .如果真 (句柄 ＝ 单选按钮1)
        性别文本 ＝ "男"
    .如果真结束
    .如果真 (句柄 ＝ 单选按钮2)
        性别文本 ＝ "女"
    .如果真结束
    .如果真 (句柄 ＝ 单选按钮3)
        性别文本 ＝ "保密"
    .如果真结束

    ' 获取兴趣爱好
    兴趣文本 ＝ 获取兴趣文本 ()

    设置标签文本_辅助 (标签1, "当前选择：" ＋ #换行符 ＋ "性别：" ＋ 性别文本 ＋ #换行符 ＋ "兴趣爱好：" ＋ 兴趣文本)
.如果真结束


.子程序 复选框回调, , , stdcall
.参数 句柄, 整数型
.参数 选中, 逻辑型
.局部变量 性别文本, 文本型
.局部变量 兴趣文本, 文本型

调试输出 ("复选框回调被触发！句柄=" ＋ 到文本 (句柄) ＋ ", 选中=" ＋ 到文本 (选中))

' 获取性别
.如果真 (获取单选按钮状态 (单选按钮1))
    性别文本 ＝ "男"
.如果真结束
.如果真 (获取单选按钮状态 (单选按钮2))
    性别文本 ＝ "女"
.如果真结束
.如果真 (获取单选按钮状态 (单选按钮3))
    性别文本 ＝ "保密"
.如果真结束

' 获取兴趣爱好
兴趣文本 ＝ 获取兴趣文本 ()

设置标签文本_辅助 (标签1, "当前选择：" ＋ #换行符 ＋ "性别：" ＋ 性别文本 ＋ #换行符 ＋ "兴趣爱好：" ＋ 兴趣文本)


.子程序 获取兴趣文本, 文本型
.局部变量 兴趣列表, 文本型, , "0"
.局部变量 兴趣文本, 文本型

.如果真 (获取复选框状态 (复选框1))
    加入成员 (兴趣列表, "编程")
.如果真结束
.如果真 (获取复选框状态 (复选框2))
    加入成员 (兴趣列表, "音乐")
.如果真结束
.如果真 (获取复选框状态 (复选框3))
    加入成员 (兴趣列表, "运动")
.如果真结束

.如果真 (取数组成员数 (兴趣列表) ＝ 0)
    兴趣文本 ＝ "无"
.否则
    兴趣文本 ＝ 数组到文本 (兴趣列表, "、")
.如果真结束

返回 (兴趣文本)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型

.如果真 (按钮ID ＝ 按钮1)
    启用分组框 (分组框1, 假)
.如果真结束

.如果真 (按钮ID ＝ 按钮2)
    启用分组框 (分组框1, 真)
.如果真结束

.如果真 (按钮ID ＝ 按钮3)
    显示分组框 (分组框2, 假)
.如果真结束

.如果真 (按钮ID ＝ 按钮4)
    显示分组框 (分组框2, 真)
.如果真结束

