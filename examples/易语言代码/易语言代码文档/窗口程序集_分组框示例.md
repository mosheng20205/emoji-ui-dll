.版本 2
.支持库 spec
.支持库 eAPI

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 分组框1, 整数型
.程序集变量 分组框2, 整数型
.程序集变量 标签_结果, 整数型
.程序集变量 按钮_获取标题, 整数型
.程序集变量 按钮_获取位置, 整数型
.程序集变量 按钮_获取可视, 整数型
.程序集变量 按钮_获取启用, 整数型
.程序集变量 按钮_获取颜色, 整数型
.程序集变量 按钮_获取全部, 整数型
.程序集变量 按钮_隐藏分组框, 整数型
.程序集变量 按钮_显示分组框, 整数型
.程序集变量 按钮_禁用分组框, 整数型
.程序集变量 按钮_启用分组框, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 utf8字节集, 字节集
.局部变量 按钮文本字节集, 字节集
.局部变量 字体名utf8, 字节集

' 创建窗口
utf8字节集 ＝ 编码_Ansi到Utf8 ("分组框属性操作示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 820, 680, 到RGB (70, 130, 180))

' 创建演示用分组框1（性别选择）
utf8字节集 ＝ 编码_Ansi到Utf8 ("性别选择")
分组框1 ＝ 创建分组框 (主窗口句柄, 20, 20, 260, 150, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 100, 149, 237), 到ARGB (255, 245, 248, 255))

' 创建演示用分组框2（兴趣爱好）
utf8字节集 ＝ 编码_Ansi到Utf8 ("兴趣爱好")
分组框2 ＝ 创建分组框 (主窗口句柄, 300, 20, 260, 150, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 100, 200, 100), 到ARGB (255, 245, 255, 245))

' 创建结果显示标签
utf8字节集 ＝ 编码_Ansi到Utf8 ("点击下方按钮查看分组框属性")
字体名utf8 ＝ 编码_Ansi到Utf8 ("微软雅黑")
标签_结果 ＝ 创建标签 (主窗口句柄, 20, 190, 780, 220, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 0, 0, 0), 到ARGB (255, 245, 245, 245), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

' 创建属性获取按钮（第一行）
' "获取标题"
utf8字节集 ＝ { 240, 159, 147, 157 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取标题")
按钮_获取标题 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 430, 130, 40, 到ARGB (255, 100, 180, 100))

' "获取位置"
utf8字节集 ＝ { 240, 159, 147, 144 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取位置")
按钮_获取位置 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 165, 430, 130, 40, 到ARGB (255, 100, 180, 100))

' "获取可视"
utf8字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取可视")
按钮_获取可视 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 310, 430, 130, 40, 到ARGB (255, 100, 180, 100))

' "获取启用"
utf8字节集 ＝ { 226, 156, 133 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取启用")
按钮_获取启用 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 455, 430, 130, 40, 到ARGB (255, 100, 180, 100))

' "获取颜色"
utf8字节集 ＝ { 240, 159, 142, 168 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取颜色")
按钮_获取颜色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 600, 430, 130, 40, 到ARGB (255, 100, 180, 100))

' "获取全部属性"
utf8字节集 ＝ { 240, 159, 147, 138 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取全部属性")
按钮_获取全部 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 485, 200, 40, 到ARGB (255, 100, 100, 200))

' 创建控制按钮（第二行）
' "隐藏分组框1"
utf8字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("隐藏分组框1")
按钮_隐藏分组框 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 240, 485, 130, 40, 到ARGB (255, 200, 100, 100))

' "显示分组框1"
utf8字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("显示分组框1")
按钮_显示分组框 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 385, 485, 130, 40, 到ARGB (255, 100, 180, 100))

' "禁用分组框2"
utf8字节集 ＝ { 226, 157, 140 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("禁用分组框2")
按钮_禁用分组框 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 530, 485, 130, 40, 到ARGB (255, 200, 100, 100))

' "启用分组框2"
utf8字节集 ＝ { 226, 156, 133 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("启用分组框2")
按钮_启用分组框 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 675, 485, 130, 40, 到ARGB (255, 100, 180, 100))

' 设置按钮点击回调
设置按钮点击回调 (&按钮点击回调)

' 设置消息循环主窗口
设置消息循环主窗口 (主窗口句柄)

' 运行消息循环
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 标题长度, 整数型
.局部变量 标题缓冲区, 字节集
.局部变量 结果字节集, 字节集
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型
.局部变量 可视状态, 整数型
.局部变量 启用状态, 整数型
.局部变量 边框颜色, 整数型
.局部变量 背景色, 整数型

.判断开始 (按钮ID ＝ 按钮_获取标题)
    ' 获取分组框1标题（两次调用模式）
    标题长度 ＝ 获取分组框标题 (分组框1, 0, 0)
    .如果 (标题长度 ＞ 0)
        标题缓冲区 ＝ 取空白字节集 (标题长度)
        获取分组框标题 (分组框1, 取变量数据地址 (标题缓冲区), 标题长度)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取分组框标题】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("分组框1标题：") ＋ 标题缓冲区 ＋ { 10 } ＋ 编码_Ansi到Utf8 ("字节长度：") ＋ 编码_Ansi到Utf8 (到文本 (标题长度))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("获取分组框标题失败（返回值：") ＋ 编码_Ansi到Utf8 (到文本 (标题长度)) ＋ 编码_Ansi到Utf8 ("）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取位置)
    ' 获取分组框1位置和大小
    .如果 (获取分组框位置 (分组框1, X, Y, 宽度, 高度) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取分组框位置】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("X坐标：") ＋ 编码_Ansi到Utf8 (到文本 (X)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("Y坐标：") ＋ 编码_Ansi到Utf8 (到文本 (Y)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("宽度：") ＋ 编码_Ansi到Utf8 (到文本 (宽度)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("高度：") ＋ 编码_Ansi到Utf8 (到文本 (高度))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("获取分组框位置失败")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取可视)
    ' 获取分组框1可视状态
    可视状态 ＝ 获取分组框可视状态 (分组框1)
    .判断开始 (可视状态 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取分组框可视状态】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("分组框1状态：可见（1）")
    .判断 (可视状态 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取分组框可视状态】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("分组框1状态：不可见（0）")
    .默认
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取分组框可视状态】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("获取失败（-1）")
    .判断结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取启用)
    ' 获取分组框2启用状态
    启用状态 ＝ 获取分组框启用状态 (分组框2)
    .判断开始 (启用状态 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取分组框启用状态】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("分组框2状态：已启用（1）")
    .判断 (启用状态 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取分组框启用状态】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("分组框2状态：已禁用（0）")
    .默认
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取分组框启用状态】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("获取失败（-1）")
    .判断结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取颜色)
    ' 获取分组框1颜色
    .如果 (获取分组框颜色 (分组框1, 边框颜色, 背景色) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取分组框颜色】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("边框颜色（ARGB）：") ＋ 编码_Ansi到Utf8 (到文本 (边框颜色)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("背景色（ARGB）：") ＋ 编码_Ansi到Utf8 (到文本 (背景色)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("边框颜色十六进制：#") ＋ 编码_Ansi到Utf8 (_十六进制 (边框颜色)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("背景色十六进制：#") ＋ 编码_Ansi到Utf8 (_十六进制 (背景色))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("获取分组框颜色失败")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取全部)
    ' 获取分组框1全部属性
    结果字节集 ＝ 编码_Ansi到Utf8 ("【分组框1全部属性汇总】") ＋ { 10 }

    ' 获取标题
    标题长度 ＝ 获取分组框标题 (分组框1, 0, 0)
    .如果真 (标题长度 ＞ 0)
        标题缓冲区 ＝ 取空白字节集 (标题长度)
        获取分组框标题 (分组框1, 取变量数据地址 (标题缓冲区), 标题长度)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("标题：") ＋ 标题缓冲区 ＋ { 10 }
    .如果真结束

    ' 获取位置
    .如果真 (获取分组框位置 (分组框1, X, Y, 宽度, 高度) ＝ 0)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("位置：(") ＋ 编码_Ansi到Utf8 (到文本 (X)) ＋ 编码_Ansi到Utf8 (", ") ＋ 编码_Ansi到Utf8 (到文本 (Y)) ＋ 编码_Ansi到Utf8 (")  大小：") ＋ 编码_Ansi到Utf8 (到文本 (宽度)) ＋ 编码_Ansi到Utf8 ("x") ＋ 编码_Ansi到Utf8 (到文本 (高度)) ＋ { 10 }
    .如果真结束

    ' 获取可视状态
    可视状态 ＝ 获取分组框可视状态 (分组框1)
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("可视：") ＋ 编码_Ansi到Utf8 (到文本 (可视状态)) ＋ { 10 }

    ' 获取启用状态
    启用状态 ＝ 获取分组框启用状态 (分组框1)
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("启用：") ＋ 编码_Ansi到Utf8 (到文本 (启用状态)) ＋ { 10 }

    ' 获取颜色
    .如果真 (获取分组框颜色 (分组框1, 边框颜色, 背景色) ＝ 0)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("边框色：#") ＋ 编码_Ansi到Utf8 (_十六进制 (边框颜色)) ＋ 编码_Ansi到Utf8 ("  背景色：#") ＋ 编码_Ansi到Utf8 (_十六进制 (背景色))
    .如果真结束

    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_隐藏分组框)
    显示分组框 (分组框1, 假)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已隐藏分组框1，可视状态：") ＋ 编码_Ansi到Utf8 (到文本 (获取分组框可视状态 (分组框1)))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_显示分组框)
    显示分组框 (分组框1, 真)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已显示分组框1，可视状态：") ＋ 编码_Ansi到Utf8 (到文本 (获取分组框可视状态 (分组框1)))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_禁用分组框)
    启用分组框 (分组框2, 假)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已禁用分组框2，启用状态：") ＋ 编码_Ansi到Utf8 (到文本 (获取分组框启用状态 (分组框2)))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_启用分组框)
    启用分组框 (分组框2, 真)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已启用分组框2，启用状态：") ＋ 编码_Ansi到Utf8 (到文本 (获取分组框启用状态 (分组框2)))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.默认

.判断结束


.子程序 _十六进制, 文本型
.参数 数值, 整数型

返回 (格式化文本 ("%08X", 数值))

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
