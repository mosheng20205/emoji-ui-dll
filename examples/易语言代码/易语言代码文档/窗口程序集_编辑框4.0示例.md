.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 编辑框句柄, 整数型
.程序集变量 密码编辑框句柄
.程序集变量 标签_状态, 整数型
.程序集变量 按钮_读取文本, 整数型
.程序集变量 按钮_写入文本, 整数型
.程序集变量 按钮_读取字体, 整数型
.程序集变量 按钮_设置字体, 整数型
.程序集变量 按钮_读取字号, 整数型
.程序集变量 按钮_设置字号, 整数型
.程序集变量 按钮_读取对齐, 整数型
.程序集变量 按钮_设为左对齐, 整数型
.程序集变量 按钮_设为居中, 整数型
.程序集变量 按钮_设为右对齐, 整数型
.程序集变量 按钮_读取字色, 整数型
.程序集变量 按钮_设置字色, 整数型
.程序集变量 按钮_切换启用, 整数型
.程序集变量 按钮_切换可视, 整数型
.程序集变量 编辑框已启用, 逻辑型
.程序集变量 编辑框已可视, 逻辑型
.程序集变量 当前字体为雅黑, 逻辑型
.程序集变量 当前字号, 整数型
.程序集变量 当前文字为蓝色, 逻辑型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 图标字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 (“编辑框4.0示例”)
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 100, 100, 920, 620, 到RGB (64, 158, 255), 0)

图标字节集 ＝ #图片_icon
设置窗口图标_字节集 (主窗口句柄, 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))

字体字节集 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
文本字节集 ＝ 编码_Ansi到Utf8 (“这页单独演示 EditBox 的文本、字体、字号、对齐、颜色和状态控制。”)
创建标签 (主窗口句柄, 24, 10, 860, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 假)

文本字节集 ＝ 编码_Ansi到Utf8 (“这是一个单行编辑框，可继续输入。”)
编辑框句柄 ＝ 创建编辑框 (主窗口句柄, 24, 48, 836, 44, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 14, 假, 假, 假, 0, 假, 假, 假, 真, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“状态输出会显示在这里。可读取/设置文本、字体、字号、对齐和颜色。”)
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 296, 860, 250, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“316466”)
' 原生 EditBox 在 Win32 下密码框与单行垂直居中存在样式冲突，这里改用 D2D 自绘编辑框来同时满足“密文 + 垂直居中”
密码编辑框句柄 ＝ 创建D2D彩色Emoji编辑框 (主窗口句柄, 24, 400, 836, 44, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 14, 假, 假, 假, 1, 假, 假, 真, 真, 真)


emoji字节集 ＝ { 240, 159, 147, 157 }
文本字节集 ＝ 编码_Ansi到Utf8 (“读取文本”)
按钮_读取文本 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 126, 126, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 226, 156, 143, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“写入文本”)
按钮_写入文本 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 166, 126, 126, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 240, 159, 148, 164 }
文本字节集 ＝ 编码_Ansi到Utf8 (“读取字体”)
按钮_读取字体 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 308, 126, 126, 38, 到ARGB (255, 144, 147, 153))

emoji字节集 ＝ { 240, 159, 148, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“设置字体”)
按钮_设置字体 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 450, 126, 126, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 240, 159, 147, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“读取字号”)
按钮_读取字号 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 592, 126, 126, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 240, 159, 147, 144 }
文本字节集 ＝ 编码_Ansi到Utf8 (“设置字号”)
按钮_设置字号 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 734, 126, 126, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 240, 159, 147, 144 }
文本字节集 ＝ 编码_Ansi到Utf8 (“读取对齐”)
按钮_读取对齐 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 178, 126, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 226, 172, 133 }
文本字节集 ＝ 编码_Ansi到Utf8 (“左对齐”)
按钮_设为左对齐 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 166, 178, 126, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 226, 134, 148 }
文本字节集 ＝ 编码_Ansi到Utf8 (“设为居中”)
按钮_设为居中 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 308, 178, 126, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 226, 158, 161 }
文本字节集 ＝ 编码_Ansi到Utf8 (“右对齐”)
按钮_设为右对齐 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 450, 178, 126, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 240, 159, 142, 168 }
文本字节集 ＝ 编码_Ansi到Utf8 (“读取字色”)
按钮_读取字色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 592, 178, 126, 38, 到ARGB (255, 144, 147, 153))

emoji字节集 ＝ { 240, 159, 150, 140 }
文本字节集 ＝ 编码_Ansi到Utf8 (“设置字色”)
按钮_设置字色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 734, 178, 126, 38, 到ARGB (255, 245, 108, 108))

emoji字节集 ＝ { 226, 154, 161 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换启用”)
按钮_切换启用 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 230, 126, 38, 到ARGB (255, 245, 108, 108))

emoji字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换可视”)
按钮_切换可视 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 166, 230, 126, 38, 到ARGB (255, 144, 147, 153))

编辑框已启用 ＝ 真
编辑框已可视 ＝ 真
当前字体为雅黑 ＝ 真
当前字号 ＝ 14
当前文字为蓝色 ＝ 假

设置编辑框按键回调 (编辑框句柄, 到整数 (&编辑框按键回调))
设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 文本长度, 整数型
.局部变量 缓冲区长度, 整数型
.局部变量 文本缓冲区, 字节集
.局部变量 结果字节集, 字节集
.局部变量 字体缓冲区, 字节集
.局部变量 字体长度, 整数型
.局部变量 字体大小, 整数型
.局部变量 粗体, 整数型
.局部变量 斜体, 整数型
.局部变量 下划线, 整数型
.局部变量 对齐方式, 整数型
.局部变量 前景色, 整数型
.局部变量 背景色, 整数型

.判断开始 (按钮ID ＝ 按钮_读取文本)
    文本长度 ＝ 获取编辑框文本 (编辑框句柄, 0, 0)
    .如果 (文本长度 ＞ 0)
        缓冲区长度 ＝ 文本长度 ＋ 1
        文本缓冲区 ＝ 取空白字节集 (缓冲区长度)
        获取编辑框文本 (编辑框句柄, 取变量数据地址 (文本缓冲区), 缓冲区长度)
        结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框文本：”) ＋ 取字节集左边 (文本缓冲区, 文本长度)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框当前为空，或读取结果为空。”)
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_写入文本)
    结果字节集 ＝ 编码_Ansi到Utf8 (“新的输入内容：第 ”) ＋ 编码_Ansi到Utf8 (到文本 (取随机数 (1, 99))) ＋ 编码_Ansi到Utf8 (“ 次写入”)
    设置编辑框文本 (编辑框句柄, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_读取字体)
    字体长度 ＝ 获取编辑框字体 (编辑框句柄, 0, 0, 字体大小, 粗体, 斜体, 下划线)
    .如果 (字体长度 ＞ 0)
        字体缓冲区 ＝ 取空白字节集 (字体长度)
        获取编辑框字体 (编辑框句柄, 取变量数据地址 (字体缓冲区), 字体长度, 字体大小, 粗体, 斜体, 下划线)
        结果字节集 ＝ 编码_Ansi到Utf8 (“当前字体：”) ＋ 取字节集左边 (字体缓冲区, 字体长度) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“粗体：”) ＋ 编码_Ansi到Utf8 (到文本 (粗体)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“斜体：”) ＋ 编码_Ansi到Utf8 (到文本 (斜体)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“下划线：”) ＋ 编码_Ansi到Utf8 (到文本 (下划线))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“读取字体失败。”)
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_设置字体)
    当前字体为雅黑 ＝ 取反 (当前字体为雅黑)
    .如果 (当前字体为雅黑)
        字体缓冲区 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
    .否则
        字体缓冲区 ＝ 编码_Ansi到Utf8 (“宋体”)
    .如果结束
    设置编辑框字体 (编辑框句柄, 取变量数据地址 (字体缓冲区), 取字节集长度 (字体缓冲区), 当前字号, 假, 假, 假)
    结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框字体已设置为：”) ＋ 字体缓冲区 ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前字号：”) ＋ 编码_Ansi到Utf8 (到文本 (当前字号)) ＋ 编码_Ansi到Utf8 (“ px”)
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_读取字号)
    字体长度 ＝ 获取编辑框字体 (编辑框句柄, 0, 0, 字体大小, 粗体, 斜体, 下划线)
    .如果 (字体长度 ＞ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“当前字号：”) ＋ 编码_Ansi到Utf8 (到文本 (字体大小)) ＋ 编码_Ansi到Utf8 (“ px”)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“读取字号失败。”)
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_设置字号)
    .如果 (当前字号 ＝ 14)
        当前字号 ＝ 18
        设置编辑框位置 (编辑框句柄, 24, 58, 836, 48)
    .否则
        当前字号 ＝ 14
        设置编辑框位置 (编辑框句柄, 24, 58, 836, 44)
    .如果结束
    .如果 (当前字体为雅黑)
        字体缓冲区 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
    .否则
        字体缓冲区 ＝ 编码_Ansi到Utf8 (“宋体”)
    .如果结束
    设置编辑框字体 (编辑框句柄, 取变量数据地址 (字体缓冲区), 取字节集长度 (字体缓冲区), 当前字号, 假, 假, 假)
    结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框字号已设置为：”) ＋ 编码_Ansi到Utf8 (到文本 (当前字号)) ＋ 编码_Ansi到Utf8 (“ px”)
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_读取对齐)
    对齐方式 ＝ 获取编辑框对齐方式 (编辑框句柄)
    .判断开始 (对齐方式 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“当前对齐：左对齐”)
    .判断 (对齐方式 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 (“当前对齐：居中”)
    .判断 (对齐方式 ＝ 2)
        结果字节集 ＝ 编码_Ansi到Utf8 (“当前对齐：右对齐”)
    .默认
        结果字节集 ＝ 编码_Ansi到Utf8 (“读取对齐方式失败。”)
    .判断结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_设为左对齐)
    设置编辑框对齐方式 (编辑框句柄, 0)
    结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框已设置为左对齐。”)
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_设为居中)
    设置编辑框对齐方式 (编辑框句柄, 1)
    结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框已设置为居中对齐。”)
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_设为右对齐)
    设置编辑框对齐方式 (编辑框句柄, 2)
    结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框已设置为右对齐。”)
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_读取字色)
    .如果 (获取编辑框颜色 (编辑框句柄, 前景色, 背景色) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 (“当前文字颜色：”) ＋ 编码_Ansi到Utf8 (到文本 (前景色)) ＋ { 10 } ＋ 编码_Ansi到Utf8 (“当前背景色：”) ＋ 编码_Ansi到Utf8 (到文本 (背景色))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“读取文字颜色失败。”)
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_设置字色)
    .如果 (获取编辑框颜色 (编辑框句柄, 前景色, 背景色) ＝ 0)
        当前文字为蓝色 ＝ 取反 (当前文字为蓝色)
        .如果 (当前文字为蓝色)
            前景色 ＝ 到ARGB (255, 64, 158, 255)
        .否则
            前景色 ＝ 到ARGB (255, 48, 49, 51)
        .如果结束
        设置编辑框颜色 (编辑框句柄, 前景色, 背景色)
        结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框文字颜色已设置为：”) ＋ 编码_Ansi到Utf8 (到文本 (前景色))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“设置文字颜色失败。”)
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换启用)
    编辑框已启用 ＝ 取反 (编辑框已启用)
    启用编辑框 (编辑框句柄, 编辑框已启用)
    .如果 (编辑框已启用)
        结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框已重新启用。”)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框已禁用。”)
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换可视)
    编辑框已可视 ＝ 取反 (编辑框已可视)
    显示编辑框 (编辑框句柄, 编辑框已可视)
    .如果 (编辑框已可视)
        结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框已显示。”)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 (“编辑框已隐藏。”)
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.默认

.判断结束


.子程序 编辑框按键回调, , , stdcall
.参数 hEdit, 整数型
.参数 key_code, 整数型
.参数 key_down, 整数型
.参数 shift, 整数型
.参数 ctrl, 整数型
.参数 alt, 整数型
.局部变量 结果字节集, 字节集

.如果 (key_down ＝ 1)
    结果字节集 ＝ 编码_Ansi到Utf8 (“按键回调：key=”) ＋ 编码_Ansi到Utf8 (到文本 (key_code)) ＋ 编码_Ansi到Utf8 (“ shift=”) ＋ 编码_Ansi到Utf8 (到文本 (shift)) ＋ 编码_Ansi到Utf8 (“ ctrl=”) ＋ 编码_Ansi到Utf8 (到文本 (ctrl)) ＋ 编码_Ansi到Utf8 (“ alt=”) ＋ 编码_Ansi到Utf8 (到文本 (alt))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
.否则

.如果结束


.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
