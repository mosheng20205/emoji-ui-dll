.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_流式布局示例
.程序集变量 窗口句柄, 整数型
.程序集变量 按钮1, 整数型
.程序集变量 按钮2, 整数型
.程序集变量 按钮3, 整数型
.程序集变量 按钮4, 整数型
.程序集变量 按钮5, 整数型
.程序集变量 按钮6, 整数型
.程序集变量 标签1, 整数型

.子程序 _窗口_流式布局示例_创建完毕
.局部变量 utf8字节集, 字节集

utf8字节集 ＝ 编码_Ansi到Utf8 (“流式布局示例 - 水平自动换行”)
窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 500, 400, 0)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ “\icon.ico”)

' 设置水平流式布局，间距10像素
设置布局管理器 (窗口句柄, #LAYOUT_FLOW_H, 0, 0, 10)
设置布局内边距 (窗口句柄, 20, 20, 20, 20)

' 创建多个按钮，布局管理器会自动排列
按钮1 ＝ 创建Emoji按钮_辅助 (窗口句柄, “”, “按钮A”, 0, 0, 100, 40, #COLOR_PRIMARY)
按钮2 ＝ 创建Emoji按钮_辅助 (窗口句柄, “”, “按钮B”, 0, 0, 120, 40, #COLOR_SUCCESS)
按钮3 ＝ 创建Emoji按钮_辅助 (窗口句柄, “”, “按钮C”, 0, 0, 80, 40, #COLOR_WARNING)
按钮4 ＝ 创建Emoji按钮_辅助 (窗口句柄, “”, “按钮D”, 0, 0, 130, 40, #COLOR_DANGER)
按钮5 ＝ 创建Emoji按钮_辅助 (窗口句柄, “”, “按钮E”, 0, 0, 90, 40, #COLOR_INFO)
按钮6 ＝ 创建Emoji按钮_辅助 (窗口句柄, “”, “按钮F”, 0, 0, 110, 40, #COLOR_PRIMARY)

' 创建状态标签
标签1 ＝ 创建标签_辅助 (窗口句柄, 0, 0, 460, 60, “拖动窗口边缘改变大小，按钮会自动换行排列”, #COLOR_TEXT_REGULAR, #COLOR_BG_WHITE, , , , , , , )

' 将控件添加到布局管理器
添加控件到布局 (窗口句柄, 按钮1)
添加控件到布局 (窗口句柄, 按钮2)
添加控件到布局 (窗口句柄, 按钮3)
添加控件到布局 (窗口句柄, 按钮4)
添加控件到布局 (窗口句柄, 按钮5)
添加控件到布局 (窗口句柄, 按钮6)
添加控件到布局 (窗口句柄, 标签1)

' 设置标签水平拉伸
设置控件布局属性 (标签1, 0, 10, 0, 0, #DOCK_NONE, 真, 假)

' 立即应用布局
更新布局 (窗口句柄)

' 设置按钮点击回调
设置按钮点击回调 (&按钮点击回调)

运行消息循环 ()


.子程序 _启动窗口_将被销毁

销毁窗口 (窗口句柄)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型

调试输出 (“按钮被点击: ” ＋ 到文本 (按钮ID))
