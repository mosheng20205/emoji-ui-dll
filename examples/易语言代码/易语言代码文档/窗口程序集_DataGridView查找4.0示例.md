.版本 2
.支持库 spec

.程序集 程序集1

.子程序 _启动子程序, 整数型
.局部变量 行索引, 整数型
.局部变量 emoji字节集, 字节集
.局部变量 图标字节集, 字节集

窗口句柄 ＝ 创建Emoji窗口 (“DataGridView查找4.0示例”, 80, 40, 1140, 780)
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 20, 1080, 530, 假, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
' 设置窗口图标 (窗口句柄, 取运行目录 () ＋ “\icon.ico”)  ' 这是一种写法
图标字节集 ＝ #图片_icon
设置窗口图标_字节集 (窗口句柄, 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))  ' 这是一种写法

DG_添加文本列 (表格句柄, “编号”, 120)
DG_添加文本列 (表格句柄, “姓名”, 180)
DG_添加文本列 (表格句柄, “城市”, 180)
DG_添加文本列 (表格句柄, “岗位”, 180)
DG_添加文本列 (表格句柄, “说明”, 380)

添加示例行 (“1001”, “张三”, “深圳”, “前端开发”, “用于查找'深圳'和'开发'。”)
添加示例行 (“1002”, “李四”, “杭州”, “测试开发”, “仍然包含'开发'。”)
添加示例行 (“1003”, “王五”, “深圳”, “产品经理”, “用于查找下一个'深圳'。”)
添加示例行 (“1004”, “赵六”, “成都”, “后端开发”, “继续匹配'开发'。”)
添加示例行 (“1005”, “孙七”, “上海”, “设计师”, “不参与命中。”)

表格_设置冻结首行 (表格句柄, 真)
表格_设置颜色 (表格句柄, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 到ARGB (255, 245, 247, 250), 到ARGB (255, 48, 49, 51), 到ARGB (255, 160, 210, 255), 到ARGB (255, 223, 236, 255), 到ARGB (255, 235, 238, 245))
最后找到行 ＝ -1
最后找到列 ＝ -1
当前查找词 ＝ ""

emoji字节集 ＝ { 240, 159, 148, 142 }  ' ??
按钮_查找深圳 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, “查找 深圳”, 20, 600, 160, 44, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 148, 142 }  ' ??
按钮_查找开发 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, “查找 开发”, 196, 600, 160, 44, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 226, 158, 161 }  ' ?
按钮_查找下一条 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, “查找下一条”, 372, 600, 170, 44, 到ARGB (255, 230, 162, 60))

状态标签 ＝ 创建标签_辅助 (窗口句柄, 20, 660, 1080, 60, "状态：点击按钮触发 DataGrid_FindText，命中后会使用更明显的选中色高亮单元格。", 到ARGB (255, 48, 49, 51), 到ARGB (255, 244, 247, 250), "Microsoft YaHei UI", 11, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型

.如果真 (按钮ID ＝ 按钮_查找深圳)
    当前查找词 ＝ “深圳”
    执行查找 (当前查找词, 假)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_查找开发)
    当前查找词 ＝ “开发”
    执行查找 (当前查找词, 假)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_查找下一条)
    .如果 (当前查找词 ＝ “”)
        写状态 (“状态：请先点击'查找 深圳'或'查找 开发'。”)
    .否则
        执行查找 (当前查找词, 真)
    .如果结束

.如果真结束


.子程序 执行查找
.参数 关键字, 文本型
.参数 下一条, 逻辑型
.局部变量 字节集, 字节集
.局部变量 起始行, 整数型
.局部变量 起始列, 整数型
.局部变量 找到行, 整数型
.局部变量 找到列, 整数型
.局部变量 列数, 整数型

起始行 ＝ 0
起始列 ＝ 0
列数 ＝ 表格_获取列数 (表格句柄)

.如果 (下一条 且 最后找到行 ≥ 0)
    起始行 ＝ 最后找到行
    起始列 ＝ 最后找到列 ＋ 1
    .如果 (起始列 ≥ 列数)
        起始列 ＝ 0
        起始行 ＝ 起始行 ＋ 1
    .否则

    .如果结束
    .如果 (起始行 ≥ 表格_获取行数 (表格句柄))
        起始行 ＝ 0
    .否则

    .如果结束

.否则
.如果结束

字节集 ＝ 编码_Ansi到Utf8 (关键字)
.如果 (表格_查找文本 (表格句柄, 取变量数据地址 (字节集), 取字节集长度 (字节集), 起始行, 起始列, 假, 假, 真, 找到行, 找到列))
    最后找到行 ＝ 找到行
    最后找到列 ＝ 找到列
    表格_设置选中单元格 (表格句柄, 找到行, 找到列)
    表格_刷新 (表格句柄)
    写状态 (“状态：已找到 [” ＋ 到文本 (找到行) ＋ “,” ＋ 到文本 (找到列) ＋ “] => ” ＋ DG_取文本 (表格句柄, 找到行, 找到列))

.否则
    写状态 (“状态：未找到关键字 '” ＋ 关键字 ＋ “'。”)
.如果结束



.子程序 添加示例行
.参数 编号, 文本型
.参数 姓名, 文本型
.参数 城市, 文本型
.参数 岗位, 文本型
.参数 说明, 文本型
.局部变量 行索引, 整数型

行索引 ＝ 表格_添加行 (表格句柄)
DG_设置文本 (表格句柄, 行索引, 0, 编号)
DG_设置文本 (表格句柄, 行索引, 1, 姓名)
DG_设置文本 (表格句柄, 行索引, 2, 城市)
DG_设置文本 (表格句柄, 行索引, 3, 岗位)
DG_设置文本 (表格句柄, 行索引, 4, 说明)

.子程序 DG_添加文本列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集

字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加文本列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_设置文本
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集

字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置单元格文本 (表格, 行, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 DG_取文本, 文本型
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.局部变量 缓冲区, 字节集
.局部变量 长度, 整数型

长度 ＝ 表格_获取单元格文本 (表格, 行, 列, 0, 0)
.如果真 (长度 ＞ 0)
    缓冲区 ＝ 取空白字节集 (长度 ＋ 1)
    长度 ＝ 表格_获取单元格文本 (表格, 行, 列, 取变量数据地址 (缓冲区), 长度 ＋ 1)
    返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 长度)))
.如果真结束
返回 (“”)

.子程序 写状态
.参数 文本, 文本型

设置标签文本_UTF8 (状态标签, 文本)
