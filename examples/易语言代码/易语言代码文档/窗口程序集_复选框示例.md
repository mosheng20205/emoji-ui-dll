.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_复选框示例
.程序集变量 窗口句柄, 整数型
.程序集变量 复选框1, 整数型
.程序集变量 复选框2, 整数型
.程序集变量 复选框3, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 集_切换复选框按钮, 整数型
.程序集变量 集_获取所有状态按钮, 整数型

.子程序 _窗口_复选框示例_创建完毕
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集

' 创建自绘窗口
窗口句柄 ＝ 创建Emoji窗口 (“复选框示例 - Element UI风格”, 500, 600)
设置窗口图标 (窗口句柄, “icon.ico”)

' 创建标题标签
创建标签_辅助 (窗口句柄, 20, 20, 460, 30, “复选框控件示例”, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, “Microsoft YaHei UI”, 16, 真, 假, 假, 1, 假)

' 创建复选框1
复选框1 ＝ 创建复选框_辅助 (窗口句柄, 50, 80, 200, 30, “接受用户协议”, 假, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE)
设置复选框回调 (复选框1, 到整数 (&复选框回调))

' 创建复选框2
复选框2 ＝ 创建复选框_辅助 (窗口句柄, 50, 130, 200, 30, “记住密码”, 真, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE)
设置复选框回调 (复选框2, 到整数 (&复选框回调))

' 创建复选框3（禁用状态）
复选框3 ＝ 创建复选框_辅助 (窗口句柄, 50, 180, 200, 30, “自动登录（禁用）”, 假, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE)
启用复选框 (复选框3, 假)

' 创建状态显示标签
标签_状态 ＝ 创建标签_辅助 (窗口句柄, 50, 250, 400, 100, “状态：等待操作...”, #COLOR_TEXT_REGULAR, #COLOR_BG_LIGHT, “Microsoft YaHei UI”, 8, 假, 假, 假, 0, 真)

' 创建测试按钮
' 集_切换复选框按钮 ＝ 创建Emoji按钮_辅助 (窗口句柄, “??”, “切换复选框1”, 50, 350, 120, 40, #COLOR_PRIMARY)

emoji字节集 ＝ { 226, 154, 153, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换复选框1”)
集_切换复选框按钮 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 50, 350, 120, 40, #COLOR_PRIMARY)

' 集_获取所有状态按钮 ＝ 创建Emoji按钮_辅助 (窗口句柄, “??”, “获取所有状态”, 190, 350, 120, 40, #COLOR_SUCCESS)
emoji字节集 ＝ { 240, 159, 147, 139 }
文本字节集 ＝ 编码_Ansi到Utf8 (“获取所有状态”)
集_获取所有状态按钮 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 190, 350, 120, 40, #COLOR_PRIMARY)


' 设置按钮回调
设置按钮点击回调 (&按钮点击回调)

' 设置窗口关闭回调
设置窗口关闭回调 (&窗口关闭回调)


.子程序 复选框回调, , , stdcall
.参数 复选框句柄, 整数型
.参数 选中状态, 逻辑型
.局部变量 状态文本, 文本型

状态文本 ＝ “复选框状态改变：” ＋ #换行符

.如果真 (复选框句柄 ＝ 复选框1)
    状态文本 ＝ 状态文本 ＋ “接受用户协议：” ＋ 到文本 (选中状态) ＋ #换行符
.如果真结束

.如果真 (复选框句柄 ＝ 复选框2)
    状态文本 ＝ 状态文本 ＋ “记住密码：” ＋ 到文本 (选中状态) ＋ #换行符
.如果真结束

状态文本 ＝ 状态文本 ＋ #换行符 ＋ “当前所有状态：” ＋ #换行符
状态文本 ＝ 状态文本 ＋ “复选框1：” ＋ 到文本 (获取复选框状态 (复选框1)) ＋ #换行符
状态文本 ＝ 状态文本 ＋ “复选框2：” ＋ 到文本 (获取复选框状态 (复选框2)) ＋ #换行符
状态文本 ＝ 状态文本 ＋ “复选框3：” ＋ 到文本 (获取复选框状态 (复选框3))

设置标签文本_辅助 (标签_状态, 状态文本)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.局部变量 状态文本, 文本型

.判断开始 (按钮ID ＝ 集_切换复选框按钮)
    ' 切换复选框1
    设置复选框状态 (复选框1, 取反 (获取复选框状态 (复选框1)))

.判断 (按钮ID ＝ 集_获取所有状态按钮)
    ' 获取所有状态
    状态文本 ＝ “当前所有复选框状态：” ＋ #换行符 ＋ #换行符
    状态文本 ＝ 状态文本 ＋ “复选框1（接受用户协议）：” ＋ 到文本 (获取复选框状态 (复选框1)) ＋ #换行符
    状态文本 ＝ 状态文本 ＋ “复选框2（记住密码）：” ＋ 到文本 (获取复选框状态 (复选框2)) ＋ #换行符
    状态文本 ＝ 状态文本 ＋ “复选框3（自动登录-禁用）：” ＋ 到文本 (获取复选框状态 (复选框3))

    设置标签文本_辅助 (标签_状态, 状态文本)

.默认

.判断结束


.子程序 窗口关闭回调, , , stdcall
.参数 窗口句柄_, 整数型

调试输出 (“窗口被关闭，句柄：”, 窗口句柄_)
结束 ()

.子程序 创建复选框_辅助, 整数型, 公开
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 文本, 文本型
.参数 选中, 逻辑型
.参数 前景色, 整数型
.参数 背景色, 整数型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取变量数据地址 (文本字节集)

返回 (创建复选框 (父窗口, x, y, 宽, 高, 文本指针, 取字节集长度 (文本字节集), 选中, 前景色, 背景色))

.子程序 创建标签_辅助, 整数型, 公开
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 文本, 文本型
.参数 前景色, 整数型
.参数 背景色, 整数型
.参数 字体名, 文本型
.参数 字体大小, 整数型
.参数 粗体, 逻辑型
.参数 斜体, 逻辑型
.参数 下划线, 逻辑型
.参数 对齐, 整数型
.参数 是否换行, 逻辑型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型
.局部变量 字体字节集, 字节集
.局部变量 字体指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取变量数据地址 (文本字节集)

字体字节集 ＝ 编码_Ansi到Utf8 (字体名)
字体指针 ＝ 取变量数据地址 (字体字节集)

返回 (创建标签 (父窗口, x, y, 宽, 高, 文本指针, 取字节集长度 (文本字节集), 前景色, 背景色, 字体指针, 取字节集长度 (字体字节集), 字体大小, 粗体, 斜体, 下划线, 对齐, 是否换行))

.子程序 设置标签文本_辅助, , 公开
.参数 标签句柄, 整数型
.参数 文本, 文本型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取变量数据地址 (文本字节集)

设置标签文本 (标签句柄, 文本指针, 取字节集长度 (文本字节集))

.子程序 创建Emoji按钮_辅助, 整数型, 公开
.参数 父窗口, 整数型
.参数 emoji, 文本型
.参数 文本, 文本型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 背景色, 整数型
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 emoji指针, 整数型
.局部变量 文本指针, 整数型

emoji字节集 ＝ 编码_Ansi到Utf8 (emoji)
文本字节集 ＝ 编码_Ansi到Utf8 (文本)
emoji指针 ＝ 取变量数据地址 (emoji字节集)
文本指针 ＝ 取变量数据地址 (文本字节集)

返回 (创建Emoji按钮_字节集 (父窗口, emoji指针, 取字节集长度 (emoji字节集), 文本指针, 取字节集长度 (文本字节集), x, y, 宽, 高, 背景色))
