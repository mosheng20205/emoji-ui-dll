.版本 2
.支持库 spec

.程序集 窗口程序集_D2D组合框示例
.程序集变量 主窗口句柄, 整数型
.程序集变量 D2D组合框1, 整数型
.程序集变量 D2D组合框2, 整数型
.程序集变量 标准组合框1, 整数型
.程序集变量 标签_D2D选中项, 整数型
.程序集变量 标签_标准选中项, 整数型
.程序集变量 按钮_添加Emoji, 整数型
.程序集变量 按钮_清空D2D, 整数型
.程序集变量 按钮_设置选中, 整数型
.程序集变量 编辑框_新项, 整数型

.子程序 _窗口_组合框示例_创建完毕
.局部变量 图标_笑脸, 字节集
.局部变量 图标_加号, 字节集
.局部变量 图标_垃圾桶, 字节集
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集

主窗口句柄 ＝ 创建Emoji窗口 ("D2D组合框4.0示例 - 彩色Emoji支持", 900, 700)
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

' 准备按钮图标字节集（UTF-8编码）
图标_笑脸 ＝ { 240, 159, 152, 128 }
图标_加号 ＝ { 226, 158, 149 }
图标_垃圾桶 ＝ { 240, 159, 151, 145, 239, 184, 143 }
字体字节集 ＝ 编码_Ansi到Utf8 ("Microsoft YaHei UI")

' ========== 标题 ==========
文本字节集 ＝ 编码_Ansi到Utf8 ("D2D组合框 vs 标准组合框对比")
创建标签 (主窗口句柄, 20, 20, 860, 40, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_PRIMARY, #COLOR_TRANSPARENT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 20, 真, 假, 假, 1, 假)

' ========== D2D组合框区域 ==========
标题字节集 ＝ { 226, 156, 133, 239, 184, 143, 32, 68, 50, 68, 231, 187, 132, 229, 144, 136, 230, 161, 134, 239, 188, 136, 231, 188, 150, 232, 190, 145, 230, 161, 134, 230, 148, 175, 230, 140, 129, 229, 189, 169, 232, 137, 178, 69, 109, 111, 106, 105, 239, 188, 137 }
创建标签 (主窗口句柄, 20, 80, 400, 35, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), #COLOR_SUCCESS, #COLOR_TRANSPARENT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 14, 真, 假, 假, 0, 假)

文本字节集 ＝ 编码_Ansi到Utf8 ("可编辑模式：")
创建标签 (主窗口句柄, 20, 120, 200, 25, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_PRIMARY, #COLOR_TRANSPARENT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 假)
D2D组合框1 ＝ 创建D2D组合框 (主窗口句柄, 20, 145, 400, 40, 假, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 40, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 16, 假, 假, 假)
设置D2D组合框回调 (D2D组合框1, &D2D组合框1_选中回调)

文本字节集 ＝ { 240, 159, 147, 129, 32, 230, 150, 135, 228, 187, 182, 229, 164, 185 }
添加D2D组合框项 (D2D组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 147, 183, 32, 231, 155, 184, 230, 156, 186 }
添加D2D组合框项 (D2D组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 152, 128, 32, 231, 172, 145, 232, 132, 184 }
添加D2D组合框项 (D2D组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 142, 137, 32, 229, 186, 134, 231, 165, 157 }
添加D2D组合框项 (D2D组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 226, 157, 164, 239, 184, 143, 32, 231, 136, 177, 229, 191, 131 }
添加D2D组合框项 (D2D组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 140, 159, 32, 230, 152, 159, 230, 152, 159 }
添加D2D组合框项 (D2D组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 142, 175, 32, 231, 155, 174, 230, 160, 135 }
添加D2D组合框项 (D2D组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 154, 128, 32, 231, 129, 171, 231, 174, 173 }
添加D2D组合框项 (D2D组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

' 创建只读D2D组合框（测试滚动功能）
文本字节集 ＝ 编码_Ansi到Utf8 ("只读模式（测试滚动）：")
创建标签 (主窗口句柄, 20, 200, 200, 25, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_PRIMARY, #COLOR_TRANSPARENT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 假)
D2D组合框2 ＝ 创建D2D组合框 (主窗口句柄, 20, 225, 400, 40, 真, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 40, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 16, 假, 假, 假)
设置D2D组合框回调 (D2D组合框2, &D2D组合框2_选中回调)

文本字节集 ＝ { 240, 159, 141, 142, 32, 232, 139, 185, 230, 158, 156 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 140, 32, 233, 166, 153, 232, 149, 137 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 135, 32, 232, 145, 161, 232, 144, 132 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 147, 32, 232, 141, 137, 232, 142, 147 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 137, 32, 232, 165, 191, 231, 147, 156 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 138, 32, 230, 161, 131, 229, 173, 144 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 139, 32, 230, 159, 160, 230, 170, 172 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 145, 32, 230, 161, 131, 229, 173, 144 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 146, 32, 230, 168, 177, 230, 161, 131 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 165, 157, 32, 231, 140, 149, 231, 140, 180, 230, 161, 131 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 133, 32, 231, 149, 170, 232, 140, 132 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 165, 145, 32, 231, 137, 155, 230, 178, 185, 230, 158, 156 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 141, 134, 32, 232, 140, 132, 229, 173, 144 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 165, 149, 32, 232, 131, 161, 232, 144, 157, 229, 141, 156 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 140, 189, 32, 231, 142, 137, 231, 177, 179, 231, 177, 179 }
添加D2D组合框项 (D2D组合框2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

设置D2D组合框选中索引 (D2D组合框2, 0)

文本字节集 ＝ 编码_Ansi到Utf8 ("当前选中：无")
标签_D2D选中项 ＝ 创建标签 (主窗口句柄, 20, 280, 400, 25, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_SECONDARY, #COLOR_BG_BASE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 假)

' ========== 标准组合框区域（对比） ==========
文本字节集 ＝ { 226, 157, 140, 32 } ＋ 编码_Ansi到Utf8 ("标准组合框（编辑框不支持彩色Emoji）")
创建标签 (主窗口句柄, 460, 80, 420, 35, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_DANGER, #COLOR_TRANSPARENT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 14, 真, 假, 假, 0, 假)

文本字节集 ＝ 编码_Ansi到Utf8 ("标准组合框：")
创建标签 (主窗口句柄, 460, 120, 200, 25, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_PRIMARY, #COLOR_TRANSPARENT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 假)
标准组合框1 ＝ 创建组合框 (主窗口句柄, 460, 145, 400, 40, 假, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 40, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 16, 假, 假, 假)

文本字节集 ＝ { 240, 159, 147, 129, 32, 230, 150, 135, 228, 187, 182, 229, 164, 185 }
添加组合框项 (标准组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 147, 183, 32, 231, 155, 184, 230, 156, 186 }
添加组合框项 (标准组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 152, 128, 32, 231, 172, 145, 232, 132, 184 }
添加组合框项 (标准组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 142, 137, 32, 229, 186, 134, 231, 165, 157 }
添加组合框项 (标准组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 226, 157, 164, 239, 184, 143, 32, 231, 136, 177, 229, 191, 131 }
添加组合框项 (标准组合框1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

文本字节集 ＝ 编码_Ansi到Utf8 ("标准组合框选中：无")
标签_标准选中项 ＝ 创建标签 (主窗口句柄, 460, 200, 400, 25, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_SECONDARY, #COLOR_BG_BASE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 假)

' ========== 说明文字 ==========
文本字节集 ＝ { 240, 159, 146, 161, 32 } ＋ 编码_Ansi到Utf8 ("对比说明：" ＋ #换行符 ＋ "• D2D组合框：编辑框和下拉列表都显示彩色Emoji ")
文本字节集 ＝ 文本字节集 ＋ { 226, 156, 133 } ＋ 编码_Ansi到Utf8 (#换行符 ＋ "• 标准组合框：编辑框显示黑白Emoji ")
文本字节集 ＝ 文本字节集 ＋ { 226, 157, 140 } ＋ 编码_Ansi到Utf8 ("，下拉列表显示彩色Emoji ")
文本字节集 ＝ 文本字节集 ＋ { 226, 156, 133 } ＋ 编码_Ansi到Utf8 (#换行符 ＋ "• 试试在可编辑的D2D组合框中输入Emoji，会看到彩色显示！")
创建标签 (主窗口句柄, 20, 330, 860, 80, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_SECONDARY, #COLOR_BG_BASE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

' ========== 操作按钮 ==========
文本字节集 ＝ 编码_Ansi到Utf8 ("添加新的Emoji项目到D2D组合框：")
创建标签 (主窗口句柄, 20, 430, 400, 30, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_PRIMARY, #COLOR_TRANSPARENT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 真, 假, 假, 0, 假)

文本字节集 ＝ {  }
编辑框_新项 ＝ 创建D2D彩色Emoji编辑框 (主窗口句柄, 20, 465, 400, 40, 取变量数据地址 (文本字节集), 0, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 16, 假, 假, 假, 0, 假, 假, 假, 真, 真)

文本字节集 ＝ 编码_Ansi到Utf8 ("添加到D2D组合框")
按钮_添加Emoji ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (图标_加号), 取字节集长度 (图标_加号), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 520, 180, 45, #COLOR_SUCCESS)
文本字节集 ＝ 编码_Ansi到Utf8 ("清空D2D组合框")
按钮_清空D2D ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (图标_垃圾桶), 取字节集长度 (图标_垃圾桶), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 210, 520, 180, 45, #COLOR_DANGER)
文本字节集 ＝ 编码_Ansi到Utf8 ("设置选中项3")
按钮_设置选中 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (图标_笑脸), 取字节集长度 (图标_笑脸), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 400, 520, 180, 45, #COLOR_WARNING)

' ========== 功能特性说明 ==========
文本字节集 ＝ { 240, 159, 142, 175, 32 } ＋ 编码_Ansi到Utf8 ("D2D组合框特性：" ＋ #换行符)
文本字节集 ＝ 文本字节集 ＋ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("完美支持彩色Emoji显示（编辑框和下拉列表）" ＋ #换行符)
文本字节集 ＝ 文本字节集 ＋ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("完整的鼠标和键盘交互（点击、悬停、滚动、方向键）" ＋ #换行符)
文本字节集 ＝ 文本字节集 ＋ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("自适应下拉列表定位（自动检测屏幕边界）" ＋ #换行符)
文本字节集 ＝ 文本字节集 ＋ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("可自定义样式（颜色、字体、项目高度等）")
创建标签 (主窗口句柄, 20, 590, 860, 90, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_PRIMARY, #COLOR_BG_BASE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)

运行消息循环 ()

.子程序 D2D组合框1_选中回调, , , stdcall
.参数 组合框句柄, 整数型
.参数 索引, 整数型
.局部变量 文本, 文本型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型
.局部变量 标签字节集, 字节集

.如果 (索引 ≥ 0)
    缓冲区 ＝ 取空白字节集 (1024)
    实际长度 ＝ 获取D2D组合框项文本 (组合框句柄, 索引, 取变量数据地址 (缓冲区), 1024)
    .如果真 (实际长度 ＞ 0)
        文本 ＝ 编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度))
    .否则
        文本 ＝ ""
    .如果真结束
    标签字节集 ＝ 编码_Ansi到Utf8 ("当前选中：索引 " ＋ 到文本 (索引) ＋ " - " ＋ 文本)
    设置标签文本 (标签_D2D选中项, 取变量数据地址 (标签字节集), 取字节集长度 (标签字节集))
.否则
    缓冲区 ＝ 取空白字节集 (1024)
    实际长度 ＝ 获取D2D组合框文本 (组合框句柄, 取变量数据地址 (缓冲区), 1024)
    .如果真 (实际长度 ＞ 0)
        文本 ＝ 编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度))
    .否则
        文本 ＝ ""
    .如果真结束
    标签字节集 ＝ 编码_Ansi到Utf8 ("编辑框文本：" ＋ 文本)
    设置标签文本 (标签_D2D选中项, 取变量数据地址 (标签字节集), 取字节集长度 (标签字节集))
.如果结束

.子程序 D2D组合框2_选中回调, , , stdcall
.参数 组合框句柄, 整数型
.参数 索引, 整数型
.局部变量 文本, 文本型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型
.局部变量 标签字节集, 字节集

.如果 (索引 ≥ 0)
    缓冲区 ＝ 取空白字节集 (1024)
    实际长度 ＝ 获取D2D组合框项文本 (组合框句柄, 索引, 取变量数据地址 (缓冲区), 1024)
    .如果真 (实际长度 ＞ 0)
        文本 ＝ 编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度))
    .否则
        文本 ＝ ""
    .如果真结束
    标签字节集 ＝ 编码_Ansi到Utf8 ("只读组合框选中：索引 " ＋ 到文本 (索引) ＋ " - " ＋ 文本)
    设置标签文本 (标签_D2D选中项, 取变量数据地址 (标签字节集), 取字节集长度 (标签字节集))
.如果结束

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型
.局部变量 空白字节集, 字节集
.局部变量 标签字节集, 字节集

.如果真 (按钮ID ＝ 按钮_添加Emoji)
    实际长度 ＝ 获取D2D编辑框文本 (编辑框_新项, 0, 0)
    .如果真 (实际长度 ＞ 0)
        缓冲区 ＝ 取空白字节集 (实际长度 ＋ 1)
        实际长度 ＝ 获取D2D编辑框文本 (编辑框_新项, 取变量数据地址 (缓冲区), 实际长度 ＋ 1)
        添加D2D组合框项 (D2D组合框1, 取变量数据地址 (缓冲区), 实际长度)
        空白字节集 ＝ {  }
        设置D2D编辑框文本 (编辑框_新项, 取变量数据地址 (空白字节集), 0)
        标签字节集 ＝ { 226, 156, 133, 32 } ＋ 取字节集左边 (缓冲区, 实际长度)
        标签字节集 ＝ 编码_Ansi到Utf8 ("已添加：") ＋ 标签字节集
        设置标签文本 (标签_D2D选中项, 取变量数据地址 (标签字节集), 取字节集长度 (标签字节集))
    .如果真结束

.如果真 (按钮ID ＝ 按钮_清空D2D)
    清空D2D组合框 (D2D组合框1)
    标签字节集 ＝ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("已清空D2D组合框")
    设置标签文本 (标签_D2D选中项, 取变量数据地址 (标签字节集), 取字节集长度 (标签字节集))

.如果真 (按钮ID ＝ 按钮_设置选中)
    设置D2D组合框选中索引 (D2D组合框1, 2)
    标签字节集 ＝ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("已设置选中项为索引2")
    设置标签文本 (标签_D2D选中项, 取变量数据地址 (标签字节集), 取字节集长度 (标签字节集))

.如果真结束
