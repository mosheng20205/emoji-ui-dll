.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_DataGridView行样式4_0示例

.程序集变量 窗口句柄, 整数型
.程序集变量 表格句柄, 整数型
.程序集变量 状态标签, 整数型
.程序集变量 按钮_标记告警, 整数型
.程序集变量 按钮_标记完成, 整数型
.程序集变量 按钮_恢复默认, 整数型

.子程序 _启动子程序, 整数型
.局部变量 行索引, 整数型

窗口句柄 ＝ 创建Emoji窗口 ("DataGridView行样式4.0示例", 80, 40, 1120, 780)
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 20, 1060, 560, 假, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))

DG_添加文本列 (表格句柄, "任务", 220)
DG_添加文本列 (表格句柄, "负责人", 180)
DG_添加文本列 (表格句柄, "状态", 180)
DG_添加文本列 (表格句柄, "说明", 440)

添加示例行 ("接口联调", "张三", "进行中", "用于演示整行告警样式。")
添加示例行 ("回归测试", "李四", "已完成", "用于演示整行完成样式。")
添加示例行 ("上线准备", "王五", "待处理", "恢复默认后应回到普通样式。")

表格_设置冻结首行 (表格句柄, 真)
表格_设置选择模式 (表格句柄, #DGSEL_ROW)
表格_设置选中单元格 (表格句柄, 0, 0)

按钮_标记告警 ＝ 创建Emoji按钮_辅助 (窗口句柄, "A", "标记当前行为告警", 20, 600, 180, 44, 到ARGB (255, 245, 108, 108))
按钮_标记完成 ＝ 创建Emoji按钮_辅助 (窗口句柄, "C", "标记当前行为完成", 216, 600, 180, 44, 到ARGB (255, 103, 194, 58))
按钮_恢复默认 ＝ 创建Emoji按钮_辅助 (窗口句柄, "D", "恢复当前行默认样式", 412, 600, 190, 44, 到ARGB (255, 144, 147, 153))
状态标签 ＝ 创建标签_辅助 (窗口句柄, 20, 660, 1060, 56, "状态：先选中一行，再点击按钮应用整行样式。", 到ARGB (255, 48, 49, 51), 到ARGB (255, 244, 247, 250), "Microsoft YaHei UI", 11, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 行, 整数型

行 ＝ 表格_获取选中行 (表格句柄)
.如果 (行 ＝ -1)
    行 ＝ 0
    表格_设置选中单元格 (表格句柄, 0, 0)
.如果结束

.如果真 (按钮ID ＝ 按钮_标记告警)
    表格_设置行样式 (表格句柄, 行, 到ARGB (255, 245, 108, 108), 到ARGB (255, 254, 240, 240), 真, 假)
    表格_设置单元格样式 (表格句柄, 行, 2, 到ARGB (255, 245, 108, 108), 0, 真, 假)
    表格_刷新 (表格句柄)
    写状态 ("状态：第 " ＋ 到文本 (行 ＋ 1) ＋ " 行已应用告警样式。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_标记完成)
    表格_设置行样式 (表格句柄, 行, 到ARGB (255, 103, 194, 58), 到ARGB (255, 240, 249, 235), 假, 假)
    表格_设置单元格样式 (表格句柄, 行, 2, 到ARGB (255, 103, 194, 58), 0, 真, 假)
    表格_刷新 (表格句柄)
    写状态 ("状态：第 " ＋ 到文本 (行 ＋ 1) ＋ " 行已应用完成样式。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_恢复默认)
    表格_设置行样式 (表格句柄, 行, 0, 0, 假, 假)
    表格_设置单元格样式 (表格句柄, 行, 2, 0, 0, 假, 假)
    表格_刷新 (表格句柄)
    写状态 ("状态：第 " ＋ 到文本 (行 ＋ 1) ＋ " 行已恢复默认样式。")
.如果真结束

.子程序 添加示例行
.参数 任务, 文本型
.参数 负责人, 文本型
.参数 状态, 文本型
.参数 说明, 文本型
.局部变量 行索引, 整数型

行索引 ＝ 表格_添加行 (表格句柄)
DG_设置文本 (表格句柄, 行索引, 0, 任务)
DG_设置文本 (表格句柄, 行索引, 1, 负责人)
DG_设置文本 (表格句柄, 行索引, 2, 状态)
DG_设置文本 (表格句柄, 行索引, 3, 说明)

.子程序 DG_添加文本列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加文本列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_设置文本
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置单元格文本 (表格, 行, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 写状态
.参数 文本, 文本型
设置标签文本_UTF8 (状态标签, 文本)
