.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_DataGridView冻结计数4_0示例

.程序集变量 窗口句柄, 整数型
.程序集变量 表格句柄, 整数型
.程序集变量 状态标签, 整数型
.程序集变量 按钮_冻结0, 整数型
.程序集变量 按钮_冻结22, 整数型
.程序集变量 按钮_冻结31, 整数型

.子程序 _启动子程序, 整数型
.局部变量 列, 整数型
.局部变量 行, 整数型
.局部变量 行索引, 整数型
.局部变量 emoji字节集, 字节集

窗口句柄 ＝ 创建Emoji窗口 ("DataGridView冻结计数4.0示例", 80, 40, 1180, 780)
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 20, 1120, 600, 假, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))

.计次循环首 (8, 列)
    DG_添加文本列 (表格句柄, "列" ＋ 到文本 (列), 220)
.计次循环尾 ()

.计次循环首 (20, 行)
    行索引 ＝ 表格_添加行 (表格句柄)
    .计次循环首 (8, 列)
        DG_设置文本 (表格句柄, 行索引, 列 - 1, "R" ＋ 到文本 (行) ＋ "-C" ＋ 到文本 (列))
    .计次循环尾 ()
.计次循环尾 ()

表格_设置冻结首行 (表格句柄, 真)
表格_设置默认行高 (表格句柄, 40)
表格_设置显示网格线 (表格句柄, 真)

emoji字节集 ＝ { 48, 239, 184, 143, 226, 131, 163 }  ' 0️⃣
按钮_冻结0 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "取消冻结", 20, 640, 160, 44, 到ARGB (255, 144, 147, 153))
emoji字节集 ＝ { 50, 239, 184, 143, 226, 131, 163 }  ' 2️⃣
按钮_冻结22 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "冻结2列2行", 196, 640, 180, 44, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 51, 239, 184, 143, 226, 131, 163 }  ' 3️⃣
按钮_冻结31 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "冻结3列1行", 392, 640, 180, 44, 到ARGB (255, 103, 194, 58))

状态标签 ＝ 创建标签_辅助 (窗口句柄, 20, 700, 1120, 48, "状态：列宽已加大，表格会出现横向滚动条，方便观察冻结列效果。", 到ARGB (255, 48, 49, 51), 到ARGB (255, 244, 247, 250), "Microsoft YaHei UI", 11, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型

.如果真 (按钮ID ＝ 按钮_冻结0)
    表格_设置冻结列数 (表格句柄, 0)
    表格_设置冻结行数 (表格句柄, 0)
    表格_刷新 (表格句柄)
    写状态 ("状态：已取消冻结列数和冻结行数，仅保留冻结首行。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_冻结22)
    表格_设置冻结列数 (表格句柄, 2)
    表格_设置冻结行数 (表格句柄, 2)
    表格_刷新 (表格句柄)
    写状态 ("状态：已设置冻结列数=2，冻结行数=2。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_冻结31)
    表格_设置冻结列数 (表格句柄, 3)
    表格_设置冻结行数 (表格句柄, 1)
    表格_刷新 (表格句柄)
    写状态 ("状态：已设置冻结列数=3，冻结行数=1。")
.如果真结束

.子程序 DG_添加文本列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加文本列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_设置文本
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置单元格文本 (表格, 行, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 写状态
.参数 文本, 文本型
设置标签文本_UTF8 (状态标签, 文本)
