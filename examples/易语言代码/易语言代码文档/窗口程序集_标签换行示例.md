.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 窗口句柄, 整数型
.程序集变量 标签1
.程序集变量 标签2
.程序集变量 标签3
.程序集变量 标签4

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 utf8字节集, 字节集
.局部变量 按钮文本字节集, 字节集
.局部变量 字体名utf8, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体名, 字节集

窗口句柄 ＝ 创建Emoji窗口 (“标签换行显示示例”, 600, 400)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ “\icon.ico”)

文本字节集 ＝ 到UTF8 (“这是一个很长的文本，如果不换行的话会被截断显示不完整”)
字体名 ＝ 到UTF8 (“微软雅黑”)

' ========== 示例1: 不换行的标签（单行显示） ==========
标签1 ＝ 创建标签 (窗口句柄, 20, 20, 560, 30, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 十六进制到整数 (“FF333333”), 十六进制到整数 (“FFFFFFFF”), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 假)

' ========== 示例2: 换行的标签（多行显示） ==========
文本字节集 ＝ 到UTF8 (“这是一个很长的文本，启用换行后会自动根据宽度换行显示。这样就可以完整显示所有内容了。支持中文、英文、数字等各种字符的自动换行。”)
标签2 ＝ 创建标签 (窗口句柄, 20, 70, 560, 80, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 十六进制到整数 (“FF333333”), 十六进制到整数 (“FFFFFFFF”), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 真, 0, 真)

' ========== 示例3: 居中对齐 + 换行 ==========
文本字节集 ＝ 到UTF8 (“这是居中对齐的换行文本。” ＋ #换行符 ＋ “可以手动添加换行符。” ＋ #换行符 ＋ “也可以自动换行。”)
标签3 ＝ 创建标签 (窗口句柄, 20, 170, 560, 80, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 十六进制到整数 (“FF409EFF”), 十六进制到整数 (“FFECF5FF”), 取变量数据地址 (字体名), 取字节集长度 (字体名), 16, 假, 假, 假, 1, 真)

' ========== 示例4: 右对齐 + 换行 ==========
文本字节集 ＝ 到UTF8 (“这是右对齐的换行文本。文本会自动在右侧对齐，并且支持自动换行显示。适合显示一些需要右对齐的长文本内容。”)
标签4 ＝ 创建标签 (窗口句柄, 20, 270, 560, 80, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 十六进制到整数 (“FF67C23A”), 十六进制到整数 (“FFF0F9FF”), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 2, 真)

' 设置消息循环主窗口
设置消息循环主窗口 (窗口句柄)

' 运行消息循环
运行消息循环 ()
返回 (0)

.子程序 十六进制到整数, 整数型, , 将十六进制文本转为整数型（如 "FF4682B4" → 对应整数）
.参数 十六进制文本, 文本型
.局部变量 i, 整数型
.局部变量 长度, 整数型
.局部变量 结果, 整数型
.局部变量 当前字符, 文本型
.局部变量 字符值, 整数型

长度 ＝ 取文本长度 (十六进制文本)
结果 ＝ 0

.计次循环首 (长度, i)
    当前字符 ＝ 取文本中间 (十六进制文本, i, 1)

    .判断开始 (当前字符 ＝ “0”)
        字符值 ＝ 0
    .判断 (当前字符 ＝ “1”)
        字符值 ＝ 1
    .判断 (当前字符 ＝ “2”)
        字符值 ＝ 2
    .判断 (当前字符 ＝ “3”)
        字符值 ＝ 3
    .判断 (当前字符 ＝ “4”)
        字符值 ＝ 4
    .判断 (当前字符 ＝ “5”)
        字符值 ＝ 5
    .判断 (当前字符 ＝ “6”)
        字符值 ＝ 6
    .判断 (当前字符 ＝ “7”)
        字符值 ＝ 7
    .判断 (当前字符 ＝ “8”)
        字符值 ＝ 8
    .判断 (当前字符 ＝ “9”)
        字符值 ＝ 9
    .判断 (当前字符 ＝ “A” 或 当前字符 ＝ “a”)
        字符值 ＝ 10
    .判断 (当前字符 ＝ “B” 或 当前字符 ＝ “b”)
        字符值 ＝ 11
    .判断 (当前字符 ＝ “C” 或 当前字符 ＝ “c”)
        字符值 ＝ 12
    .判断 (当前字符 ＝ “D” 或 当前字符 ＝ “d”)
        字符值 ＝ 13
    .判断 (当前字符 ＝ “E” 或 当前字符 ＝ “e”)
        字符值 ＝ 14
    .判断 (当前字符 ＝ “F” 或 当前字符 ＝ “f”)
        字符值 ＝ 15
    .默认
        字符值 ＝ 0
    .判断结束

    结果 ＝ 左移 (结果, 4) ＋ 字符值
.计次循环尾 ()

返回 (结果)

.子程序 到UTF8, 字节集
.参数 文本, 文本型
.局部变量 字节集, 字节集
.局部变量 长度, 整数型

字节集 ＝ 编码_Ansi到Utf8 (文本)
长度 ＝ 取字节集长度 (字节集)
返回 (字节集)
