.版本 2
.支持库 spec

.程序集 窗口程序集_标签换行示例

.子程序 _启动窗口_创建完毕

窗口句柄 ＝ 创建Emoji窗口 ("标签换行显示示例", 600, 400)

' ========== 示例1: 不换行的标签（单行显示） ==========
标签1 ＝ 创建标签 (窗口句柄, 20, 20, 560, 30, 到UTF8 ("这是一个很长的文本，如果不换行的话会被截断显示不完整"), 0xFF333333, 0xFFFFFFFF, 到UTF8 ("微软雅黑"), 14, 假, 假, 假, 0, 假)

' ========== 示例2: 换行的标签（多行显示） ==========
标签2 ＝ 创建标签 (窗口句柄, 20, 70, 560, 80, 到UTF8 ("这是一个很长的文本，启用换行后会自动根据宽度换行显示。这样就可以完整显示所有内容了。支持中文、英文、数字等各种字符的自动换行。"), 0xFF333333, 0xFFFFFFFF, 到UTF8 ("微软雅黑"), 14, 假, 假, 假, 0, 真)

' ========== 示例3: 居中对齐 + 换行 ==========
标签3 ＝ 创建标签 (窗口句柄, 20, 170, 560, 80, 到UTF8 ("这是居中对齐的换行文本。" ＋ #换行符 ＋ "可以手动添加换行符。" ＋ #换行符 ＋ "也可以自动换行。"), 0xFF409EFF, 0xFFECF5FF, 到UTF8 ("微软雅黑"), 16, 假, 假, 假, 1, 真)

' ========== 示例4: 右对齐 + 换行 ==========
标签4 ＝ 创建标签 (窗口句柄, 20, 270, 560, 80, 到UTF8 ("这是右对齐的换行文本。文本会自动在右侧对齐，并且支持自动换行显示。适合显示一些需要右对齐的长文本内容。"), 0xFF67C23A, 0xFFF0F9FF, 到UTF8 ("微软雅黑"), 14, 假, 假, 假, 2, 真)

运行消息循环 ()


.子程序 到UTF8, 字节集
.参数 文本, 文本型
.局部变量 字节集, 字节集
.局部变量 长度, 整数型

字节集 ＝ 到字节集 (文本)
长度 ＝ 取字节集长度 (字节集)
返回 (字节集)
