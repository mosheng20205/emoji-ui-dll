.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 按钮1, 整数型
.程序集变量 按钮2, 整数型
.程序集变量 按钮3, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 集_操作按钮, 整数型
.程序集变量 集_修改按钮1文本, 整数型
.程序集变量 集_修改按钮1Emoji, 整数型
.程序集变量 集_获取按钮2位置, 整数型
.程序集变量 集_修改按钮2颜色, 整数型
.程序集变量 集_切换按钮3可见, 整数型
.程序集变量 集_切换按钮3启用, 整数型
.程序集变量 集_移动按钮1, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 窗口句柄, 整数型
.局部变量 标题字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 按钮文本, 字节集
.局部变量 字体名, 字节集

' 创建窗口
标题字节集 ＝ 编码_Ansi到Utf8 (“按钮属性操作示例”)
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 800, 650, 0)
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ “\icon.ico”)
' 创建测试按钮
emoji字节集 ＝ { 240, 159, 142, 168 }
按钮文本 ＝ 编码_Ansi到Utf8 (“按钮1”)
按钮1 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 50, 50, 150, 40, 到ARGB (255, 100, 150, 255))

emoji字节集 ＝ { 240, 159, 148, 167 }
按钮文本 ＝ 编码_Ansi到Utf8 (“按钮2”)
按钮2 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 220, 50, 150, 40, 到ARGB (255, 255, 150, 100))

emoji字节集 ＝ { 226, 154, 153, 239, 184, 143 }
按钮文本 ＝ 编码_Ansi到Utf8 (“按钮3”)
按钮3 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 390, 50, 150, 40, 到ARGB (255, 150, 255, 100))
' 创建状态显示标签

按钮文本 ＝ 编码_Ansi到Utf8 (“点击下方按钮测试各种属性操作”)
字体名 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
标签_状态 ＝ 创建标签 (主窗口句柄, 50, 120, 700, 400, 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 到ARGB (255, 0, 0, 0), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 12, 假, 假, 假, 0, 真)

' 创建操作按钮
emoji字节集 ＝ { 240, 159, 147, 157 }
按钮文本 ＝ 编码_Ansi到Utf8 (“获取按钮1文本”)
集_操作按钮 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 50, 530, 150, 40, 到ARGB (255, 100, 200, 100))

emoji字节集 ＝ { 226, 156, 143, 239, 184, 143 }
按钮文本 ＝ 编码_Ansi到Utf8 (“修改按钮1文本”)
集_修改按钮1文本 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 220, 530, 150, 40, 到ARGB (255, 100, 200, 100))

emoji字节集 ＝ { 240, 159, 152, 128 }
按钮文本 ＝ 编码_Ansi到Utf8 (“修改按钮1Emoji”)
集_修改按钮1Emoji ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 390, 530, 150, 40, 到ARGB (255, 100, 200, 100))

emoji字节集 ＝ { 240, 159, 147, 141 }
按钮文本 ＝ 编码_Ansi到Utf8 (“获取按钮2位置”)
集_获取按钮2位置 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 560, 530, 150, 40, 到ARGB (255, 100, 200, 100))

emoji字节集 ＝ { 240, 159, 142, 168 }
按钮文本 ＝ 编码_Ansi到Utf8 (“修改按钮2颜色”)
集_修改按钮2颜色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 50, 480, 150, 40, 到ARGB (255, 200, 100, 100))

emoji字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
按钮文本 ＝ 编码_Ansi到Utf8 (“切换按钮3可见”)
集_切换按钮3可见 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 220, 480, 150, 40, 到ARGB (255, 200, 100, 100))

emoji字节集 ＝ { 240, 159, 148, 146 }
按钮文本 ＝ 编码_Ansi到Utf8 (“切换按钮3启用”)
集_切换按钮3启用 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 390, 480, 150, 40, 到ARGB (255, 200, 100, 100))

emoji字节集 ＝ { 240, 159, 147, 143 }
按钮文本 ＝ 编码_Ansi到Utf8 (“移动按钮1”)
集_移动按钮1 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本), 取字节集长度 (按钮文本), 560, 480, 150, 40, 到ARGB (255, 200, 100, 100))

' 设置按钮点击回调
设置按钮点击回调 (&按钮点击回调)

' 设置消息循环主窗口
设置消息循环主窗口 (窗口句柄)

' 运行消息循环
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 文本长度, 整数型
.局部变量 文本缓冲区, 字节集
.局部变量 文本内容, 字节集
.局部变量 状态信息, 字节集
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型
.局部变量 背景色, 整数型
.局部变量 可见状态, 逻辑型
.局部变量 启用状态, 逻辑型
.局部变量 随机数, 整数型
.局部变量 emoji列表, 字节集, , "5"


.判断开始 (按钮ID ＝ 集_操作按钮)  ' 获取按钮1文本
    ' 第一次调用获取长度
    文本长度 ＝ 获取按钮文本 (按钮1, 0, 0)
    .如果真 (文本长度 ＞ 0)
        ' 第二次调用获取内容
        文本缓冲区 ＝ 取空白字节集 (文本长度)
        获取按钮文本 (按钮1, 取变量数据地址 (文本缓冲区), 文本长度)
        文本内容 ＝ 文本缓冲区
        状态信息 ＝ 编码_Ansi到Utf8 (“按钮1的文本是：”) ＋ 文本内容
        设置标签文本 (标签_状态, 取变量数据地址 (状态信息), 取字节集长度 (状态信息))
    .如果真结束

.判断 (按钮ID ＝ 集_修改按钮1文本)  ' 修改按钮1文本
    文本内容 ＝ 编码_Ansi到Utf8 (“新文本”) ＋ 编码_Ansi到Utf8 (到文本 (取随机数 (1, 100)))
    设置按钮文本 (按钮1, 取变量数据地址 (文本内容), 取字节集长度 (文本内容))
    状态信息 ＝ 编码_Ansi到Utf8 (“已将按钮1文本修改为：”) ＋ 文本内容
    设置标签文本 (标签_状态, 取变量数据地址 (状态信息), 取字节集长度 (状态信息))

.判断 (按钮ID ＝ 集_修改按钮1Emoji)  ' 修改按钮1Emoji
    emoji列表 [1] ＝ { 240, 159, 152, 128 }
    emoji列表 [2] ＝ { 240, 159, 142, 137 }
    emoji列表 [3] ＝ { 240, 159, 154, 128 }
    emoji列表 [4] ＝ { 226, 173, 144 }
    emoji列表 [5] ＝ { 240, 159, 146, 161 }
    随机数 ＝ 取随机数 (1, 5)
    文本内容 ＝ emoji列表 [随机数]
    设置按钮Emoji (按钮1, 取变量数据地址 (文本内容), 取字节集长度 (文本内容))
    状态信息 ＝ 编码_Ansi到Utf8 (“已将按钮1的Emoji修改为：”) ＋ 文本内容
    设置标签文本 (标签_状态, 取变量数据地址 (状态信息), 取字节集长度 (状态信息))

.判断 (按钮ID ＝ 集_获取按钮2位置)  ' 获取按钮2位置
    获取按钮位置 (按钮2, X, Y, 宽度, 高度)
    状态信息 ＝ 编码_Ansi到Utf8 (“按钮2的位置：” ＋ #换行符 ＋ “X=” ＋ 到文本 (X) ＋ “, Y=” ＋ 到文本 (Y) ＋ #换行符 ＋ “宽度=” ＋ 到文本 (宽度) ＋ “, 高度=” ＋ 到文本 (高度))
    设置标签文本 (标签_状态, 取变量数据地址 (状态信息), 取字节集长度 (状态信息))

.判断 (按钮ID ＝ 集_修改按钮2颜色)  ' 修改按钮2颜色
    ' 生成随机颜色
    背景色 ＝ 到ARGB (255, 取随机数 (50, 255), 取随机数 (50, 255), 取随机数 (50, 255))
    设置按钮背景色 (按钮2, 背景色)
    状态信息 ＝ 编码_Ansi到Utf8 (“已将按钮2的背景色修改为随机颜色”)
    设置标签文本 (标签_状态, 取变量数据地址 (状态信息), 取字节集长度 (状态信息))

.判断 (按钮ID ＝ 集_切换按钮3可见)  ' 切换按钮3可见
    可见状态 ＝ 获取按钮可视状态 (按钮3)
    显示隐藏按钮 (按钮3, 取反 (可见状态))
    .如果 (可见状态)
        状态信息 ＝ 编码_Ansi到Utf8 (“已隐藏按钮3”)
    .否则
        状态信息 ＝ 编码_Ansi到Utf8 (“已显示按钮3”)
    .如果结束

    设置标签文本 (标签_状态, 取变量数据地址 (状态信息), 取字节集长度 (状态信息))

.判断 (按钮ID ＝ 集_切换按钮3启用)  ' 切换按钮3启用
    启用状态 ＝ 获取按钮启用状态 (按钮3)
    .如果 (启用状态)
        禁用按钮 (主窗口句柄, 按钮3)
        状态信息 ＝ 编码_Ansi到Utf8 (“已禁用按钮3”)

    .否则
        启用按钮 (主窗口句柄, 按钮3, 真)
        状态信息 ＝ 编码_Ansi到Utf8 (“已启用按钮3”)

    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (状态信息), 取字节集长度 (状态信息))

.判断 (按钮ID ＝ 集_移动按钮1)  ' 移动按钮1
    ' 获取当前位置
    获取按钮位置 (按钮1, X, Y, 宽度, 高度)
    ' 移动到新位置（随机偏移）
    X ＝ X ＋ 取随机数 (-50, 50)
    Y ＝ Y ＋ 取随机数 (-20, 20)
    ' 确保不超出窗口范围
    .如果真 (X ＜ 10)
        X ＝ 10
    .如果真结束
    .如果真 (Y ＜ 10)
        Y ＝ 10
    .如果真结束
    .如果真 (X ＋ 宽度 ＞ 790)
        X ＝ 790 － 宽度
    .如果真结束
    .如果真 (Y ＋ 高度 ＞ 110)
        Y ＝ 110 － 高度
    .如果真结束
    设置按钮位置 (按钮1, X, Y, 宽度, 高度)
    状态信息 ＝ 编码_Ansi到Utf8 (“已将按钮1移动到新位置：X=” ＋ 到文本 (X) ＋ “, Y=” ＋ 到文本 (Y))
    设置标签文本 (标签_状态, 取变量数据地址 (状态信息), 取字节集长度 (状态信息))

.默认

.判断结束


.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
