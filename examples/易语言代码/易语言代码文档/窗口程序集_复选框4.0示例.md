.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_复选框示例
.程序集变量 窗口句柄, 整数型
.程序集变量 复选框1, 整数型
.程序集变量 复选框2, 整数型
.程序集变量 复选框3, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 集_切换复选框按钮, 整数型
.程序集变量 集_获取所有状态按钮, 整数型

.子程序 _窗口_复选框示例_创建完毕
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集

' 创建自绘窗口
窗口句柄 ＝ 创建Emoji窗口 (“复选框4.0示例”, 500, 600)
设置窗口图标 (窗口句柄, “icon.ico”)
字体字节集 ＝ 编码_Ansi到Utf8 ("Microsoft YaHei UI")

' 创建标题标签
文本字节集 ＝ 编码_Ansi到Utf8 ("复选框控件示例")
创建标签 (窗口句柄, 20, 20, 460, 30, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 16, 真, 假, 假, 1, 假)

' 创建复选框1
文本字节集 ＝ 编码_Ansi到Utf8 ("接受用户协议")
复选框1 ＝ 创建复选框 (窗口句柄, 50, 80, 200, 30, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 假, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假)
设置复选框回调 (复选框1, 到整数 (&复选框回调))

' 创建复选框2
文本字节集 ＝ 编码_Ansi到Utf8 ("记住密码")
复选框2 ＝ 创建复选框 (窗口句柄, 50, 130, 200, 30, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 真, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假)
设置复选框回调 (复选框2, 到整数 (&复选框回调))

' 创建复选框3（禁用状态）
文本字节集 ＝ 编码_Ansi到Utf8 ("自动登录（禁用）")
复选框3 ＝ 创建复选框 (窗口句柄, 50, 180, 200, 30, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 假, #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假)
启用复选框 (复选框3, 假)

' 创建状态显示标签
文本字节集 ＝ 编码_Ansi到Utf8 ("状态：等待操作...")
标签_状态 ＝ 创建标签 (窗口句柄, 50, 250, 400, 100, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_REGULAR, #COLOR_BG_LIGHT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 8, 假, 假, 假, 0, 真)

' 创建测试按钮
' 旧版简化调用示意已移除，下面改为完整版按钮创建

emoji字节集 ＝ { 226, 154, 153, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换复选框1”)
集_切换复选框按钮 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 50, 350, 120, 40, #COLOR_PRIMARY)

' 旧版简化调用示意已移除，下面改为完整版按钮创建
emoji字节集 ＝ { 240, 159, 147, 139 }
文本字节集 ＝ 编码_Ansi到Utf8 (“获取所有状态”)
集_获取所有状态按钮 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 190, 350, 120, 40, #COLOR_PRIMARY)


' 设置按钮回调
设置按钮点击回调 (&按钮点击回调)

' 设置窗口关闭回调
设置窗口关闭回调 (&窗口关闭回调)


.子程序 复选框回调, , , stdcall
.参数 复选框句柄, 整数型
.参数 选中状态, 逻辑型
.局部变量 状态文本, 文本型
.局部变量 状态字节集, 字节集

状态文本 ＝ “复选框状态改变：” ＋ #换行符

.如果真 (复选框句柄 ＝ 复选框1)
    状态文本 ＝ 状态文本 ＋ “接受用户协议：” ＋ 到文本 (选中状态) ＋ #换行符
.如果真结束

.如果真 (复选框句柄 ＝ 复选框2)
    状态文本 ＝ 状态文本 ＋ “记住密码：” ＋ 到文本 (选中状态) ＋ #换行符
.如果真结束

状态文本 ＝ 状态文本 ＋ #换行符 ＋ “当前所有状态：” ＋ #换行符
状态文本 ＝ 状态文本 ＋ “复选框1：” ＋ 到文本 (获取复选框状态 (复选框1)) ＋ #换行符
状态文本 ＝ 状态文本 ＋ “复选框2：” ＋ 到文本 (获取复选框状态 (复选框2)) ＋ #换行符
状态文本 ＝ 状态文本 ＋ “复选框3：” ＋ 到文本 (获取复选框状态 (复选框3))

状态字节集 ＝ 编码_Ansi到Utf8 (状态文本)
设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 状态文本, 文本型
.局部变量 状态字节集, 字节集

.判断开始 (按钮ID ＝ 集_切换复选框按钮)
    ' 切换复选框1
    设置复选框状态 (复选框1, 取反 (获取复选框状态 (复选框1)))

.判断 (按钮ID ＝ 集_获取所有状态按钮)
    ' 获取所有状态
    状态文本 ＝ “当前所有复选框状态：” ＋ #换行符 ＋ #换行符
    状态文本 ＝ 状态文本 ＋ “复选框1（接受用户协议）：” ＋ 到文本 (获取复选框状态 (复选框1)) ＋ #换行符
    状态文本 ＝ 状态文本 ＋ “复选框2（记住密码）：” ＋ 到文本 (获取复选框状态 (复选框2)) ＋ #换行符
    状态文本 ＝ 状态文本 ＋ “复选框3（自动登录-禁用）：” ＋ 到文本 (获取复选框状态 (复选框3))

    状态字节集 ＝ 编码_Ansi到Utf8 (状态文本)
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.默认

.判断结束


.子程序 窗口关闭回调, , , stdcall
.参数 窗口句柄_, 整数型

调试输出 (“窗口被关闭，句柄：”, 窗口句柄_)
结束 ()
