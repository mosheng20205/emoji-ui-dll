.版本 2
.支持库 spec

.DLL命令 设置输入焦点, 整数型, "user32.dll", "SetFocus", , , 设置指定窗口焦点
    .参数 窗口句柄, 整数型

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 默认字体字节集, 字节集
.程序集变量 菜单栏句柄, 整数型
.程序集变量 导航树句柄, 整数型
.程序集变量 主TabControl句柄, 整数型
.程序集变量 弹出菜单句柄, 整数型
.程序集变量 当前通知句柄, 整数型
.程序集变量 表格行计数器, 整数型
.程序集变量 当前图片缩放模式, 整数型
.程序集变量 当前图片透明度百分比, 整数型
.程序集变量 图片框当前可见, 逻辑型
.程序集变量 图片框当前启用, 逻辑型
.程序集变量 当前图片背景色, 整数型
.程序集变量 当前图片背景名称, 文本型
.程序集变量 当前Tooltip主题, 整数型
.程序集变量 当前Tooltip触发方式, 整数型
.程序集变量 当前Tooltip位置, 整数型
.程序集变量 当前表格选择模式, 整数型
.程序集变量 表格冻结首列已开启, 逻辑型
.程序集变量 当前表格导出说明, 文本型
.程序集变量 当前窗口背景色, 整数型
.程序集变量 当前窗口背景名称, 文本型
.程序集变量 当前窗口主题名称, 文本型
.程序集变量 当前基础页单选文本, 文本型
.程序集变量 标签_页头标题, 整数型
.程序集变量 标签_页头说明, 整数型
.程序集变量 标签_全局状态, 整数型
.程序集变量 标签_窗口属性读取, 整数型
.程序集变量 标签_窗口读取摘要, 整数型
.程序集变量 标签_窗口状态, 整数型
.程序集变量 标签_基础摘要, 整数型
.程序集变量 标签_基础状态, 整数型
.程序集变量 标签_选择摘要, 整数型
.程序集变量 标签_选择状态, 整数型
.程序集变量 标签_显示摘要, 整数型
.程序集变量 标签_显示状态, 整数型
.程序集变量 标签_弹窗状态, 整数型
.程序集变量 标签_导航摘要, 整数型
.程序集变量 标签_导航状态, 整数型
.程序集变量 标签_表格顶部模式, 整数型
.程序集变量 标签_表格顶部状态, 整数型
.程序集变量 标签_表格顶部导出, 整数型
.程序集变量 标签_表格摘要, 整数型
.程序集变量 标签_表格单元格摘要, 整数型
.程序集变量 标签_表格导出摘要, 整数型
.程序集变量 标签_表格状态, 整数型
.程序集变量 标签_Tooltip目标, 整数型
.程序集变量 标签_右键目标, 整数型
.程序集变量 编辑框_普通, 整数型
.程序集变量 编辑框_彩色Emoji, 整数型
.程序集变量 复选框_功能, 整数型
.程序集变量 单选按钮_浅色, 整数型
.程序集变量 单选按钮_深色, 整数型
.程序集变量 进度条_任务, 整数型
.程序集变量 滑块_音量, 整数型
.程序集变量 开关_通知, 整数型
.程序集变量 分组框_基础, 整数型
.程序集变量 面板_基础信息, 整数型
.程序集变量 列表框_分类, 整数型
.程序集变量 组合框_城市, 整数型
.程序集变量 D2D组合框_主题, 整数型
.程序集变量 日期时间_计划, 整数型
.程序集变量 热键控件_快捷, 整数型
.程序集变量 图片框_预览, 整数型
.程序集变量 提示层_说明, 整数型
.程序集变量 内层树句柄, 整数型
.程序集变量 内层TabControl句柄, 整数型
.程序集变量 表格句柄, 整数型
.程序集变量 窗口页已构建, 逻辑型
.程序集变量 基础页已构建, 逻辑型
.程序集变量 选择页已构建, 逻辑型
.程序集变量 显示页已构建, 逻辑型
.程序集变量 弹窗页已构建, 逻辑型
.程序集变量 导航页已构建, 逻辑型
.程序集变量 数据页已构建, 逻辑型
.程序集变量 窗口页句柄, 整数型
.程序集变量 基础页句柄, 整数型
.程序集变量 选择页句柄, 整数型
.程序集变量 显示页句柄, 整数型
.程序集变量 弹窗页句柄, 整数型
.程序集变量 导航页句柄, 整数型
.程序集变量 数据页句柄, 整数型
.程序集变量 按钮_窗口标题_产品, 整数型
.程序集变量 按钮_窗口标题_调试, 整数型
.程序集变量 按钮_窗口标题_彩色, 整数型
.程序集变量 按钮_窗口尺寸_紧凑, 整数型
.程序集变量 按钮_窗口尺寸_标准, 整数型
.程序集变量 按钮_窗口尺寸_加宽, 整数型
.程序集变量 按钮_窗口位置_重置, 整数型
.程序集变量 按钮_窗口位置_右移, 整数型
.程序集变量 按钮_窗口位置_下移, 整数型
.程序集变量 按钮_窗口位置_读取, 整数型
.程序集变量 按钮_窗口主题_浅色, 整数型
.程序集变量 按钮_窗口主题_深色, 整数型
.程序集变量 按钮_标题栏背景_蓝, 整数型
.程序集变量 按钮_标题栏背景_黑, 整数型
.程序集变量 按钮_标题栏背景_绿, 整数型
.程序集变量 按钮_标题栏背景_跟随, 整数型
.程序集变量 按钮_标题栏文字_亮, 整数型
.程序集变量 按钮_标题栏文字_暗, 整数型
.程序集变量 按钮_标题栏文字_蓝, 整数型
.程序集变量 按钮_标题栏文字_跟随, 整数型
.程序集变量 按钮_客户区背景_白, 整数型
.程序集变量 按钮_客户区背景_灰, 整数型
.程序集变量 按钮_客户区背景_深, 整数型
.程序集变量 按钮_客户区背景_跟随, 整数型
.程序集变量 按钮_基础演示, 整数型
.程序集变量 按钮_读取编辑框, 整数型
.程序集变量 按钮_写入编辑框, 整数型
.程序集变量 按钮_推进进度, 整数型
.程序集变量 按钮_切换开关, 整数型
.程序集变量 按钮_重置基础页, 整数型
.程序集变量 按钮_读取选择结果, 整数型
.程序集变量 按钮_设置快捷键, 整数型
.程序集变量 按钮_选择_新增列表项, 整数型
.程序集变量 按钮_选择_选中第三项, 整数型
.程序集变量 按钮_选择_组合框写入, 整数型
.程序集变量 按钮_选择_热键AltF4, 整数型
.程序集变量 按钮_加载图片, 整数型
.程序集变量 按钮_清除图片, 整数型
.程序集变量 按钮_切换图片模式, 整数型
.程序集变量 按钮_图片透明75, 整数型
.程序集变量 按钮_图片透明100, 整数型
.程序集变量 按钮_图片透明25, 整数型
.程序集变量 按钮_图片背景冷, 整数型
.程序集变量 按钮_图片背景暖, 整数型
.程序集变量 按钮_图片切换可见, 整数型
.程序集变量 按钮_图片切换启用, 整数型
.程序集变量 按钮_显示Tooltip, 整数型
.程序集变量 按钮_隐藏Tooltip, 整数型
.程序集变量 按钮_Tooltip浅色, 整数型
.程序集变量 按钮_Tooltip下方, 整数型
.程序集变量 按钮_信息通知, 整数型
.程序集变量 按钮_成功通知, 整数型
.程序集变量 按钮_警告通知, 整数型
.程序集变量 按钮_错误通知, 整数型
.程序集变量 按钮_连续通知, 整数型
.程序集变量 按钮_关闭通知, 整数型
.程序集变量 按钮_打开消息框, 整数型
.程序集变量 按钮_打开确认框, 整数型
.程序集变量 按钮_手动弹出菜单, 整数型
.程序集变量 按钮_新增行, 整数型
.程序集变量 按钮_删除选中行, 整数型
.程序集变量 按钮_按状态排序, 整数型
.程序集变量 按钮_导出CSV, 整数型
.程序集变量 按钮_表格_单元格模式, 整数型
.程序集变量 按钮_表格_整行模式, 整数型
.程序集变量 按钮_表格_写入单元格, 整数型
.程序集变量 按钮_表格_冻结首列, 整数型
.程序集变量 按钮_表格_定位首行, 整数型
.程序集变量 按钮_表格_刷新摘要, 整数型
.程序集变量 节点_基础根, 整数型
.程序集变量 节点_基础_EmojiWindow, 整数型
.程序集变量 节点_基础_按钮, 整数型
.程序集变量 节点_基础_标签, 整数型
.程序集变量 节点_基础_编辑框, 整数型
.程序集变量 节点_基础_彩色编辑框, 整数型
.程序集变量 节点_基础_复选框, 整数型
.程序集变量 节点_基础_单选框, 整数型
.程序集变量 节点_基础_进度条, 整数型
.程序集变量 节点_基础_滑块, 整数型
.程序集变量 节点_基础_开关, 整数型
.程序集变量 节点_基础_分组框, 整数型
.程序集变量 节点_基础_面板, 整数型
.程序集变量 节点_选择根, 整数型
.程序集变量 节点_选择_列表框, 整数型
.程序集变量 节点_选择_组合框, 整数型
.程序集变量 节点_选择_D2D组合框, 整数型
.程序集变量 节点_选择_日期时间, 整数型
.程序集变量 节点_选择_热键, 整数型
.程序集变量 节点_显示根, 整数型
.程序集变量 节点_显示_图片框, 整数型
.程序集变量 节点_显示_Tooltip, 整数型
.程序集变量 节点_显示_Notification, 整数型
.程序集变量 节点_弹窗根, 整数型
.程序集变量 节点_弹窗_MessageBox, 整数型
.程序集变量 节点_弹窗_ConfirmBox, 整数型
.程序集变量 节点_导航根, 整数型
.程序集变量 节点_导航_TabControl, 整数型
.程序集变量 节点_导航_TreeView, 整数型
.程序集变量 节点_导航_MenuBar, 整数型
.程序集变量 节点_导航_PopupMenu, 整数型
.程序集变量 节点_数据根, 整数型
.程序集变量 节点_数据_表格, 整数型
.程序集变量 内层节点_总览, 整数型
.程序集变量 内层节点_设置, 整数型
.程序集变量 内层节点_帮助, 整数型
.程序集变量 内层节点_最新事件, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集

默认字体字节集 ＝ 编码_Ansi到Utf8 (“微软雅黑”)
标题字节集 ＝ 编码_Ansi到Utf8 (“所有控件树形增强版4.0示例”)
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 80, 40, 1600, 960, 到RGB (64, 158, 255), 到ARGB (255, 248, 250, 252))
.如果真 (主窗口句柄 ＝ 0)
    信息框 (“创建主窗口失败”, 0, , )
    返回 (0)
.如果真结束

当前窗口背景色 ＝ 到ARGB (255, 248, 250, 252)
当前窗口背景名称 ＝ “主题默认背景”
当前窗口主题名称 ＝ “浅色主题”
当前基础页单选文本 ＝ “浅色主题”

_创建顶部菜单 ()
_创建页头区域 ()
_创建导航树 ()
_创建主TabControl ()

' 导航树节点和页面都创建完后再绑定回调，避免初始化阶段回调访问未创建的TabControl
设置树形框回调 (导航树句柄, 1, &导航树选中回调)
设置树形框回调 (导航树句柄, 2, &导航树展开回调)
设置树形框回调 (导航树句柄, 3, &导航树折叠回调)

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)

设置页头 (“基础组件 / 窗口”, “基础组件中的窗口页，负责主题切换、标题栏颜色、客户区背景和实时状态读取。”)
更新状态 (“树形增强版 4.0 示例已启动。左侧 TreeView 对应 Python demo_all_components_tree_enhanced.py 的全部控件分类。”)

设置选中树节点 (导航树句柄, 节点_基础_EmojiWindow)
切换到Tab (主TabControl句柄, 0)

运行消息循环 ()
返回 (0)

.子程序 _创建顶部菜单
.局部变量 文本字节集, 字节集

菜单栏句柄 ＝ 创建菜单栏 (主窗口句柄)

文本字节集 ＝ { 240, 159, 147, 129, 32, 230, 150, 135, 228, 187, 182 }
菜单栏添加项 (菜单栏句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 900)
文本字节集 ＝ { 240, 159, 167, 173, 32, 229, 175, 188, 232, 136, 170 }
菜单栏添加项 (菜单栏句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1000)
文本字节集 ＝ { 240, 159, 146, 172, 32, 229, 175, 185, 232, 175, 157 }
菜单栏添加项 (菜单栏句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1100)
文本字节集 ＝ { 240, 159, 147, 138, 32, 230, 149, 176, 230, 141, 174 }
菜单栏添加项 (菜单栏句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1200)

文本字节集 ＝ { 240, 159, 147, 157, 32, 230, 182, 136, 230, 129, 175 }
菜单栏添加子项 (菜单栏句柄, 900, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 910)
文本字节集 ＝ { 240, 159, 146, 172, 32, 230, 152, 190, 231, 164, 186, 230, 182, 136, 230, 129, 175, 230, 161, 134 }
菜单栏添加子项 (菜单栏句柄, 910, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 911)
文本字节集 ＝ { 226, 157, 147, 32, 230, 152, 190, 231, 164, 186, 231, 161, 174, 232, 174, 164, 230, 161, 134 }
菜单栏添加子项 (菜单栏句柄, 910, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 912)
文本字节集 ＝ { 240, 159, 147, 130, 32, 229, 175, 188, 229, 135, 186 }
菜单栏添加子项 (菜单栏句柄, 900, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 920)
文本字节集 ＝ { 240, 159, 146, 190, 32, 229, 175, 188, 229, 135, 186, 32, 67, 83, 86 }
菜单栏添加子项 (菜单栏句柄, 920, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 921)
文本字节集 ＝ { 240, 159, 154, 170, 32, 233, 128, 128, 229, 135, 186 }
菜单栏添加子项 (菜单栏句柄, 900, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 930)
文本字节集 ＝ { 240, 159, 154, 170, 32, 233, 128, 128, 229, 135, 186, 231, 164, 186, 228, 190, 139 }
菜单栏添加子项 (菜单栏句柄, 930, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 931)

文本字节集 ＝ { 240, 159, 148, 152, 32 } ＋ 编码_Ansi到Utf8 (“基础”)
菜单栏添加子项 (菜单栏句柄, 1000, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1001)
文本字节集 ＝ { 240, 159, 143, 160, 32 } ＋ 编码_Ansi到Utf8 (“窗口”)
菜单栏添加子项 (菜单栏句柄, 1000, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1006)
文本字节集 ＝ { 240, 159, 147, 139, 32 } ＋ 编码_Ansi到Utf8 (“选择”)
菜单栏添加子项 (菜单栏句柄, 1000, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1002)
文本字节集 ＝ { 240, 159, 150, 188, 32 } ＋ 编码_Ansi到Utf8 (“显示”)
菜单栏添加子项 (菜单栏句柄, 1000, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1003)
文本字节集 ＝ { 240, 159, 146, 172, 32 } ＋ 编码_Ansi到Utf8 (“弹窗”)
菜单栏添加子项 (菜单栏句柄, 1000, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1004)
文本字节集 ＝ { 240, 159, 167, 173, 32 } ＋ 编码_Ansi到Utf8 (“导航”)
菜单栏添加子项 (菜单栏句柄, 1000, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1005)
文本字节集 ＝ { 240, 159, 147, 138, 32 } ＋ 编码_Ansi到Utf8 (“数据”)
菜单栏添加子项 (菜单栏句柄, 1000, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1007)

文本字节集 ＝ { 240, 159, 146, 140, 32, 230, 137, 147, 229, 188, 128, 32, 77, 101, 115, 115, 97, 103, 101, 66, 111, 120 }
菜单栏添加子项 (菜单栏句柄, 1100, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1101)
文本字节集 ＝ { 226, 157, 147, 32, 230, 137, 147, 229, 188, 128, 32, 67, 111, 110, 102, 105, 114, 109, 66, 111, 120 }
菜单栏添加子项 (菜单栏句柄, 1100, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1102)
文本字节集 ＝ { 226, 156, 133, 32, 230, 152, 190, 231, 164, 186, 230, 136, 144, 229, 138, 159, 233, 128, 154, 231, 159, 165 }
菜单栏添加子项 (菜单栏句柄, 1100, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1103)

文本字节集 ＝ { 226, 158, 149, 32, 230, 150, 176, 229, 162, 158, 228, 184, 128, 232, 161, 140 }
菜单栏添加子项 (菜单栏句柄, 1200, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1201)
文本字节集 ＝ { 240, 159, 146, 190, 32, 229, 175, 188, 229, 135, 186, 32, 67, 83, 86 }
菜单栏添加子项 (菜单栏句柄, 1200, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1202)

设置菜单栏回调 (菜单栏句柄, &菜单栏回调)

.子程序 _创建页头区域
.局部变量 文本字节集, 字节集

文本字节集 ＝ 编码_Ansi到Utf8 (“等待选择”)
标签_页头标题 ＝ 创建标签 (主窗口句柄, 300, 56, 960, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 248, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 19, 真, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“从左侧树形框选择一个分类或控件。”)
标签_页头说明 ＝ 创建标签 (主窗口句柄, 300, 88, 1240, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 248, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“准备就绪”)
标签_全局状态 ＝ 创建标签 (主窗口句柄, 20, 900, 1540, 30, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 248, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

.子程序 _创建导航树
.局部变量 文本字节集, 字节集
.局部变量 emoji字节集, 字节集

导航树句柄 ＝ 创建树形框 (主窗口句柄, 20, 56, 250, 820, 到ARGB (255, 255, 255, 255), 到ARGB (255, 48, 49, 51), 0)
设置树形框侧栏模式 (导航树句柄, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“基础组件”)
emoji字节集 ＝ { 240, 159, 167, 177 }
节点_基础根 ＝ 添加根节点 (导航树句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“EmojiWindow”)
emoji字节集 ＝ { 240, 159, 170, 159 }
节点_基础_EmojiWindow ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“EmojiButton”)
emoji字节集 ＝ { 240, 159, 148, 152 }
节点_基础_按钮 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“Label”)
emoji字节集 ＝ { 240, 159, 143, 183, 239, 184, 143 }
节点_基础_标签 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“EditBox”)
emoji字节集 ＝ { 226, 140, 168, 239, 184, 143 }
节点_基础_编辑框 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“彩色 Emoji 编辑框”)
emoji字节集 ＝ { 240, 159, 140, 136 }
节点_基础_彩色编辑框 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“CheckBox”)
emoji字节集 ＝ { 226, 152, 145, 239, 184, 143 }
节点_基础_复选框 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“RadioButton”)
emoji字节集 ＝ { 240, 159, 148, 152 }
节点_基础_单选框 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“ProgressBar”)
emoji字节集 ＝ { 240, 159, 147, 136 }
节点_基础_进度条 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“Slider”)
emoji字节集 ＝ { 240, 159, 142, 154, 239, 184, 143 }
节点_基础_滑块 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“Switch”)
emoji字节集 ＝ { 240, 159, 148, 148 }
节点_基础_开关 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“GroupBox”)
emoji字节集 ＝ { 240, 159, 151, 131, 239, 184, 143 }
节点_基础_分组框 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“Panel”)
emoji字节集 ＝ { 240, 159, 170, 159 }
节点_基础_面板 ＝ 添加子节点 (导航树句柄, 节点_基础根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))

文本字节集 ＝ 编码_Ansi到Utf8 (“选择类组件”)
emoji字节集 ＝ { 240, 159, 142, 155, 239, 184, 143 }
节点_选择根 ＝ 添加根节点 (导航树句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“ListBox”)
emoji字节集 ＝ { 240, 159, 147, 156 }
节点_选择_列表框 ＝ 添加子节点 (导航树句柄, 节点_选择根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“ComboBox”)
emoji字节集 ＝ { 240, 159, 147, 130 }
节点_选择_组合框 ＝ 添加子节点 (导航树句柄, 节点_选择根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“D2DComboBox”)
emoji字节集 ＝ { 240, 159, 142, 168 }
节点_选择_D2D组合框 ＝ 添加子节点 (导航树句柄, 节点_选择根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“D2DDateTimePicker”)
emoji字节集 ＝ { 240, 159, 147, 133 }
节点_选择_日期时间 ＝ 添加子节点 (导航树句柄, 节点_选择根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“HotKeyControl”)
emoji字节集 ＝ { 226, 140, 168, 239, 184, 143 }
节点_选择_热键 ＝ 添加子节点 (导航树句柄, 节点_选择根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))

文本字节集 ＝ 编码_Ansi到Utf8 (“显示类组件”)
emoji字节集 ＝ { 240, 159, 150, 188, 239, 184, 143 }
节点_显示根 ＝ 添加根节点 (导航树句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“PictureBox”)
emoji字节集 ＝ { 240, 159, 150, 188, 239, 184, 143 }
节点_显示_图片框 ＝ 添加子节点 (导航树句柄, 节点_显示根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“Tooltip”)
emoji字节集 ＝ { 240, 159, 146, 161 }
节点_显示_Tooltip ＝ 添加子节点 (导航树句柄, 节点_显示根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“Notification”)
emoji字节集 ＝ { 240, 159, 148, 148 }
节点_显示_Notification ＝ 添加子节点 (导航树句柄, 节点_显示根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))

文本字节集 ＝ 编码_Ansi到Utf8 (“弹窗类组件”)
emoji字节集 ＝ { 240, 159, 170, 132 }
节点_弹窗根 ＝ 添加根节点 (导航树句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“MessageBox”)
emoji字节集 ＝ { 240, 159, 146, 140 }
节点_弹窗_MessageBox ＝ 添加子节点 (导航树句柄, 节点_弹窗根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“ConfirmBox”)
emoji字节集 ＝ { 226, 157, 147 }
节点_弹窗_ConfirmBox ＝ 添加子节点 (导航树句柄, 节点_弹窗根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))

文本字节集 ＝ 编码_Ansi到Utf8 (“页签与导航”)
emoji字节集 ＝ { 240, 159, 167, 173 }
节点_导航根 ＝ 添加根节点 (导航树句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“TabControl”)
emoji字节集 ＝ { 240, 159, 147, 145 }
节点_导航_TabControl ＝ 添加子节点 (导航树句柄, 节点_导航根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“TreeView”)
emoji字节集 ＝ { 240, 159, 140, 178 }
节点_导航_TreeView ＝ 添加子节点 (导航树句柄, 节点_导航根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“MenuBar”)
emoji字节集 ＝ { 240, 159, 167, 173 }
节点_导航_MenuBar ＝ 添加子节点 (导航树句柄, 节点_导航根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“EmojiPopupMenu”)
emoji字节集 ＝ { 240, 159, 147, 139 }
节点_导航_PopupMenu ＝ 添加子节点 (导航树句柄, 节点_导航根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))

文本字节集 ＝ 编码_Ansi到Utf8 (“数据组件”)
emoji字节集 ＝ { 240, 159, 167, 190 }
节点_数据根 ＝ 添加根节点 (导航树句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“DataGridView”)
emoji字节集 ＝ { 240, 159, 167, 174 }
节点_数据_表格 ＝ 添加子节点 (导航树句柄, 节点_数据根, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))

展开树节点 (导航树句柄, 节点_基础根)
展开树节点 (导航树句柄, 节点_选择根)
展开树节点 (导航树句柄, 节点_显示根)
展开树节点 (导航树句柄, 节点_弹窗根)
展开树节点 (导航树句柄, 节点_导航根)
展开树节点 (导航树句柄, 节点_数据根)

.子程序 _创建主TabControl
.局部变量 文本字节集, 字节集

主TabControl句柄 ＝ 创建TabControl (主窗口句柄, 300, 120, 1270, 760)
文本字节集 ＝ { 240, 159, 143, 160, 32 } ＋ 编码_Ansi到Utf8 (“窗口”)
添加Tab页 (主TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)
文本字节集 ＝ { 240, 159, 148, 152, 32 } ＋ 编码_Ansi到Utf8 (“基础”)
添加Tab页 (主TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)
文本字节集 ＝ { 240, 159, 147, 139, 32 } ＋ 编码_Ansi到Utf8 (“选择”)
添加Tab页 (主TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)
文本字节集 ＝ { 240, 159, 150, 188, 32 } ＋ 编码_Ansi到Utf8 (“显示”)
添加Tab页 (主TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)
文本字节集 ＝ { 240, 159, 146, 172, 32 } ＋ 编码_Ansi到Utf8 (“弹窗”)
添加Tab页 (主TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)
文本字节集 ＝ { 240, 159, 167, 173, 32 } ＋ 编码_Ansi到Utf8 (“导航”)
添加Tab页 (主TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)
文本字节集 ＝ { 240, 159, 147, 138, 32 } ＋ 编码_Ansi到Utf8 (“数据”)
添加Tab页 (主TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)

设置Tab切换回调 (主TabControl句柄, &Tab切换回调)

窗口页已构建 ＝ 假
基础页已构建 ＝ 假
选择页已构建 ＝ 假
显示页已构建 ＝ 假
弹窗页已构建 ＝ 假
导航页已构建 ＝ 假
数据页已构建 ＝ 假

_确保Tab页已构建 (0)

.子程序 _确保Tab页已构建
.参数 索引, 整数型
.局部变量 页面句柄, 整数型

.判断开始 (索引 ＝ 0)
    .如果真 (窗口页已构建 ＝ 假)
        页面句柄 ＝ 获取Tab内容窗口 (主TabControl句柄, 0)
        窗口页句柄 ＝ 页面句柄
        _构建窗口页 (页面句柄)
        窗口页已构建 ＝ 真
    .如果真结束
.判断 (索引 ＝ 1)
    .如果真 (基础页已构建 ＝ 假)
        页面句柄 ＝ 获取Tab内容窗口 (主TabControl句柄, 1)
        基础页句柄 ＝ 页面句柄
        _构建基础页 (页面句柄)
        基础页已构建 ＝ 真
    .如果真结束
.判断 (索引 ＝ 2)
    .如果真 (选择页已构建 ＝ 假)
        页面句柄 ＝ 获取Tab内容窗口 (主TabControl句柄, 2)
        选择页句柄 ＝ 页面句柄
        _构建选择页 (页面句柄)
        选择页已构建 ＝ 真
    .如果真结束
.判断 (索引 ＝ 3)
    .如果真 (显示页已构建 ＝ 假)
        页面句柄 ＝ 获取Tab内容窗口 (主TabControl句柄, 3)
        显示页句柄 ＝ 页面句柄
        _构建显示页 (页面句柄)
        显示页已构建 ＝ 真
    .如果真结束
.判断 (索引 ＝ 4)
    .如果真 (弹窗页已构建 ＝ 假)
        页面句柄 ＝ 获取Tab内容窗口 (主TabControl句柄, 4)
        弹窗页句柄 ＝ 页面句柄
        _构建弹窗页 (页面句柄)
        弹窗页已构建 ＝ 真
    .如果真结束
.判断 (索引 ＝ 5)
    .如果真 (导航页已构建 ＝ 假)
        页面句柄 ＝ 获取Tab内容窗口 (主TabControl句柄, 5)
        导航页句柄 ＝ 页面句柄
        _构建导航页 (页面句柄)
        导航页已构建 ＝ 真
    .如果真结束
.判断 (索引 ＝ 6)
    .如果真 (数据页已构建 ＝ 假)
        页面句柄 ＝ 获取Tab内容窗口 (主TabControl句柄, 6)
        数据页句柄 ＝ 页面句柄
        _构建数据页 (页面句柄)
        数据页已构建 ＝ 真
    .如果真结束
.默认
.判断结束

.子程序 _构建窗口页
.参数 页面句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 分组框_窗口读取, 整数型
.局部变量 分组框_窗口操作, 整数型
.局部变量 分组框_窗口主题, 整数型
.局部变量 分组框_窗口说明, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (“这一页专门补齐 Python 示例里的 EmojiWindow 属性操作：标题读取与设置、窗口位置尺寸、主题切换、标题栏背景色、标题栏文字色、客户区背景色。”)
创建标签 (页面句柄, 20, 18, 1180, 42, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“窗口页状态：等待操作。”)
标签_窗口状态 ＝ 创建标签 (页面句柄, 20, 718, 1180, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)

文本字节集 ＝ 编码_Ansi到Utf8 (“EmojiWindow 实时属性读取”)
分组框_窗口读取 ＝ 创建分组框 (页面句柄, 20, 80, 760, 220, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_窗口读取, 1)
设置分组框标题颜色 (分组框_窗口读取, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“这里直接读取主窗口的实时属性，包含标题、句柄、位置尺寸、主题色、标题栏颜色与客户区背景。”)
创建标签 (页面句柄, 40, 118, 500, 52, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“读取策略卡” ＋ #换行符 ＋ “直接从主窗口句柄回读，不依赖静态示意文本。”)
创建标签 (页面句柄, 556, 118, 184, 52, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“等待读取窗口属性…”)
标签_窗口属性读取 ＝ 创建标签 (页面句柄, 40, 186, 500, 96, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“等待窗口策略摘要…”)
标签_窗口读取摘要 ＝ 创建标签 (页面句柄, 556, 186, 184, 96, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“标题 / 尺寸 / 位置快捷设置”)
分组框_窗口操作 ＝ 创建分组框 (页面句柄, 20, 320, 760, 250, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_窗口操作, 1)
设置分组框标题颜色 (分组框_窗口操作, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“标题预设”)
创建标签 (页面句柄, 40, 356, 140, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“产品 / 调试 / 彩色三套窗口标题方案。”)
创建标签 (页面句柄, 206, 356, 312, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“产品标题”)
按钮_窗口标题_产品 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 40, 388, 156, 36, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“调试标题”)
按钮_窗口标题_调试 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 210, 388, 156, 36, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“彩色标题”)
按钮_窗口标题_彩色 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 380, 388, 156, 36, 到ARGB (255, 142, 68, 173))

文本字节集 ＝ 编码_Ansi到Utf8 (“尺寸预设”)
创建标签 (页面句柄, 40, 438, 140, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“紧凑 / 标准 / 加宽三档布局，便于对照 Python 窗口舞台。”)
创建标签 (页面句柄, 206, 438, 334, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)
emoji字节集 ＝ { 240, 159, 147, 144 }
文本字节集 ＝ 编码_Ansi到Utf8 (“紧凑 1600x900”)
按钮_窗口尺寸_紧凑 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 40, 470, 164, 36, 到ARGB (255, 230, 162, 60))
emoji字节集 ＝ { 240, 159, 147, 144 }
文本字节集 ＝ 编码_Ansi到Utf8 (“标准 1820x980”)
按钮_窗口尺寸_标准 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 214, 470, 164, 36, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 147, 144 }
文本字节集 ＝ 编码_Ansi到Utf8 (“加宽 1920x1040”)
按钮_窗口尺寸_加宽 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 388, 470, 164, 36, 到ARGB (255, 103, 194, 58))

文本字节集 ＝ 编码_Ansi到Utf8 (“位置调整”)
创建标签 (页面句柄, 576, 356, 140, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“恢复起点、横向偏移、纵向偏移和立即回读四个动作入口。”)
创建标签 (页面句柄, 576, 380, 160, 34, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)
emoji字节集 ＝ { 240, 159, 147, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“恢复到 40,40”)
按钮_窗口位置_重置 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 576, 422, 156, 36, 到ARGB (255, 144, 147, 153))
emoji字节集 ＝ { 240, 159, 147, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“向右移动 80”)
按钮_窗口位置_右移 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 576, 456, 156, 36, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 147, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“向下移动 60”)
按钮_窗口位置_下移 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 576, 490, 156, 36, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 240, 159, 147, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“立即读取”)
按钮_窗口位置_读取 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 576, 524, 156, 36, 到ARGB (255, 142, 68, 173))

文本字节集 ＝ 编码_Ansi到Utf8 (“主题 / 标题栏 / 背景色”)
分组框_窗口主题 ＝ 创建分组框 (页面句柄, 800, 80, 400, 490, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_窗口主题, 1)
设置分组框标题颜色 (分组框_窗口主题, 5)

文本字节集 ＝ 编码_Ansi到Utf8 (“主题切换”)
创建标签 (页面句柄, 826, 122, 150, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“浅色 / 深色主题切换会直接影响窗口底色、读数卡和默认跟随策略。”)
创建标签 (页面句柄, 826, 146, 350, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“浅色主题”)
按钮_窗口主题_浅色 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 826, 166, 168, 36, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“深色主题”)
按钮_窗口主题_深色 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1008, 166, 168, 36, 到ARGB (255, 48, 49, 51))

文本字节集 ＝ 编码_Ansi到Utf8 (“标题栏背景色”)
创建标签 (页面句柄, 826, 218, 180, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“支持固定颜色和恢复跟随主题，便于快速对照不同标题栏方案。”)
创建标签 (页面句柄, 826, 242, 350, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)
emoji字节集 ＝ { 240, 159, 142, 168 }
文本字节集 ＝ 编码_Ansi到Utf8 (“科技蓝”)
按钮_标题栏背景_蓝 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 826, 266, 90, 36, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 142, 168 }
文本字节集 ＝ 编码_Ansi到Utf8 (“深空黑”)
按钮_标题栏背景_黑 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 922, 266, 90, 36, 到ARGB (255, 48, 49, 51))
emoji字节集 ＝ { 240, 159, 142, 168 }
文本字节集 ＝ 编码_Ansi到Utf8 (“青绿色”)
按钮_标题栏背景_绿 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1018, 266, 90, 36, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 226, 134, 186 }
文本字节集 ＝ 编码_Ansi到Utf8 (“跟随”)
按钮_标题栏背景_跟随 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1114, 266, 74, 36, 到ARGB (255, 144, 147, 153))

文本字节集 ＝ 编码_Ansi到Utf8 (“标题栏文字色”)
创建标签 (页面句柄, 826, 314, 180, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“亮字 / 暗字 / 蓝字 / 跟随主题自动对比，适合验证标题栏对比度。”)
创建标签 (页面句柄, 826, 338, 350, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“亮字”)
按钮_标题栏文字_亮 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 826, 366, 82, 36, 到ARGB (255, 144, 147, 153))
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“暗字”)
按钮_标题栏文字_暗 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 914, 366, 82, 36, 到ARGB (255, 48, 49, 51))
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“蓝字”)
按钮_标题栏文字_蓝 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1002, 366, 82, 36, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“跟随”)
按钮_标题栏文字_跟随 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1090, 366, 98, 36, 到ARGB (255, 103, 194, 58))
文本字节集 ＝ 编码_Ansi到Utf8 (“0 代表恢复为跟随主题自动对比。”)
创建标签 (页面句柄, 826, 402, 280, 18, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“若客户区仍是主题默认背景，切换浅色 / 深色主题会自动联动刷新。”)
创建标签 (页面句柄, 826, 420, 350, 18, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“客户区背景色”)
创建标签 (页面句柄, 826, 442, 180, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“客户区背景既可切固定演示色，也可回到主题默认背景参与联动。”)
创建标签 (页面句柄, 826, 466, 350, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“纯白背景”)
按钮_客户区背景_白 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 826, 492, 90, 36, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“灰蓝背景”)
按钮_客户区背景_灰 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 922, 492, 90, 36, 到ARGB (255, 144, 147, 153))
emoji字节集 ＝ { 240, 159, 170, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“深色背景”)
按钮_客户区背景_深 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1018, 492, 90, 36, 到ARGB (255, 48, 49, 51))
emoji字节集 ＝ { 226, 134, 186 }
文本字节集 ＝ 编码_Ansi到Utf8 (“跟随”)
按钮_客户区背景_跟随 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1114, 492, 74, 36, 到ARGB (255, 64, 158, 255))
文本字节集 ＝ 编码_Ansi到Utf8 (“'跟随' 表示客户区背景回到主题默认值，浅色 / 深色切换时会自动同步。”)
创建标签 (页面句柄, 826, 530, 350, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“Window API 说明”)
分组框_窗口说明 ＝ 创建分组框 (页面句柄, 20, 588, 1180, 112, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_窗口说明, 1)
设置分组框标题颜色 (分组框_窗口说明, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“1. set_window_title / GetWindowTitle：读取和修改主窗口标题。”)
创建标签 (页面句柄, 40, 624, 560, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“2. GetWindowBounds / SetWindowBounds：直接读取和修改窗口位置与尺寸。”)
创建标签 (页面句柄, 40, 652, 620, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“3. GetWindowVisible / GetWindowTitlebarColor / GetTitleBarTextColor：实时读取可见态和标题栏颜色。”)
创建标签 (页面句柄, 40, 680, 700, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“4. SetWindowBackgroundColor / SetDarkMode：客户区底色和主题切换联动演示。”)
创建标签 (页面句柄, 760, 624, 400, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“5. 这一页保留窗口级属性操作，用来验证主窗口改动不会影响彩色 emoji 与说明文本。”)
创建标签 (页面句柄, 760, 652, 400, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“6. 标题栏背景 / 文字色都支持恢复为 '跟随主题'，便于回到默认对比策略。”)
创建标签 (页面句柄, 760, 690, 400, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

_刷新窗口属性读取 (“窗口页已加载，可直接测试主窗口属性读取与设置。”)

.子程序 _构建基础页
.参数 页面句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 分组框_状态, 整数型
.局部变量 分组框_容器, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (“这一页完整放入 Python 示例中的基础控件。当前宿主窗口本身就是 EmojiWindow，下面继续创建按钮、标签、编辑框、复选框、单选按钮、进度条、滑块、开关、分组框和 Panel。”)
创建标签 (页面句柄, 20, 18, 1180, 42, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“基础页状态：等待操作。”)
标签_基础状态 ＝ 创建标签 (页面句柄, 20, 680, 1200, 54, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“基础输入与状态”)
分组框_基础 ＝ 创建分组框 (页面句柄, 20, 80, 440, 360, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_基础, 1)
设置分组框标题颜色 (分组框_基础, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“状态控件与快捷操作”)
分组框_状态 ＝ 创建分组框 (页面句柄, 470, 80, 360, 470, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_状态, 1)
设置分组框标题颜色 (分组框_状态, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“组件清单 / Panel 卡片”)
分组框_容器 ＝ 创建分组框 (页面句柄, 860, 80, 350, 470, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_容器, 1)
设置分组框标题颜色 (分组框_容器, 5)


文本字节集 ＝ 编码_Ansi到Utf8 (“普通 EditBox”)
添加子控件到分组框 (分组框_基础, 创建标签 (页面句柄, 40, 118, 140, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假))
文本字节集 ＝ 编码_Ansi到Utf8 (“Admin”)
编辑框_普通 ＝ 创建编辑框 (页面句柄, 40, 148, 220, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 假, 假, 假, 0, 假, 假, 假, 真, 真)
添加子控件到分组框 (分组框_基础, 编辑框_普通)

emoji字节集 ＝ { 240, 159, 148, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“读取”)
按钮_读取编辑框 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 280, 146, 120, 36, 到ARGB (255, 64, 158, 255))
添加子控件到分组框 (分组框_基础, 按钮_读取编辑框)

emoji字节集 ＝ { 226, 156, 143, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“写入”)
按钮_写入编辑框 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 280, 190, 120, 36, 到ARGB (255, 103, 194, 58))
添加子控件到分组框 (分组框_基础, 按钮_写入编辑框)

文本字节集 ＝ 编码_Ansi到Utf8 (“彩色 Emoji 编辑框”)
添加子控件到分组框 (分组框_基础, 创建标签 (页面句柄, 40, 238, 180, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假))
文本字节集 ＝ 编码_Ansi到Utf8 (“欢迎使用 4.0 完整示例” ＋ #换行符 ＋ “这里演示彩色 Emoji 编辑框。”)
编辑框_彩色Emoji ＝ 创建彩色Emoji编辑框 (页面句柄, 40, 268, 360, 92, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 假, 假, 假, 0, 真, 假, 假, 真, 假)
添加子控件到分组框 (分组框_基础, 编辑框_彩色Emoji)

文本字节集 ＝ 编码_Ansi到Utf8 (“启用高级功能”)
复选框_功能 ＝ 创建复选框 (页面句柄, 40, 366, 180, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假)
设置复选框回调 (复选框_功能, &复选框回调)
添加子控件到分组框 (分组框_基础, 复选框_功能)

文本字节集 ＝ 编码_Ansi到Utf8 (“浅色主题”)
单选按钮_浅色 ＝ 创建单选按钮 (页面句柄, 240, 364, 120, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假)
设置单选按钮回调 (单选按钮_浅色, &单选按钮_浅色回调)
添加子控件到分组框 (分组框_基础, 单选按钮_浅色)

文本字节集 ＝ 编码_Ansi到Utf8 (“深色主题”)
单选按钮_深色 ＝ 创建单选按钮 (页面句柄, 240, 396, 120, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1, 假, 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假)
设置单选按钮回调 (单选按钮_深色, &单选按钮_深色回调)
添加子控件到分组框 (分组框_基础, 单选按钮_深色)
当前基础页单选文本 ＝ “浅色主题”

emoji字节集 ＝ { 240, 159, 145, 139 }
文本字节集 ＝ 编码_Ansi到Utf8 (“基础演示按钮”)
按钮_基础演示 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 500, 110, 200, 42, 到ARGB (255, 64, 158, 255))

文本字节集 ＝ 编码_Ansi到Utf8 (“ProgressBar”)
创建标签 (页面句柄, 500, 180, 180, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
进度条_任务 ＝ 创建进度条 (页面句柄, 500, 210, 320, 28, 35, 到ARGB (255, 64, 158, 255), 到ARGB (255, 228, 231, 237), 真, 到ARGB (255, 48, 49, 51))
设置进度条值 (进度条_任务, 35)
设置进度条显示文本 (进度条_任务, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“Slider”)
创建标签 (页面句柄, 500, 260, 180, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
滑块_音量 ＝ 创建滑块 (页面句柄, 500, 292, 320, 40, 0, 100, 35, 5, 到ARGB (255, 103, 194, 58), 到ARGB (255, 228, 231, 237))
设置滑块回调 (滑块_音量, 到整数 (&滑块回调))

文本字节集 ＝ 编码_Ansi到Utf8 (“Switch”)
创建标签 (页面句柄, 500, 360, 180, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“开启”)
emoji字节集 ＝ 编码_Ansi到Utf8 (“关闭”)
开关_通知 ＝ 创建开关 (页面句柄, 500, 392, 128, 36, 真, 到ARGB (255, 64, 158, 255), 到ARGB (255, 220, 223, 230), 文本字节集, 取字节集长度 (文本字节集), emoji字节集, 取字节集长度 (emoji字节集))
设置开关回调 (开关_通知, 到整数 (&开关回调))

emoji字节集 ＝ { 226, 158, 149 }
文本字节集 ＝ 编码_Ansi到Utf8 (“推进进度”)
按钮_推进进度 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 500, 448, 150, 38, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 240, 159, 148, 128 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换开关”)
按钮_切换开关 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 668, 448, 150, 38, 到ARGB (255, 230, 162, 60))
emoji字节集 ＝ { 240, 159, 148, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“重置基础页”)
按钮_重置基础页 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 500, 498, 318, 38, 到ARGB (255, 144, 147, 153))

面板_基础信息 ＝ 创建面板容器 (页面句柄, 886, 120, 298, 396, 到ARGB (255, 255, 255, 255))
文本字节集 ＝ 编码_Ansi到Utf8 (“基础组件总览”)
创建标签 (面板_基础信息, 20, 18, 180, 26, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假, 0, 假)
文本字节集 ＝ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“窗口 / 按钮 / 标签”)
创建标签 (面板_基础信息, 20, 58, 220, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“EditBox / 彩色 Emoji 编辑框”)
创建标签 (面板_基础信息, 20, 86, 240, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“CheckBox / RadioButton”)
创建标签 (面板_基础信息, 20, 114, 220, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“Progress / Slider / Switch”)
创建标签 (面板_基础信息, 20, 142, 220, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“GroupBox / Panel”)
创建标签 (面板_基础信息, 20, 170, 180, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“说明”)
创建标签 (面板_基础信息, 20, 214, 120, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“基础组件树形版在 Python 里已经拆成多页；当前易语言页先保留成熟综合舞台，并用这块 Panel 承载 overview 卡片层级。”)
创建标签 (面板_基础信息, 20, 242, 258, 54, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“重点接口”)
创建标签 (面板_基础信息, 20, 316, 120, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 103, 194, 58), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“SetLabelText / SetCheckBoxState” ＋ #换行符 ＋ “SetProgressValue / GetSliderValue” ＋ #换行符 ＋ “SetSwitchState”)
创建标签 (面板_基础信息, 20, 344, 258, 60, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 230, 162, 60), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“状态读取区”)
创建标签 (页面句柄, 20, 548, 160, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“这里集中回显 EditBox、CheckBox、Radio、Progress、Slider、Switch 当前值，更接近 Python 基础页的读数卡。”)
创建标签 (页面句柄, 20, 576, 900, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“等待基础页读数刷新…”)
标签_基础摘要 ＝ 创建标签 (页面句柄, 20, 608, 1200, 56, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

_刷新基础页摘要 (“基础页已加载，当前读数卡与状态区已就绪。”)

.子程序 _构建选择页
.参数 页面句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 分组框_选择清单, 整数型
.局部变量 分组框_选择说明, 整数型
.局部变量 分组框_选择重点, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (“这一页对应 Python 示例中的选择类控件：ListBox、ComboBox、D2DComboBox、D2DDateTimePicker、HotKeyControl。”)
创建标签 (页面句柄, 20, 18, 1180, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“选择页状态：等待回调。”)
标签_选择状态 ＝ 创建标签 (页面句柄, 20, 732, 1200, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“ListBox”)
创建标签 (页面句柄, 20, 82, 120, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“6 个分类项，可由程序追加并联动选中。”)
创建标签 (页面句柄, 108, 84, 210, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)
列表框_分类 ＝ 创建列表框 (页面句柄, 20, 112, 300, 460, 假, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
文本字节集 ＝ 编码_Ansi到Utf8 (“基础组件”)
添加列表项 (列表框_分类, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“选择类组件”)
添加列表项 (列表框_分类, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“显示类组件”)
添加列表项 (列表框_分类, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“弹窗类组件”)
添加列表项 (列表框_分类, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“页签与导航”)
添加列表项 (列表框_分类, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“数据组件”)
添加列表项 (列表框_分类, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
设置列表框回调 (列表框_分类, &列表框选中回调)

文本字节集 ＝ 编码_Ansi到Utf8 (“ComboBox”)
创建标签 (页面句柄, 380, 82, 160, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“普通下拉框，兼顾选中索引与编辑文本写入。”)
创建标签 (页面句柄, 472, 84, 250, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)
组合框_城市 ＝ 创建组合框 (页面句柄, 380, 112, 300, 36, 假, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 40, 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 假, 假, 假)
设置组合框回调 (组合框_城市, &组合框回调)
文本字节集 ＝ 编码_Ansi到Utf8 (“北京”)
添加组合框项 (组合框_城市, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“上海”)
添加组合框项 (组合框_城市, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“深圳”)
添加组合框项 (组合框_城市, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
设置组合框选中索引 (组合框_城市, 1)

文本字节集 ＝ 编码_Ansi到Utf8 (“D2DComboBox”)
创建标签 (页面句柄, 380, 184, 180, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“D2D 风格更接近 Python 版的主题选择器舞台。”)
创建标签 (页面句柄, 504, 186, 250, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)
D2D组合框_主题 ＝ 创建D2D组合框 (页面句柄, 380, 214, 300, 40, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 40, 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 假, 假, 假)
设置D2D组合框回调 (D2D组合框_主题, &D2D组合框回调)
文本字节集 ＝ { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 (“默认主题”)
添加D2D组合框项 (D2D组合框_主题, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 (“商务蓝”)
添加D2D组合框项 (D2D组合框_主题, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 (“浅绿主题”)
添加D2D组合框项 (D2D组合框_主题, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
设置D2D组合框选中索引 (D2D组合框_主题, 0)

文本字节集 ＝ 编码_Ansi到Utf8 (“D2DDateTimePicker”)
创建标签 (页面句柄, 380, 286, 220, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“当前精度对齐到分钟，便于和 Python 版的 preset 动作一致。”)
创建标签 (页面句柄, 550, 288, 260, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)
日期时间_计划 ＝ 创建D2D日期时间选择器 (页面句柄, 380, 316, 300, 36, 1, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 到ARGB (255, 220, 223, 230), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 假, 假, 假)
设置D2D日期时间选择器回调 (日期时间_计划, &日期时间回调)
设置D2D日期时间选择器日期时间 (日期时间_计划, 2026, 4, 2, 10, 30, 0)
设置D2D日期时间选择器精度 (日期时间_计划, 3)

文本字节集 ＝ 编码_Ansi到Utf8 (“HotKeyControl”)
创建标签 (页面句柄, 380, 390, 180, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“默认 Ctrl+S，可切换到 Ctrl+Shift+K 或 Alt+F4。”)
创建标签 (页面句柄, 490, 392, 250, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)
热键控件_快捷 ＝ 创建热键控件 (页面句柄, 380, 420, 300, 36, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
设置热键回调 (热键控件_快捷, &热键回调)
设置热键值 (热键控件_快捷, 83, 1)

文本字节集 ＝ 编码_Ansi到Utf8 (“程序动作区”)
创建标签 (页面句柄, 380, 458, 100, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“读取 / 预设 / 写入三类动作集中在这一区域，更接近 Python 页面的 preset + readout 组合。”)
创建标签 (页面句柄, 468, 458, 390, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)

emoji字节集 ＝ { 240, 159, 148, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“读取选择结果”)
按钮_读取选择结果 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 380, 488, 160, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 226, 156, 143, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“设为 Ctrl+Shift+K”)
按钮_设置快捷键 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 552, 488, 220, 38, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 226, 158, 149 }
文本字节集 ＝ 编码_Ansi到Utf8 (“新增列表项”)
按钮_选择_新增列表项 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 380, 536, 150, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 147, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“选中第 3 项”)
按钮_选择_选中第三项 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 548, 536, 150, 38, 到ARGB (255, 230, 162, 60))
emoji字节集 ＝ { 240, 159, 146, 172 }
文本字节集 ＝ 编码_Ansi到Utf8 (“组合框写入”)
按钮_选择_组合框写入 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 380, 584, 150, 38, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 226, 140, 168, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“热键 Alt+F4”)
按钮_选择_热键AltF4 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 548, 584, 150, 38, 到ARGB (255, 144, 147, 153))

文本字节集 ＝ 编码_Ansi到Utf8 (“选择读数卡”)
创建标签 (页面句柄, 20, 628, 160, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“这里集中回显 ListBox / ComboBox / D2DComboBox / HotKey 当前值，更接近 Python 选择页的 readout 卡片。”)
创建标签 (页面句柄, 20, 654, 940, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“等待选择页摘要刷新…”)
标签_选择摘要 ＝ 创建标签 (页面句柄, 20, 680, 1160, 42, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“选择总览卡”)
分组框_选择清单 ＝ 创建分组框 (页面句柄, 720, 82, 460, 180, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_选择清单, 1)
设置分组框标题颜色 (分组框_选择清单, 5)
文本字节集 ＝ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“ListBox” ＋ #换行符) ＋ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“ComboBox” ＋ #换行符) ＋ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“D2DComboBox” ＋ #换行符) ＋ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“D2DDateTimePicker” ＋ #换行符) ＋ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“HotKeyControl”)
创建标签 (页面句柄, 740, 122, 200, 118, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“Python 版把选择类控件拆成列表/组合框页与日期时间/热键页；当前综合页先把读写与回调稳定收口到一个舞台。”)
创建标签 (页面句柄, 938, 122, 220, 88, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“布局说明卡”)
分组框_选择说明 ＝ 创建分组框 (页面句柄, 720, 282, 460, 118, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_选择说明, 1)
设置分组框标题颜色 (分组框_选择说明, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“左侧列表承担分类导航感，中央区域承担真实控件读写，这种分区更接近 Python 的 overview + detail 组合。”)
创建标签 (页面句柄, 740, 322, 410, 54, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“接口映射卡”)
分组框_选择重点 ＝ 创建分组框 (页面句柄, 720, 420, 460, 152, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_选择重点, 1)
设置分组框标题颜色 (分组框_选择重点, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“重点接口: AddListItem / SetSelectedIndex / AddComboItem / SetComboSelectedIndex / AddD2DComboItem / SetD2DDateTimePickerDateTime / SetHotKey / GetHotKey”)
创建标签 (页面句柄, 740, 460, 410, 88, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

_刷新选择页摘要 (“选择页已加载，当前读数卡与按钮区已就绪。”)

.子程序 _构建显示页
.参数 页面句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 分组框_显示清单, 整数型
.局部变量 分组框_显示说明, 整数型
.局部变量 分组框_显示重点, 整数型
.局部变量 标签_Tooltip上方, 整数型
.局部变量 标签_Tooltip下方, 整数型
.局部变量 标签_Tooltip左侧, 整数型
.局部变量 提示层_上方, 整数型
.局部变量 提示层_下方, 整数型
.局部变量 提示层_左侧, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (“这一页对应 Python 示例中的 PictureBox、Tooltip、Notification。弹窗组件已单独拆到独立主 Tab，避免显示类内容和对话框入口混在同一页。”)
创建标签 (页面句柄, 20, 18, 1180, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“显示页状态：等待操作。”)
标签_显示状态 ＝ 创建标签 (页面句柄, 20, 704, 1200, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“显示总览卡”)
分组框_显示清单 ＝ 创建分组框 (页面句柄, 900, 86, 280, 160, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_显示清单, 1)
设置分组框标题颜色 (分组框_显示清单, 5)
文本字节集 ＝ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“PictureBox” ＋ #换行符) ＋ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“Tooltip” ＋ #换行符) ＋ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“Notification”)
创建标签 (页面句柄, 920, 126, 120, 78, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“显示类组件在 Python 版是独立可验证页；当前页保留三块成熟舞台，避免图片、提示层和通知互相打断。”)
创建标签 (页面句柄, 1002, 126, 158, 86, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“布局说明卡”)
分组框_显示说明 ＝ 创建分组框 (页面句柄, 900, 266, 280, 126, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_显示说明, 1)
设置分组框标题颜色 (分组框_显示说明, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“左侧图片预览、中部 Tooltip 目标、下方通知控制，形成更像 Python overview 页的分区舞台。”)
创建标签 (页面句柄, 920, 306, 240, 56, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“接口映射卡”)
分组框_显示重点 ＝ 创建分组框 (页面句柄, 900, 412, 280, 180, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_显示重点, 1)
设置分组框标题颜色 (分组框_显示重点, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“重点接口: SetPictureBoxScaleMode / SetImageOpacity / SetTooltipTheme / SetTooltipTrigger / ShowNotification / SetNotificationCallback”)
创建标签 (页面句柄, 920, 452, 238, 96, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

图片框_预览 ＝ 创建图片框 (页面句柄, 20, 86, 360, 240, 2, 到ARGB (255, 245, 247, 250))
设置图片透明度 (图片框_预览, 0.95)
设置图片框缩放模式 (图片框_预览, 2)
当前图片缩放模式 ＝ 2
当前图片透明度百分比 ＝ 95
图片框当前可见 ＝ 真
图片框当前启用 ＝ 真
当前图片背景色 ＝ 到ARGB (255, 245, 247, 250)
当前图片背景名称 ＝ “默认浅灰底”

emoji字节集 ＝ { 240, 159, 147, 129 }
文本字节集 ＝ 编码_Ansi到Utf8 (“加载图片”)
按钮_加载图片 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 348, 150, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 151, 145, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“清除图片”)
按钮_清除图片 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 186, 348, 150, 38, 到ARGB (255, 245, 108, 108))
emoji字节集 ＝ { 240, 159, 148, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换缩放模式”)
按钮_切换图片模式 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 398, 316, 38, 到ARGB (255, 230, 162, 60))
emoji字节集 ＝ { 240, 159, 142, 154, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“透明 75%”)
按钮_图片透明75 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 448, 150, 38, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 240, 159, 146, 175 }
文本字节集 ＝ 编码_Ansi到Utf8 (“透明 100%”)
按钮_图片透明100 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 186, 448, 150, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 140, 171 }
文本字节集 ＝ 编码_Ansi到Utf8 (“透明 25%”)
按钮_图片透明25 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 498, 150, 38, 到ARGB (255, 245, 108, 108))
emoji字节集 ＝ { 240, 159, 146, 153 }
文本字节集 ＝ 编码_Ansi到Utf8 (“冷色底”)
按钮_图片背景冷 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 186, 498, 150, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 167, 161 }
文本字节集 ＝ 编码_Ansi到Utf8 (“暖色底”)
按钮_图片背景暖 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 548, 150, 38, 到ARGB (255, 230, 162, 60))
emoji字节集 ＝ { 240, 159, 145, 129 }
文本字节集 ＝ 编码_Ansi到Utf8 (“显示切换”)
按钮_图片切换可见 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 186, 548, 150, 38, 到ARGB (255, 103, 194, 58))

文本字节集 ＝ 编码_Ansi到Utf8 (“Top / Dark / Hover”)
标签_Tooltip上方 ＝ 创建标签 (页面句柄, 430, 108, 176, 34, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 31, 94, 153), 到ARGB (255, 234, 243, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 1, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“Bottom / Light / Hover”)
标签_Tooltip下方 ＝ 创建标签 (页面句柄, 620, 108, 184, 34, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 58, 122, 45), 到ARGB (255, 240, 249, 235), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 1, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“Left / Custom / Hover”)
标签_Tooltip左侧 ＝ 创建标签 (页面句柄, 430, 152, 176, 34, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 140, 74, 0), 到ARGB (255, 255, 247, 230), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 1, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“Right / Dark / Click”)
标签_Tooltip目标 ＝ 创建标签 (页面句柄, 620, 152, 184, 34, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 124, 58, 173), 到ARGB (255, 244, 240, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 1, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“Hover Top / dark 主题 / 上方。”)
提示层_上方 ＝ 创建提示层 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), 0, 0, 0)
设置提示层主题 (提示层_上方, 0)
设置提示层位置 (提示层_上方, 0)
设置提示层触发方式 (提示层_上方, 0)
绑定提示层到控件 (提示层_上方, 标签_Tooltip上方)

文本字节集 ＝ 编码_Ansi到Utf8 (“Hover Bottom / light 主题 / 下方。”)
提示层_下方 ＝ 创建提示层 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), 1, 0, 0)
设置提示层主题 (提示层_下方, 1)
设置提示层位置 (提示层_下方, 1)
设置提示层触发方式 (提示层_下方, 0)
绑定提示层到控件 (提示层_下方, 标签_Tooltip下方)

文本字节集 ＝ 编码_Ansi到Utf8 (“Hover Left / custom 主题 / 左侧。”)
提示层_左侧 ＝ 创建提示层 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), 2, 0, 0)
设置提示层主题 (提示层_左侧, 2)
设置提示层颜色 (提示层_左侧, 到ARGB (255, 255, 247, 230), 到ARGB (255, 140, 74, 0), 到ARGB (255, 227, 178, 97))
设置提示层位置 (提示层_左侧, 2)
设置提示层触发方式 (提示层_左侧, 0)
绑定提示层到控件 (提示层_左侧, 标签_Tooltip左侧)

文本字节集 ＝ 编码_Ansi到Utf8 (“Click Right / dark 主题 / 点击后再点击空白处收起。”)
提示层_说明 ＝ 创建提示层 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), 3, 0, 0)
设置提示层主题 (提示层_说明, 0)
设置提示层位置 (提示层_说明, 3)
设置提示层触发方式 (提示层_说明, 1)
绑定提示层到控件 (提示层_说明, 标签_Tooltip目标)
当前Tooltip主题 ＝ 0
当前Tooltip触发方式 ＝ 1
当前Tooltip位置 ＝ 3

文本字节集 ＝ 编码_Ansi到Utf8 (“Tooltip 四方向卡已固定展示；下方按钮继续控制右侧主控卡。”)
创建标签 (页面句柄, 430, 82, 374, 18, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)

emoji字节集 ＝ { 240, 159, 146, 172 }
文本字节集 ＝ 编码_Ansi到Utf8 (“显示 Tooltip”)
按钮_显示Tooltip ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 430, 190, 160, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 153, 136 }
文本字节集 ＝ 编码_Ansi到Utf8 (“隐藏 Tooltip”)
按钮_隐藏Tooltip ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 606, 190, 160, 38, 到ARGB (255, 144, 147, 153))
emoji字节集 ＝ { 240, 159, 164, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“主控浅色”)
按钮_Tooltip浅色 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 430, 230, 160, 38, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 240, 159, 147, 165 }
文本字节集 ＝ 编码_Ansi到Utf8 (“下方 Hover”)
按钮_Tooltip下方 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 606, 230, 160, 38, 到ARGB (255, 230, 162, 60))

文本字节集 ＝ 编码_Ansi到Utf8 (“Notification 工具列保留 4 种语义色，并补上连续两条通知堆叠验证。”)
创建标签 (页面句柄, 430, 270, 448, 18, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)

emoji字节集 ＝ { 240, 159, 148, 148 }
文本字节集 ＝ 编码_Ansi到Utf8 (“信息通知”)
按钮_信息通知 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 430, 292, 140, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 142, 137 }
文本字节集 ＝ 编码_Ansi到Utf8 (“成功通知”)
按钮_成功通知 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 584, 292, 140, 38, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 226, 154, 160, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“警告通知”)
按钮_警告通知 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 430, 342, 140, 38, 到ARGB (255, 230, 162, 60))
emoji字节集 ＝ { 240, 159, 154, 171 }
文本字节集 ＝ 编码_Ansi到Utf8 (“错误通知”)
按钮_错误通知 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 584, 342, 140, 38, 到ARGB (255, 245, 108, 108))
emoji字节集 ＝ { 240, 159, 147, 163 }
文本字节集 ＝ 编码_Ansi到Utf8 (“连续两条”)
按钮_连续通知 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 738, 342, 140, 38, 到ARGB (255, 142, 68, 173))
emoji字节集 ＝ { 240, 159, 151, 145, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“关闭通知”)
按钮_关闭通知 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 738, 292, 140, 38, 到ARGB (255, 144, 147, 153))

文本字节集 ＝ 编码_Ansi到Utf8 (“PictureBox 负责缩放模式、透明度与占位背景验证。”)
创建标签 (页面句柄, 430, 390, 420, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“Tooltip 负责主题、方向、触发方式和手动显隐联动。”)
创建标签 (页面句柄, 430, 420, 420, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“Notification 负责右上角消息弹出、主动关闭和事件回调写回。”)
创建标签 (页面句柄, 430, 450, 460, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“Notification 分区继续保留 4 种语义色和一键连续两条，已经接近 Python 版的通知按钮组合。”)
创建标签 (页面句柄, 430, 476, 460, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“如果要验证 MessageBox / ConfirmBox，请切到新的 '弹窗类组件' 主页，结构会更接近 Python 版。”)
创建标签 (页面句柄, 430, 504, 460, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)
emoji字节集 ＝ { 240, 159, 145, 129 }
文本字节集 ＝ 编码_Ansi到Utf8 (“启用切换”)
按钮_图片切换启用 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 430, 528, 160, 38, 到ARGB (255, 245, 108, 108))
文本字节集 ＝ 编码_Ansi到Utf8 (“右侧读数卡会同时回显透明度、底色、可见性和启用状态。”)
创建标签 (页面句柄, 606, 536, 274, 30, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“显示读数卡”)
创建标签 (页面句柄, 20, 586, 160, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“这里集中回显 PictureBox、Tooltip 和 Notification 当前策略，更接近 Python 显示页的状态卡。”)
创建标签 (页面句柄, 20, 612, 960, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“等待显示页摘要刷新…”)
标签_显示摘要 ＝ 创建标签 (页面句柄, 20, 636, 1160, 60, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

_刷新显示页摘要 (“显示页已加载，当前图片 / Tooltip / Notification 读数卡已就绪。”)

.子程序 _构建弹窗页
.参数 页面句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 分组框_Message, 整数型
.局部变量 分组框_Confirm, 整数型
.局部变量 分组框_弹窗说明, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (“这一页单独对应 Python 示例中的 MessageBox 与 ConfirmBox。这里把消息提示、确认回调和状态写回独立出来，避免再和显示类控件混排。”)
创建标签 (页面句柄, 20, 18, 1180, 42, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“弹窗页状态：等待操作。”)
标签_弹窗状态 ＝ 创建标签 (页面句柄, 20, 678, 1200, 54, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“MessageBox 舞台”)
分组框_Message ＝ 创建分组框 (页面句柄, 20, 84, 560, 250, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_Message, 1)
设置分组框标题颜色 (分组框_Message, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“对应 Python 的 MessageBox 独立演示页，重点验证 show_message_box_bytes 调用链路和顶部菜单/右键菜单复用入口。”)
创建标签 (页面句柄, 40, 122, 500, 52, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
emoji字节集 ＝ { 240, 159, 147, 145 }
文本字节集 ＝ 编码_Ansi到Utf8 (“打开 MessageBox”)
按钮_打开消息框 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 40, 208, 220, 40, 到ARGB (255, 64, 158, 255))
文本字节集 ＝ 编码_Ansi到Utf8 (“消息框没有二次回调，本页会把 '已打开' 状态直接写回到底部状态区。”)
创建标签 (页面句柄, 40, 266, 460, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“ConfirmBox 舞台”)
分组框_Confirm ＝ 创建分组框 (页面句柄, 620, 84, 560, 250, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_Confirm, 1)
设置分组框标题颜色 (分组框_Confirm, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“对应 Python 的 ConfirmBox 独立演示页，重点验证确认/取消两条回调路径都能回写到状态区。”)
创建标签 (页面句柄, 640, 122, 500, 52, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
emoji字节集 ＝ { 226, 157, 147 }
文本字节集 ＝ 编码_Ansi到Utf8 (“打开 ConfirmBox”)
按钮_打开确认框 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 640, 208, 220, 40, 到ARGB (255, 230, 162, 60))
文本字节集 ＝ 编码_Ansi到Utf8 (“确认框会进入确认/取消回调；这部分状态不再写到显示页，而是回写到当前弹窗页。”)
创建标签 (页面句柄, 640, 266, 460, 24, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“接口对照 / 联动说明”)
分组框_弹窗说明 ＝ 创建分组框 (页面句柄, 20, 370, 1160, 270, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_弹窗说明, 1)
设置分组框标题颜色 (分组框_弹窗说明, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“1. MessageBox：show_message_box_bytes 风格的简单消息提示，不带确认回调。”)
创建标签 (页面句柄, 40, 410, 680, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“2. ConfirmBox：show_confirm_box_bytes 风格的确认提示，确认与取消都会回写回调结果。”)
创建标签 (页面句柄, 40, 446, 760, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“3. 顶部 MenuBar、导航页 PopupMenu 和本页按钮都会复用同一套弹窗子程序，便于验证跨区回调一致性。”)
创建标签 (页面句柄, 40, 482, 860, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“4. 这页单独拆出后，主 Tab 结构已经与 Python 的 '显示类组件 / 弹窗类组件' 分类更接近。”)
创建标签 (页面句柄, 40, 518, 760, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)

.子程序 _构建导航页
.参数 页面句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 内容句柄, 整数型
.局部变量 节点A, 整数型
.局部变量 节点B, 整数型
.局部变量 分组框_导航树, 整数型
.局部变量 分组框_导航页签, 整数型
.局部变量 分组框_导航弹窗, 整数型
.局部变量 分组框_导航说明, 整数型
.局部变量 空白字节集, 字节集

文本字节集 ＝ 编码_Ansi到Utf8 (“这一页继续把 Python 示例中的导航类组件摆出来。顶层窗口已经有 MenuBar、TreeView、TabControl，这里再补一个内层 TreeView、一个内层 TabControl 和一个 EmojiPopupMenu 的完整用法。”)
创建标签 (页面句柄, 20, 18, 1180, 42, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“导航页状态：等待操作。”)
标签_导航状态 ＝ 创建标签 (页面句柄, 20, 718, 1180, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“TreeView 导航舞台”)
分组框_导航树 ＝ 创建分组框 (页面句柄, 20, 80, 280, 520, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_导航树, 1)
设置分组框标题颜色 (分组框_导航树, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“左侧保留一个内层 TreeView，继续验证侧栏模式、多级节点和回调写回。”)
创建标签 (页面句柄, 40, 118, 220, 40, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“TabControl 页面区”)
分组框_导航页签 ＝ 创建分组框 (页面句柄, 320, 80, 540, 300, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_导航页签, 1)
设置分组框标题颜色 (分组框_导航页签, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“这里复刻 Python 导航页里的内层 TabControl，页签仍然保留 总览 / 设置 / 帮助。”)
创建标签 (页面句柄, 340, 118, 480, 40, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“PopupMenu 绑定区域”)
分组框_导航弹窗 ＝ 创建分组框 (页面句柄, 880, 80, 320, 300, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_导航弹窗, 1)
设置分组框标题颜色 (分组框_导航弹窗, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“右侧保留 PopupMenu 绑定区；既可右键，也可点击按钮手动弹出。”)
创建标签 (页面句柄, 900, 118, 280, 40, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“导航说明 / 最近状态”)
分组框_导航说明 ＝ 创建分组框 (页面句柄, 320, 400, 880, 300, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_导航说明, 1)
设置分组框标题颜色 (分组框_导航说明, 5)
空白字节集 ＝ 编码_Ansi到Utf8 (“”)

内层树句柄 ＝ 创建树形框 (页面句柄, 40, 170, 220, 390, 到ARGB (255, 255, 255, 255), 到ARGB (255, 48, 49, 51), 0)
设置树形框侧栏模式 (内层树句柄, 真)
设置树形框回调 (内层树句柄, 1, &内层树选中回调)
文本字节集 ＝ 编码_Ansi到Utf8 (“导航页”)
emoji字节集 ＝ { 240, 159, 147, 129 }
节点A ＝ 添加根节点 (内层树句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“总览”)
emoji字节集 ＝ { 240, 159, 147, 132 }
内层节点_总览 ＝ 添加子节点 (内层树句柄, 节点A, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“设置”)
emoji字节集 ＝ { 226, 154, 153, 239, 184, 143 }
内层节点_设置 ＝ 添加子节点 (内层树句柄, 节点A, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“帮助”)
emoji字节集 ＝ { 226, 157, 147 }
内层节点_帮助 ＝ 添加子节点 (内层树句柄, 节点A, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“状态日志”)
emoji字节集 ＝ { 240, 159, 147, 157 }
节点B ＝ 添加根节点 (内层树句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“最新事件”)
emoji字节集 ＝ { 240, 159, 134, 149 }
内层节点_最新事件 ＝ 添加子节点 (内层树句柄, 节点B, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集))
展开树节点 (内层树句柄, 节点A)
展开树节点 (内层树句柄, 节点B)

内层TabControl句柄 ＝ 创建TabControl (页面句柄, 340, 170, 500, 176)
文本字节集 ＝ { 240, 159, 147, 132, 32, 230, 128, 187, 232, 167, 136 }
添加Tab页 (内层TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)
文本字节集 ＝ { 226, 154, 153, 239, 184, 143, 32, 232, 174, 190, 231, 189, 174 }
添加Tab页 (内层TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)
文本字节集 ＝ { 226, 157, 147, 32, 229, 184, 174, 229, 138, 169 }
添加Tab页 (内层TabControl句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0)

内容句柄 ＝ 获取Tab内容窗口 (内层TabControl句柄, 0)
文本字节集 ＝ 编码_Ansi到Utf8 (“总览页用来放导航说明、状态摘要和当前页结构入口。”)
创建标签 (内容句柄, 18, 20, 430, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“切换到别的页签后，仍可继续验证顶部页头、状态栏和右侧 PopupMenu 区域是否协同工作。”)
创建标签 (内容句柄, 18, 64, 430, 54, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
内容句柄 ＝ 获取Tab内容窗口 (内层TabControl句柄, 1)
文本字节集 ＝ 编码_Ansi到Utf8 (“设置页适合承载布局开关、导航文案和弹出菜单绑定策略。”)
创建标签 (内容句柄, 18, 20, 430, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 103, 194, 58), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“Python 版里导航类组件已经拆得很细，这里先保留最关键的树、页签、菜单三类联动验证。”)
创建标签 (内容句柄, 18, 64, 430, 54, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
内容句柄 ＝ 获取Tab内容窗口 (内层TabControl句柄, 2)
文本字节集 ＝ 编码_Ansi到Utf8 (“帮助页用于说明：顶层 MenuBar 已经常驻窗口，本页只补内层导航舞台和 PopupMenu 绑定区。”)
创建标签 (内容句柄, 18, 20, 430, 54, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 230, 162, 60), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 13, 真, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“如果右键空白处能自动收起、点击菜单项会写回状态，就说明当前导航链路已经接近 Python 版。”)
创建标签 (内容句柄, 18, 82, 430, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“右键这个区域，或点击下面按钮手动弹出 EmojiPopupMenu。”)
标签_右键目标 ＝ 创建标签 (页面句柄, 920, 176, 240, 72, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 247, 230), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 真)
弹出菜单句柄 ＝ 创建弹出菜单 (主窗口句柄)
文本字节集 ＝ { 240, 159, 146, 140, 32, 230, 137, 147, 229, 188, 128, 32, 77, 101, 115, 115, 97, 103, 101, 66, 111, 120 }
弹出菜单添加项 (弹出菜单句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 2001)
文本字节集 ＝ { 226, 156, 133, 32, 230, 152, 190, 231, 164, 186, 230, 136, 144, 229, 138, 159, 233, 128, 154, 231, 159, 165 }
弹出菜单添加项 (弹出菜单句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 2002)
文本字节集 ＝ { 240, 159, 147, 139, 32, 232, 161, 168, 230, 160, 188, 230, 147, 141, 228, 189, 156 }
弹出菜单添加项 (弹出菜单句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 2100)
文本字节集 ＝ { 226, 158, 149, 32, 230, 150, 176, 229, 162, 158, 228, 184, 128, 232, 161, 140 }
弹出菜单添加子项 (弹出菜单句柄, 2100, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 2101)
文本字节集 ＝ { 240, 159, 148, 188, 32, 230, 140, 137, 231, 138, 182, 230, 128, 129, 229, 141, 135, 229, 186, 143 }
弹出菜单添加子项 (弹出菜单句柄, 2100, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 2102)
绑定控件菜单 (标签_右键目标, 弹出菜单句柄)
设置弹出菜单回调 (弹出菜单句柄, &弹出菜单回调)

emoji字节集 ＝ { 240, 159, 167, 183 }
文本字节集 ＝ 编码_Ansi到Utf8 (“手动弹出菜单”)
按钮_手动弹出菜单 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 920, 270, 220, 38, 到ARGB (255, 64, 158, 255))
创建标签 (页面句柄, 340, 438, 220, 126, 取变量数据地址 (空白字节集), 取字节集长度 (空白字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
创建标签 (页面句柄, 590, 438, 220, 126, 取变量数据地址 (空白字节集), 取字节集长度 (空白字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
创建标签 (页面句柄, 840, 438, 320, 126, 取变量数据地址 (空白字节集), 取字节集长度 (空白字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“组件清单”)
创建标签 (页面句柄, 360, 456, 120, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“TabControl” ＋ #换行符) ＋ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“TreeView” ＋ #换行符) ＋ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“MenuBar” ＋ #换行符) ＋ { 226, 128, 162, 32 } ＋ 编码_Ansi到Utf8 (“EmojiPopupMenu”)
创建标签 (页面句柄, 360, 486, 160, 72, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“说明”)
创建标签 (页面句柄, 610, 456, 120, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 103, 194, 58), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“顶层 MenuBar 已常驻主窗口；当前页面把内层树、内层页签和 PopupMenu 重新组织成一块成熟导航舞台。”)
创建标签 (页面句柄, 610, 486, 180, 72, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“本页重点”)
创建标签 (页面句柄, 860, 456, 120, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 230, 162, 60), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“重点接口: SetTabHeaderStyle / SetTreeViewSidebarMode / SetTreeViewRowHeight / SetMenuBarCallback / SetPopupMenuCallback”)
创建标签 (页面句柄, 860, 486, 278, 72, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“导航摘要 / 最近状态”)
创建标签 (页面句柄, 340, 584, 180, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“最近一次导航动作会写回到底部状态栏，方便快速判断当前来自树回调、页签切换还是 PopupMenu 回调。”)
创建标签 (页面句柄, 340, 610, 820, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 假)
文本字节集 ＝ 编码_Ansi到Utf8 (“等待导航摘要刷新…”)
标签_导航摘要 ＝ 创建标签 (页面句柄, 340, 642, 820, 44, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“顶部 MenuBar 已经在主窗口激活，可直接使用菜单切换页面、弹出对话框和操作数据表格。”)
创建标签 (页面句柄, 900, 340, 280, 72, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

_刷新导航摘要 (“导航舞台已就绪”, “可切换左侧内层树、内层 Tab 页签，并在右侧测试 PopupMenu 右键与手动弹出。”, “导航页已加载，可继续验证 TreeView / TabControl / PopupMenu 联动。”)

.子程序 _构建数据页
.参数 页面句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 分组框_数据概览, 整数型
.局部变量 分组框_数据重点, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (“这一页对应 Python 示例中的 DataGridView。这里使用完整版列定义、行填充、样式、回调和导出调用。”)
创建标签 (页面句柄, 20, 18, 1180, 36, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“表格页状态：等待操作。”)
标签_表格状态 ＝ 创建标签 (页面句柄, 20, 678, 1200, 54, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 (“当前模式：整行模式” ＋ #换行符 ＋ “行数=3    冻结首列=否”)
标签_表格顶部模式 ＝ 创建标签 (页面句柄, 20, 86, 220, 56, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 31, 94, 153), 到ARGB (255, 234, 243, 255), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 真, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“当前选中：未选中” ＋ #换行符 ＋ “最近动作：等待工具栏状态刷新…”)
标签_表格顶部状态 ＝ 创建标签 (页面句柄, 252, 86, 430, 56, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“导出状态：CSV 尚未导出。” ＋ #换行符 ＋ “工具栏区已接通导出写回。”)
标签_表格顶部导出 ＝ 创建标签 (页面句柄, 694, 86, 306, 56, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 140, 74, 0), 到ARGB (255, 255, 247, 230), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“主操作工具栏”)
创建标签 (页面句柄, 1030, 84, 220, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假, 0, 假)

表格句柄 ＝ 创建DataGridView (页面句柄, 20, 154, 980, 492, 假, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))
文本字节集 ＝ 编码_Ansi到Utf8 (“选择”)
表格_添加复选框列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 60)
文本字节集 ＝ 编码_Ansi到Utf8 (“编号”)
表格_添加文本列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 100)
文本字节集 ＝ 编码_Ansi到Utf8 (“名称”)
表格_添加文本列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 160)
文本字节集 ＝ 编码_Ansi到Utf8 (“城市”)
表格_添加文本列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 180)
文本字节集 ＝ 编码_Ansi到Utf8 (“状态”)
表格_添加文本列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 180)
文本字节集 ＝ 编码_Ansi到Utf8 (“操作”)
表格_添加按钮列 (表格句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 120)

表格行计数器 ＝ 3001
_向表格追加一行 ("3001", "张三", "北京", "已完成", 假)
_向表格追加一行 ("3002", "李四", "上海", "进行中", 真)
_向表格追加一行 ("3003", "王五", "深圳", "待审核", 假)
表格_设置单元格点击回调 (表格句柄, &表格单元格点击回调)
表格_设置单元格双击回调 (表格句柄, &表格单元格双击回调)
表格_设置单元格值改变回调 (表格句柄, &表格值改变回调)
表格_设置列头点击回调 (表格句柄, &表格列头点击回调)
表格_设置选择改变回调 (表格句柄, &表格选择改变回调)
表格_设置选择模式 (表格句柄, #DGSEL_ROW)
表格_设置默认行高 (表格句柄, 40)
表格_设置列头高度 (表格句柄, 46)
表格_设置显示网格线 (表格句柄, 真)
表格_设置列头对齐方式 (表格句柄, 0, 1)
表格_设置列头对齐方式 (表格句柄, 1, 1)
表格_设置列头对齐方式 (表格句柄, 4, 1)
表格_设置列头对齐方式 (表格句柄, 5, 1)
表格_设置列单元格对齐方式 (表格句柄, 0, 1)
表格_设置列单元格对齐方式 (表格句柄, 1, 1)
表格_设置列单元格对齐方式 (表格句柄, 4, 1)
表格_设置列单元格对齐方式 (表格句柄, 5, 1)
当前表格选择模式 ＝ #DGSEL_ROW
表格冻结首列已开启 ＝ 假
当前表格导出说明 ＝ “CSV 尚未导出。”

emoji字节集 ＝ { 226, 158, 149 }
文本字节集 ＝ 编码_Ansi到Utf8 (“新增一行”)
按钮_新增行 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1030, 110, 110, 38, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 240, 159, 151, 145, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“删除选中行”)
按钮_删除选中行 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1150, 110, 110, 38, 到ARGB (255, 245, 108, 108))
emoji字节集 ＝ { 240, 159, 148, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“按状态排序”)
按钮_按状态排序 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1030, 162, 110, 38, 到ARGB (255, 230, 162, 60))
emoji字节集 ＝ { 240, 159, 147, 164 }
文本字节集 ＝ 编码_Ansi到Utf8 (“导出 CSV”)
按钮_导出CSV ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1150, 162, 110, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 167, 170 }
文本字节集 ＝ 编码_Ansi到Utf8 (“单元格模式”)
按钮_表格_单元格模式 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1030, 214, 110, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 167, 190 }
文本字节集 ＝ 编码_Ansi到Utf8 (“整行模式”)
按钮_表格_整行模式 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1150, 214, 110, 38, 到ARGB (255, 103, 194, 58))
emoji字节集 ＝ { 226, 156, 143, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“写入单元格”)
按钮_表格_写入单元格 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1030, 266, 110, 38, 到ARGB (255, 230, 162, 60))
emoji字节集 ＝ { 240, 159, 167, 173 }
文本字节集 ＝ 编码_Ansi到Utf8 (“冻结首列”)
按钮_表格_冻结首列 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1150, 266, 110, 38, 到ARGB (255, 144, 147, 153))
emoji字节集 ＝ { 240, 159, 147, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“定位首行”)
按钮_表格_定位首行 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1030, 318, 110, 38, 到ARGB (255, 64, 158, 255))
emoji字节集 ＝ { 240, 159, 148, 141 }
文本字节集 ＝ 编码_Ansi到Utf8 (“刷新摘要”)
按钮_表格_刷新摘要 ＝ 创建Emoji按钮_字节集 (页面句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1150, 318, 110, 38, 到ARGB (255, 103, 194, 58))
文本字节集 ＝ 编码_Ansi到Utf8 (“运行摘要”)
分组框_数据概览 ＝ 创建分组框 (页面句柄, 1020, 384, 240, 150, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_数据概览, 1)
设置分组框标题颜色 (分组框_数据概览, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“等待表格摘要刷新…”)
标签_表格摘要 ＝ 创建标签 (页面句柄, 1040, 422, 200, 86, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“当前单元格”)
分组框_数据重点 ＝ 创建分组框 (页面句柄, 1020, 544, 240, 108, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 220, 223, 230), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 14, 真, 假, 假)
设置分组框样式 (分组框_数据重点, 1)
设置分组框标题颜色 (分组框_数据重点, 5)
文本字节集 ＝ 编码_Ansi到Utf8 (“等待单元格摘要刷新…”)
标签_表格单元格摘要 ＝ 创建标签 (页面句柄, 1040, 582, 200, 42, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 12, 假, 假, 假, 0, 真)
文本字节集 ＝ 编码_Ansi到Utf8 (“CSV 尚未导出。”)
标签_表格导出摘要 ＝ 创建标签 (页面句柄, 1020, 654, 240, 22, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 250, 252), 取变量数据地址 (默认字体字节集), 取字节集长度 (默认字体字节集), 11, 假, 假, 假, 0, 假)

_刷新数据页摘要 (“数据页已加载，当前工具栏与摘要卡已就绪。”)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 文本长度, 整数型
.局部变量 缓冲区, 字节集
.局部变量 当前值, 整数型
.局部变量 选中行, 整数型
.局部变量 选中列, 整数型
.局部变量 虚拟键码, 整数型
.局部变量 修饰键, 整数型
.局部变量 路径字节集, 字节集

.如果真 (主窗口句柄 ＝ 0)
    更新状态 (“按钮回调失败：主窗口句柄无效。”)
    返回 ()
.如果真结束

' 分组框 / 页面内按钮回调传入的可能是页面句柄或容器句柄，这里只拦截无效句柄。
.如果真 (_父窗口句柄 ＝ 0)
    更新状态 (“按钮回调失败：父窗口句柄无效。”)
    返回 ()
.如果真结束

.如果真 (_父窗口句柄 ＝ 窗口页句柄)
.如果真 (按钮ID ＝ 按钮_窗口标题_产品)
    _设置窗口标题文本 (“Tree AllDemo Enhanced / 产品演示窗口”, “窗口标题已切到产品演示风格。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口标题_调试)
    _设置窗口标题文本 (“EmojiWindow Debug Surface / 属性回归中”, “窗口标题已切到调试风格。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口标题_彩色)
    _设置窗口标题文本 (“EmojiWindow 属性页 / 彩色标题方案”, “窗口标题已切到彩色标题方案。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口尺寸_紧凑)
    _设置窗口尺寸 (1600, 900, “窗口尺寸已切到紧凑 1600x900。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口尺寸_标准)
    _设置窗口尺寸 (1820, 980, “窗口尺寸已恢复到标准 1820x980。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口尺寸_加宽)
    _设置窗口尺寸 (1920, 1040, “窗口尺寸已切到加宽 1920x1040。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口位置_重置)
    _恢复窗口位置 (“窗口位置已恢复到 (40, 40)。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口位置_右移)
    _移动窗口偏移 (80, 0, “窗口已向右移动 80 像素。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口位置_下移)
    _移动窗口偏移 (0, 60, “窗口已向下移动 60 像素。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口位置_读取)
    _刷新窗口属性读取 (“已重新读取窗口当前属性。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口主题_浅色)
    _设置窗口主题 (假, “浅色主题”, “主窗口主题已切到浅色。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_窗口主题_深色)
    _设置窗口主题 (真, “深色主题”, “主窗口主题已切到深色。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_标题栏背景_蓝)
    _设置窗口标题栏背景色 (到RGB (64, 158, 255), “标题栏背景已切到科技蓝。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_标题栏背景_黑)
    _设置窗口标题栏背景色 (到RGB (43, 47, 54), “标题栏背景已切到深空黑。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_标题栏背景_绿)
    _设置窗口标题栏背景色 (到RGB (39, 174, 96), “标题栏背景已切到青绿色。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_标题栏背景_跟随)
    _设置窗口标题栏背景色 (0, “标题栏背景已恢复为跟随主题。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_标题栏文字_亮)
    _设置窗口标题栏文字色 (到ARGB (255, 255, 255, 255), “标题栏文字已切到亮色。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_标题栏文字_暗)
    _设置窗口标题栏文字色 (到ARGB (255, 29, 30, 31), “标题栏文字已切到暗色。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_标题栏文字_蓝)
    _设置窗口标题栏文字色 (到ARGB (255, 140, 197, 255), “标题栏文字已切到蓝色。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_标题栏文字_跟随)
    _设置窗口标题栏文字色 (0, “标题栏文字已恢复为跟随主题。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_客户区背景_白)
    _设置窗口客户区背景色 (到ARGB (255, 255, 255, 255), “纯白背景”, “客户区背景已切到纯白。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_客户区背景_灰)
    _设置窗口客户区背景色 (到ARGB (255, 245, 247, 250), “主题默认背景”, “客户区背景已切到灰蓝。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_客户区背景_深)
    _设置窗口客户区背景色 (到ARGB (255, 31, 35, 41), “深色背景”, “客户区背景已切到深色。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_客户区背景_跟随)
    _设置窗口客户区背景色 (_读取当前主题色 (“background”), “主题默认背景”, “客户区背景已恢复为跟随主题。”)
    返回 ()
.如果真结束
.如果真结束

.如果真 (_父窗口句柄 ＝ 基础页句柄)
.如果真 (按钮ID ＝ 按钮_基础演示)
    _刷新基础页摘要 (“EmojiButton 点击完成，当前主窗口即为 EmojiWindow 4.0。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_读取编辑框)
    _刷新基础页摘要 (“普通 EditBox 当前文本已读取。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_写入编辑框)
    文本字节集 ＝ 编码_Ansi到Utf8 (“已从按钮写入普通 EditBox”)
    设置编辑框文本 (编辑框_普通, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    文本字节集 ＝ 编码_Ansi到Utf8 (“彩色 Emoji 编辑框已更新” ＋ #换行符 ＋ “这里是新的多行示例内容。”)
    设置编辑框文本 (编辑框_彩色Emoji, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    _刷新基础页摘要 (“普通 EditBox 与彩色 Emoji 编辑框都已写入新内容。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_推进进度)
    当前值 ＝ 获取进度条值 (进度条_任务) ＋ 10
    .如果真 (当前值 ＞ 100)
        当前值 ＝ 0
    .如果真结束
    设置进度条值 (进度条_任务, 当前值)
    设置滑块值 (滑块_音量, 当前值)
    _刷新基础页摘要 (“ProgressBar 与 Slider 已同步到 ” ＋ 到文本 (当前值) ＋ “。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_切换开关)
    设置开关状态 (开关_通知, 取反 (获取开关状态 (开关_通知)))
    _刷新基础页摘要 (“已通过按钮切换 Switch 状态。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_重置基础页)
    文本字节集 ＝ 编码_Ansi到Utf8 (“Admin”)
    设置编辑框文本 (编辑框_普通, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    文本字节集 ＝ 编码_Ansi到Utf8 (“欢迎使用 4.0 完整示例” ＋ #换行符 ＋ “这里演示彩色 Emoji 编辑框。”)
    设置编辑框文本 (编辑框_彩色Emoji, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    设置复选框状态 (复选框_功能, 真)
    设置单选按钮状态 (单选按钮_浅色, 真)
    设置单选按钮状态 (单选按钮_深色, 假)
    当前基础页单选文本 ＝ “浅色主题”
    设置进度条值 (进度条_任务, 35)
    设置滑块值 (滑块_音量, 35)
    设置开关状态 (开关_通知, 真)
    _刷新基础页摘要 (“基础页已恢复默认状态。”)
    返回 ()
.如果真结束
.如果真结束

.如果真 (_父窗口句柄 ＝ 选择页句柄)
.如果真 (按钮ID ＝ 按钮_读取选择结果)
    获取热键值 (热键控件_快捷,  虚拟键码,  修饰键)
    _刷新选择页摘要 (“选择页：已读取当前选择结果，HotKey=” ＋ _热键文本 (虚拟键码, 修饰键) ＋ “。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_设置快捷键)
    设置热键值 (热键控件_快捷, 75, 3)
    设置选中项索引 (列表框_分类, 1)
    设置组合框选中索引 (组合框_城市, 0)
    设置D2D组合框选中索引 (D2D组合框_主题, 2)
    设置D2D日期时间选择器日期时间 (日期时间_计划, 2026, 4, 8, 14, 0, 0)
    _刷新选择页摘要 (“选择页：已联动设置 Ctrl+Shift+K、北京、浅绿主题与 2026-04-08 14:00，并已把焦点切回 HotKeyControl，可直接按键验证回调。”)
    设置输入焦点 (热键控件_快捷)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_选择_新增列表项)
    文本字节集 ＝ 编码_Ansi到Utf8 (“扩展演示 #” ＋ 到文本 (获取列表项数量 (列表框_分类) ＋ 1))
    添加列表项 (列表框_分类, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    设置选中项索引 (列表框_分类, 获取列表项数量 (列表框_分类) － 1)
    _刷新选择页摘要 (“选择页：已向 ListBox 追加一项并自动选中最后一项。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_选择_选中第三项)
    .如果真 (获取列表项数量 (列表框_分类) ＞ 2)
        设置选中项索引 (列表框_分类, 2)
        _刷新选择页摘要 (“选择页：ListBox 已程序选中第 3 项。”)
    .否则
        _刷新选择页摘要 (“选择页：ListBox 当前不足 3 项，无法选中第 3 项。”)
    .如果真结束
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_选择_组合框写入)
    文本字节集 ＝ 编码_Ansi到Utf8 (“杭州”)
    设置组合框文本 (组合框_城市, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    文本字节集 ＝ { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 (“青瓷绿”)
    设置D2D组合框文本 (D2D组合框_主题, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    _刷新选择页摘要 (“选择页：已向 ComboBox / D2DComboBox 写入新的编辑文本。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_选择_热键AltF4)
    设置热键值 (热键控件_快捷, 115, 4)
    _刷新选择页摘要 (“选择页：HotKey 已程序设为 Alt+F4，并已把焦点切回 HotKeyControl，可直接按键验证回调。”)
    设置输入焦点 (热键控件_快捷)
    返回 ()
.如果真结束
.如果真结束

.如果真 (_父窗口句柄 ＝ 显示页句柄)
.如果真 (按钮ID ＝ 按钮_加载图片)
    .如果真 (从文件加载图片_辅助 (图片框_预览, 取运行目录 () ＋ "\examples\Python\微信图片.jpg"))
        _刷新显示页摘要 ("显示页：PictureBox 已加载 examples\\Python\\微信图片.jpg。")
    .否则
        _刷新显示页摘要 ("显示页：图片加载失败，请确认运行目录位于仓库根目录。")
    .如果真结束
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_清除图片)
    清除图片 (图片框_预览)
    _刷新显示页摘要 (“显示页：PictureBox 已清空。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_切换图片模式)
    当前图片缩放模式 ＝ 当前图片缩放模式 ＋ 1
    .如果真 (当前图片缩放模式 ＞ 3)
        当前图片缩放模式 ＝ 0
    .如果真结束
    设置图片框缩放模式 (图片框_预览, 当前图片缩放模式)
    _刷新显示页摘要 (“显示页：PictureBox 缩放模式已切换到 ” ＋ _图片缩放模式文本 (当前图片缩放模式) ＋ “。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_图片透明75)
    当前图片透明度百分比 ＝ 75
    设置图片透明度 (图片框_预览, 0.75)
    _刷新显示页摘要 (“显示页：PictureBox 透明度已调整为 75%。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_图片透明100)
    当前图片透明度百分比 ＝ 100
    设置图片透明度 (图片框_预览, 1.0)
    _刷新显示页摘要 (“显示页：PictureBox 透明度已恢复为 100%。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_图片透明25)
    当前图片透明度百分比 ＝ 25
    设置图片透明度 (图片框_预览, 0.25)
    _刷新显示页摘要 (“显示页：PictureBox 透明度已调整为 25%。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_图片背景冷)
    当前图片背景色 ＝ 到ARGB (255, 234, 243, 255)
    当前图片背景名称 ＝ “冷色底”
    设置图片框背景色 (图片框_预览, 当前图片背景色)
    _刷新显示页摘要 (“显示页：PictureBox 已切换到冷色背景。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_图片背景暖)
    当前图片背景色 ＝ 到ARGB (255, 255, 244, 232)
    当前图片背景名称 ＝ “暖色底”
    设置图片框背景色 (图片框_预览, 当前图片背景色)
    _刷新显示页摘要 (“显示页：PictureBox 已切换到暖色背景。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_图片切换可见)
    图片框当前可见 ＝ 取反 (图片框当前可见)
    显示图片框 (图片框_预览, 图片框当前可见)
    .如果真 (图片框当前可见)
        _刷新显示页摘要 (“显示页：PictureBox 已重新显示。”)
    .否则
        _刷新显示页摘要 (“显示页：PictureBox 已隐藏。”)
    .如果真结束
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_图片切换启用)
    图片框当前启用 ＝ 取反 (图片框当前启用)
    启用图片框 (图片框_预览, 图片框当前启用)
    .如果真 (图片框当前启用)
        _刷新显示页摘要 (“显示页：PictureBox 已重新启用。”)
    .否则
        _刷新显示页摘要 (“显示页：PictureBox 已禁用。”)
    .如果真结束
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_显示Tooltip)
    对控件显示提示层 (提示层_说明, 标签_Tooltip目标)
    _刷新显示页摘要 (“显示页：Tooltip 已在目标标签旁手动显示。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_隐藏Tooltip)
    隐藏提示层 (提示层_说明)
    _刷新显示页摘要 (“显示页：Tooltip 已隐藏。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_Tooltip浅色)
    当前Tooltip主题 ＝ 1
    设置提示层主题 (提示层_说明, 当前Tooltip主题)
    _刷新显示页摘要 (“显示页：右侧主控 Tooltip 已切换到浅色主题。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_Tooltip下方)
    当前Tooltip主题 ＝ 1
    当前Tooltip位置 ＝ 1
    当前Tooltip触发方式 ＝ 0
    设置提示层主题 (提示层_说明, 当前Tooltip主题)
    设置提示层位置 (提示层_说明, 当前Tooltip位置)
    设置提示层触发方式 (提示层_说明, 当前Tooltip触发方式)
    _刷新显示页摘要 (“显示页：右侧主控 Tooltip 已切换为下方 Hover 触发。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_信息通知)
    文本字节集 ＝ 编码_Ansi到Utf8 (“信息通知”)
    emoji字节集 ＝ 编码_Ansi到Utf8 (“这是 4.0 完整示例里 Notification 的信息类型。”)
    当前通知句柄 ＝ 显示通知 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), emoji字节集, 取字节集长度 (emoji字节集), 0, 0, 3000)
    设置通知回调 (当前通知句柄, 到整数 (&通知回调))
    _刷新显示页摘要 (“显示页：信息通知已显示。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_成功通知)
    文本字节集 ＝ 编码_Ansi到Utf8 (“成功通知”)
    emoji字节集 ＝ 编码_Ansi到Utf8 (“Notification 成功类型已触发。”)
    当前通知句柄 ＝ 显示通知 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), emoji字节集, 取字节集长度 (emoji字节集), 1, 0, 3000)
    设置通知回调 (当前通知句柄, 到整数 (&通知回调))
    _刷新显示页摘要 (“显示页：成功通知已显示。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_警告通知)
    文本字节集 ＝ 编码_Ansi到Utf8 (“警告通知”)
    emoji字节集 ＝ 编码_Ansi到Utf8 (“Notification warning 类型已触发，请留意右上角提醒层。”)
    当前通知句柄 ＝ 显示通知 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), emoji字节集, 取字节集长度 (emoji字节集), 2, 0, 3000)
    设置通知回调 (当前通知句柄, 到整数 (&通知回调))
    _刷新显示页摘要 (“显示页：警告通知已显示。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_错误通知)
    文本字节集 ＝ 编码_Ansi到Utf8 (“错误通知”)
    emoji字节集 ＝ 编码_Ansi到Utf8 (“Notification error 类型已触发，可继续测试关闭与回调写回。”)
    当前通知句柄 ＝ 显示通知 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), emoji字节集, 取字节集长度 (emoji字节集), 3, 0, 3000)
    设置通知回调 (当前通知句柄, 到整数 (&通知回调))
    _刷新显示页摘要 (“显示页：错误通知已显示。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_连续通知)
    文本字节集 ＝ 编码_Ansi到Utf8 (“连续通知 1/2”)
    emoji字节集 ＝ 编码_Ansi到Utf8 (“第一条通知用于验证 Notification 连续入栈。”)
    当前值 ＝ 显示通知 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), emoji字节集, 取字节集长度 (emoji字节集), 0, 0, 2200)
    设置通知回调 (当前值, 到整数 (&通知回调))
    文本字节集 ＝ 编码_Ansi到Utf8 (“连续通知 2/2”)
    emoji字节集 ＝ 编码_Ansi到Utf8 (“第二条 success 通知紧随其后，用于验证堆叠与回调写回。”)
    当前通知句柄 ＝ 显示通知 (主窗口句柄, 文本字节集, 取字节集长度 (文本字节集), emoji字节集, 取字节集长度 (emoji字节集), 1, 0, 3600)
    设置通知回调 (当前通知句柄, 到整数 (&通知回调))
    _刷新显示页摘要 (“显示页：连续两条 Notification 已依次弹出。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_关闭通知)
    .如果真 (当前通知句柄 ≠ 0)
        关闭通知 (当前通知句柄)
        当前通知句柄 ＝ 0
        _刷新显示页摘要 (“显示页：当前 Notification 已主动关闭。”)
    .否则
        _刷新显示页摘要 (“显示页：当前没有活动中的 Notification。”)
    .如果真结束
    返回 ()
.如果真结束
.如果真结束

.如果真 (_父窗口句柄 ＝ 弹窗页句柄)
.如果真 (按钮ID ＝ 按钮_打开消息框)
    调用子程序 (&_显示MessageBox演示, , , )
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_打开确认框)
    调用子程序 (&_显示ConfirmBox演示, , , )
    返回 ()
.如果真结束
.如果真结束

.如果真 (_父窗口句柄 ＝ 导航页句柄)
.如果真 (按钮ID ＝ 按钮_手动弹出菜单)
    显示上下文菜单 (弹出菜单句柄, -1, -1)
    _刷新导航摘要 (“右侧 PopupMenu 已手动弹出”, “当前通过按钮直接拉起绑定到标签区域的 PopupMenu，可继续测试空白处自动收起。”, “导航页：右侧 PopupMenu 已手动弹出。”)
    返回 ()
.如果真结束
.如果真结束

.如果真 (_父窗口句柄 ＝ 数据页句柄)
.如果真 (按钮ID ＝ 按钮_新增行)
    表格行计数器 ＝ 表格行计数器 ＋ 1
    _向表格追加一行 (到文本 (表格行计数器), "新用户" ＋ 到文本 (表格行计数器 － 3000), "杭州", "新建", 假)
    表格_设置选中单元格 (表格句柄, 表格_获取行数 (表格句柄) － 1, 1)
    表格_刷新 (表格句柄)
    _刷新数据页摘要 (“数据页：已新增一行，当前总行数=” ＋ 到文本 (表格_获取行数 (表格句柄)) ＋ “。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_删除选中行)
    选中行 ＝ 表格_获取选中行 (表格句柄)
    .如果真 (选中行 ≠ -1)
        表格_移除行 (表格句柄, 选中行)
        .如果真 (表格_获取行数 (表格句柄) ＞ 0)
            表格_设置选中单元格 (表格句柄, 0, 1)
        .如果真结束
        表格_刷新 (表格句柄)
        _刷新数据页摘要 ("数据页：已删除选中行，行索引=" ＋ 到文本 (选中行) ＋ "。")
    .否则
        _刷新数据页摘要 ("数据页：当前没有选中行。")
    .如果真结束
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_按状态排序)
    表格_按列排序 (表格句柄, 4, 1)
    表格_刷新 (表格句柄)
    _刷新数据页摘要 (“数据页：已按状态列升序排序。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_表格_单元格模式)
    当前表格选择模式 ＝ #DGSEL_CELL
    表格_设置选择模式 (表格句柄, 当前表格选择模式)
    .如果真 (表格_获取行数 (表格句柄) ＞ 0)
        表格_设置选中单元格 (表格句柄, 0, 1)
    .如果真结束
    表格_刷新 (表格句柄)
    _刷新数据页摘要 (“数据页：表格选择模式已切换为单元格模式。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_表格_整行模式)
    当前表格选择模式 ＝ #DGSEL_ROW
    表格_设置选择模式 (表格句柄, 当前表格选择模式)
    .如果真 (表格_获取行数 (表格句柄) ＞ 0)
        表格_设置选中单元格 (表格句柄, 0, 1)
    .如果真结束
    表格_刷新 (表格句柄)
    _刷新数据页摘要 (“数据页：表格选择模式已切换为整行模式。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_表格_写入单元格)
    选中行 ＝ 表格_获取选中行 (表格句柄)
    选中列 ＝ 表格_获取选中列 (表格句柄)
    .如果真 (选中行 ＝ -1)
        选中行 ＝ 0
    .如果真结束
    .如果真 (选中列 ＜ 1)
        选中列 ＝ 2
    .如果真结束
    .如果真 (表格_获取行数 (表格句柄) ＞ 0)
        文本字节集 ＝ 编码_Ansi到Utf8 (“已由按钮写入”)
        表格_设置单元格文本 (表格句柄, 选中行, 选中列, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
        表格_设置选中单元格 (表格句柄, 选中行, 选中列)
        表格_刷新 (表格句柄)
        _刷新数据页摘要 (“数据页：已写入单元格 [” ＋ 到文本 (选中行) ＋ “, ” ＋ 到文本 (选中列) ＋ “]。”)
    .否则
        _刷新数据页摘要 (“数据页：表格暂无数据，无法写入单元格。”)
    .如果真结束
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_表格_冻结首列)
    表格冻结首列已开启 ＝ 取反 (表格冻结首列已开启)
    表格_设置冻结首列 (表格句柄, 表格冻结首列已开启)
    表格_刷新 (表格句柄)
    _刷新数据页摘要 (“数据页：冻结首列已” ＋ _逻辑文本 (表格冻结首列已开启) ＋ “。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_表格_定位首行)
    .如果真 (表格_获取行数 (表格句柄) ＞ 0)
        表格_设置选中单元格 (表格句柄, 0, 1)
        _刷新数据页摘要 (“数据页：已定位到首行第 2 列。”)
    .否则
        _刷新数据页摘要 (“数据页：表格暂无数据，无法定位到首行。”)
    .如果真结束
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_表格_刷新摘要)
    _刷新数据页摘要 (“数据页：已手动刷新表格摘要卡。”)
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_导出CSV)
    路径字节集 ＝ 编码_Ansi到Utf8 (取运行目录 () ＋ "\all_components_tree_enhanced_4.0.csv")
    .如果真 (表格_导出CSV (表格句柄, 取变量数据地址 (路径字节集), 取字节集长度 (路径字节集)))
        当前表格导出说明 ＝ "CSV 已导出：all_components_tree_enhanced_4.0.csv"
        _刷新数据页摘要 ("数据页：CSV 导出成功：all_components_tree_enhanced_4.0.csv")
    .否则
        当前表格导出说明 ＝ "CSV 导出失败。"
        _刷新数据页摘要 ("数据页：CSV 导出失败。")
    .如果真结束
    返回 ()
.如果真结束
.如果真结束


.子程序 导航树选中回调, , , stdcall
.参数 节点ID, 整数型
.参数 _上下文, 整数型

.如果真 (节点ID ＝ 节点_基础_EmojiWindow)
    切换到Tab (主TabControl句柄, 0)
    设置页头 (“基础组件 / 窗口”, “基础组件中的窗口页，负责主题切换、标题栏颜色、客户区背景和实时状态读取。”)
    更新状态 (“导航树切换到窗口属性页。”)
    返回 ()
.如果真结束

.如果真 (节点ID ＝ 节点_基础根 或 节点ID ＝ 节点_基础_按钮 或 节点ID ＝ 节点_基础_标签 或 节点ID ＝ 节点_基础_编辑框 或 节点ID ＝ 节点_基础_彩色编辑框 或 节点ID ＝ 节点_基础_复选框 或 节点ID ＝ 节点_基础_单选框 或 节点ID ＝ 节点_基础_进度条 或 节点ID ＝ 节点_基础_滑块 或 节点ID ＝ 节点_基础_开关 或 节点ID ＝ 节点_基础_分组框 或 节点ID ＝ 节点_基础_面板)
    切换到Tab (主TabControl句柄, 1)
    设置页头 (“基础组件”, “基础组件包含按钮、标签、编辑框、状态控件和容器；当前综合页承载这些核心演示。”)
    更新状态 (“导航树切换到基础组件页。”)
    返回 ()
.如果真结束

.如果真 (节点ID ＝ 节点_选择根 或 节点ID ＝ 节点_选择_列表框 或 节点ID ＝ 节点_选择_组合框 或 节点ID ＝ 节点_选择_D2D组合框 或 节点ID ＝ 节点_选择_日期时间 或 节点ID ＝ 节点_选择_热键)
    切换到Tab (主TabControl句柄, 2)
    设置页头 (“选择类组件”, “选择类组件按列表、组合框、日期时间和热键分开展示；当前综合页承载核心读写与回调。”)
    更新状态 (“导航树切换到选择类组件页。”)
    返回 ()

.如果真结束

.如果真 (节点ID ＝ 节点_显示根 或 节点ID ＝ 节点_显示_图片框 或 节点ID ＝ 节点_显示_Tooltip 或 节点ID ＝ 节点_显示_Notification)
    切换到Tab (主TabControl句柄, 3)
    设置页头 (“显示类组件”, “显示类组件包括 PictureBox、Tooltip 和 Notification，分别验证图片、提示层和右上角通知行为。”)
    更新状态 (“导航树切换到显示类组件页。”)
    返回 ()

.如果真结束

.如果真 (节点ID ＝ 节点_弹窗根 或 节点ID ＝ 节点_弹窗_MessageBox 或 节点ID ＝ 节点_弹窗_ConfirmBox)
    切换到Tab (主TabControl句柄, 4)
    设置页头 (“弹窗类组件”, “弹窗类组件单独提供 MessageBox 与 ConfirmBox 触发页，便于快速验证消息提示和确认回调。”)
    更新状态 (“导航树切换到弹窗类组件页。”)
    返回 ()

.如果真结束

.如果真 (节点ID ＝ 节点_导航根 或 节点ID ＝ 节点_导航_TabControl 或 节点ID ＝ 节点_导航_TreeView 或 节点ID ＝ 节点_导航_MenuBar 或 节点ID ＝ 节点_导航_PopupMenu)
    切换到Tab (主TabControl句柄, 5)
    设置页头 (“页签与导航”, “页签与导航集中展示 TabControl、TreeView、MenuBar 和 EmojiPopupMenu，继续保留成熟的综合交互舞台。”)
    更新状态 (“导航树切换到页签与导航页。”)
    返回 ()

.如果真结束

.如果真 (节点ID ＝ 节点_数据根 或 节点ID ＝ 节点_数据_表格)
    切换到Tab (主TabControl句柄, 6)
    设置页头 (“数据组件”, “数据组件当前重点是 DataGridView 综合演示页，覆盖列定义、行填充、排序、导出与回调。”)
    更新状态 (“导航树切换到数据组件页。”)
    返回 ()

.如果真结束



.子程序 导航树展开回调, , , stdcall
.参数 节点ID, 整数型
.参数 _上下文, 整数型
更新状态 ("导航树已展开节点。")

.子程序 导航树折叠回调, , , stdcall
.参数 节点ID, 整数型
.参数 _上下文, 整数型
更新状态 ("导航树已折叠节点。")

.子程序 Tab切换回调, , , stdcall
.参数 _hTabControl, 整数型
.参数 选中索引, 整数型

_确保Tab页已构建 (选中索引)

.判断开始 (选中索引 ＝ 0)
    设置页头 ("基础组件 / 窗口", "基础组件中的窗口页，负责主题切换、标题栏颜色、客户区背景和实时状态读取。")
.判断 (选中索引 ＝ 1)
    设置页头 ("基础组件", "基础组件包含按钮、标签、编辑框、状态控件和容器；当前综合页承载这些核心演示。")
.判断 (选中索引 ＝ 2)
    设置页头 ("选择类组件", "选择类组件按列表、组合框、日期时间和热键分开展示；当前综合页承载核心读写与回调。")
.判断 (选中索引 ＝ 3)
    设置页头 ("显示类组件", "显示类组件包括 PictureBox、Tooltip 和 Notification，分别验证图片、提示层和右上角通知行为。")
.判断 (选中索引 ＝ 4)
    设置页头 ("弹窗类组件", "弹窗类组件单独提供 MessageBox 与 ConfirmBox 触发页，便于快速验证消息提示和确认回调。")
.判断 (选中索引 ＝ 5)
    设置页头 ("页签与导航", "页签与导航集中展示 TabControl、TreeView、MenuBar 和 EmojiPopupMenu，继续保留成熟的综合交互舞台。")
.判断 (选中索引 ＝ 6)
    设置页头 ("数据组件", "数据组件当前重点是 DataGridView 综合演示页，覆盖列定义、行填充、排序、导出与回调。")
.默认

.判断结束


.子程序 复选框回调, , , stdcall
.参数 _复选框句柄, 整数型
.参数 选中状态, 逻辑型

_刷新基础页摘要 (“CheckBox 状态改变：” ＋ 到文本 (选中状态))

.子程序 单选按钮_浅色回调, , , stdcall
.参数 _句柄, 整数型
.参数 _分组ID, 整数型
.参数 选中, 整数型

.如果真 (选中 ≠ 0)
    当前基础页单选文本 ＝ “浅色主题”
    _刷新基础页摘要 ("RadioButton 当前选择：" ＋ 当前基础页单选文本)
.如果真结束

.子程序 单选按钮_深色回调, , , stdcall
.参数 _句柄, 整数型
.参数 _分组ID, 整数型
.参数 选中, 整数型

.如果真 (选中 ≠ 0)
    当前基础页单选文本 ＝ “深色主题”
    _刷新基础页摘要 ("RadioButton 当前选择：" ＋ 当前基础页单选文本)
.如果真结束


.子程序 滑块回调, , , stdcall
.参数 _hSlider, 整数型
.参数 值, 整数型

_刷新基础页摘要 (“Slider 当前值：” ＋ 到文本 (值) ＋ “；ProgressBar 当前值：” ＋ 到文本 (获取进度条值 (进度条_任务)))

.子程序 开关回调, , , stdcall
.参数 _hSwitch, 整数型
.参数 checked, 整数型

_刷新基础页摘要 (“Switch 当前状态：” ＋ 到文本 (checked))

.子程序 列表框选中回调, , , stdcall
.参数 _列表框句柄, 整数型
.参数 索引, 整数型

_刷新选择页摘要 (“选择页：ListBox 回调触发，当前索引=” ＋ 到文本 (索引) ＋ “。”)

.子程序 组合框回调, , , stdcall
.参数 _组合框句柄, 整数型
.参数 索引, 整数型

_刷新选择页摘要 (“选择页：ComboBox 回调触发，当前索引=” ＋ 到文本 (索引) ＋ “。”)

.子程序 D2D组合框回调, , , stdcall
.参数 _组合框句柄, 整数型
.参数 索引, 整数型

_刷新选择页摘要 (“选择页：D2DComboBox 回调触发，当前索引=” ＋ 到文本 (索引) ＋ “。”)

.子程序 日期时间回调, , , stdcall
.参数 _控件句柄, 整数型

_刷新选择页摘要 (“选择页：D2DDateTimePicker 已发生值变化。”)

.子程序 热键回调, , , stdcall
.参数 _热键句柄, 整数型
.参数 虚拟键码, 整数型
.参数 修饰键, 整数型

_刷新选择页摘要 (“选择页：HotKey 回调触发，当前值=” ＋ _热键文本 (虚拟键码, 修饰键) ＋ “。”)

.子程序 内层树选中回调, , , stdcall
.参数 节点ID, 整数型
.参数 _上下文, 整数型

.如果真 (节点ID ＝ 内层节点_总览)
    _刷新导航摘要 (“内层 TreeView：总览”, “当前聚焦导航说明与页签概览，可继续切换右侧 PopupMenu 与顶部 MenuBar 验证联动。”, “导航页：内层 TreeView 选中了总览节点。”)
    返回 ()
.如果真结束

.如果真 (节点ID ＝ 内层节点_设置)
    _刷新导航摘要 (“内层 TreeView：设置”, “当前聚焦布局开关、导航文案与绑定策略，适合验证多级菜单和页签结构。”, “导航页：内层 TreeView 选中了设置节点。”)
    返回 ()
.如果真结束

.如果真 (节点ID ＝ 内层节点_帮助)
    _刷新导航摘要 (“内层 TreeView：帮助”, “当前聚焦 MenuBar 常驻、PopupMenu 自动收起和回调写回这三类说明。”, “导航页：内层 TreeView 选中了帮助节点。”)
    返回 ()
.如果真结束

.如果真 (节点ID ＝ 内层节点_最新事件)
    _刷新导航摘要 (“内层 TreeView：最新事件”, “当前聚焦最近一次回调链路，可结合底部状态栏确认节点、菜单和页签动作来源。”, “导航页：内层 TreeView 选中了最新事件节点。”)
    返回 ()
.如果真结束

_刷新导航摘要 (“内层 TreeView：未知节点”, “当前命中了未显式映射的节点，可结合节点 ID 继续排查导航结构。”, “内层 TreeView 选中了节点 ID=” ＋ 到文本 (节点ID))

.子程序 菜单栏回调, , , stdcall
.参数 _menu_id, 整数型
.参数 item_id, 整数型

.判断开始 (item_id ＝ 910)
    更新状态 ("文件菜单：已展开 '消息' 子菜单。")
.判断 (item_id ＝ 911)
    调用子程序 (&_显示MessageBox演示, , , )
.判断 (item_id ＝ 912)
    调用子程序 (&_显示ConfirmBox演示, , , )
.判断 (item_id ＝ 920)
    更新状态 ("文件菜单：已展开 '导出' 子菜单。")
.判断 (item_id ＝ 921)
    _确保Tab页已构建 (6)
    按钮点击回调 (按钮_导出CSV, 数据页句柄)
.判断 (item_id ＝ 930)
    更新状态 ("文件菜单：已展开 '退出' 子菜单。")
.判断 (item_id ＝ 931)
    销毁窗口 (主窗口句柄)
    返回 ()
.判断 (item_id ＝ 1001)
    切换到Tab (主TabControl句柄, 1)
.判断 (item_id ＝ 1006)
    切换到Tab (主TabControl句柄, 0)
.判断 (item_id ＝ 1002)
    切换到Tab (主TabControl句柄, 2)
.判断 (item_id ＝ 1003)
    切换到Tab (主TabControl句柄, 3)
.判断 (item_id ＝ 1004)
    切换到Tab (主TabControl句柄, 4)
.判断 (item_id ＝ 1005)
    切换到Tab (主TabControl句柄, 5)
.判断 (item_id ＝ 1007)
    切换到Tab (主TabControl句柄, 6)
.判断 (item_id ＝ 1101)
    调用子程序 (&_显示MessageBox演示, , , )
.判断 (item_id ＝ 1102)
    调用子程序 (&_显示ConfirmBox演示, , , )
.判断 (item_id ＝ 1103)
    _确保Tab页已构建 (3)
    按钮点击回调 (按钮_成功通知, 显示页句柄)
.判断 (item_id ＝ 1201)
    _确保Tab页已构建 (6)
    按钮点击回调 (按钮_新增行, 数据页句柄)
.判断 (item_id ＝ 1202)
    _确保Tab页已构建 (6)
    按钮点击回调 (按钮_导出CSV, 数据页句柄)
.默认

.判断结束

_刷新导航摘要 (“顶部 MenuBar 已触发”, “当前动作来自主窗口顶部多层菜单，可继续切换到右侧 PopupMenu 或左侧内层树验证跨区域联动。”, “MenuBar 操作完成，item_id=” ＋ 到文本 (item_id))

.子程序 弹出菜单回调, , , stdcall
.参数 _menu_id, 整数型
.参数 item_id, 整数型

.判断开始 (item_id ＝ 2001)
    调用子程序 (&_显示MessageBox演示, , , )
.判断 (item_id ＝ 2002)
    _确保Tab页已构建 (3)
    按钮点击回调 (按钮_成功通知, 显示页句柄)
.判断 (item_id ＝ 2101)
    _确保Tab页已构建 (6)
    按钮点击回调 (按钮_新增行, 数据页句柄)
.判断 (item_id ＝ 2102)
    _确保Tab页已构建 (6)
    按钮点击回调 (按钮_按状态排序, 数据页句柄)
.默认

.判断结束

.判断开始 (item_id ＝ 2001)
    _刷新导航摘要 (“PopupMenu：MessageBox”, “右键菜单已调起 MessageBox 演示，说明 PopupMenu 回调与主窗口链路正常。”, “导航页：PopupMenu 触发了 MessageBox。”)
.判断 (item_id ＝ 2002)
    _刷新导航摘要 (“PopupMenu：成功通知”, “右键菜单已复用显示页的成功通知逻辑，跨页按钮回调链路保持有效。”, “导航页：PopupMenu 触发了成功通知。”)
.判断 (item_id ＝ 2101)
    _刷新导航摘要 (“PopupMenu：新增一行”, “右键菜单已复用数据页新增行逻辑，可继续检查表格页是否同步追加记录。”, “导航页：PopupMenu 触发了新增行。”)
.判断 (item_id ＝ 2102)
    _刷新导航摘要 (“PopupMenu：按状态排序”, “右键菜单已复用数据页排序逻辑，可继续切到数据页查看状态列排序结果。”, “导航页：PopupMenu 触发了按状态排序。”)
.默认
    _刷新导航摘要 (“PopupMenu 回调已完成”, “当前右键菜单动作已经写回状态栏，说明 PopupMenu 多级菜单与回调分发链路正常。”, “PopupMenu 回调完成，item_id=” ＋ 到文本 (item_id))
.判断结束

.子程序 通知回调, , , stdcall
.参数 _hNotification, 整数型
.参数 event_type, 整数型

.如果真 (event_type ≠ 1)
    当前通知句柄 ＝ 0
.如果真结束
_刷新显示页摘要 (“显示页：Notification 回调触发，event_type=” ＋ 到文本 (event_type) ＋ “。”)

.子程序 确认框回调, , , stdcall
.参数 确认, 整数型

.如果真 (确认 ＝ 1)
    .如果真 (标签_弹窗状态 ≠ 0)
        写标签 (标签_弹窗状态, "ConfirmBox 结果：用户点击了确认。")
    .如果真结束
    更新状态 ("弹窗页：ConfirmBox 已确认。")
.否则
    .如果真 (标签_弹窗状态 ≠ 0)
        写标签 (标签_弹窗状态, "ConfirmBox 结果：用户点击了取消。")
    .如果真结束
    更新状态 ("弹窗页：ConfirmBox 已取消。")
.如果真结束

.子程序 表格单元格点击回调, , , stdcall
.参数 _表格, 整数型
.参数 行, 整数型
.参数 列, 整数型

_刷新数据页摘要 (“数据页：DataGridView 单元格点击，行=” ＋ 到文本 (行) ＋ “，列=” ＋ 到文本 (列) ＋ “。”)

.子程序 表格单元格双击回调, , , stdcall
.参数 _表格, 整数型
.参数 行, 整数型
.参数 列, 整数型

_刷新数据页摘要 (“数据页：DataGridView 单元格双击，行=” ＋ 到文本 (行) ＋ “，列=” ＋ 到文本 (列) ＋ “。”)

.子程序 表格值改变回调, , , stdcall
.参数 _表格, 整数型
.参数 行, 整数型
.参数 列, 整数型

_刷新数据页摘要 (“数据页：DataGridView 值改变，行=” ＋ 到文本 (行) ＋ “，列=” ＋ 到文本 (列) ＋ “。”)

.子程序 表格列头点击回调, , , stdcall
.参数 _表格, 整数型
.参数 列, 整数型

_刷新数据页摘要 (“数据页：DataGridView 列头点击，列=” ＋ 到文本 (列) ＋ “。”)

.子程序 表格选择改变回调, , , stdcall
.参数 _表格, 整数型
.参数 行, 整数型
.参数 列, 整数型

_刷新数据页摘要 (“数据页：DataGridView 选择改变，行=” ＋ 到文本 (行) ＋ “，列=” ＋ 到文本 (列) ＋ “。”)

.子程序 _显示MessageBox演示
.局部变量 标题字节集, 字节集
.局部变量 内容字节集, 字节集
.局部变量 图标字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 (“MessageBox 4.0 演示”)
内容字节集 ＝ 编码_Ansi到Utf8 (“这是根据 Python 示例补齐的 MessageBox 完整演示入口。”)
图标字节集 ＝ 编码_Ansi到Utf8 (“i”)
信息提示框_ (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))
.如果真 (标签_弹窗状态 ≠ 0)
    写标签 (标签_弹窗状态, “MessageBox 已打开。”)
.如果真结束
更新状态 (“弹窗页：已打开 MessageBox。”)

.子程序 _显示ConfirmBox演示
.局部变量 标题字节集, 字节集
.局部变量 内容字节集, 字节集
.局部变量 图标字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 (“ConfirmBox 4.0 演示”)
内容字节集 ＝ 编码_Ansi到Utf8 (“是否确认执行 4.0 完整示例中的确认框回调？”)
图标字节集 ＝ 编码_Ansi到Utf8 (“?”)
确认框_ (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集), &确认框回调)
.如果真 (标签_弹窗状态 ≠ 0)
    写标签 (标签_弹窗状态, “ConfirmBox 已打开。”)
.如果真结束
更新状态 (“弹窗页：已打开 ConfirmBox。”)

.子程序 _向表格追加一行
.参数 编号, 文本型
.参数 名称, 文本型
.参数 城市, 文本型
.参数 状态, 文本型
.参数 已选中, 逻辑型
.局部变量 行索引, 整数型
.局部变量 文本字节集, 字节集

行索引 ＝ 表格_添加行 (表格句柄)
文本字节集 ＝ 编码_Ansi到Utf8 (编号)
表格_设置单元格文本 (表格句柄, 行索引, 1, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (名称)
表格_设置单元格文本 (表格句柄, 行索引, 2, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (城市)
表格_设置单元格文本 (表格句柄, 行索引, 3, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (状态)
表格_设置单元格文本 (表格句柄, 行索引, 4, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
文本字节集 ＝ 编码_Ansi到Utf8 (“详情”)
表格_设置单元格文本 (表格句柄, 行索引, 5, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
表格_设置单元格复选状态 (表格句柄, 行索引, 0, 已选中)

.如果 (状态 ＝ “已完成”)
    表格_设置单元格样式 (表格句柄, 行索引, 4, 到ARGB (255, 103, 194, 58), 0, 真, 假)
.否则

.如果结束
.如果 (状态 ＝ “进行中”)
    表格_设置单元格样式 (表格句柄, 行索引, 4, 到ARGB (255, 64, 158, 255), 0, 真, 假)
.否则

.如果结束
.如果 (状态 ＝ “待审核”)
    表格_设置单元格样式 (表格句柄, 行索引, 4, 到ARGB (255, 230, 162, 60), 0, 假, 真)
.否则

.如果结束


.子程序 _读取当前主题色, 整数型
.参数 颜色名称, 文本型
.局部变量 文本字节集, 字节集

文本字节集 ＝ 编码_Ansi到Utf8 (颜色名称)
返回 (取主题颜色 (取变量数据地址 (文本字节集), 取字节集长度 (文本字节集)))

.子程序 _刷新窗口属性读取
.参数 状态说明, 文本型
.局部变量 标题长度, 整数型
.局部变量 标题缓冲区, 字节集
.局部变量 标题字节集, 字节集
.局部变量 属性字节集, 字节集
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型
.局部变量 可见状态, 整数型
.局部变量 可见性文本, 文本型
.局部变量 标题栏颜色, 整数型
.局部变量 标题栏说明, 文本型
.局部变量 标题栏文字颜色, 整数型
.局部变量 标题栏文字说明, 文本型
.局部变量 主题主色, 整数型
.局部变量 主题背景色, 整数型
.局部变量 摘要字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 (“(空标题)”)
标题长度 ＝ 获取窗口标题 (主窗口句柄, 0, 0)
.如果真 (标题长度 ＞ 0)
    标题缓冲区 ＝ 取空白字节集 (标题长度 ＋ 1)
    获取窗口标题 (主窗口句柄, 取变量数据地址 (标题缓冲区), 标题长度 ＋ 1)
    标题字节集 ＝ 取字节集左边 (标题缓冲区, 标题长度)
.如果真结束

获取窗口位置 (主窗口句柄,  X,  Y,  宽度,  高度)
可见状态 ＝ 获取窗口可视状态 (主窗口句柄)
.如果真 (可见状态 ＝ 1)
    可见性文本 ＝ “显示”
.否则
    .如果真 (可见状态 ＝ 0)
        可见性文本 ＝ “隐藏”
    .否则
        可见性文本 ＝ “错误”
    .如果真结束
.如果真结束

标题栏颜色 ＝ 获取窗口标题栏颜色 (主窗口句柄)
.如果真 (标题栏颜色 ＝ -1)
    标题栏说明 ＝ “读取失败”
.否则
    .如果真 (标题栏颜色 ＝ 0)
        标题栏说明 ＝ “跟随主题”
    .否则
        标题栏说明 ＝ “0x” ＋ _十六进制 (标题栏颜色)
    .如果真结束
.如果真结束

标题栏文字颜色 ＝ 获取标题栏文字颜色 (主窗口句柄)
.如果真 (标题栏文字颜色 ＝ 0)
    标题栏文字说明 ＝ “跟随主题”
.否则
    标题栏文字说明 ＝ “0x” ＋ _十六进制 (标题栏文字颜色)
.如果真结束

主题主色 ＝ _读取当前主题色 (“primary”)
主题背景色 ＝ _读取当前主题色 (“background”)

属性字节集 ＝ 编码_Ansi到Utf8 (“标题：”) ＋ 标题字节集
属性字节集 ＝ 属性字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “句柄：0x” ＋ _十六进制 (主窗口句柄) ＋ “    可见性：” ＋ 可见性文本)
属性字节集 ＝ 属性字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “位置尺寸：x=” ＋ 到文本 (X) ＋ “, y=” ＋ 到文本 (Y) ＋ “, w=” ＋ 到文本 (宽度) ＋ “, h=” ＋ 到文本 (高度))
属性字节集 ＝ 属性字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “当前主题：” ＋ 当前窗口主题名称 ＋ “    primary=0x” ＋ _十六进制 (主题主色) ＋ “    background=0x” ＋ _十六进制 (主题背景色))
属性字节集 ＝ 属性字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “标题栏背景：” ＋ 标题栏说明 ＋ “    标题栏文字：” ＋ 标题栏文字说明)
属性字节集 ＝ 属性字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “客户区：” ＋ 当前窗口背景名称 ＋ “ / 0x” ＋ _十六进制 (当前窗口背景色))
写标签_字节集 (标签_窗口属性读取, 属性字节集)
.如果真 (标签_窗口读取摘要 ≠ 0)
    摘要字节集 ＝ 编码_Ansi到Utf8 (“当前策略”)
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “可见：” ＋ 可见性文本)
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “主题：” ＋ 当前窗口主题名称)
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “标题栏：” ＋ 标题栏说明)
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “客户区：” ＋ 当前窗口背景名称)
    写标签_字节集 (标签_窗口读取摘要, 摘要字节集)
.如果真结束
写标签 (标签_窗口状态, 状态说明)
更新状态 (状态说明)

.子程序 _刷新基础页摘要
.参数 状态说明, 文本型
.局部变量 文本长度, 整数型
.局部变量 缓冲区, 字节集
.局部变量 编辑框字节集, 字节集
.局部变量 摘要字节集, 字节集
.局部变量 单选文本, 文本型

.如果真 (标签_基础摘要 ≠ 0)
    编辑框字节集 ＝ 编码_Ansi到Utf8 (“（空）”)
    文本长度 ＝ 获取编辑框文本 (编辑框_普通, 0, 0)
    .如果真 (文本长度 ＞ 0)
        缓冲区 ＝ 取空白字节集 (文本长度 ＋ 1)
        获取编辑框文本 (编辑框_普通, 取变量数据地址 (缓冲区), 文本长度 ＋ 1)
        编辑框字节集 ＝ 取字节集左边 (缓冲区, 文本长度)
    .如果真结束

    单选文本 ＝ 当前基础页单选文本

    摘要字节集 ＝ 编码_Ansi到Utf8 (“EditBox：”) ＋ 编辑框字节集
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “CheckBox=” ＋ 到文本 (获取复选框状态 (复选框_功能)) ＋ “    Radio=” ＋ 单选文本 ＋ “    Progress=” ＋ 到文本 (获取进度条值 (进度条_任务)))
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “Slider=” ＋ 到文本 (获取滑块值 (滑块_音量)) ＋ “    Switch=” ＋ 到文本 (获取开关状态 (开关_通知)) ＋ “    当前主题=” ＋ 当前窗口主题名称)
    写标签_字节集 (标签_基础摘要, 摘要字节集)
.如果真结束

.如果真 (标签_基础状态 ≠ 0)
    写标签 (标签_基础状态, 状态说明)
.如果真结束
更新状态 (状态说明)

.子程序 _刷新导航摘要
.参数 场景文本, 文本型
.参数 说明文本, 文本型
.参数 状态说明, 文本型
.局部变量 摘要字节集, 字节集

.如果真 (标签_导航摘要 ≠ 0)
    摘要字节集 ＝ 编码_Ansi到Utf8 (“当前场景：” ＋ 场景文本)
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ 说明文本)
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “验证链路：内层 TreeView / 内层 TabControl / PopupMenu / 顶部 MenuBar”)
    写标签_字节集 (标签_导航摘要, 摘要字节集)
.如果真结束

.如果真 (标签_导航状态 ≠ 0)
    写标签 (标签_导航状态, 状态说明)
.如果真结束
更新状态 (状态说明)

.子程序 _取列表框项文本字节集, 字节集
.参数 列表框句柄, 整数型
.参数 索引, 整数型
.局部变量 文本长度, 整数型
.局部变量 缓冲区, 字节集

.如果真 (索引 ＜ 0)
    返回 (编码_Ansi到Utf8 (“（未选中）”))
.如果真结束

缓冲区 ＝ 取空白字节集 (256)
文本长度 ＝ 获取列表项文本 (列表框句柄, 索引, 取变量数据地址 (缓冲区), 取字节集长度 (缓冲区))
.如果真 (文本长度 ＞ 0)
    返回 (取字节集左边 (缓冲区, 文本长度))
.如果真结束

返回 (编码_Ansi到Utf8 (“（空）”))

.子程序 _取组合框文本字节集, 字节集
.参数 组合框句柄, 整数型
.局部变量 文本长度, 整数型
.局部变量 缓冲区, 字节集

缓冲区 ＝ 取空白字节集 (256)
文本长度 ＝ 获取组合框文本 (组合框句柄, 取变量数据地址 (缓冲区), 取字节集长度 (缓冲区))
.如果真 (文本长度 ＞ 0)
    返回 (取字节集左边 (缓冲区, 文本长度))
.如果真结束

返回 (编码_Ansi到Utf8 (“（空）”))

.子程序 _取D2D组合框文本字节集, 字节集
.参数 组合框句柄, 整数型
.局部变量 文本长度, 整数型
.局部变量 缓冲区, 字节集

缓冲区 ＝ 取空白字节集 (256)
文本长度 ＝ 获取D2D组合框文本 (组合框句柄, 取变量数据地址 (缓冲区), 取字节集长度 (缓冲区))
.如果真 (文本长度 ＞ 0)
    返回 (取字节集左边 (缓冲区, 文本长度))
.如果真结束

返回 (编码_Ansi到Utf8 (“（空）”))

.子程序 _取表格单元格文本字节集, 字节集
.参数 行索引, 整数型
.参数 列索引, 整数型
.局部变量 文本长度, 整数型
.局部变量 缓冲区, 字节集

.如果真 (行索引 ＜ 0 或 列索引 ＜ 0)
    返回 (编码_Ansi到Utf8 (“（未选中）”))
.如果真结束

缓冲区 ＝ 取空白字节集 (512)
文本长度 ＝ 表格_获取单元格文本 (表格句柄, 行索引, 列索引, 取变量数据地址 (缓冲区), 取字节集长度 (缓冲区))
.如果真 (文本长度 ＞ 0)
    返回 (取字节集左边 (缓冲区, 文本长度))
.如果真结束

返回 (编码_Ansi到Utf8 (“（空）”))

.子程序 _图片缩放模式文本, 文本型
.参数 缩放模式, 整数型

.判断开始 (缩放模式 ＝ 0)
    返回 (“原始大小”)
.判断 (缩放模式 ＝ 1)
    返回 (“拉伸铺满”)
.判断 (缩放模式 ＝ 2)
    返回 (“等比适应”)
.判断 (缩放模式 ＝ 3)
    返回 (“居中显示”)
.默认
    返回 (“未知模式 ” ＋ 到文本 (缩放模式))
.判断结束

.子程序 _Tooltip主题文本, 文本型
.参数 主题, 整数型

.判断开始 (主题 ＝ 0)
    返回 (“dark”)
.判断 (主题 ＝ 1)
    返回 (“light”)
.判断 (主题 ＝ 2)
    返回 (“custom”)
.默认
    返回 (“theme=” ＋ 到文本 (主题))
.判断结束

.子程序 _Tooltip触发文本, 文本型
.参数 触发方式, 整数型

.如果真 (触发方式 ＝ 0)
    返回 (“hover”)
.如果真结束
.如果真 (触发方式 ＝ 1)
    返回 (“click”)
.如果真结束
返回 (“trigger=” ＋ 到文本 (触发方式))

.子程序 _Tooltip位置文本, 文本型
.参数 位置, 整数型

.判断开始 (位置 ＝ 0)
    返回 (“上方”)
.判断 (位置 ＝ 1)
    返回 (“下方”)
.判断 (位置 ＝ 2)
    返回 (“左侧”)
.判断 (位置 ＝ 3)
    返回 (“右侧”)
.默认
    返回 (“位置=” ＋ 到文本 (位置))
.判断结束

.子程序 _逻辑文本, 文本型
.参数 状态, 逻辑型

.如果真 (状态)
    返回 (“开启”)
.如果真结束
返回 (“关闭”)

.子程序 _表格选择模式文本, 文本型
.参数 选择模式, 整数型

.如果真 (选择模式 ＝ #DGSEL_CELL)
    返回 (“单元格模式”)
.如果真结束
.如果真 (选择模式 ＝ #DGSEL_ROW)
    返回 (“整行模式”)
.如果真结束
返回 (“模式=” ＋ 到文本 (选择模式))

.子程序 _热键文本, 文本型
.参数 虚拟键码, 整数型
.参数 修饰键, 整数型
.局部变量 文本, 文本型

.如果真 (虚拟键码 ＝ 0)
    返回 (“（未设置）”)
.如果真结束

文本 ＝ “”
.如果真 (位与 (修饰键, 1) ≠ 0)
    文本 ＝ 文本 ＋ “Ctrl+”
.如果真结束
.如果真 (位与 (修饰键, 2) ≠ 0)
    文本 ＝ 文本 ＋ “Shift+”
.如果真结束
.如果真 (位与 (修饰键, 4) ≠ 0)
    文本 ＝ 文本 ＋ “Alt+”
.如果真结束

.如果真 (虚拟键码 ≥ 65 且 虚拟键码 ≤ 90)
    返回 (文本 ＋ 字符 (虚拟键码))
.如果真结束
.如果真 (虚拟键码 ≥ 48 且 虚拟键码 ≤ 57)
    返回 (文本 ＋ 字符 (虚拟键码))
.如果真结束
.如果真 (虚拟键码 ≥ 112 且 虚拟键码 ≤ 123)
    返回 (文本 ＋ “F” ＋ 到文本 (虚拟键码 － 111))
.如果真结束

.判断开始 (虚拟键码 ＝ 32)
    返回 (文本 ＋ “Space”)
.判断 (虚拟键码 ＝ 13)
    返回 (文本 ＋ “Enter”)
.判断 (虚拟键码 ＝ 8)
    返回 (文本 ＋ “Backspace”)
.判断 (虚拟键码 ＝ 9)
    返回 (文本 ＋ “Tab”)
.判断 (虚拟键码 ＝ 27)
    返回 (文本 ＋ “Esc”)
.默认
    返回 (文本 ＋ “VK” ＋ 到文本 (虚拟键码))
.判断结束

.子程序 _刷新选择页摘要
.参数 状态说明, 文本型
.局部变量 列表索引, 整数型
.局部变量 组合索引, 整数型
.局部变量 D2D索引, 整数型
.局部变量 列表项数量, 整数型
.局部变量 年, 整数型
.局部变量 月, 整数型
.局部变量 日, 整数型
.局部变量 时, 整数型
.局部变量 分, 整数型
.局部变量 秒, 整数型
.局部变量 虚拟键码, 整数型
.局部变量 修饰键, 整数型
.局部变量 列表项字节集, 字节集
.局部变量 组合框字节集, 字节集
.局部变量 D2D文本字节集, 字节集
.局部变量 摘要字节集, 字节集

列表索引 ＝ 获取选中项索引 (列表框_分类)
组合索引 ＝ 获取组合框选中索引 (组合框_城市)
D2D索引 ＝ 获取D2D组合框选中索引 (D2D组合框_主题)
列表项数量 ＝ 获取列表项数量 (列表框_分类)
列表项字节集 ＝ _取列表框项文本字节集 (列表框_分类, 列表索引)
组合框字节集 ＝ _取组合框文本字节集 (组合框_城市)
D2D文本字节集 ＝ _取D2D组合框文本字节集 (D2D组合框_主题)
获取D2D日期时间选择器日期时间 (日期时间_计划,  年,  月,  日,  时,  分,  秒)
获取热键值 (热键控件_快捷,  虚拟键码,  修饰键)

.如果真 (标签_选择摘要 ≠ 0)
    摘要字节集 ＝ 编码_Ansi到Utf8 (“ListBox: 共 ” ＋ 到文本 (列表项数量) ＋ “ 项 / 当前=”)
    摘要字节集 ＝ 摘要字节集 ＋ 列表项字节集
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (“ / index=” ＋ 到文本 (列表索引))
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “ComboBox: index=” ＋ 到文本 (组合索引) ＋ “ / text=”)
    摘要字节集 ＝ 摘要字节集 ＋ 组合框字节集
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (“    D2D: index=” ＋ 到文本 (D2D索引) ＋ “ / text=”)
    摘要字节集 ＝ 摘要字节集 ＋ D2D文本字节集
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “DateTime: ” ＋ 到文本 (年) ＋ “-” ＋ 到文本 (月) ＋ “-” ＋ 到文本 (日) ＋ “ ” ＋ 到文本 (时) ＋ “:” ＋ 到文本 (分) ＋ “    HotKey: ” ＋ _热键文本 (虚拟键码, 修饰键))
    写标签_字节集 (标签_选择摘要, 摘要字节集)
.如果真结束

.如果真 (标签_选择状态 ≠ 0)
    写标签 (标签_选择状态, 状态说明)
.如果真结束
更新状态 (状态说明)

.子程序 _刷新显示页摘要
.参数 状态说明, 文本型
.局部变量 摘要字节集, 字节集
.局部变量 通知文本, 文本型

.如果真 (当前通知句柄 ＝ 0)
    通知文本 ＝ “无活动通知”
.否则
    通知文本 ＝ “handle=” ＋ 到文本 (当前通知句柄)
.如果真结束

.如果真 (标签_显示摘要 ≠ 0)
    摘要字节集 ＝ 编码_Ansi到Utf8 (“PictureBox: 模式=” ＋ _图片缩放模式文本 (当前图片缩放模式) ＋ “    透明度=” ＋ 到文本 (当前图片透明度百分比) ＋ “%” ＋ “    可见=” ＋ _逻辑文本 (图片框当前可见) ＋ “    启用=” ＋ _逻辑文本 (图片框当前启用))
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “背景: ” ＋ 当前图片背景名称 ＋ “ / 0x” ＋ _十六进制 (当前图片背景色) ＋ “    Tooltip: ” ＋ _Tooltip主题文本 (当前Tooltip主题) ＋ “ / ” ＋ _Tooltip位置文本 (当前Tooltip位置) ＋ “ / ” ＋ _Tooltip触发文本 (当前Tooltip触发方式))
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “Notification: ” ＋ 通知文本 ＋ “    主控 Tooltip=右侧交互卡”)
    写标签_字节集 (标签_显示摘要, 摘要字节集)
.如果真结束

.如果真 (标签_显示状态 ≠ 0)
    写标签 (标签_显示状态, 状态说明)
.如果真结束
更新状态 (状态说明)

.子程序 _刷新数据页摘要
.参数 状态说明, 文本型
.局部变量 行数, 整数型
.局部变量 选中行, 整数型
.局部变量 选中列, 整数型
.局部变量 读取列, 整数型
.局部变量 选中说明, 文本型
.局部变量 单元格字节集, 字节集
.局部变量 摘要字节集, 字节集

行数 ＝ 表格_获取行数 (表格句柄)
选中行 ＝ 表格_获取选中行 (表格句柄)
选中列 ＝ 表格_获取选中列 (表格句柄)
读取列 ＝ 选中列
.如果真 (读取列 ＜ 0)
    读取列 ＝ 1
.如果真结束
单元格字节集 ＝ _取表格单元格文本字节集 (选中行, 读取列)

.如果真 (选中行 ＜ 0)
    选中说明 ＝ “未选中”
.否则
    .如果真 (选中列 ＜ 0)
        选中说明 ＝ “row=” ＋ 到文本 (选中行) ＋ “ / 整行”
    .否则
        选中说明 ＝ “row=” ＋ 到文本 (选中行) ＋ “ / col=” ＋ 到文本 (选中列)
    .如果真结束
.如果真结束

.如果真 (标签_表格摘要 ≠ 0)
    摘要字节集 ＝ 编码_Ansi到Utf8 (“行数: ” ＋ 到文本 (行数) ＋ “    选择模式: ” ＋ _表格选择模式文本 (当前表格选择模式) ＋ “    冻结首列: ” ＋ _逻辑文本 (表格冻结首列已开启))
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “当前选中: ” ＋ 选中说明 ＋ “    值=”)
    摘要字节集 ＝ 摘要字节集 ＋ 单元格字节集
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “回调覆盖: 单元格点击 / 双击 / 值改变 / 列头点击 / 选择改变”)
    写标签_字节集 (标签_表格摘要, 摘要字节集)
.如果真结束

.如果真 (标签_表格顶部模式 ≠ 0)
    摘要字节集 ＝ 编码_Ansi到Utf8 (“当前模式：” ＋ _表格选择模式文本 (当前表格选择模式))
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “行数=” ＋ 到文本 (行数) ＋ “    冻结首列=” ＋ _逻辑文本 (表格冻结首列已开启))
    写标签_字节集 (标签_表格顶部模式, 摘要字节集)
.如果真结束

.如果真 (标签_表格顶部状态 ≠ 0)
    摘要字节集 ＝ 编码_Ansi到Utf8 (“当前选中：” ＋ 选中说明)
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “最近动作：” ＋ 状态说明)
    写标签_字节集 (标签_表格顶部状态, 摘要字节集)
.如果真结束

.如果真 (标签_表格顶部导出 ≠ 0)
    摘要字节集 ＝ 编码_Ansi到Utf8 (“导出状态：” ＋ 当前表格导出说明)
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “工具栏区已接通排序 / 写入 / 导出写回”)
    写标签_字节集 (标签_表格顶部导出, 摘要字节集)
.如果真结束

.如果真 (标签_表格单元格摘要 ≠ 0)
    摘要字节集 ＝ 编码_Ansi到Utf8 (“row=” ＋ 到文本 (选中行) ＋ “    col=” ＋ 到文本 (选中列))
    摘要字节集 ＝ 摘要字节集 ＋ 编码_Ansi到Utf8 (#换行符 ＋ “value=”)
    摘要字节集 ＝ 摘要字节集 ＋ 单元格字节集
    写标签_字节集 (标签_表格单元格摘要, 摘要字节集)
.如果真结束

.如果真 (标签_表格导出摘要 ≠ 0)
    写标签 (标签_表格导出摘要, 当前表格导出说明)
.如果真结束

.如果真 (标签_表格状态 ≠ 0)
    写标签 (标签_表格状态, 状态说明)
.如果真结束
更新状态 (状态说明)

.子程序 _设置窗口标题文本
.参数 标题文本, 文本型
.参数 状态说明, 文本型
.局部变量 标题字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 (标题文本)
设置窗口标题 (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集))
_刷新窗口属性读取 (状态说明)

.子程序 _设置窗口尺寸
.参数 宽度, 整数型
.参数 高度, 整数型
.参数 状态说明, 文本型
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 当前宽度, 整数型
.局部变量 当前高度, 整数型

.如果真 (主窗口句柄 ＝ 0)
    更新状态 (“窗口尺寸设置失败：主窗口句柄无效。”)
    返回 ()
.如果真结束

获取窗口位置 (主窗口句柄,  X,  Y,  当前宽度,  当前高度)
设置窗口位置 (主窗口句柄, X, Y, 宽度, 高度)
_刷新窗口属性读取 (状态说明)

.子程序 _恢复窗口位置
.参数 状态说明, 文本型
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型

.如果真 (主窗口句柄 ＝ 0)
    更新状态 (“窗口位置恢复失败：主窗口句柄无效。”)
    返回 ()
.如果真结束

获取窗口位置 (主窗口句柄,  X,  Y,  宽度,  高度)
设置窗口位置 (主窗口句柄, 40, 40, 宽度, 高度)
_刷新窗口属性读取 (状态说明)

.子程序 _移动窗口偏移
.参数 偏移X, 整数型
.参数 偏移Y, 整数型
.参数 状态说明, 文本型
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型

.如果真 (主窗口句柄 ＝ 0)
    更新状态 (“窗口位置移动失败：主窗口句柄无效。”)
    返回 ()
.如果真结束

获取窗口位置 (主窗口句柄,  X,  Y,  宽度,  高度)
设置窗口位置 (主窗口句柄, X ＋ 偏移X, Y ＋ 偏移Y, 宽度, 高度)
_刷新窗口属性读取 (状态说明)

.子程序 _设置窗口主题
.参数 暗色模式, 逻辑型
.参数 主题名称, 文本型
.参数 状态说明, 文本型

设置暗色模式 (暗色模式)
当前窗口主题名称 ＝ 主题名称
.如果真 (当前窗口背景名称 ＝ “主题默认背景”)
    当前窗口背景色 ＝ _读取当前主题色 (“background”)
    设置窗口背景色 (主窗口句柄, 当前窗口背景色)
.如果真结束
_刷新窗口属性读取 (状态说明)

.子程序 _设置窗口标题栏背景色
.参数 颜色, 整数型
.参数 状态说明, 文本型

设置窗口标题栏颜色 (主窗口句柄, 颜色)
_刷新窗口属性读取 (状态说明)

.子程序 _设置窗口标题栏文字色
.参数 颜色, 整数型
.参数 状态说明, 文本型

设置标题栏文字颜色 (主窗口句柄, 颜色)
_刷新窗口属性读取 (状态说明)

.子程序 _设置窗口客户区背景色
.参数 颜色, 整数型
.参数 背景名称, 文本型
.参数 状态说明, 文本型

当前窗口背景色 ＝ 颜色
当前窗口背景名称 ＝ 背景名称
设置窗口背景色 (主窗口句柄, 颜色)
_刷新窗口属性读取 (状态说明)

.子程序 设置页头
.参数 标题, 文本型
.参数 说明, 文本型

写标签 (标签_页头标题, 标题)
写标签 (标签_页头说明, 说明)

.子程序 更新状态
.参数 文本, 文本型

写标签 (标签_全局状态, 文本)

.子程序 写标签
.参数 标签句柄, 整数型
.参数 文本, 文本型
.局部变量 utf8字节集, 字节集

utf8字节集 ＝ 编码_Ansi到Utf8 (文本)
设置标签文本 (标签句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集))

.子程序 写标签_字节集
.参数 标签句柄, 整数型
.参数 utf8字节集, 字节集

设置标签文本 (标签句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集))

.子程序 _十六进制, 文本型
.参数 数值, 整数型

返回 (格式化文本 (“%08X”, 数值))

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
