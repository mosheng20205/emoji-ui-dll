.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_浏览器
.程序集变量 浏览器句柄, 整数型, , , 最近打开的浏览器窗口句柄
.程序集变量 浏览器句柄数组, 整数型, , "0", 存储多个浏览器窗口句柄（可重定义数组）
.程序集变量 浏览器数量, 整数型, , , 已打开的浏览器窗口数量
.程序集变量 地址栏句柄数组, 整数型, , "0", 存储每个浏览器窗口的地址栏句柄
.程序集变量 内容区句柄数组, 整数型, , "0", 存储每个浏览器窗口的内容区句柄
.程序集变量 按钮_首页_ID数组, 整数型, , "0", 存储每个浏览器窗口的首页按钮ID
.程序集变量 按钮_刷新_ID数组, 整数型, , "0", 存储每个浏览器窗口的刷新按钮ID
.程序集变量 按钮_后退_ID数组, 整数型, , "0", 存储每个浏览器窗口的后退按钮ID
.程序集变量 按钮_前进_ID数组, 整数型, , "0", 存储每个浏览器窗口的前进按钮ID

.子程序 窗口大小改变回调, , 公开
.参数 参数_窗口句柄, 整数型
.参数 新宽度, 整数型
.参数 新高度, 整数型
.局部变量 i, 整数型
.局部变量 工具栏高, 整数型
.局部变量 地址栏句柄, 整数型
.局部变量 内容区句柄, 整数型
.局部变量 browsr, 类_FBrowser_浏览器

工具栏高 ＝ 48

' 查找是哪个浏览器窗口
.变量循环首 (1, 浏览器数量, 1, i)
    .如果真 (浏览器句柄数组 [i] ＝ 参数_窗口句柄)
        地址栏句柄 ＝ 地址栏句柄数组 [i]
        内容区句柄 ＝ 内容区句柄数组 [i]

        ' 调整地址栏宽度（保持左边距180，右边距20）
        .如果真 (地址栏句柄 ≠ 0)
            设置编辑框位置 (地址栏句柄, 180, 8, 新宽度 － 180 － 20, 32)
        .如果真结束

        ' 调整内容区大小（占据工具栏下方的所有空间）
        .如果真 (内容区句柄 ≠ 0)
            设置标签位置 (内容区句柄, 0, 工具栏高, 新宽度, 新高度 － 工具栏高)
            browsr ＝ FBrowser_浏览器_通过用户标识取浏览器 (到文本 (内容区句柄))
            .如果真 (browsr.是否为空 () ＝ 假 且 browsr.是否已关闭 () ＝ 假)
                browsr.移动窗口 (0, 0, 新宽度, 新高度 － 工具栏高, 真)
            .如果真结束

        .如果真结束

        调试输出 (取程序名称 (1), 参数_窗口句柄, “窗口大小改变: ” ＋ 到文本 (新宽度) ＋ “ x ” ＋ 到文本 (新高度) ＋ “ - 已调整控件”)
        返回 ()
    .如果真结束

.变量循环尾 ()

.子程序 浏览器按钮点击处理, , 公开, 处理浏览器窗口的按钮点击事件
.参数 按钮ID, 整数型
.局部变量 i, 整数型
.局部变量 _标题, 字节集
.局部变量 _消息, 字节集
.局部变量 _图标, 字节集

' 查找是哪个浏览器窗口的按钮
.变量循环首 (1, 浏览器数量, 1, i)
    .判断开始 (按钮ID ＝ 按钮_首页_ID数组 [i])
        ' 首页按钮
        _标题 ＝ { 233, 166, 150, 233, 161, 181 }  ' "首页"
        _消息 ＝ { 232, 191, 148, 229, 155, 158, 233, 166, 150, 233, 161, 181, 239, 188, 129, 240, 159, 143, 160 }  ' "返回首页！??"
        _图标 ＝ { 226, 156, 133 }  ' "?"
        信息提示框_ (浏览器句柄数组 [i], 取变量数据地址 (_标题), 取字节集长度 (_标题), 取变量数据地址 (_消息), 取字节集长度 (_消息), 取变量数据地址 (_图标), 取字节集长度 (_图标))
        返回 ()

    .判断 (按钮ID ＝ 按钮_刷新_ID数组 [i])
        ' 刷新按钮
        _标题 ＝ { 229, 136, 183, 230, 150, 176 }  ' "刷新"
        _消息 ＝ { 230, 173, 163, 229, 156, 168, 229, 136, 183, 230, 150, 176, 233, 161, 181, 233, 157, 162, 46, 46, 46, 240, 159, 148, 132 }  ' "正在刷新页面...??"
        _图标 ＝ { 226, 156, 133 }  ' "?"
        信息提示框_ (浏览器句柄数组 [i], 取变量数据地址 (_标题), 取字节集长度 (_标题), 取变量数据地址 (_消息), 取字节集长度 (_消息), 取变量数据地址 (_图标), 取字节集长度 (_图标))
        返回 ()

    .判断 (按钮ID ＝ 按钮_后退_ID数组 [i])
        ' 后退按钮
        _标题 ＝ { 229, 144, 142, 233, 128, 128 }  ' "后退"
        _消息 ＝ { 232, 191, 148, 229, 155, 158, 228, 184, 138, 228, 184, 128, 233, 161, 181, 226, 172, 133 }  ' "返回上一页?"
        _图标 ＝ { 226, 156, 133 }  ' "?"
        信息提示框_ (浏览器句柄数组 [i], 取变量数据地址 (_标题), 取字节集长度 (_标题), 取变量数据地址 (_消息), 取字节集长度 (_消息), 取变量数据地址 (_图标), 取字节集长度 (_图标))
        返回 ()

    .判断 (按钮ID ＝ 按钮_前进_ID数组 [i])
        ' 前进按钮
        _标题 ＝ { 229, 137, 141, 232, 191, 155 }  ' "前进"
        _消息 ＝ { 229, 137, 141, 232, 191, 155, 228, 184, 139, 228, 184, 128, 233, 161, 181, 226, 158, 161 }  ' "前进下一页?"
        _图标 ＝ { 226, 156, 133 }  ' "?"
        信息提示框_ (浏览器句柄数组 [i], 取变量数据地址 (_标题), 取字节集长度 (_标题), 取变量数据地址 (_消息), 取字节集长度 (_消息), 取变量数据地址 (_图标), 取字节集长度 (_图标))
        返回 ()

    .默认

    .判断结束

.变量循环尾 ()

.子程序 _按钮_打开浏览器_被单击, , , 打开浏览器
.局部变量 窗口句柄, 整数型
.局部变量 工具栏高, 整数型
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 地址栏文本, 字节集
.局部变量 字体名, 字节集
.局部变量 空文本, 字节集
.局部变量 地址栏句柄, 整数型
.局部变量 内容区句柄, 整数型
.局部变量 内容区宽度, 整数型
.局部变量 内容区高度, 整数型
.局部变量 按钮_首页_ID, 整数型
.局部变量 按钮_刷新_ID, 整数型
.局部变量 按钮_后退_ID, 整数型
.局部变量 按钮_前进_ID, 整数型
.局部变量 url, 文本型
.局部变量 同步类, 类_FBrowser_同步辅助类
.局部变量 字典, 类_FBrowser_字典值

工具栏高 ＝ 48
窗口句柄 ＝ 创建Emoji窗口 (“浏览器”, 900, 600)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ “\icon.ico”)
调试输出 (取程序名称 (1), “窗口句柄: ”, 窗口句柄)

.如果真 (窗口句柄 ＝ 0)
    返回 ()
.如果真结束

设置窗口关闭回调 (&浏览器窗口关闭回调)
设置窗口大小改变回调 (&窗口大小改变回调)
设置按钮点击回调 (&浏览器按钮点击处理)
' 工具栏左侧：四个导航按钮（首页、刷新、后退、前进）
emoji字节集 ＝ { 240, 159, 143, 160 }  ' "??" 首页
文本字节集 ＝ {  }  ' 空
按钮_首页_ID ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 10, 8, 36, 36, 4294967295)

emoji字节集 ＝ { 240, 159, 148, 132 }  ' "??" 刷新
按钮_刷新_ID ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 52, 8, 36, 36, 4294967295)

emoji字节集 ＝ { 226, 172, 133 }  ' "?" 后退
按钮_后退_ID ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 94, 8, 36, 36, 4294967295)

emoji字节集 ＝ { 226, 158, 161 }  ' "?" 前进
按钮_前进_ID ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 136, 8, 36, 36, 4294967295)

' 地址栏（编辑框，内容 www.example.com）
url ＝ “www.baidu.com”
地址栏文本 ＝ 编码_Ansi到Utf8 (url)
字体名 ＝ { 229, 190, 174, 232, 189, 175, 233, 155, 133, 233, 187, 145 }  ' "微软雅黑"
地址栏句柄 ＝ 创建编辑框 (窗口句柄, 180, 8, 700, 32, 取变量数据地址 (地址栏文本), 取字节集长度 (地址栏文本), 4278190080, 4294967295, 取变量数据地址 (字体名), 取字节集长度 (字体名), 12, 假, 假, 假, 0, 假, 假, 假, 真, 真)

' 内容区（大标签，浅灰背景，无文字）
空文本 ＝ {  }
内容区宽度 ＝ 900
内容区高度 ＝ 600 － 工具栏高
内容区句柄 ＝ 创建标签 (窗口句柄, 0, 工具栏高, 内容区宽度, 内容区高度, 取变量数据地址 (空文本), 0, 4210752255, 4293782485, 取变量数据地址 (字体名), 取字节集长度 (字体名), 12, 假, 假, 假, 0)

' 记录句柄供关闭所有使用和窗口大小改变使用
浏览器数量 ＝ 浏览器数量 ＋ 1

加入成员 (浏览器句柄数组, 窗口句柄)
加入成员 (地址栏句柄数组, 地址栏句柄)
加入成员 (内容区句柄数组, 内容区句柄)
加入成员 (按钮_首页_ID数组, 按钮_首页_ID)
加入成员 (按钮_刷新_ID数组, 按钮_刷新_ID)
加入成员 (按钮_后退_ID数组, 按钮_后退_ID)
加入成员 (按钮_前进_ID数组, 按钮_前进_ID)

调试输出 (取程序名称 (1), “浏览器句柄数组”, 浏览器句柄数组)
同步类.创建 ()
字典.创建 ()
字典.置整数值 (“浏览器数量”, 浏览器数量)
字典.置整数值 (“窗口句柄”, 内容区句柄)
字典.置整数值 (“内容区宽度”, 内容区宽度)
字典.置整数值 (“内容区高度”, 内容区高度)
字典.置文本值 (“url”, url)
同步类.置字典值 (字典)

FBrowser_任务运行器_投递任务 (#线程ID_UI, 到整数 (&同步创建浏览器), 同步类.取指针 ())

' 不在此处调用 运行消息循环，由易语言主窗口消息循环统一派发，以便可点击「关闭所有浏览器」

.子程序 同步创建浏览器
.参数 flag, 整数型
.局部变量 窗口信息, FBrowser_窗口信息
.局部变量 browser, 类_FBrowser_浏览器
.局部变量 请求环境, 类_FBrowser_请求环境
.局部变量 请求环境配置, FBrowser_请求环境配置
.局部变量 同步类, 类_FBrowser_同步辅助类
.局部变量 字典, 类_FBrowser_字典值

同步类.绑定 (flag)
字典 ＝ 同步类.取字典值 ()
同步类.销毁 ()

窗口信息.父窗口句柄 ＝ 字典.取整数值 (“窗口句柄”)
窗口信息.横坐标 ＝ 0
窗口信息.纵坐标 ＝ 0
窗口信息.宽度 ＝ 字典.取整数值 (“内容区宽度”)
窗口信息.高度 ＝ 字典.取整数值 (“内容区高度”)

请求环境配置.保留session ＝ 真
请求环境配置.缓存目录 ＝ 取运行目录 () ＋ “\CacheData\” ＋ 到文本 (字典.取整数值 (“浏览器数量”))
请求环境.创建 (请求环境配置)
browser ＝ FBrowser_创建浏览器_同步 (“”, 窗口信息, , 请求环境, , , 到文本 (字典.取整数值 (“窗口句柄”)))

browser.取主框架 ().载入地址 (字典.取文本值 (“url”))
' browser.取主框架 ().执行JS代码 (“alert('hello fbrowser')”, , )
字典.置空 ()

.子程序 浏览器窗口关闭回调, , 公开
.参数 已关闭的窗口句柄, 整数型
.局部变量 i, 整数型
.局部变量 j, 整数型

.如果真 (浏览器数量 ≤ 0)
    返回 ()
.如果真结束
.变量循环首 (1, 浏览器数量, 1, i)
    .如果真 (浏览器句柄数组 [i] ＝ 已关闭的窗口句柄)
        ' 将后面的元素前移（包括所有相关数组）
        .变量循环首 (i, 浏览器数量 － 1, 1, j)
            浏览器句柄数组 [j] ＝ 浏览器句柄数组 [j ＋ 1]
            地址栏句柄数组 [j] ＝ 地址栏句柄数组 [j ＋ 1]
            内容区句柄数组 [j] ＝ 内容区句柄数组 [j ＋ 1]
            按钮_首页_ID数组 [j] ＝ 按钮_首页_ID数组 [j ＋ 1]
            按钮_刷新_ID数组 [j] ＝ 按钮_刷新_ID数组 [j ＋ 1]
            按钮_后退_ID数组 [j] ＝ 按钮_后退_ID数组 [j ＋ 1]
            按钮_前进_ID数组 [j] ＝ 按钮_前进_ID数组 [j ＋ 1]
        .变量循环尾 ()
        浏览器数量 ＝ 浏览器数量 － 1
        .如果真 (浏览器句柄 ＝ 已关闭的窗口句柄)
            浏览器句柄 ＝ 0
        .如果真结束
        返回 ()
    .如果真结束

.变量循环尾 ()

.子程序 _按钮_关闭所有浏览器_被单击, , , 关闭所有浏览器
.局部变量 i, 整数型
.局部变量 当前数量, 整数型

.如果真 (浏览器数量 ≤ 0)
    返回 ()
.如果真结束

' 保存当前数量，因为关闭过程中浏览器数量会变化
当前数量 ＝ 浏览器数量
调试输出 (取程序名称 (1), 当前数量, 浏览器句柄数组)
.计次循环首 (当前数量, i)
    PostMessage (浏览器句柄数组 [i], #WM_CLOSE, 0, 0)
.计次循环尾 ()

' 注意：不要在这里清空浏览器数量和句柄
' 让 浏览器窗口关闭回调 自然处理数组清理

.子程序 更新地址栏文本, , 公开, 通过浏览器窗口句柄更新地址栏文本
.参数 参数_窗口句柄, 整数型, , 浏览器窗口句柄
.参数 新地址, 文本型, , 新的URL地址
.局部变量 i, 整数型
.局部变量 地址栏句柄, 整数型
.局部变量 地址文本字节集, 字节集

' 查找对应的浏览器窗口
.变量循环首 (1, 浏览器数量, 1, i)
    .如果真 (浏览器句柄数组 [i] ＝ 参数_窗口句柄)
        地址栏句柄 ＝ 地址栏句柄数组 [i]
        
        .如果真 (地址栏句柄 ≠ 0)
            ' 转换文本为UTF-8字节集
            地址文本字节集 ＝ 编码_Ansi到Utf8 (新地址)
            ' 更新编辑框文本
            设置编辑框文本 (地址栏句柄, 取变量数据地址 (地址文本字节集), 取字节集长度 (地址文本字节集))
            调试输出 (取程序名称 (1), "已更新地址栏: ", 新地址)
        .如果真结束
        
        返回 ()
    .如果真结束

.变量循环尾 ()

调试输出 (取程序名称 (1), "未找到窗口句柄: ", 参数_窗口句柄)

.子程序 更新窗口标题, , 公开, 通过浏览器窗口句柄更新窗口标题
.参数 参数_窗口句柄, 整数型, , 浏览器窗口句柄
.参数 新标题, 文本型, , 新的窗口标题
.局部变量 i, 整数型
.局部变量 标题字节集, 字节集

' 查找对应的浏览器窗口
.变量循环首 (1, 浏览器数量, 1, i)
    .如果真 (浏览器句柄数组 [i] ＝ 参数_窗口句柄)
        ' 转换文本为UTF-8字节集
        标题字节集 ＝ 编码_Ansi到Utf8 (新标题)
        ' 更新窗口标题
        设置窗口标题 (参数_窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集))
        调试输出 (取程序名称 (1), "已更新窗口标题: ", 新标题)
        返回 ()
    .如果真结束

.变量循环尾 ()

调试输出 (取程序名称 (1), "未找到窗口句柄: ", 参数_窗口句柄)

.子程序 _窗口_浏览器_将被销毁

可视 ＝ 假
FBrowser_关闭 (真)
