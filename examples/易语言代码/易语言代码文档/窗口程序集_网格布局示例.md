.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_网格布局示例
.程序集变量 窗口句柄, 整数型
.程序集变量 按钮, 整数型, , "12"
.程序集变量 标签1, 整数型

.子程序 _窗口_网格布局示例_创建完毕
.局部变量 i, 整数型
.局部变量 颜色列表, 整数型, , "6"

窗口句柄 ＝ 创建Emoji窗口 (“网格布局示例 - 3行4列”, 600, 500)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ “\icon.ico”)

' 设置3行4列网格布局，间距8像素
设置布局管理器 (窗口句柄, #LAYOUT_GRID, 3, 4, 8)
设置布局内边距 (窗口句柄, 15, 15, 15, 15)

' 准备颜色
颜色列表 [1] ＝ #COLOR_PRIMARY
颜色列表 [2] ＝ #COLOR_SUCCESS
颜色列表 [3] ＝ #COLOR_WARNING
颜色列表 [4] ＝ #COLOR_DANGER
颜色列表 [5] ＝ #COLOR_INFO
颜色列表 [6] ＝ #COLOR_PRIMARY

' 创建12个按钮并添加到网格布局
.计次循环首 (12, i)
    按钮 [i] ＝ 创建Emoji按钮_辅助 (窗口句柄, “”, “格子” ＋ 到文本 (i), 0, 0, 80, 30, 颜色列表 [(i － 1) ％ 6 ＋ 1])
    添加控件到布局 (窗口句柄, 按钮 [i])
    ' 设置所有按钮水平和垂直拉伸以填满单元格
    设置控件布局属性 (按钮 [i], 2, 2, 2, 2, #DOCK_NONE, 真, 真)
.计次循环尾 ()

' 立即应用布局
更新布局 (窗口句柄)

' 设置按钮点击回调
设置按钮点击回调 (&按钮点击回调)

运行消息循环 ()


.子程序 _启动窗口_将被销毁

销毁窗口 (窗口句柄)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型

调试输出 (“网格按钮被点击: ” ＋ 到文本 (按钮ID))
