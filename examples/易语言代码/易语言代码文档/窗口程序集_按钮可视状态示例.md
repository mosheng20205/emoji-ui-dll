.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 按钮1, 整数型
.程序集变量 按钮2, 整数型
.程序集变量 按钮3, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 窗口句柄, 整数型
.局部变量 标题字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集

' 创建窗口
标题字节集 ＝ { 230, 140, 137, 233, 146, 174, 229, 143, 175, 232, 167, 134, 231, 138, 182, 230, 128, 129, 230, 181, 139, 232, 175, 149, 32, 240, 159, 145, 129, 239, 184, 143 }
窗口句柄 ＝ 创建Emoji窗口_字节集 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 400, 400)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ “\icon.ico”)

' 创建测试按钮1 - 可见按钮
emoji字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
文本字节集 ＝ 到UTF8 (“可见按钮”)
按钮1 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 50, 50, 150, 40, 到ARGB (255, 76, 175, 80))

' 创建测试按钮2 - 隐藏按钮
emoji字节集 ＝ { 240, 159, 153, 136 }
文本字节集 ＝ 到UTF8 (“隐藏按钮”)
按钮2 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 50, 110, 150, 40, 到ARGB (255, 244, 67, 54))

' 创建控制按钮 - 切换按钮2的可视状态
emoji字节集 ＝ { 240, 159, 148, 132 }
文本字节集 ＝ 到UTF8 (“切换可视”)
按钮3 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 50, 170, 150, 40, 到ARGB (255, 33, 150, 243))

' 初始化：隐藏按钮2
显示隐藏按钮 (按钮2, 假)

' 设置按钮点击回调
设置按钮点击回调 (&按钮点击回调)

' 设置消息循环主窗口
设置消息循环主窗口 (窗口句柄)

' 运行消息循环
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , 按钮点击回调函数
.参数 按钮ID, 整数型
.参数 父窗口句柄, 整数型
.局部变量 可见状态, 逻辑型
.局部变量 启用状态, 逻辑型
.局部变量 信息, 文本型

.判断开始 (按钮ID ＝ 按钮1)  ' 按钮1
    ' 显示按钮1的状态信息
    可见状态 ＝ 获取按钮可视状态 (按钮ID)
    启用状态 ＝ 获取按钮启用状态 (按钮ID)
    信息 ＝ “按钮1状态：” ＋ #换行符 ＋ “可见：” ＋ 到文本 (可见状态) ＋ #换行符 ＋ “启用：” ＋ 到文本 (启用状态)
    信息框 (信息, 0, “按钮状态”, )

.判断 (按钮ID ＝ 按钮2)  ' 按钮2
    ' 这个按钮被隐藏时不应该响应点击
    信息框 (“按钮2被点击了！”, 0, “提示”, )

.判断 (按钮ID ＝ 按钮3)  ' 按钮3 - 切换按钮
    ' 切换按钮2的可视状态
    可见状态 ＝ 获取按钮可视状态 (1001)
    显示隐藏按钮 (1001, 取反 (可见状态))

    ' 显示当前状态
    可见状态 ＝ 获取按钮可视状态 (1001)
    .如果真 (可见状态)
        信息框 (“按钮2现在是可见的”, 0, “状态”, )
    .如果真结束
    .如果 (取反 (可见状态))
        信息框 (“按钮2现在是隐藏的”, 0, “状态”, )
    .否则

    .如果结束

.默认

.判断结束


.子程序 到UTF8, 字节集, 公开
.参数 文本, 文本型
.局部变量 宽字符, 字节集
.局部变量 宽字符长度, 整数型
.局部变量 UTF8字节集, 字节集
.局部变量 UTF8长度, 整数型

' 转换为宽字符
宽字符长度 ＝ MultiByteToWideChar (936, 0, 文本, 取文本长度 (文本), {  }, 0)
宽字符 ＝ 取空白字节集 (宽字符长度 × 2)
MultiByteToWideChar (936, 0, 文本, 取文本长度 (文本), 宽字符, 宽字符长度)

' 转换为UTF-8
UTF8长度 ＝ WideCharToMultiByte_整数 (65001, 0, 取变量数据地址 (宽字符), 宽字符长度, 0, 0, 0, 0)
UTF8字节集 ＝ 取空白字节集 (UTF8长度)
WideCharToMultiByte_整数 (65001, 0, 取变量数据地址 (宽字符), 宽字符长度, 取变量数据地址 (UTF8字节集), UTF8长度, 0, 0)

返回 (UTF8字节集)

.子程序 到ARGB, 整数型, 公开
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
