.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_DataGridView列管理4_0示例

.程序集变量 窗口句柄, 整数型
.程序集变量 表格句柄, 整数型
.程序集变量 状态标签, 整数型
.程序集变量 按钮_移除最后列, 整数型
.程序集变量 按钮_清空所有列, 整数型
.程序集变量 按钮_重建列, 整数型

.子程序 _启动子程序, 整数型
.局部变量 emoji字节集, 字节集
窗口句柄 ＝ 创建Emoji窗口 ("DataGridView列管理4.0示例", 80, 40, 1080, 760)
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 20, 1020, 560, 假, 真, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255))

重建表格列 ()

emoji字节集 ＝ { 226, 158, 150 }  ' ➖
按钮_移除最后列 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "移除最后列", 20, 600, 170, 44, 到ARGB (255, 245, 108, 108))
emoji字节集 ＝ { 240, 159, 167, 185 }  ' 🧹
按钮_清空所有列 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "清空所有列", 206, 600, 170, 44, 到ARGB (255, 230, 162, 60))
emoji字节集 ＝ { 240, 159, 148, 129 }  ' 🔁
按钮_重建列 ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "重建列", 392, 600, 150, 44, 到ARGB (255, 64, 158, 255))

状态标签 ＝ 创建标签_辅助 (窗口句柄, 20, 660, 1020, 56, "状态：当前列数=" ＋ 到文本 (表格_获取列数 (表格句柄)), 到ARGB (255, 48, 49, 51), 到ARGB (255, 244, 247, 250), "Microsoft YaHei UI", 11, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 列数, 整数型

.如果真 (按钮ID ＝ 按钮_移除最后列)
    列数 ＝ 表格_获取列数 (表格句柄)
    .如果 (列数 > 0)
        表格_移除列 (表格句柄, 列数 - 1)
        表格_刷新 (表格句柄)
        写状态 ("状态：已移除最后一列，当前列数=" ＋ 到文本 (表格_获取列数 (表格句柄)))
    .否则
        写状态 ("状态：当前没有可移除的列。")
    .如果结束
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_清空所有列)
    表格_清空所有列 (表格句柄)
    表格_刷新 (表格句柄)
    写状态 ("状态：已清空所有列。")
    返回 ()
.如果真结束

.如果真 (按钮ID ＝ 按钮_重建列)
    重建表格列 ()
    写状态 ("状态：已重建列与示例数据，当前列数=" ＋ 到文本 (表格_获取列数 (表格句柄)))
.如果真结束

.子程序 重建表格列
.局部变量 行索引, 整数型

表格_清空所有列 (表格句柄)
DG_添加文本列 (表格句柄, "编号", 100)
DG_添加文本列 (表格句柄, "姓名", 180)
DG_添加文本列 (表格句柄, "部门", 180)
DG_添加文本列 (表格句柄, "说明", 500)

行索引 ＝ 表格_添加行 (表格句柄)
DG_设置文本 (表格句柄, 行索引, 0, "1001")
DG_设置文本 (表格句柄, 行索引, 1, "张三")
DG_设置文本 (表格句柄, 行索引, 2, "研发")
DG_设置文本 (表格句柄, 行索引, 3, "列重建后会同步重建行结构。")

行索引 ＝ 表格_添加行 (表格句柄)
DG_设置文本 (表格句柄, 行索引, 0, "1002")
DG_设置文本 (表格句柄, 行索引, 1, "李四")
DG_设置文本 (表格句柄, 行索引, 2, "测试")
DG_设置文本 (表格句柄, 行索引, 3, "表格_清空所有列 会同时清空现有行。")

表格_设置冻结首行 (表格句柄, 真)
表格_刷新 (表格句柄)

.子程序 DG_添加文本列, 整数型
.参数 表格, 整数型
.参数 标题, 文本型
.参数 宽度, 整数型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (标题)
返回 (表格_添加文本列 (表格, 取变量数据地址 (字节集), 取字节集长度 (字节集), 宽度))

.子程序 DG_设置文本
.参数 表格, 整数型
.参数 行, 整数型
.参数 列, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
字节集 ＝ 编码_Ansi到Utf8 (文本)
表格_设置单元格文本 (表格, 行, 列, 取变量数据地址 (字节集), 取字节集长度 (字节集))

.子程序 写状态
.参数 文本, 文本型
设置标签文本_UTF8 (状态标签, 文本)
