.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 日期时间句柄, 整数型
.程序集变量 标签_当前值, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 字体名, 字节集
.局部变量 说明, 字节集

主窗口句柄 ＝ 创建Emoji窗口 (“D2D日期时间选择器示例”, -1, -1, 720, 520)
.如果真 (主窗口句柄 ＝ 0)
    信息框 (“创建主窗口失败”, 0, , )
    返回 (0)
.如果真结束

字体名 ＝ 编码_Ansi到Utf8 (“Microsoft YaHei UI”)

' 颜色：UINT32 为 ARGB。易语言里不能写 #FF303133（# 后会被当成「常量名」），请用 到ARGB (255, R, G, B) 或十进制整数。
' 初始精度 1＝年月日；控件高建议 32～40
日期时间句柄 ＝ 创建D2D日期时间选择器 (主窗口句柄, 24, 56, 340, 36, 1, 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 到ARGB (255, 220, 223, 230), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假)
.如果真 (日期时间句柄 ＝ 0)
    信息框 (“创建日期时间选择器失败”, 0, , )
    返回 (0)
.如果真结束

设置D2D日期时间选择器回调 (日期时间句柄, &回调_日期时间值改变)

' 演示：程序设置日期（仍受当前精度影响，年月日精度下时分秒会被规范）
设置D2D日期时间选择器日期时间 (日期时间句柄, 2024, 6, 15, 14, 30, 45)

说明 ＝ 编码_Ansi到Utf8 (“操作：点击输入区或右侧箭头打开面板；年月日精度点选日期即关闭。含时分秒时需调时间后点确定。” ＋ #换行符 ＋ “下方标签在值改变回调中刷新（含程序设置）。”)
标签_当前值 ＝ 创建标签 (主窗口句柄, 24, 108, 660, 120, 取变量数据地址 (说明), 取字节集长度 (说明), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体名), 取字节集长度 (字体名), 12, 假, 假, 假, 0, 真)

设置D2D日期时间选择器精度 (日期时间句柄, 4)
调用子程序 (&_刷新标签显示, , , )


' ===== 可选：演示切换精度为「到秒」，再读回 =====

' 设置D2D日期时间选择器日期时间 (日期时间句柄, 2025, 3, 26, 9, 0, 0)
' 调用子程序 _刷新标签显示 ()

设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 _刷新标签显示
.局部变量 年, 整数型
.局部变量 月, 整数型
.局部变量 日, 整数型
.局部变量 时, 整数型
.局部变量 分, 整数型
.局部变量 秒, 整数型
.局部变量 精度, 整数型
.局部变量 行, 文本型
.局部变量 文本u8, 字节集

精度 ＝ 获取D2D日期时间选择器精度 (日期时间句柄)
获取D2D日期时间选择器日期时间 (日期时间句柄, 取变量数据地址 (年), 取变量数据地址 (月), 取变量数据地址 (日), 取变量数据地址 (时), 取变量数据地址 (分), 取变量数据地址 (秒))

行 ＝ “当前精度：” ＋ 到文本 (精度) ＋ “（0年 1年月日 2到时 3到分 4到秒）” ＋ #换行符
行 ＝ 行 ＋ “年:” ＋ 到文本 (年) ＋ “ 月:” ＋ 到文本 (月) ＋ “ 日:” ＋ 到文本 (日) ＋ #换行符
行 ＝ 行 ＋ “时:” ＋ 到文本 (时) ＋ “ 分:” ＋ 到文本 (分) ＋ “ 秒:” ＋ 到文本 (秒)

文本u8 ＝ 编码_Ansi到Utf8 (行)
设置标签文本 (标签_当前值, 取变量数据地址 (文本u8), 取字节集长度 (文本u8))

.子程序 回调_日期时间值改变, , , stdcall
.参数 控件句柄, 整数型

' 值改变时刷新（与程序调用 设置日期时间/设置精度 触发的回调相同）
调用子程序 (&_刷新标签显示, , , )
