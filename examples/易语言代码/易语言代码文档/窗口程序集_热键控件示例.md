.版本 2
.支持库 spec

.程序集 窗口程序集_热键控件示例
.程序集变量 主窗口句柄, 整数型
.程序集变量 热键控件1, 整数型
.程序集变量 热键控件2, 整数型
.程序集变量 热键控件3, 整数型
.程序集变量 标签_热键1, 整数型
.程序集变量 标签_热键2, 整数型
.程序集变量 标签_热键3, 整数型
.程序集变量 按钮_获取热键, 整数型
.程序集变量 按钮_设置热键, 整数型
.程序集变量 按钮_清除热键, 整数型
.程序集变量 按钮_启用禁用, 整数型
.程序集变量 热键已启用, 逻辑型

.子程序 _启动窗口_创建完毕

主窗口句柄 ＝ 创建Emoji窗口 ("热键控件示例", 800, 650)
热键已启用 ＝ 真

' 创建标题标签
创建标签_辅助 (主窗口句柄, 20, 20, 760, 40, "热键控件示例", COLOR_TEXT_PRIMARY, COLOR_TRANSPARENT, "Microsoft YaHei UI", 18, 真, 假, 假, 1, 假)

' 创建热键控件1
创建标签_辅助 (主窗口句柄, 20, 80, 200, 30, "热键1（通用快捷键）：", COLOR_TEXT_PRIMARY, COLOR_TRANSPARENT, "", 12, 假, 假, 假, 0, 假)
热键控件1 ＝ 创建热键控件_辅助 (主窗口句柄, 20, 110, 300, 35, COLOR_TEXT_PRIMARY, COLOR_BG_WHITE)
设置热键回调 (热键控件1, 到整数 (&热键1_改变回调))
标签_热键1 ＝ 创建标签_辅助 (主窗口句柄, 330, 110, 450, 35, "当前热键：无", COLOR_TEXT_SECONDARY, COLOR_BG_BASE, "", 12, 假, 假, 假, 0, 假)

' 创建热键控件2
创建标签_辅助 (主窗口句柄, 20, 170, 200, 30, "热键2（功能键）：", COLOR_TEXT_PRIMARY, COLOR_TRANSPARENT, "", 12, 假, 假, 假, 0, 假)
热键控件2 ＝ 创建热键控件_辅助 (主窗口句柄, 20, 200, 300, 35, COLOR_TEXT_PRIMARY, COLOR_BG_WHITE)
设置热键回调 (热键控件2, 到整数 (&热键2_改变回调))
标签_热键2 ＝ 创建标签_辅助 (主窗口句柄, 330, 200, 450, 35, "当前热键：无", COLOR_TEXT_SECONDARY, COLOR_BG_BASE, "", 12, 假, 假, 假, 0, 假)

' 创建热键控件3
创建标签_辅助 (主窗口句柄, 20, 260, 200, 30, "热键3（字母键）：", COLOR_TEXT_PRIMARY, COLOR_TRANSPARENT, "", 12, 假, 假, 假, 0, 假)
热键控件3 ＝ 创建热键控件_辅助 (主窗口句柄, 20, 290, 300, 35, COLOR_TEXT_PRIMARY, COLOR_BG_WHITE)
设置热键回调 (热键控件3, 到整数 (&热键3_改变回调))
标签_热键3 ＝ 创建标签_辅助 (主窗口句柄, 330, 290, 450, 35, "当前热键：无", COLOR_TEXT_SECONDARY, COLOR_BG_BASE, "", 12, 假, 假, 假, 0, 假)

' 创建说明标签
创建标签_辅助 (主窗口句柄, 20, 350, 760, 80, "使用说明：" ＋ #换行符 ＋ "1. 点击热键控件，然后按下键盘上的任意键组合" ＋ #换行符 ＋ "2. 支持 Ctrl、Shift、Alt 修饰键" ＋ #换行符 ＋ "3. 支持字母键、数字键、功能键等", COLOR_TEXT_SECONDARY, COLOR_BG_LIGHT, "", 12, 假, 假, 假, 0, 真)

' 创建操作按钮
按钮_获取热键 ＝ 创建Emoji按钮_辅助 (主窗口句柄, "📋", "获取热键1", 20, 450, 150, 40, COLOR_PRIMARY)
按钮_设置热键 ＝ 创建Emoji按钮_辅助 (主窗口句柄, "✏️", "设置Ctrl+S", 180, 450, 150, 40, COLOR_SUCCESS)
按钮_清除热键 ＝ 创建Emoji按钮_辅助 (主窗口句柄, "🗑️", "清除热键1", 340, 450, 150, 40, COLOR_WARNING)
按钮_启用禁用 ＝ 创建Emoji按钮_辅助 (主窗口句柄, "🔒", "禁用热键1", 500, 450, 150, 40, COLOR_DANGER)

' 创建预设热键示例
创建标签_辅助 (主窗口句柄, 20, 510, 760, 30, "预设热键示例：", COLOR_TEXT_PRIMARY, COLOR_TRANSPARENT, "", 12, 真, 假, 假, 0, 假)
创建标签_辅助 (主窗口句柄, 20, 545, 760, 60, "• Ctrl+S (保存)" ＋ #换行符 ＋ "• Ctrl+Shift+P (命令面板)" ＋ #换行符 ＋ "• F5 (刷新)", COLOR_TEXT_SECONDARY, COLOR_BG_BASE, "", 11, 假, 假, 假, 0, 真)

' 设置按钮回调
设置按钮点击回调 (到整数 (&按钮点击回调))

运行消息循环 ()

.子程序 热键1_改变回调, , , stdcall
.参数 热键句柄, 整数型
.参数 虚拟键码, 整数型
.参数 修饰键, 整数型
.局部变量 热键文本, 文本型
.局部变量 Ctrl, 逻辑型
.局部变量 Shift, 逻辑型
.局部变量 Alt, 逻辑型
.局部变量 剩余, 整数型

Ctrl ＝ 假
Shift ＝ 假
Alt ＝ 假
剩余 ＝ 修饰键

.如果真 (剩余 ≥ 4)
    Alt ＝ 真
    剩余 ＝ 剩余 － 4
.如果真结束
.如果真 (剩余 ≥ 2)
    Shift ＝ 真
    剩余 ＝ 剩余 － 2
.如果真结束
.如果真 (剩余 ≥ 1)
    Ctrl ＝ 真
.如果真结束

热键文本 ＝ 获取热键文本 (虚拟键码, Ctrl, Shift, Alt)
设置标签文本_辅助 (标签_热键1, "当前热键：" ＋ 热键文本)

.子程序 热键2_改变回调, , , stdcall
.参数 热键句柄, 整数型
.参数 虚拟键码, 整数型
.参数 修饰键, 整数型
.局部变量 热键文本, 文本型
.局部变量 Ctrl, 逻辑型
.局部变量 Shift, 逻辑型
.局部变量 Alt, 逻辑型
.局部变量 剩余, 整数型

Ctrl ＝ 假
Shift ＝ 假
Alt ＝ 假
剩余 ＝ 修饰键

.如果真 (剩余 ≥ 4)
    Alt ＝ 真
    剩余 ＝ 剩余 － 4
.如果真结束
.如果真 (剩余 ≥ 2)
    Shift ＝ 真
    剩余 ＝ 剩余 － 2
.如果真结束
.如果真 (剩余 ≥ 1)
    Ctrl ＝ 真
.如果真结束

热键文本 ＝ 获取热键文本 (虚拟键码, Ctrl, Shift, Alt)
设置标签文本_辅助 (标签_热键2, "当前热键：" ＋ 热键文本)

.子程序 热键3_改变回调, , , stdcall
.参数 热键句柄, 整数型
.参数 虚拟键码, 整数型
.参数 修饰键, 整数型
.局部变量 热键文本, 文本型
.局部变量 Ctrl, 逻辑型
.局部变量 Shift, 逻辑型
.局部变量 Alt, 逻辑型
.局部变量 剩余, 整数型

Ctrl ＝ 假
Shift ＝ 假
Alt ＝ 假
剩余 ＝ 修饰键

.如果真 (剩余 ≥ 4)
    Alt ＝ 真
    剩余 ＝ 剩余 － 4
.如果真结束
.如果真 (剩余 ≥ 2)
    Shift ＝ 真
    剩余 ＝ 剩余 － 2
.如果真结束
.如果真 (剩余 ≥ 1)
    Ctrl ＝ 真
.如果真结束

热键文本 ＝ 获取热键文本 (虚拟键码, Ctrl, Shift, Alt)
设置标签文本_辅助 (标签_热键3, "当前热键：" ＋ 热键文本)

.子程序 获取热键文本, 文本型
.参数 虚拟键码, 整数型
.参数 Ctrl, 逻辑型
.参数 Shift, 逻辑型
.参数 Alt, 逻辑型
.局部变量 文本, 文本型
.局部变量 键名, 文本型

.如果真 (虚拟键码 ＝ 0)
    返回 ("无")
.如果真结束

文本 ＝ ""

' 添加修饰键
.如果真 (Ctrl)
    文本 ＝ 文本 ＋ "Ctrl+"
.如果真结束
.如果真 (Shift)
    文本 ＝ 文本 ＋ "Shift+"
.如果真结束
.如果真 (Alt)
    文本 ＝ 文本 ＋ "Alt+"
.如果真结束

' 获取键名
键名 ＝ 获取键名称 (虚拟键码)
文本 ＝ 文本 ＋ 键名

返回 (文本)

.子程序 获取键名称, 文本型
.参数 虚拟键码, 整数型

' 字母键
.如果真 (虚拟键码 ≥ VK_A 且 虚拟键码 ≤ VK_Z)
    返回 (字符 (虚拟键码))
.如果真结束

' 数字键
.如果真 (虚拟键码 ≥ VK_0 且 虚拟键码 ≤ VK_9)
    返回 (字符 (虚拟键码))
.如果真结束

' 功能键
.如果真 (虚拟键码 ≥ VK_F1 且 虚拟键码 ≤ VK_F12)
    返回 ("F" ＋ 到文本 (虚拟键码 － VK_F1 ＋ 1))
.如果真结束

' 特殊键
.判断开始 (虚拟键码 ＝ VK_SPACE)
    返回 ("Space")
.判断 (虚拟键码 ＝ VK_RETURN)
    返回 ("Enter")
.判断 (虚拟键码 ＝ VK_BACK)
    返回 ("Backspace")
.判断 (虚拟键码 ＝ VK_TAB)
    返回 ("Tab")
.判断 (虚拟键码 ＝ VK_ESCAPE)
    返回 ("Esc")
.判断 (虚拟键码 ＝ VK_DELETE)
    返回 ("Delete")
.判断 (虚拟键码 ＝ VK_INSERT)
    返回 ("Insert")
.判断 (虚拟键码 ＝ VK_HOME)
    返回 ("Home")
.判断 (虚拟键码 ＝ VK_END)
    返回 ("End")
.判断 (虚拟键码 ＝ VK_PRIOR)
    返回 ("PageUp")
.判断 (虚拟键码 ＝ VK_NEXT)
    返回 ("PageDown")
.判断 (虚拟键码 ＝ VK_LEFT)
    返回 ("Left")
.判断 (虚拟键码 ＝ VK_RIGHT)
    返回 ("Right")
.判断 (虚拟键码 ＝ VK_UP)
    返回 ("Up")
.判断 (虚拟键码 ＝ VK_DOWN)
    返回 ("Down")
.默认
    返回 ("Key" ＋ 到文本 (虚拟键码))
.判断结束

.子程序 按钮点击回调, , , stdcall
.参数 按钮句柄, 整数型
.局部变量 虚拟键码, 整数型
.局部变量 Ctrl, 整数型
.局部变量 Shift, 整数型
.局部变量 Alt, 整数型
.局部变量 热键文本, 文本型

.判断开始 (按钮句柄 ＝ 按钮_获取热键)
    .局部变量 修饰键, 整数型
    .局部变量 剩余, 整数型
    获取热键值 (热键控件1, 传址 虚拟键码, 传址 修饰键)
    Ctrl ＝ 0
    Shift ＝ 0
    Alt ＝ 0
    剩余 ＝ 修饰键
    .如果真 (剩余 ≥ 4)
        Alt ＝ 1
        剩余 ＝ 剩余 － 4
    .如果真结束
    .如果真 (剩余 ≥ 2)
        Shift ＝ 1
        剩余 ＝ 剩余 － 2
    .如果真结束
    .如果真 (剩余 ≥ 1)
        Ctrl ＝ 1
    .如果真结束
    热键文本 ＝ 获取热键文本 (虚拟键码, Ctrl ≠ 0, Shift ≠ 0, Alt ≠ 0)
    信息框 ("热键1当前值：" ＋ #换行符 ＋ 热键文本 ＋ #换行符 ＋ #换行符 ＋ "虚拟键码：" ＋ 到文本 (虚拟键码) ＋ #换行符 ＋ "Ctrl：" ＋ 到文本 (Ctrl) ＋ #换行符 ＋ "Shift：" ＋ 到文本 (Shift) ＋ #换行符 ＋ "Alt：" ＋ 到文本 (Alt), 0, "获取热键")
.判断 (按钮句柄 ＝ 按钮_设置热键)
    ' 设置为 Ctrl+S
    设置热键值 (热键控件1, VK_S, 1)
    信息框 ("已设置热键1为 Ctrl+S", 0, "设置热键")
.判断 (按钮句柄 ＝ 按钮_清除热键)
    清除热键 (热键控件1)
    设置标签文本_辅助 (标签_热键1, "当前热键：无")
    信息框 ("已清除热键1", 0, "清除热键")
.判断 (按钮句柄 ＝ 按钮_启用禁用)
    热键已启用 ＝ 取反 (热键已启用)
    启用热键控件 (热键控件1, 热键已启用)
    .如果真 (热键已启用)
        设置标签文本_辅助 (按钮_启用禁用, "🔒 禁用热键1")
        信息框 ("已启用热键1", 0, "启用/禁用")
    .否则
        设置标签文本_辅助 (按钮_启用禁用, "🔓 启用热键1")
        信息框 ("已禁用热键1", 0, "启用/禁用")
    .如果真结束
.默认

.判断结束
