.版本 2
.支持库 spec
.支持库 internet

.程序集 窗口程序集_窗口_图片框示例
.程序集变量 窗口句柄, 整数型
.程序集变量 图片框1, 整数型
.程序集变量 图片框2, 整数型
.程序集变量 图片框3, 整数型
.程序集变量 图片框4, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 当前透明度, 整数型
.程序集变量 集_按钮_加载本地图片, 整数型
.程序集变量 集_按钮_清除图片, 整数型
.程序集变量 集_按钮_增加透明度, 整数型
.程序集变量 集_按钮_减少透明度, 整数型
.程序集变量 集_按钮_切换缩放模式, 整数型
.程序集变量 集_按钮_加载网络图片, 整数型

.子程序 _窗口_图片框示例_创建完毕
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集

' 创建自绘窗口
窗口句柄 ＝ 创建Emoji窗口 (“图片框4.0示例”, 800, 600)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ “\icon.ico”)
字体字节集 ＝ 编码_Ansi到Utf8 ("Microsoft YaHei UI")

' 创建标题标签
文本字节集 ＝ 编码_Ansi到Utf8 ("图片框控件示例 - 支持PNG、JPG、BMP、GIF")
创建标签 (窗口句柄, 20, 20, 760, 30, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_PRIMARY, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 16, 真, 假, 假, 1, 假)

' 创建图片框1 - 拉伸模式
图片框1 ＝ 创建图片框 (窗口句柄, 50, 80, 150, 150, #SCALE_STRETCH, #COLOR_BG_LIGHT)

' 创建图片框2 - 等比缩放模式
图片框2 ＝ 创建图片框 (窗口句柄, 220, 80, 150, 150, #SCALE_FIT, #COLOR_BG_LIGHT)

' 创建图片框3 - 居中模式
图片框3 ＝ 创建图片框 (窗口句柄, 390, 80, 150, 150, #SCALE_CENTER, #COLOR_BG_LIGHT)

' 创建图片框4 - 不缩放模式
图片框4 ＝ 创建图片框 (窗口句柄, 560, 80, 150, 150, #SCALE_NONE, #COLOR_BG_LIGHT)

' 创建说明标签
文本字节集 ＝ 编码_Ansi到Utf8 ("拉伸填充")
创建标签 (窗口句柄, 50, 240, 150, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_REGULAR, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 1, 假)
文本字节集 ＝ 编码_Ansi到Utf8 ("等比缩放")
创建标签 (窗口句柄, 220, 240, 150, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_REGULAR, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 1, 假)
文本字节集 ＝ 编码_Ansi到Utf8 ("居中显示")
创建标签 (窗口句柄, 390, 240, 150, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_REGULAR, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 1, 假)
文本字节集 ＝ 编码_Ansi到Utf8 ("不缩放")
创建标签 (窗口句柄, 560, 240, 150, 20, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_REGULAR, #COLOR_BG_WHITE, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 1, 假)

' 创建状态显示标签
文本字节集 ＝ 编码_Ansi到Utf8 ("状态：等待操作..." ＋ #换行符 ＋ "提示：请将图片文件放在examples文件夹中")
标签_状态 ＝ 创建标签 (窗口句柄, 50, 280, 700, 80, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), #COLOR_TEXT_REGULAR, #COLOR_BG_LIGHT, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 假)

' 创建功能按钮 - 使用字节集处理emoji
' ? 文i件夹 emoji
emoji字节集 ＝ { 240, 159, 147, 129 }
文本字节集 ＝ 编码_Ansi到Utf8 (“加载本地图片”)
集_按钮_加载本地图片 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 40, 380, 130, 40, #COLOR_PRIMARY)

emoji字节集 ＝ { 240, 159, 140, 144 }
文本字节集 ＝ 编码_Ansi到Utf8 (“加载网络图片”)
集_按钮_加载网络图片 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 40, 430, 130, 40, #COLOR_PRIMARY)

' ??? 垃圾桶 emoji
emoji字节集 ＝ { 240, 159, 151, 145, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 (“清除图片”)
集_按钮_清除图片 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 190, 380, 120, 40, #COLOR_DANGER)

' ? 加号 emoji
emoji字节集 ＝ { 226, 158, 149 }
文本字节集 ＝ 编码_Ansi到Utf8 (“增加透明度”)
集_按钮_增加透明度 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 330, 380, 120, 40, #COLOR_SUCCESS)

' ? 减号 emoji
emoji字节集 ＝ { 226, 158, 150 }
文本字节集 ＝ 编码_Ansi到Utf8 (“减少透明度”)
集_按钮_减少透明度 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 470, 380, 120, 40, #COLOR_WARNING)

' ?? 循环箭头 emoji
emoji字节集 ＝ { 240, 159, 148, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 (“切换缩放模式”)
集_按钮_切换缩放模式 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 610, 380, 140, 40, #COLOR_INFO)

' 初始化透明度
当前透明度 ＝ 100

' 设置按钮回调
设置按钮点击回调 (&按钮点击回调)

' 设置窗口关闭回调
设置窗口关闭回调 (&窗口关闭回调)

运行消息循环 ()


.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 状态文本, 文本型
.局部变量 文件路径, 文本型
.局部变量 结果, 逻辑型
.局部变量 成功emoji, 字节集
.局部变量 失败emoji, 字节集
.局部变量 状态字节集, 字节集
.局部变量 临时字节集, 字节集
.局部变量 路径字节集, 字节集
.局部变量 图片数据, 字节集
.局部变量 数据指针, 整数型

' ? 成功标记 emoji (UTF-8: E2 9C 85)
成功emoji ＝ { 226, 156, 133 }

' ? 失败标记 emoji (UTF-8: E2 9D 8C)
失败emoji ＝ { 226, 157, 140 }

.判断开始 (按钮ID ＝ 集_按钮_加载本地图片)
    ' 1、加载图片
    文件路径 ＝ 取运行目录 () ＋ “\test.png”  ' 这里应该是图片文件路径

    ' 尝试加载到所有图片框
    路径字节集 ＝ 编码_Ansi到Utf8 (文件路径)
    结果 ＝ 从文件加载图片 (图片框1, 取变量数据地址 (路径字节集), 取字节集长度 (路径字节集))
    从文件加载图片 (图片框2, 取变量数据地址 (路径字节集), 取字节集长度 (路径字节集))
    从文件加载图片 (图片框3, 取变量数据地址 (路径字节集), 取字节集长度 (路径字节集))
    从文件加载图片 (图片框4, 取变量数据地址 (路径字节集), 取字节集长度 (路径字节集))

    ' 2、从内存加载本地图片示例
    ' 图片数据 = 读入文件 ("photo.png")
    ' 数据指针 = 取变量数据地址 (图片数据)
    ' 结果 = 从内存加载图片 (图片框句柄, 数据指针, 取字节集长度 (图片数据))

    ' 3、内存读取网络图片
    ' 全局_图片数据 ＝ HTTP读文件 (“https://img.msblog.cc/img/150.png”)  ' 必须是全局变量，否则图片显示黑色
    ' 数据指针 ＝ 取变量数据地址 (全局_图片数据)
    ' 结果 ＝ 从内存加载图片 (图片框1, 数据指针, 取字节集长度 (全局_图片数据))
    ' 从内存加载图片 (图片框2, 数据指针, 取字节集长度 (全局_图片数据))
    ' 从内存加载图片 (图片框3, 数据指针, 取字节集长度 (全局_图片数据))
    ' 从内存加载图片 (图片框4, 数据指针, 取字节集长度 (全局_图片数据))

    .如果 (结果)
        ' ? 成功标记 emoji (UTF-8: E2 9C 85)
        状态字节集 ＝ { 226, 156, 133 }
        临时字节集 ＝ 编码_Ansi到Utf8 (“ 图片加载成功！” ＋ #换行符 ＋ “文件：” ＋ 文件路径)
        状态字节集 ＝ 状态字节集 ＋ 临时字节集
    .否则
        ' ? 失败标记 emoji (UTF-8: E2 9D 8C)
        状态字节集 ＝ { 226, 157, 140 }
        临时字节集 ＝ 编码_Ansi到Utf8 (“ 图片加载失败！” ＋ #换行符 ＋ “请确保文件存在：” ＋ 文件路径 ＋ #换行符 ＋ “支持格式：PNG、JPG、BMP、GIF”)
        状态字节集 ＝ 状态字节集 ＋ 临时字节集
    .如果结束

    ' 直接使用字节集设置标签文本
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))




.判断 (按钮ID ＝ 集_按钮_清除图片)
    ' 清除图片
    清除图片 (图片框1)
    清除图片 (图片框2)
    清除图片 (图片框3)
    清除图片 (图片框4)
    ' ??? 垃圾桶 emoji (UTF-8: F0 9F 97 91 EF B8 8F)
    状态字节集 ＝ { 240, 159, 151, 145, 239, 184, 143 }
    临时字节集 ＝ 编码_Ansi到Utf8 (“ 所有图片已清除”)
    状态字节集 ＝ 状态字节集 ＋ 临时字节集
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 集_按钮_增加透明度)
    ' 增加透明度
    当前透明度 ＝ 当前透明度 ＋ 10
    .如果真 (当前透明度 ＞ 100)
        当前透明度 ＝ 100
    .如果真结束

    设置图片透明度 (图片框1, 当前透明度 ÷ 100)
    设置图片透明度 (图片框2, 当前透明度 ÷ 100)
    设置图片透明度 (图片框3, 当前透明度 ÷ 100)
    设置图片透明度 (图片框4, 当前透明度 ÷ 100)

    ' ?? 高亮度 emoji (UTF-8: F0 9F 94 86)
    状态字节集 ＝ { 240, 159, 148, 134 }
    临时字节集 ＝ 编码_Ansi到Utf8 (“ 透明度已设置为：” ＋ 到文本 (当前透明度) ＋ “%”)
    状态字节集 ＝ 状态字节集 ＋ 临时字节集
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 集_按钮_减少透明度)
    ' 减少透明度
    当前透明度 ＝ 当前透明度 － 10
    .如果真 (当前透明度 ＜ 0)
        当前透明度 ＝ 0
    .如果真结束

    设置图片透明度 (图片框1, 当前透明度 ÷ 100)
    设置图片透明度 (图片框2, 当前透明度 ÷ 100)
    设置图片透明度 (图片框3, 当前透明度 ÷ 100)
    设置图片透明度 (图片框4, 当前透明度 ÷ 100)
    ' ?? 低亮度 emoji (UTF-8: F0 9F 94 85)
    状态字节集 ＝ { 240, 159, 148, 133 }
    临时字节集 ＝ 编码_Ansi到Utf8 (“ 透明度已设置为：” ＋ 到文本 (当前透明度) ＋ “%”)
    状态字节集 ＝ 状态字节集 ＋ 临时字节集
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 集_按钮_切换缩放模式)
    ' 切换缩放模式（循环切换）
    ' 图片框1: 拉伸 -> 等比 -> 居中 -> 不缩放 -> 拉伸
    设置图片框缩放模式 (图片框1, #SCALE_FIT)  ' 等比缩放
    设置图片框缩放模式 (图片框2, #SCALE_CENTER)  ' 居中
    设置图片框缩放模式 (图片框3, #SCALE_NONE)  ' 不缩放
    设置图片框缩放模式 (图片框4, #SCALE_STRETCH)  ' 拉伸

    ' ?? 循环箭头 emoji (UTF-8: F0 9F 94 84)
    状态字节集 ＝ { 240, 159, 148, 132 }
    临时字节集 ＝ 编码_Ansi到Utf8 (“ 缩放模式已切换” ＋ #换行符 ＋ “图片框1→等比 | 图片框2→居中 | 图片框3→不缩放 | 图片框4→拉伸”)
    状态字节集 ＝ 状态字节集 ＋ 临时字节集
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 集_按钮_加载网络图片)
    ' 3、内存读取网络图片
    全局_图片数据 ＝ HTTP读文件 (“https://img.msblog.cc/img/150.png”)  ' 必须是全局变量，否则图片显示黑色
    数据指针 ＝ 取变量数据地址 (全局_图片数据)
    结果 ＝ 从内存加载图片 (图片框1, 数据指针, 取字节集长度 (全局_图片数据))
    从内存加载图片 (图片框2, 数据指针, 取字节集长度 (全局_图片数据))
    从内存加载图片 (图片框3, 数据指针, 取字节集长度 (全局_图片数据))
    从内存加载图片 (图片框4, 数据指针, 取字节集长度 (全局_图片数据))

    .如果 (结果)
        ' ? 成功标记 emoji (UTF-8: E2 9C 85)
        状态字节集 ＝ { 226, 156, 133 }
        临时字节集 ＝ 编码_Ansi到Utf8 (“ 图片加载成功！” ＋ #换行符 ＋ “文件：” ＋ 文件路径)
        状态字节集 ＝ 状态字节集 ＋ 临时字节集
    .否则
        ' ? 失败标记 emoji (UTF-8: E2 9D 8C)
        状态字节集 ＝ { 226, 157, 140 }
        临时字节集 ＝ 编码_Ansi到Utf8 (“ 图片加载失败！” ＋ #换行符 ＋ “请确保文件存在：” ＋ 文件路径 ＋ #换行符 ＋ “支持格式：PNG、JPG、BMP、GIF”)
        状态字节集 ＝ 状态字节集 ＋ 临时字节集
    .如果结束

    ' 直接使用字节集设置标签文本
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.默认

.判断结束


.子程序 窗口关闭回调, , , stdcall
.参数 窗口句柄_, 整数型

调试输出 (“窗口被关闭，句柄：”, 窗口句柄_)
结束 ()
