.版本 2
.支持库 spec

' 【启动即崩排查】① 创建窗口请用「创建Emoji窗口_字节集」传 UTF-8 字节集，勿用「创建Emoji窗口(文本型)」：DLL 内 create_window 按系统 ANSI 解析 char*，与 Unicode 文本型传参易错位崩溃。② 工程里 DLL 声明与 DLL命令.md 完全一致（含创建标签 17 参数、创建分组框 15 参数）。③ 树回调子程序 stdcall，两参数（节点ID、上下文）。④ 易语言工程必须加载 32 位 `emoji_window.dll`；当前仓库根目录与 `examples/Python` 下的是 64 位，易语言请用 `examples\易语言代码\emoji_window.dll` 这份 32 位 DLL。⑤ 若工程里引用了“辅助程序集”，请同步更新其中的 `创建分组框_辅助`，旧版 9 参数调用会直接崩。⑥ 本示例已完全绕开“父节点查询 DLL 调用”，子节点通过显式节点 ID 映射到对应分组框。

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 树形框句柄, 整数型
.程序集变量 节点_商户中心, 整数型
.程序集变量 节点_订单管理, 整数型
.程序集变量 节点_订单_全部订单, 整数型
.程序集变量 节点_订单_售后单, 整数型
.程序集变量 节点_通道管理, 整数型
.程序集变量 节点_通道_账号管理, 整数型
.程序集变量 节点_通道_轮询池, 整数型
.程序集变量 节点_通道_支付设置, 整数型
.程序集变量 节点_财务中心, 整数型
.程序集变量 节点_财务_对账明细, 整数型
.程序集变量 节点_财务_结算记录, 整数型
.程序集变量 节点_API, 整数型
.程序集变量 节点_API_演示子项, 整数型
.程序集变量 节点_账号管理, 整数型
.程序集变量 节点_账号_个人中心, 整数型
.程序集变量 分组_商户, 整数型
.程序集变量 分组_订单, 整数型
.程序集变量 分组_通道, 整数型
.程序集变量 分组_财务, 整数型
.程序集变量 分组_API, 整数型
.程序集变量 分组_账号, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 文本, 字节集
.局部变量 图标, 字节集
.局部变量 字体名, 字节集
.局部变量 标签句柄, 整数型

文本 ＝ 编码_Ansi到Utf8 (“侧栏+分组框切换”)
主窗口句柄 ＝ 创建Emoji窗口_字节集 (取变量数据地址 (文本), 取字节集长度 (文本), -1, -1, 900, 600)
.如果真 (主窗口句柄 ＝ 0)
    信息框 (“创建主窗口失败”, 0, , )
    返回 (0)
.如果真结束

树形框句柄 ＝ 创建树形框 (主窗口句柄, 10, 40, 320, 520, 4294967295, 4281348403, 0)
.如果真 (树形框句柄 ＝ 0)
    信息框 (“创建树形框失败”, 0, , )
    返回 (0)
.如果真结束

清空树形框 (树形框句柄)
调用子程序 (&_演示侧栏样式, , , )
设置树形框侧栏模式 (树形框句柄, 真)

' ---------- 构建树 ----------
文本 ＝ 编码_Ansi到Utf8 (“商户中心”)
图标 ＝ { 240, 159, 143, 162 }
节点_商户中心 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))

文本 ＝ 编码_Ansi到Utf8 (“订单管理”)
图标 ＝ { 240, 159, 147, 166 }
节点_订单管理 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“全部订单”)
节点_订单_全部订单 ＝ 添加子节点 (树形框句柄, 节点_订单管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“售后单”)
节点_订单_售后单 ＝ 添加子节点 (树形框句柄, 节点_订单管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

文本 ＝ 编码_Ansi到Utf8 (“通道管理”)
图标 ＝ { 240, 159, 142, 175 }
节点_通道管理 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“账号管理”)
节点_通道_账号管理 ＝ 添加子节点 (树形框句柄, 节点_通道管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“轮询池”)
节点_通道_轮询池 ＝ 添加子节点 (树形框句柄, 节点_通道管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“支付设置”)
节点_通道_支付设置 ＝ 添加子节点 (树形框句柄, 节点_通道管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

文本 ＝ 编码_Ansi到Utf8 (“财务中心”)
图标 ＝ { 240, 159, 146, 176 }
节点_财务中心 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“对账明细”)
节点_财务_对账明细 ＝ 添加子节点 (树形框句柄, 节点_财务中心, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“结算记录”)
节点_财务_结算记录 ＝ 添加子节点 (树形框句柄, 节点_财务中心, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

文本 ＝ 编码_Ansi到Utf8 (“API”)
图标 ＝ { 240, 159, 146, 187 }
节点_API ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“演示子项”)
节点_API_演示子项 ＝ 添加子节点 (树形框句柄, 节点_API, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

文本 ＝ 编码_Ansi到Utf8 (“账号管理”)
图标 ＝ { 240, 159, 145, 164 }
节点_账号管理 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“个人中心”)
节点_账号_个人中心 ＝ 添加子节点 (树形框句柄, 节点_账号管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

展开树节点 (树形框句柄, 节点_通道管理)
展开树节点 (树形框句柄, 节点_账号管理)
折叠树节点 (树形框句柄, 节点_订单管理)
折叠树节点 (树形框句柄, 节点_财务中心)
折叠树节点 (树形框句柄, 节点_API)

' ---------- 右侧：6 个分组框叠放（340,40,540,500） ----------
' 【重要】标签父窗口请用「主窗口句柄」+ 主窗口客户区绝对坐标（与 Python 示例一致）。
' 创建标签后仍须「添加子控件到分组框」，ShowGroupBox 才能同步子控件列表。
' 本示例不再调用父节点查询 DLL，子节点全部显式映射到所属分组框。
字体名 ＝ 编码_Ansi到Utf8 (“微软雅黑”)

文本 ＝ 编码_Ansi到Utf8 (“商户中心 · 内容区”)
分组_商户 ＝ 创建分组框 (主窗口句柄, 340, 40, 540, 500, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 200, 200, 200), 到ARGB (255, 255, 255, 255), 0, 0, 0, 假, 假, 假)
文本 ＝ 编码_Ansi到Utf8 (“欢迎使用商户中心。此处可放统计、公告等控件。”)
标签句柄 ＝ 创建标签 (主窗口句柄, 370, 105, 500, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 250, 250, 250), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
添加子控件到分组框 (分组_商户, 标签句柄)

文本 ＝ 编码_Ansi到Utf8 (“订单管理 · 内容区”)
分组_订单 ＝ 创建分组框 (主窗口句柄, 340, 40, 540, 500, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 200, 200, 200), 到ARGB (255, 255, 255, 255), 0, 0, 0, 假, 假, 假)
文本 ＝ 编码_Ansi到Utf8 (“订单相关：列表、筛选、导出等。”)
标签句柄 ＝ 创建标签 (主窗口句柄, 370, 105, 500, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
添加子控件到分组框 (分组_订单, 标签句柄)

文本 ＝ 编码_Ansi到Utf8 (“通道管理 · 内容区”)
分组_通道 ＝ 创建分组框 (主窗口句柄, 340, 40, 540, 500, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 200, 200, 200), 到ARGB (255, 255, 255, 255), 0, 0, 0, 假, 假, 假)
文本 ＝ 编码_Ansi到Utf8 (“通道、轮询、支付配置。”)
标签句柄 ＝ 创建标签 (主窗口句柄, 370, 105, 500, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
添加子控件到分组框 (分组_通道, 标签句柄)

文本 ＝ 编码_Ansi到Utf8 (“财务中心 · 内容区”)
分组_财务 ＝ 创建分组框 (主窗口句柄, 340, 40, 540, 500, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 200, 200, 200), 到ARGB (255, 255, 255, 255), 0, 0, 0, 假, 假, 假)
文本 ＝ 编码_Ansi到Utf8 (“对账、结算、报表。”)
标签句柄 ＝ 创建标签 (主窗口句柄, 370, 105, 500, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
添加子控件到分组框 (分组_财务, 标签句柄)

文本 ＝ 编码_Ansi到Utf8 (“API · 内容区”)
分组_API ＝ 创建分组框 (主窗口句柄, 340, 40, 540, 500, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 200, 200, 200), 到ARGB (255, 255, 255, 255), 0, 0, 0, 假, 假, 假)
文本 ＝ 编码_Ansi到Utf8 (“接口文档、密钥、调试工具。”)
标签句柄 ＝ 创建标签 (主窗口句柄, 370, 105, 500, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
添加子控件到分组框 (分组_API, 标签句柄)

文本 ＝ 编码_Ansi到Utf8 (“账号管理 · 内容区”)
分组_账号 ＝ 创建分组框 (主窗口句柄, 340, 40, 540, 500, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 200, 200, 200), 到ARGB (255, 255, 255, 255), 0, 0, 0, 假, 假, 假)
文本 ＝ 编码_Ansi到Utf8 (“个人资料、安全、套餐等。”)
标签句柄 ＝ 创建标签 (主窗口句柄, 370, 105, 500, 60, 取变量数据地址 (文本), 取字节集长度 (文本), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 真)
添加子控件到分组框 (分组_账号, 标签句柄)

设置选中树节点 (树形框句柄, 节点_API)
调用子程序 (&_按节点显示分组框, 节点_API, , )
设置树形框回调 (树形框句柄, 1, &回调_树选中)
设置树形框回调 (树形框句柄, 2, &回调_树展开)
设置树形框回调 (树形框句柄, 3, &回调_树折叠)

设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 _演示侧栏样式

设置树形框行高 (树形框句柄, 38)
设置树形框行间距 (树形框句柄, 2)
设置树形框文字色 (树形框句柄, 4281348403)
设置树形框选中背景色 (树形框句柄, 859870950)
设置树形框选中前景色 (树形框句柄, 4282423014)
设置树形框悬停背景色 (树形框句柄, 4294309882)

.子程序 _隐藏全部分组框

显示分组框 (分组_商户, 假)
显示分组框 (分组_订单, 假)
显示分组框 (分组_通道, 假)
显示分组框 (分组_财务, 假)
显示分组框 (分组_API, 假)
显示分组框 (分组_账号, 假)

.子程序 _按节点显示分组框
.参数 节点ID, 整数型

调用子程序 (&_隐藏全部分组框, , , )

.判断开始 (节点ID ＝ 节点_商户中心)
    显示分组框 (分组_商户, 真)
.判断 (节点ID ＝ 节点_订单管理)
    显示分组框 (分组_订单, 真)
.判断 (节点ID ＝ 节点_订单_全部订单)
    显示分组框 (分组_订单, 真)
.判断 (节点ID ＝ 节点_订单_售后单)
    显示分组框 (分组_订单, 真)
.判断 (节点ID ＝ 节点_通道管理)
    显示分组框 (分组_通道, 真)
.判断 (节点ID ＝ 节点_通道_账号管理)
    显示分组框 (分组_通道, 真)
.判断 (节点ID ＝ 节点_通道_轮询池)
    显示分组框 (分组_通道, 真)
.判断 (节点ID ＝ 节点_通道_支付设置)
    显示分组框 (分组_通道, 真)
.判断 (节点ID ＝ 节点_财务中心)
    显示分组框 (分组_财务, 真)
.判断 (节点ID ＝ 节点_财务_对账明细)
    显示分组框 (分组_财务, 真)
.判断 (节点ID ＝ 节点_财务_结算记录)
    显示分组框 (分组_财务, 真)
.判断 (节点ID ＝ 节点_API)
    显示分组框 (分组_API, 真)
.判断 (节点ID ＝ 节点_API_演示子项)
    显示分组框 (分组_API, 真)
.判断 (节点ID ＝ 节点_账号管理)
    显示分组框 (分组_账号, 真)
.判断 (节点ID ＝ 节点_账号_个人中心)
    显示分组框 (分组_账号, 真)
.默认
    显示分组框 (分组_商户, 真)
.判断结束

.子程序 回调_树选中, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型

显示分组框 (分组_商户, 假)
显示分组框 (分组_订单, 假)
显示分组框 (分组_通道, 假)
显示分组框 (分组_财务, 假)
显示分组框 (分组_API, 假)
显示分组框 (分组_账号, 假)

.判断开始 (节点ID ＝ 节点_商户中心)
    显示分组框 (分组_商户, 真)
.判断 (节点ID ＝ 节点_订单管理)
    显示分组框 (分组_订单, 真)
.判断 (节点ID ＝ 节点_订单_全部订单)
    显示分组框 (分组_订单, 真)
.判断 (节点ID ＝ 节点_订单_售后单)
    显示分组框 (分组_订单, 真)
.判断 (节点ID ＝ 节点_通道管理)
    显示分组框 (分组_通道, 真)
.判断 (节点ID ＝ 节点_通道_账号管理)
    显示分组框 (分组_通道, 真)
.判断 (节点ID ＝ 节点_通道_轮询池)
    显示分组框 (分组_通道, 真)
.判断 (节点ID ＝ 节点_通道_支付设置)
    显示分组框 (分组_通道, 真)
.判断 (节点ID ＝ 节点_财务中心)
    显示分组框 (分组_财务, 真)
.判断 (节点ID ＝ 节点_财务_对账明细)
    显示分组框 (分组_财务, 真)
.判断 (节点ID ＝ 节点_财务_结算记录)
    显示分组框 (分组_财务, 真)
.判断 (节点ID ＝ 节点_API)
    显示分组框 (分组_API, 真)
.判断 (节点ID ＝ 节点_API_演示子项)
    显示分组框 (分组_API, 真)
.判断 (节点ID ＝ 节点_账号管理)
    显示分组框 (分组_账号, 真)
.判断 (节点ID ＝ 节点_账号_个人中心)
    显示分组框 (分组_账号, 真)
.默认
    显示分组框 (分组_商户, 真)
.判断结束

.子程序 回调_树展开, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型

.子程序 回调_树折叠, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型
