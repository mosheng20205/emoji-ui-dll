.版本 2
.支持库 spec

.程序集 窗口程序集_窗口_主题切换示例
.程序集变量 窗口句柄, 整数型
.程序集变量 按钮ID_亮色, 整数型
.程序集变量 按钮ID_暗色, 整数型
.程序集变量 按钮ID_自定义, 整数型
.程序集变量 标签句柄, 整数型
.程序集变量 复选框句柄, 整数型
.程序集变量 进度条句柄, 整数型
.程序集变量 列表框句柄, 整数型
.程序集变量 utf8字节集, 字节集
.程序集变量 utf8长度, 整数型

.子程序 _窗口_主题切换示例_创建完毕

' 创建窗口 - "主题切换示例 彩色标题测试"
utf8字节集 ＝ 编码_Ansi到Utf8 (“主题切换示例 ”) ＋ { 240, 159, 142, 168, 32, 240, 159, 148, 165, 32, 240, 159, 152, 128 }
窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集), 600, 500, 16382715)
设置窗口图标 (窗口句柄, 取运行目录 () ＋ “\icon.ico”)
设置窗口标题栏颜色 (窗口句柄, 16382715)
' ===== 主题切换按钮 =====
' 太阳 亮色主题"
utf8字节集 ＝ { 226, 152, 128, 239, 184, 143, 32 } ＋ 编码_Ansi到Utf8 (“亮色主题”)
按钮ID_亮色 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集), 0, 0, 20, 20, 160, 40, #COLOR_PRIMARY)

' 月亮 暗色主题"
utf8字节集 ＝ { 240, 159, 140, 153, 32 } ＋ 编码_Ansi到Utf8 (“暗色主题”)
按钮ID_暗色 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集), 0, 0, 200, 20, 160, 40, #COLOR_INFO)

' 自定义主题"
utf8字节集 ＝ { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 (“自定义主题”)
按钮ID_自定义 ＝ 创建Emoji按钮_字节集 (窗口句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集), 0, 0, 380, 20, 160, 40, #COLOR_SUCCESS)

' ===== 标签 =====
' "当前主题：亮色 ☀️ 标题栏颜色：浅色"
utf8字节集 ＝ 编码_Ansi到Utf8 (“当前主题：亮色 ”) ＋ { 226, 152, 128, 239, 184, 143, 32 } ＋ 编码_Ansi到Utf8 (“标题栏颜色：浅色”)
标签句柄 ＝ 创建标签 (窗口句柄, 20, 80, 560, 30, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集), #THEME_COLOR_TEXT_PRIMARY, #THEME_COLOR_BACKGROUND, 0, 0, 12, 假, 假, 假, 0, 假)

' ===== 复选框 =====
' "启用通知 ??"
utf8字节集 ＝ 编码_Ansi到Utf8 (“启用通知 ”) ＋ { 240, 159, 147, 162 }
复选框句柄 ＝ 创建复选框 (窗口句柄, 20, 130, 200, 30, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集), 假, #THEME_COLOR_TEXT_PRIMARY, #THEME_COLOR_BACKGROUND)

' ===== 进度条 =====
进度条句柄 ＝ 创建进度条 (窗口句柄, 20, 180, 560, 25, 0, #THEME_COLOR_PRIMARY, #THEME_COLOR_BACKGROUND, 真, #THEME_COLOR_TEXT_PRIMARY)
设置进度条值 (进度条句柄, 65)
设置进度条显示文本 (进度条句柄, 真)

' ===== 列表框 =====
列表框句柄 ＝ 创建列表框 (窗口句柄, 20, 230, 560, 200, 真, #THEME_COLOR_TEXT_PRIMARY, #THEME_COLOR_BACKGROUND)

' 主题系统支持亮色和暗色两种模式"
utf8字节集 ＝ { 240, 159, 142, 175, 32 } ＋ 编码_Ansi到Utf8 (“主题系统支持亮色和暗色两种模式”)
添加列表项 (列表框句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))

' 可以从JSON文件加载自定义主题"
utf8字节集 ＝ { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 (“可以从JSON文件加载自定义主题”)
添加列表项 (列表框句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))

' 彩色Emoji标题栏支持运行时更新"
utf8字节集 ＝ { 240, 159, 150, 188, 32 } ＋ 编码_Ansi到Utf8 (“彩色Emoji标题栏支持运行时更新”)
添加列表项 (列表框句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))

' 主题包含颜色、字体、尺寸三大类配置"
utf8字节集 ＝ { 240, 159, 147, 144, 32 } ＋ 编码_Ansi到Utf8 (“主题包含颜色、字体、尺寸三大类配置”)
添加列表项 (列表框句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))

' 支持 Element UI 标准配色方案"
utf8字节集 ＝ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 (“支持 Element UI 标准配色方案”)
添加列表项 (列表框句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))

' 设置按钮回调
设置按钮点击回调 (&按钮点击处理)

' 运行消息循环
运行消息循环 ()

.子程序 按钮点击处理, , , 按钮点击回调
.参数 按钮ID, 整数型
.局部变量 utf8字节集, 字节集
.局部变量 json字节集, 字节集
.局部变量 json文本, 文本型
.局部变量 引号, 文本型

调试输出 (取程序名称 (1), 按钮ID)
引号 ＝ 字符 (34)

.判断开始 (按钮ID ＝ 按钮ID_亮色)
    ' 亮色主题
    utf8字节集 ＝ 编码_Ansi到Utf8 (“light”)
    设置主题 (取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))
    设置窗口标题栏颜色 (窗口句柄, 16382715)
    utf8字节集 ＝ 编码_Ansi到Utf8 (“主题切换示例 ”) ＋ { 226, 152, 128, 239, 184, 143, 32, 240, 159, 142, 168 }
    设置窗口标题 (窗口句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))
    utf8字节集 ＝ 编码_Ansi到Utf8 (“当前主题：亮色 ”) ＋ { 226, 152, 128, 239, 184, 143, 32 } ＋ 编码_Ansi到Utf8 (“标题栏颜色：浅色”)
    设置标签文本 (标签句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))
.判断 (按钮ID ＝ 按钮ID_暗色)
    ' 暗色主题
    设置暗色模式 (真)
    设置窗口标题栏颜色 (窗口句柄, 1120295)
    utf8字节集 ＝ 编码_Ansi到Utf8 (“主题切换示例 ”) ＋ { 240, 159, 140, 153, 32, 240, 159, 148, 165 }
    设置窗口标题 (窗口句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))
    utf8字节集 ＝ 编码_Ansi到Utf8 (“当前主题：暗色 ”) ＋ { 240, 159, 140, 153, 32 } ＋ 编码_Ansi到Utf8 (“标题栏颜色：深色”)
    设置标签文本 (标签句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))
.判断 (按钮ID ＝ 按钮ID_自定义)
    ' 自定义主题（紫色主题示例）
    json文本 ＝ “{” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “name” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “custom_purple” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “dark_mode” ＋ 引号 ＋ “: false,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “primary” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#7C3AED” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “success” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#10B981” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “warning” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#F59E0B” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “danger” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#EF4444” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “info” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#6B7280” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “text_primary” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#1F2937” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “text_regular” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#4B5563” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “text_secondary” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#9CA3AF” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “text_placeholder” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#D1D5DB” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “border_base” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#E5E7EB” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “border_light” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#F3F4F6” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “border_lighter” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#F9FAFB” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “background” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#FFFFFF” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “background_light” ＋ 引号 ＋ “: ” ＋ 引号 ＋ “#F9FAFB” ＋ 引号 ＋ “,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “border_radius” ＋ 引号 ＋ “: 8,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “title_size” ＋ 引号 ＋ “: 18,” ＋ #换行符
    json文本 ＝ json文本 ＋ “  ” ＋ 引号 ＋ “body_size” ＋ 引号 ＋ “: 14” ＋ #换行符 ＋ “}”
    json字节集 ＝ 编码_Ansi到Utf8 (json文本)
    从JSON加载主题 (取字节集指针 (json字节集), 取字节集长度 (json字节集))
    设置窗口标题栏颜色 (窗口句柄, 8141549)
    utf8字节集 ＝ 编码_Ansi到Utf8 (“主题切换示例 ”) ＋ { 240, 159, 142, 168, 32, 240, 159, 146, 156 }
    设置窗口标题 (窗口句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))
    utf8字节集 ＝ 编码_Ansi到Utf8 (“当前主题：自定义 ”) ＋ { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 (“标题栏颜色：紫色”)
    设置标签文本 (标签句柄, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集))
.默认

.判断结束