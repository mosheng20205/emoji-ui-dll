.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 树形框句柄, 整数型
.程序集变量 节点_商户中心, 整数型
.程序集变量 节点_订单管理, 整数型
.程序集变量 节点_通道管理, 整数型
.程序集变量 节点_财务中心, 整数型
.程序集变量 节点_API, 整数型
.程序集变量 节点_账号管理, 整数型
.程序集变量 节点_演示子项

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 文本, 字节集
.局部变量 图标, 字节集

主窗口句柄 ＝ 创建Emoji窗口 (“侧栏菜单示例”, -1, -1, 900, 600)
.如果真 (主窗口句柄 ＝ 0)
    信息框 (“创建主窗口失败”, 0, , )
    返回 (0)
.如果真结束

树形框句柄 ＝ 创建树形框 (主窗口句柄, 10, 40, 320, 520, 4294967295, 4281348403, 0)
.如果真 (树形框句柄 ＝ 0)
    信息框 (“创建树形框失败”, 0, , )
    返回 (0)
.如果真结束

清空树形框 (树形框句柄)

' ---------- 侧栏样式（可在 设置树形框侧栏模式 之前或之后调用） ----------
调用子程序 (&_演示侧栏全部读写API, , , )

设置树形框侧栏模式 (树形框句柄, 真)

设置树形框回调 (树形框句柄, 1, &回调_树选中)
设置树形框回调 (树形框句柄, 2, &回调_树展开)
设置树形框回调 (树形框句柄, 3, &回调_树折叠)
设置树形框回调 (树形框句柄, 8, &回调_树移动)

' ===================== 构建菜单树（多根节点） =====================
' 图标：UTF-8 四字节 Emoji 字节集；勿在 IDE 中直接粘贴彩色 Emoji

' —— 1. 商户中心（叶子，无子则右侧无箭头） ——
文本 ＝ 编码_Ansi到Utf8 (“商户中心”)
图标 ＝ { 240, 159, 143, 162 }
节点_商户中心 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))

' —— 2. 订单管理（带子节点，默认折叠 → 右侧 ∨） ——
文本 ＝ 编码_Ansi到Utf8 (“订单管理”)
图标 ＝ { 240, 159, 147, 166 }
节点_订单管理 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“全部订单”)
添加子节点 (树形框句柄, 节点_订单管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“售后单”)
添加子节点 (树形框句柄, 节点_订单管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

' —— 3. 通道管理 + 二级：账号管理 / 轮询池 / 支付设置 ——
文本 ＝ 编码_Ansi到Utf8 (“通道管理”)
图标 ＝ { 240, 159, 142, 175 }
节点_通道管理 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“账号管理”)
添加子节点 (树形框句柄, 节点_通道管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“轮询池”)
添加子节点 (树形框句柄, 节点_通道管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“支付设置”)
添加子节点 (树形框句柄, 节点_通道管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

' —— 4. 财务中心（带子节点，默认折叠） ——
文本 ＝ 编码_Ansi到Utf8 (“财务中心”)
图标 ＝ { 240, 159, 146, 176 }
节点_财务中心 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“对账明细”)
添加子节点 (树形框句柄, 节点_财务中心, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“结算记录”)
添加子节点 (树形框句柄, 节点_财务中心, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

' —— 5. API（一级，与截图一致：与「订单管理」同级，非其子节点） ——
文本 ＝ 编码_Ansi到Utf8 (“API”)
图标 ＝ { 240, 159, 146, 187 }
节点_API ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“演示子项”)
节点_演示子项 ＝ 添加子节点 (树形框句柄, 节点_API, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

' —— 6. 账号管理 + 二级（个人中心…登录日志） ——
文本 ＝ 编码_Ansi到Utf8 (“账号管理”)
图标 ＝ { 240, 159, 145, 164 }
节点_账号管理 ＝ 添加根节点 (树形框句柄, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“个人中心”)
添加子节点 (树形框句柄, 节点_账号管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“实名认证”)
添加子节点 (树形框句柄, 节点_账号管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“套餐购买”)
添加子节点 (树形框句柄, 节点_账号管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“邀请返利”)
添加子节点 (树形框句柄, 节点_账号管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“修改密码”)
添加子节点 (树形框句柄, 节点_账号管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“登录日志”)
添加子节点 (树形框句柄, 节点_账号管理, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

' —— 展开/折叠，贴近截图：通道管理、账号管理展开；订单、财务折叠 ——
展开树节点 (树形框句柄, 节点_通道管理)
展开树节点 (树形框句柄, 节点_账号管理)
折叠树节点 (树形框句柄, 节点_订单管理)
折叠树节点 (树形框句柄, 节点_财务中心)
折叠树节点 (树形框句柄, 节点_API)

' 默认选中「API」一级项
设置选中树节点 (树形框句柄, 节点_API)

' ===== 消息循环 =====
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 _演示侧栏全部读写API
.局部变量 字体族, 字节集
.局部变量 字号, 小数型
.局部变量 字重, 整数型
.局部变量 斜体, 逻辑型

设置树形框行高 (树形框句柄, 38)
设置树形框行间距 (树形框句柄, 2)
调试输出 (“行高=” ＋ 到文本 (取树形框行高 (树形框句柄)))
调试输出 (“行间距=” ＋ 到文本 (取树形框行间距 (树形框句柄)))

' ARGB 须用十进制，勿写 #FF303133（# 后为常量名会报错 55）
设置树形框文字色 (树形框句柄, 4281348403)
设置树形框选中背景色 (树形框句柄, 859870950)
设置树形框选中前景色 (树形框句柄, 4282423014)
设置树形框悬停背景色 (树形框句柄, 4294309882)
调试输出 (“默认字色=” ＋ 到文本 (取树形框文字色 (树形框句柄)))
调试输出 (“选中背景=” ＋ 到文本 (取树形框选中背景色 (树形框句柄)))
调试输出 (“选中前景=” ＋ 到文本 (取树形框选中前景色 (树形框句柄)))
调试输出 (“悬停背景=” ＋ 到文本 (取树形框悬停背景色 (树形框句柄)))

字体族 ＝ 编码_Ansi到Utf8 (“Segoe UI Emoji”)
.如果真 (设置树形框字体 (树形框句柄, 取变量数据地址 (字体族), 取字节集长度 (字体族), 14, 400, 假))
    字号 ＝ 0
    字重 ＝ 0
    斜体 ＝ 假
    字体族 ＝ 取空白字节集 (256)
    取树形框字体 (树形框句柄, 取变量数据地址 (字体族), 256, 字号, 字重, 斜体)
    调试输出 (“字体字号=” ＋ 到文本 (字号) ＋ “ 字重=” ＋ 到文本 (字重))
.如果真结束

调试输出 (“侧栏模式=” ＋ 到文本 (取树形框侧栏模式 (树形框句柄)))

.子程序 回调_树选中, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型

调试输出 (“[选中] id=” ＋ 到文本 (节点ID))

.子程序 回调_树展开, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型

调试输出 (“[展开] id=” ＋ 到文本 (节点ID))

.子程序 回调_树折叠, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型

调试输出 (“[折叠] id=” ＋ 到文本 (节点ID))

.子程序 回调_树移动, , , stdcall
.参数 节点ID, 整数型
.参数 新父ID, 整数型
.参数 插入索引, 整数型
.参数 上下文, 整数型

调试输出 (“[移动] id=” ＋ 到文本 (节点ID) ＋ “ parent=” ＋ 到文本 (新父ID) ＋ “ idx=” ＋ 到文本 (插入索引))


' 设计器可加按钮：演示 移动树形框节点、取树节点文本

.子程序 _按钮_移动子项到根_被单击

.如果 (移动树形框节点 (树形框句柄, 节点_演示子项, -1, 0))
    信息框 (“移动树形框节点 成功”, 0, , )
.否则
    信息框 (“移动树形框节点 失败”, 0, , )
.如果结束


.子程序 _按钮_读取文本_被单击
.局部变量 buf, 字节集
.局部变量 n, 整数型

buf ＝ 取空白字节集 (512)
n ＝ 取树节点文本 (树形框句柄, 节点_API, 取变量数据地址 (buf), 取字节集长度 (buf))
信息框 (“取树节点文本 字节数=” ＋ 到文本 (n), 0, , )
