.版本 2
.支持库 spec
.支持库 eAPI

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 标签_结果, 整数型
.程序集变量 TabControl句柄, 整数型
.程序集变量 按钮_获取Tab标题, 整数型
.程序集变量 按钮_设置Tab标题, 整数型
.程序集变量 按钮_获取位置, 整数型
.程序集变量 按钮_设置位置, 整数型
.程序集变量 按钮_获取可视状态, 整数型
.程序集变量 按钮_切换可视, 整数型
.程序集变量 按钮_切换启用, 整数型
.程序集变量 TabControl是否可见, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 utf8字节集, 字节集
.局部变量 按钮文本字节集, 字节集
.局部变量 字体名utf8, 字节集
.局部变量 Tab标题字节集, 字节集

' 创建窗口
utf8字节集 ＝ 编码_Ansi到Utf8 ("TabControl属性操作示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 850, 680, 到RGB (70, 130, 180))

' 创建结果显示标签
utf8字节集 ＝ 编码_Ansi到Utf8 ("点击下方按钮操作TabControl属性")
字体名utf8 ＝ 编码_Ansi到Utf8 ("微软雅黑")
标签_结果 ＝ 创建标签 (主窗口句柄, 20, 10, 810, 120, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 0, 0, 0), 到ARGB (255, 245, 245, 245), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

' 创建TabControl（3个Tab页）
TabControl句柄 ＝ 创建TabControl (主窗口句柄, 20, 145, 810, 350)

' 添加Tab页
' Tab0: "📋 基本信息"  📋 = { 240, 159, 147, 139 }
Tab标题字节集 ＝ { 240, 159, 147, 139, 32 } ＋ 编码_Ansi到Utf8 ("基本信息")
添加Tab页 (TabControl句柄, 取变量数据地址 (Tab标题字节集), 取字节集长度 (Tab标题字节集), 0)

' Tab1: "⚙ 设置选项"  ⚙ = { 226, 154, 153 }
Tab标题字节集 ＝ { 226, 154, 153, 32 } ＋ 编码_Ansi到Utf8 ("设置选项")
添加Tab页 (TabControl句柄, 取变量数据地址 (Tab标题字节集), 取字节集长度 (Tab标题字节集), 0)

' Tab2: "🔧 高级功能"  🔧 = { 240, 159, 148, 167 }
Tab标题字节集 ＝ { 240, 159, 148, 167, 32 } ＋ 编码_Ansi到Utf8 ("高级功能")
添加Tab页 (TabControl句柄, 取变量数据地址 (Tab标题字节集), 取字节集长度 (Tab标题字节集), 0)

' 初始化可视状态标志（1=可见）
TabControl是否可见 ＝ 1

' 创建操作按钮（保存返回的按钮ID到程序集变量）

' "📝 获取Tab标题"  📝 = { 240, 159, 147, 157 }
utf8字节集 ＝ { 240, 159, 147, 157 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取Tab标题")
按钮_获取Tab标题 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 510, 150, 40, 到ARGB (255, 100, 180, 100))

' "✏ 设置Tab标题"  ✏ = { 226, 156, 143 }
utf8字节集 ＝ { 226, 156, 143 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("设置Tab标题")
按钮_设置Tab标题 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 185, 510, 150, 40, 到ARGB (255, 100, 180, 100))

' "📐 获取位置大小"  📐 = { 240, 159, 147, 144 }
utf8字节集 ＝ { 240, 159, 147, 144 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取位置大小")
按钮_获取位置 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 350, 510, 150, 40, 到ARGB (255, 100, 180, 100))

' "🔄 设置位置大小"  🔄 = { 240, 159, 148, 132 }
utf8字节集 ＝ { 240, 159, 148, 132 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("设置位置大小")
按钮_设置位置 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 515, 510, 150, 40, 到ARGB (255, 100, 180, 100))

' "👁 获取可视状态"  👁 = { 240, 159, 145, 129, 239, 184, 143 }
utf8字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取可视状态")
按钮_获取可视状态 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 565, 150, 40, 到ARGB (255, 180, 100, 100))

' "🔀 切换显示/隐藏"  🔀 = { 240, 159, 148, 128 }
utf8字节集 ＝ { 240, 159, 148, 128 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("切换显示/隐藏")
按钮_切换可视 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 185, 565, 150, 40, 到ARGB (255, 180, 100, 100))

' "⚡ 切换启用/禁用"  ⚡ = { 226, 154, 161 }
utf8字节集 ＝ { 226, 154, 161 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("切换启用/禁用")
按钮_切换启用 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 350, 565, 150, 40, 到ARGB (255, 180, 100, 100))

' 设置按钮点击回调
设置按钮点击回调 (&按钮点击回调)

' 设置消息循环主窗口
设置消息循环主窗口 (主窗口句柄)

' 运行消息循环
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 结果字节集, 字节集
.局部变量 标题长度, 整数型
.局部变量 标题缓冲区, 字节集
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型
.局部变量 可视状态, 整数型
.局部变量 新标题字节集, 字节集
.局部变量 i, 整数型
.局部变量 Tab数量, 整数型

.判断开始 (按钮ID ＝ 按钮_获取Tab标题)  ' 获取所有Tab页标题
    Tab数量 ＝ 获取Tab数量 (TabControl句柄)
    结果字节集 ＝ 编码_Ansi到Utf8 ("【获取Tab页标题】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("Tab总数：") ＋ 编码_Ansi到Utf8 (到文本 (Tab数量)) ＋ { 10 }
    i ＝ 0
    .判断循环首 (i ＜ Tab数量)
        标题长度 ＝ 获取Tab页标题 (TabControl句柄, i, 0, 0)
        .如果 (标题长度 ＞ 0)
            标题缓冲区 ＝ 取空白字节集 (标题长度)
            获取Tab页标题 (TabControl句柄, i, 取变量数据地址 (标题缓冲区), 标题长度)
            结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("Tab[") ＋ 编码_Ansi到Utf8 (到文本 (i)) ＋ 编码_Ansi到Utf8 ("]：") ＋ 标题缓冲区 ＋ { 10 }
        .否则
            结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("Tab[") ＋ 编码_Ansi到Utf8 (到文本 (i)) ＋ 编码_Ansi到Utf8 ("]：（空）") ＋ { 10 }
        .如果结束
        i ＝ i ＋ 1
    .判断循环尾 ()
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_设置Tab标题)  ' 设置Tab[0]标题
    ' "✅ 已修改标题"  ✅ = { 226, 156, 133 }
    新标题字节集 ＝ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("已修改标题")
    .如果 (设置Tab页标题 (TabControl句柄, 0, 取变量数据地址 (新标题字节集), 取字节集长度 (新标题字节集)) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【设置Tab页标题】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("已将Tab[0]标题修改为：") ＋ 新标题字节集 ＋ { 10 } ＋ 编码_Ansi到Utf8 ("（点击"获取Tab标题"按钮可验证）")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【设置Tab页标题】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("设置失败（返回-1）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取位置)  ' 获取TabControl位置和大小
    .如果 (获取TabControl位置 (TabControl句柄, X, Y, 宽度, 高度) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取TabControl位置大小】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("X坐标：") ＋ 编码_Ansi到Utf8 (到文本 (X)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("Y坐标：") ＋ 编码_Ansi到Utf8 (到文本 (Y)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("宽度：") ＋ 编码_Ansi到Utf8 (到文本 (宽度)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("高度：") ＋ 编码_Ansi到Utf8 (到文本 (高度))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取TabControl位置大小】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("获取失败（返回-1）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_设置位置)  ' 设置TabControl位置和大小
    .如果 (设置TabControl位置 (TabControl句柄, 30, 155, 790, 330) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【设置TabControl位置大小】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("已设置为：X=30, Y=155, 宽=790, 高=330") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("（点击"获取位置大小"按钮可验证）")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【设置TabControl位置大小】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("设置失败（返回-1）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取可视状态)  ' 获取可视状态
    可视状态 ＝ 获取TabControl可视状态 (TabControl句柄)
    .判断开始 (可视状态 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取TabControl可视状态】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("状态：可见（1）")
    .判断 (可视状态 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取TabControl可视状态】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("状态：不可见（0）")
    .默认
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取TabControl可视状态】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("状态：获取失败（-1）")
    .判断结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_切换可视)  ' 切换显示/隐藏
    .如果 (TabControl是否可见 ＝ 1)
        TabControl是否可见 ＝ 0
    .否则
        TabControl是否可见 ＝ 1
    .如果结束
    .如果 (显示隐藏TabControl (TabControl句柄, TabControl是否可见) ＝ 0)
        .如果 (TabControl是否可见 ＝ 1)
            结果字节集 ＝ 编码_Ansi到Utf8 ("【切换TabControl显示/隐藏】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("已显示TabControl")
        .否则
            结果字节集 ＝ 编码_Ansi到Utf8 ("【切换TabControl显示/隐藏】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("已隐藏TabControl（再次点击可重新显示）")
        .如果结束
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【切换TabControl显示/隐藏】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("操作失败（返回-1）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_切换启用)  ' 禁用后立即重新启用（演示效果）
    .如果 (启用禁用TabControl (TabControl句柄, 0) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【切换TabControl启用/禁用】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("已禁用TabControl（Tab页不可切换）") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("正在自动重新启用...")
        启用禁用TabControl (TabControl句柄, 1)
        结果字节集 ＝ 结果字节集 ＋ { 10 } ＋ 编码_Ansi到Utf8 ("已重新启用TabControl")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【切换TabControl启用/禁用】") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("操作失败（返回-1）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.默认

.判断结束


.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
