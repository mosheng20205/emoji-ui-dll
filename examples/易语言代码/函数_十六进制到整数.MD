.子程序 十六进制到整数, 整数型, , 将十六进制文本转为整数型（如 "FF4682B4" → 对应整数）
.参数 十六进制文本, 文本型
.局部变量 i, 整数型
.局部变量 长度, 整数型
.局部变量 结果, 整数型
.局部变量 当前字符, 文本型
.局部变量 字符值, 整数型

长度 ＝ 取文本长度 (十六进制文本)
结果 ＝ 0

.计次循环首 (长度, i)
    当前字符 ＝ 取文本中间 (十六进制文本, i, 1)

    .判断开始 (当前字符 ＝ "0")
        字符值 ＝ 0
    .判断 (当前字符 ＝ "1")
        字符值 ＝ 1
    .判断 (当前字符 ＝ "2")
        字符值 ＝ 2
    .判断 (当前字符 ＝ "3")
        字符值 ＝ 3
    .判断 (当前字符 ＝ "4")
        字符值 ＝ 4
    .判断 (当前字符 ＝ "5")
        字符值 ＝ 5
    .判断 (当前字符 ＝ "6")
        字符值 ＝ 6
    .判断 (当前字符 ＝ "7")
        字符值 ＝ 7
    .判断 (当前字符 ＝ "8")
        字符值 ＝ 8
    .判断 (当前字符 ＝ "9")
        字符值 ＝ 9
    .判断 (当前字符 ＝ "A" 或 当前字符 ＝ "a")
        字符值 ＝ 10
    .判断 (当前字符 ＝ "B" 或 当前字符 ＝ "b")
        字符值 ＝ 11
    .判断 (当前字符 ＝ "C" 或 当前字符 ＝ "c")
        字符值 ＝ 12
    .判断 (当前字符 ＝ "D" 或 当前字符 ＝ "d")
        字符值 ＝ 13
    .判断 (当前字符 ＝ "E" 或 当前字符 ＝ "e")
        字符值 ＝ 14
    .判断 (当前字符 ＝ "F" 或 当前字符 ＝ "f")
        字符值 ＝ 15
    .默认
        字符值 ＝ 0
    .判断结束

    结果 ＝ 左移 (结果, 4) ＋ 字符值
.计次循环尾 ()

返回 (结果)
