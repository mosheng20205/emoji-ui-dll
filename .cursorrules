# 易语言编程规则

## 条件语句语法规则

### 1. 单分支条件语句（只有 if，没有 else）
使用 `.如果真` 和 `.如果真结束`：

```
.如果真 (条件)
    ' 执行代码
.如果真结束
```

### 2. 双分支条件语句（有 if-else）
使用 `.如果`、`.否则` 和 `.如果结束`：

```
.如果 (条件)
    ' 条件为真时执行
.否则
    ' 条件为假时执行
.如果结束
```

### 3. 多分支条件语句
使用 `.判断开始`、`.判断`、`.默认` 和 `.判断结束`：

```
.判断开始 (条件1)
    ' 条件1为真时执行
.判断 (条件2)
    ' 条件2为真时执行
.判断 (条件3)
    ' 条件3为真时执行
.默认
    ' 所有条件都不满足时执行
.判断结束
```

## 关键规则总结

1. **有 `.否则` 必须用 `.如果` + `.如果结束`**
2. **没有 `.否则` 必须用 `.如果真` + `.如果真结束`**
3. **不能混用！** `.如果真` 不能和 `.否则` 一起使用
4. **嵌套时每层都要遵守上述规则**

## 常见错误示例

❌ **错误：** 混用 `.如果真` 和 `.否则`
```
.如果真 (条件)
    ' 代码
.否则
    ' 代码
.如果真结束
```

✅ **正确：** 使用 `.如果` 和 `.否则`
```
.如果 (条件)
    ' 代码
.否则
    ' 代码
.如果结束
```

## 其他易语言编程规范

### 变量命名
- 使用中文或拼音命名变量
- 变量名要有意义，避免使用 a, b, c 等单字母

### 代码注释
- 使用 `'` 进行单行注释
- 重要逻辑必须添加注释说明

### 数组操作
- 数组下标从 1 开始（不是 0）
- 使用 `取数组成员数()` 获取数组长度
- 使用 `加入成员()` 添加数组元素
- 使用 `删除成员()` 删除数组元素

### API 调用
- 传址参数使用 `传址` 关键字
- 字符串参数注意编码（ANSI/Unicode）
- 结构体参数使用 `取变量数据地址()` 获取地址

### 内存操作
- 使用 `RtlMoveMemory` 进行内存复制
- 注意指针运算和偏移量
- 及时释放创建的 GDI 对象（画刷、画笔、字体等）

### 资源管理
- 创建的句柄要及时释放
- GDI 对象使用完要 `DeleteObject()`
- 窗口销毁时清理所有资源

### GDI+ 使用规范
- 使用前必须调用 `GdiplusStartup()` 初始化
- 使用完毕调用 `GdiplusShutdown()` 清理
- Graphics、Path、Brush 等对象用完要删除
- ARGB 颜色格式：0xAARRGGBB（Alpha, Red, Green, Blue）

### 常量使用规范
- **使用常量值时必须在前面加井号 `#`**
- 常量定义时不需要井号，使用时必须加井号

#### 常见错误示例

❌ **错误：** 使用常量时没有加井号
```
易语言Chrome版本 ＝ 取随机数 (CHROME_MIN_VERSION, CHROME_MAX_VERSION)
```

✅ **正确：** 使用常量时加井号
```
易语言Chrome版本 ＝ 取随机数 (#CHROME_MIN_VERSION, #CHROME_MAX_VERSION)
```

#### 常量定义和使用示例

```
' 定义常量（不需要井号）
.常量 CHROME_MIN_VERSION, 100
.常量 CHROME_MAX_VERSION, 120

' 使用常量（必须加井号）
易语言Chrome版本 ＝ 取随机数 (#CHROME_MIN_VERSION, #CHROME_MAX_VERSION)
.如果真 (易语言Chrome版本 ≥ #CHROME_MIN_VERSION)
    ' 执行代码
.如果真结束
```

在易语言的ide里Unicode特殊符号不支持的会显示问号，需要转换为字节集。
在易语言中，只要是经过ANSI编码，导致emoji变成问号，所有Unicode特殊符号都需要使用字节集

当编译C++的dll需要编译为32位，因为易语言只支持32位的dll