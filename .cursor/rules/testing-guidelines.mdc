---
alwaysApply: true
---

# 测试指南

## 测试语言优先级

本项目的测试应优先使用Python编写,原因如下:

1. **跨平台兼容性**: Python测试可以在不同平台上运行
2. **易于维护**: Python代码简洁易读,便于后续维护
3. **快速迭代**: Python无需编译,测试迭代速度快
4. **丰富的生态**: Python有丰富的测试和断言库

## 测试文件位置

所有Python测试文件应放置在: `examples/Python/`

### 命名规范

- 功能测试: `test_<功能名称>.py`
- 示例: `test_window_title.py`, `test_button_properties.py`

## 测试文件结构

```python
"""
测试<功能名称>
简要描述测试内容
"""
import ctypes
from ctypes import wintypes
import sys
import os

# 添加当前目录到路径
sys.path.insert(0, os.path.dirname(__file__))

# 加载DLL
try:
    dll = ctypes.CDLL('./emoji_window.dll')
except OSError:
    print("错误: 无法加载emoji_window.dll")
    sys.exit(1)

# 定义函数原型
# ... 函数声明 ...

def test_功能名称():
    """测试主函数"""
    print("=" * 60)
    print("测试<功能名称>")
    print("=" * 60)
    
    # 测试用例
    # ...
    
    return True  # 或 False

if __name__ == "__main__":
    success = test_功能名称()
    sys.exit(0 if success else 1)
```

## 测试要求

1. **完整性**: 测试应覆盖正常情况、边界情况和异常情况
2. **清晰的输出**: 使用清晰的打印信息,标明测试步骤和结果
3. **返回值**: 测试成功返回0,失败返回1
4. **资源清理**: 确保测试后正确清理资源(如销毁窗口)

## 测试用例示例

每个功能至少应包含以下测试:

1. **正常功能测试**: 验证基本功能是否正常工作
2. **边界测试**: 测试空值、最大值、最小值等边界情况
3. **错误处理测试**: 测试无效参数、NULL指针等错误情况
4. **编码测试**: 对于文本功能,测试UTF-8、中文、emoji等
5. **两次调用模式测试**: 对于返回文本的函数,测试两次调用模式

## C++测试的使用场景

仅在以下情况下使用C++测试:

1. 需要测试底层性能
2. 需要测试特定的C++ API行为
3. Python无法方便地测试的场景

C++测试文件应放在项目根目录,命名为 `test_<功能名称>.cpp`

## 运行测试

```bash
# Python测试
cd examples/Python
python test_window_title.py

# 或使用批处理
cd examples/Python
run.bat
```

## 测试文档

每个测试文件应在文件头部包含:
- 测试目的
- 测试的功能点
- 预期结果
- 特殊说明(如果有)
