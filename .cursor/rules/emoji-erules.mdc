---
alwaysApply: true
---

# 易语言 Emoji 表情使用注意事项

## 问题

易语言 IDE 使用 ANSI 编码，不支持 Unicode 特殊符号。代码中直接写 emoji（如 `"🎨"`）在 IDE 里会显示为问号 `?`，编译后传给 DLL 的也是问号，emoji 丢失。

## 解决方案

将 emoji 以 UTF-8 原始字节集的形式硬编码，中文部分用 `编码_Ansi到Utf8()` 转换，两者用 `＋` 拼接。

### 写法对比

```
' ❌ 错误 - emoji 经过 ANSI 编码会变成问号
utf8字节集 = 到UTF8字节集 ("🎨 自定义主题")

' ✅ 正确 - emoji 用字节集，中文用编码转换，拼接
utf8字节集 = { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 ("自定义主题")
'              🎨 emoji UTF-8字节    空格
```

### 拼接规则

| 部分 | 处理方式 | 示例 |
|------|---------|------|
| Emoji | 直接写 UTF-8 字节数组 `{ ... }` | `{ 240, 159, 142, 168 }` |
| 空格 | 字节 `32` | 放在 emoji 字节集末尾 |
| 中文/ASCII 文本 | `编码_Ansi到Utf8("文本")` | `编码_Ansi到Utf8 ("自定义主题")` |
| 拼接 | 用 `＋` 连接字节集 | `emoji字节集 ＋ 文本字节集` |

### 窗口标题也需要字节集

`创建Emoji窗口()` 的标题参数是文本型（ANSI），emoji 同样会丢失。使用 `创建Emoji窗口_字节集()` 代替：

```
' ❌ 错误 - 标题中的 emoji 会变问号
窗口句柄 = 创建Emoji窗口 ("主题切换示例 🎨", 600, 500)

' ✅ 正确 - 用字节集版本
utf8字节集 = 编码_Ansi到Utf8 ("主题切换示例 ") ＋ { 240, 159, 142, 168 }
窗口句柄 = 创建Emoji窗口_字节集 (取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集), 600, 500)
```

## 常用 Emoji UTF-8 字节对照表

| Emoji | 字节集 | 说明 |
|-------|--------|------|
| 🎨 | `{ 240, 159, 142, 168 }` | 调色板 |
| ☀️ | `{ 226, 152, 128, 239, 184, 143 }` | 太阳（含变体选择符） |
| 🌙 | `{ 240, 159, 140, 153 }` | 月亮 |
| 📢 | `{ 240, 159, 147, 162 }` | 喇叭 |
| 🎯 | `{ 240, 159, 142, 175 }` | 靶心 |
| 🔄 | `{ 240, 159, 148, 132 }` | 循环箭头 |
| 📐 | `{ 240, 159, 147, 144 }` | 三角尺 |
| ✅ | `{ 226, 156, 133 }` | 勾选 |
| 😀 | `{ 240, 159, 152, 128 }` | 笑脸 |
| ➕ | `{ 226, 158, 149 }` | 加号 |
| ❌ | `{ 226, 157, 140 }` | 叉号 |
| 📁 | `{ 240, 159, 147, 129 }` | 文件夹 |
| 📷 | `{ 240, 159, 147, 183 }` | 相机 |
| 🗑️ | `{ 240, 159, 151, 145, 239, 184, 143 }` | 垃圾桶（含变体选择符） |

> 注释中保留 emoji 原文方便阅读，如 `' "🎨 自定义主题"`
