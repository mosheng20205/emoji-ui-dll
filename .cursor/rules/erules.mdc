---
alwaysApply: true
---

<!------------------------------------------------------------------------------------
   Add rules to this file or a short description and have Kiro refine them for you.
   
   Learn about inclusion modes: https://kiro.dev/docs/steering/#inclusion-modes
-------------------------------------------------------------------------------------> 

## 不要使用`取字节集指针`命令，这个是错的
正确的命令：取变量数据地址
### 错误的示例：
```
标签_演示 = 创建标签 (主窗口句柄, 50, 30, 700, 60, 取字节集指针 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 255, 255, 255), 到ARGB (255, 70, 130, 180), 取文本指针 ("微软雅黑"), 取文本字节数 ("微软雅黑"), 16, 真, 假, 假, 1, 假)
```
### 正确的示例：
```
标签_演示 ＝ 创建标签 (主窗口句柄, 50, 30, 700, 60, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 255, 255, 255), 到ARGB (255, 70, 130, 180), 取文本指针 (“微软雅黑”), 取文本字节数 (“微软雅黑”), 16, 真, 假, 假, 1, 假)
```

## 不要使用`取字节集数据地址`命令，这个是错的
正确的命令：取变量数据地址
### 错误的示例：
```
获取标签字体 (标签_演示, 取字节集数据地址 (字体名缓冲区), 字体名长度, 字体大小, 粗体, 斜体, 下划线)
```
### 正确的示例：
```
获取标签字体 (标签_演示, 取变量数据地址 (字体名缓冲区), 字体名长度, 字体大小, 粗体, 斜体, 下划线)
```

## 本项目按钮点击回调
### 参考文档：docs\controls\button.md
### 错误的示例：
```
.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
```
### 正确的用法：
```
.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
```

#### 易语言没有 位左移 这个命令，正确的命令是 左移

## 按钮ID必须保存到程序集变量，不能用硬编码数字判断

`创建Emoji按钮_字节集` 返回的按钮ID是动态分配的，不保证从1开始连续递增。在回调中用固定数字（如 `按钮ID ＝ 1`）判断会导致按钮点击无响应。

### 错误的示例：
```
' 创建时丢弃返回值
创建Emoji按钮_字节集 (主窗口句柄, ...)

' 回调中用硬编码数字判断
.判断开始 (按钮ID ＝ 1)
    ' 处理逻辑
.判断 (按钮ID ＝ 2)
    ' 处理逻辑
.判断结束
```

### 正确的示例：
```
' 程序集变量保存按钮ID
.程序集变量 按钮_获取文本, 整数型
.程序集变量 按钮_获取字体, 整数型

' 创建时保存返回值
按钮_获取文本 ＝ 创建Emoji按钮_字节集 (主窗口句柄, ...)
按钮_获取字体 ＝ 创建Emoji按钮_字节集 (主窗口句柄, ...)

' 回调中用程序集变量判断
.判断开始 (按钮ID ＝ 按钮_获取文本)
    ' 处理逻辑
.判断 (按钮ID ＝ 按钮_获取字体)
    ' 处理逻辑
.判断结束
```

## 易语言符号必须使用全角

易语言中赋值、运算、比较等符号必须使用全角字符，半角符号会导致语法错误。

| 用途 | 错误（半角） | 正确（全角） |
|------|------------|------------|
| 赋值/等于 | `=` | `＝` |
| 加法/拼接 | `+` | `＋` |
| 大于 | `>` | `＞` |
| 小于 | `<` | `＜` |
| 大于等于 | `>=` | `≥` |
| 小于等于 | `<=` | `≤` |
| 不等于 | `!=` | `≠` |

## 易语言条件判断语法

易语言的条件判断有两种形式，不能混用：

### `.如果` / `.如果结束`（支持 `.否则`）
```
.如果 (文本长度 ＞ 0)
    ' 条件为真时执行
.否则
    ' 条件为假时执行
.如果结束
```

### `.如果真` / `.如果真结束`（不支持 `.否则`）
```
.如果真 (文本长度 ＞ 0)
    ' 仅条件为真时执行，无法写否则分支
.如果真结束
```

需要 `.否则` 分支时必须用 `.如果` / `.如果结束`，不能用 `.如果真`。

## DLL返回UTF-8字节集时，不能用`到文本()`转换，必须全程用字节集拼接

`设置标签文本`、`设置按钮文本` 等接受 UTF-8 字节集指针的函数，显示内容必须是 UTF-8 字节集。
DLL 返回的文本缓冲区（如 `获取标签文本`、`获取标签字体` 的缓冲区）本身就是 UTF-8 字节集，**不能用 `到文本()` 或 `编码_Utf8到Ansi()` 转换**，否则中文和 emoji 会变成乱码菱形。

正确做法：结果变量声明为**字节集**，所有字符串字面量用 `编码_Ansi到Utf8()` 转换后拼接，数字用 `编码_Ansi到Utf8(到文本(...))` 转换，最后用 `取变量数据地址` 传给显示函数。

### 错误的示例：
```
' ❌ 到文本() 按ANSI解释UTF-8字节，中文/emoji变乱码
文本内容 = 到文本 (文本缓冲区)
结果信息 = "文本内容：" + 文本内容
设置标签文本 (标签_结果, 取文本指针 (结果信息), 取文本字节数 (结果信息))

' ❌ 编码_Utf8到Ansi 同样会导致乱码
文本内容 = 到文本 (编码_Utf8到Ansi (文本缓冲区))
```

### 正确的示例：
```
' ✅ 结果声明为字节集，全程UTF-8拼接
.局部变量 结果字节集, 字节集

' DLL返回的缓冲区直接参与拼接，不做任何转换
结果字节集 = 编码_Ansi到Utf8 ("文本内容：") ＋ 文本缓冲区 ＋ { 10 } ＋ 编码_Ansi到Utf8 ("字节长度：") ＋ 编码_Ansi到Utf8 (到文本 (文本长度))

' 用取变量数据地址传给设置标签文本
设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
```

### 换行符用字节 `{ 10 }`，不用 `#换行符`
在字节集拼接中，换行用 `{ 10 }` 直接插入，不能用易语言的 `#换行符` 常量（那是文本型）。
