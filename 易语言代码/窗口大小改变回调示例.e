.版本 2

.程序集 窗口程序集_窗口大小改变示例

.子程序 _按钮被单击, , 公开
.参数 按钮ID, 整数型

    调试输出 ("按钮被点击: " + 到文本 (按钮ID))


.子程序 _窗口大小改变, , 公开
.参数 窗口句柄, 整数型
.参数 新宽度, 整数型
.参数 新高度, 整数型

    调试输出 ("窗口大小改变: " + 到文本 (新宽度) + " x " + 到文本 (新高度))
    
    ' 调整 TabControl 大小以适应新窗口（距离边缘 10 像素）
    .如果真 (Tab控件 ≠ 0)
        ' 注意：这里需要实现 SetWindowPos 来调整 TabControl 大小
        ' 或者使用 Windows API MoveWindow
        MoveWindow (Tab控件, 10, 10, 新宽度 - 20, 新高度 - 20, 真)
    .如果真结束
    
    ' 调整编辑框大小
    .如果真 (编辑框1 ≠ 0)
        设置编辑框位置 (编辑框1, 30, 30, 新宽度 - 60, 40)
    .如果真结束
    
    ' 调整标签位置
    .如果真 (标签1 ≠ 0)
        设置标签位置 (标签1, 30, 80, 新宽度 - 60, 30)
    .如果真结束


.子程序 __启动窗口_创建完毕

    主窗口句柄 = 创建Emoji窗口 ("窗口大小改变示例", 800, 600)
    
    ' 设置回调函数
    设置按钮点击回调 (&_按钮被单击)
    设置窗口大小改变回调 (&_窗口大小改变)
    
    ' 创建 TabControl
    Tab控件 = 创建TabControl (主窗口句柄, 10, 10, 780, 550)
    
    ' 添加 Tab 页
    .局部变量 标题, 字节集
    .局部变量 Tab1内容, 整数型
    
    标题 = 到UTF8 ("首页")
    Tab1内容 = 添加Tab页 (Tab控件, 取变量数据地址 (标题), 取字节集长度 (标题), 0)
    
    ' 在 Tab1 中创建控件
    .局部变量 文本, 字节集
    .局部变量 字体名, 字节集
    
    文本 = 到UTF8 ("用户名：")
    字体名 = 到UTF8 ("微软雅黑")
    标签1 = 创建标签 (Tab1内容, 30, 30, 100, 30, 取变量数据地址 (文本), 取字节集长度 (文本), 4281545011, 4294967295, 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 2)
    
    文本 = 到UTF8 ("请输入用户名")
    编辑框1 = 创建编辑框 (Tab1内容, 140, 30, 280, 40, 取变量数据地址 (文本), 取字节集长度 (文本), 4281545011, 4294967295, 取变量数据地址 (字体名), 取字节集长度 (字体名), 14, 假, 假, 假, 0, 假, 假, 假, 真)
    
    ' 创建一个按钮
    .局部变量 Emoji, 字节集
    Emoji = { 240, 159, 152, 128 }  ' 😀
    文本 = 到UTF8 ("测试按钮")
    创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (Emoji), 取字节集长度 (Emoji), 取变量数据地址 (文本), 取字节集长度 (文本), 50, 50, 150, 50, 4287201082)
    
    ' 运行消息循环
    运行消息循环 ()


.子程序 到UTF8, 字节集, 公开
.参数 文本内容, 文本型
.局部变量 宽字符长度, 整数型
.局部变量 宽字符缓冲, 字节集
.局部变量 UTF8长度, 整数型
.局部变量 UTF8缓冲, 字节集

    ' 第一步：文本型转宽字符
    宽字符长度 = MultiByteToWideChar (0, 0, 文本内容, -1, { }, 0)
    .如果真 (宽字符长度 ≤ 0)
        返回 ({ })
    .如果真结束
    
    宽字符缓冲 = 取空白字节集 (宽字符长度 × 2)
    MultiByteToWideChar (0, 0, 文本内容, -1, 宽字符缓冲, 宽字符长度)
    
    ' 第二步：宽字符转UTF-8
    UTF8长度 = WideCharToMultiByte_整数 (65001, 0, 取变量数据地址 (宽字符缓冲), 宽字符长度 - 1, 0, 0, 0, 0)
    .如果真 (UTF8长度 ≤ 0)
        返回 ({ })
    .如果真结束
    
    UTF8缓冲 = 取空白字节集 (UTF8长度)
    WideCharToMultiByte_整数 (65001, 0, 取变量数据地址 (宽字符缓冲), 宽字符长度 - 1, 取变量数据地址 (UTF8缓冲), UTF8长度, 0, 0)
    
    返回 (UTF8缓冲)


.子程序 MoveWindow, 逻辑型
.参数 窗口句柄, 整数型
.参数 X, 整数型
.参数 Y, 整数型
.参数 宽度, 整数型
.参数 高度, 整数型
.参数 重绘, 逻辑型

    ' 这是一个 Windows API 函数，需要在 DLL 命令中声明
    ' 或者直接使用易语言的 API 声明
    返回 (假)

