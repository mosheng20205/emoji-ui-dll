.版本 2
.支持库 spec

.程序集 辅助程序集

.子程序 创建复选框_辅助, 整数型, 公开
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 文本, 文本型
.参数 选中, 逻辑型
.参数 前景色, 整数型
.参数 背景色, 整数型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取变量数据地址 (文本字节集)

返回 (创建复选框 (父窗口, x, y, 宽, 高, 文本指针, 取字节集长度 (文本字节集), 选中, 前景色, 背景色))

.子程序 设置编辑框垂直居中, , "emoji_window.dll", "SetEditBoxVerticalCenter", , , 设置编辑框文本是否垂直居中（仅单行有效）
    .参数 编辑框句柄, 整数型
    .参数 垂直居中, 逻辑型

.子程序 设置编辑框按键回调, , "emoji_window.dll", "SetEditBoxKeyCallback", , , 设置编辑框按键回调（key_down: 1=按下 0=松开）
    .参数 编辑框句柄, 整数型
    .参数 回调子程序指针, 整数型, , 子程序需 stdcall，参数：hEdit, key_code, key_down, shift, ctrl, alt（均为整数型）

.子程序 创建彩色Emoji编辑框_辅助, 整数型, 公开, 创建支持彩色Emoji的编辑框（使用RichEdit控件）
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 文本, 文本型, 可空
.参数 前景色, 整数型
.参数 背景色, 整数型
.参数 字体名, 文本型, 可空, 留空则是 Microsoft YaHei UI
.参数 字体大小, 整数型, 可空, 留空则是12
.参数 粗体, 逻辑型, 可空
.参数 斜体, 逻辑型, 可空
.参数 下划线, 逻辑型, 可空
.参数 对齐, 整数型, 可空, 0=左 1=中 2=右
.参数 多行, 逻辑型, 可空
.参数 只读, 逻辑型, 可空
.参数 密码, 逻辑型, 可空
.参数 边框, 逻辑型, 可空
.参数 垂直居中, 逻辑型, 可空, 仅单行有效
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型
.局部变量 字体字节集, 字节集
.局部变量 字体指针, 整数型

.如果真 (字体名 ＝ "")
    字体名 ＝ "Microsoft YaHei UI"
.如果真结束
.如果真 (字体大小 ＝ 0)
    字体大小 ＝ 12
.如果真结束

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取变量数据地址 (文本字节集)

字体字节集 ＝ 编码_Ansi到Utf8 (字体名)
字体指针 ＝ 取变量数据地址 (字体字节集)

返回 (创建彩色Emoji编辑框 (父窗口, x, y, 宽, 高, 文本指针, 取字节集长度 (文本字节集), 前景色, 背景色, 字体指针, 取字节集长度 (字体字节集), 字体大小, 粗体, 斜体, 下划线, 对齐, 多行, 只读, 密码, 边框, 垂直居中))

.子程序 创建标签_辅助, 整数型, 公开
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 文本, 文本型
.参数 前景色, 整数型
.参数 背景色, 整数型
.参数 字体名, 文本型, 可空, 留空则是 Microsoft YaHei UI
.参数 字体大小, 整数型, 可空, 留空则是12
.参数 粗体, 逻辑型, 可空
.参数 斜体, 逻辑型, 可空
.参数 下划线, 逻辑型, 可空
.参数 对齐, 整数型, 可空, 0=左 1=中 2=右
.参数 是否换行, 逻辑型, 可空
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型
.局部变量 字体字节集, 字节集
.局部变量 字体指针, 整数型

.如果真 (字体名 ＝ “”)
    字体名 ＝ “Microsoft YaHei UI”
.如果真结束
.如果真 (字体大小 ＝ 0)
    字体大小 ＝ 12
.如果真结束
文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取变量数据地址 (文本字节集)

字体字节集 ＝ 编码_Ansi到Utf8 (字体名)
字体指针 ＝ 取变量数据地址 (字体字节集)

返回 (创建标签 (父窗口, x, y, 宽, 高, 文本指针, 取字节集长度 (文本字节集), 前景色, 背景色, 字体指针, 取字节集长度 (字体字节集), 字体大小, 粗体, 斜体, 下划线, 对齐, 是否换行))

.子程序 设置标签文本_辅助, , 公开
.参数 标签句柄, 整数型
.参数 文本, 文本型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取变量数据地址 (文本字节集)

设置标签文本 (标签句柄, 文本指针, 取字节集长度 (文本字节集))

.子程序 创建Emoji按钮_辅助, 整数型, 公开
.参数 父窗口, 整数型
.参数 emoji, 文本型
.参数 文本, 文本型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 背景色, 整数型
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 emoji指针, 整数型
.局部变量 文本指针, 整数型

emoji字节集 ＝ 编码_Ansi到Utf8 (emoji)
文本字节集 ＝ 编码_Ansi到Utf8 (文本)
emoji指针 ＝ 取变量数据地址 (emoji字节集)
文本指针 ＝ 取变量数据地址 (文本字节集)

返回 (创建Emoji按钮_字节集 (父窗口, emoji指针, 取字节集长度 (emoji字节集), 文本指针, 取字节集长度 (文本字节集), x, y, 宽, 高, 背景色))

.子程序 创建Emoji按钮_字节集辅助, 整数型, 公开, 使用字节集参数创建Emoji按钮
.参数 父窗口, 整数型
.参数 emoji字节集, 字节集
.参数 文本, 文本型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 背景色, 整数型
.局部变量 文本字节集, 字节集
.局部变量 emoji指针, 整数型
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
emoji指针 ＝ 取变量数据地址 (emoji字节集)
文本指针 ＝ 取变量数据地址 (文本字节集)

返回 (创建Emoji按钮_字节集 (父窗口, emoji指针, 取字节集长度 (emoji字节集), 文本指针, 取字节集长度 (文本字节集), x, y, 宽, 高, 背景色))

.子程序 设置标签文本_UTF8
.参数 _标签句柄, 整数型
.参数 _文本值, 文本型
.局部变量 文本字节集, 字节集

文本字节集 ＝ 编码_Ansi到Utf8 (_文本值)

设置标签文本 (_标签句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))


.子程序 Utf8到文本, 文本型, 公开
.参数 utf8字节集, 字节集

返回 (编码_Utf8到Ansi (utf8字节集))

.子程序 从文件加载图片_辅助, 逻辑型, 公开
.参数 图片框句柄, 整数型
.参数 文件路径, 文本型
.局部变量 路径字节集, 字节集
.局部变量 路径指针, 整数型

路径字节集 ＝ 编码_Ansi到Utf8 (文件路径)
路径指针 ＝ 取变量数据地址 (路径字节集)

返回 (从文件加载图片 (图片框句柄, 路径指针, 取字节集长度 (路径字节集)))

.子程序 创建单选按钮_辅助, 整数型, , 创建单选按钮的辅助函数
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 文本, 文本型
.参数 分组ID, 整数型
.参数 选中, 逻辑型
.参数 前景色, 整数型
.参数 背景色, 整数型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取字节集数据地址 (文本字节集)

返回 (创建单选按钮 (父窗口, x, y, 宽, 高, 文本指针, 取字节集长度 (文本字节集), 分组ID, 选中, 前景色, 背景色))

.子程序 取字节集数据地址, 整数型
.参数 文本字节集, 字节集

返回 (取变量数据地址 (文本字节集))


.子程序 创建分组框_辅助, 整数型, , 创建分组框的辅助函数
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 标题, 文本型
.参数 边框颜色, 整数型
.参数 背景色, 整数型
.局部变量 标题字节集, 字节集
.局部变量 标题指针, 整数型

标题字节集 ＝ 编码_Ansi到Utf8 (标题)
标题指针 ＝ 取字节集数据地址 (标题字节集)

返回 (创建分组框 (父窗口, x, y, 宽, 高, 标题指针, 取字节集长度 (标题字节集), 边框颜色, 背景色))

.子程序 设置分组框标题_辅助, , , 设置分组框标题的辅助函数
.参数 分组框句柄, 整数型
.参数 标题, 文本型
.局部变量 标题字节集, 字节集
.局部变量 标题指针, 整数型

标题字节集 ＝ 编码_Ansi到Utf8 (标题)
标题指针 ＝ 取字节集数据地址 (标题字节集)

设置分组框标题 (分组框句柄, 标题指针, 取字节集长度 (标题字节集))

.子程序 创建组合框_辅助, 整数型, , 创建组合框的辅助函数（增强版）
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 只读模式, 逻辑型
.参数 前景色, 整数型
.参数 背景色, 整数型
.参数 表项高度, 整数型, 可空, 默认35
.参数 字体名, 文本型, 可空, 默认Microsoft YaHei UI
.参数 字体大小, 整数型, 可空, 默认14
.参数 粗体, 逻辑型, 可空
.参数 斜体, 逻辑型, 可空
.参数 下划线, 逻辑型, 可空
.局部变量 字体字节集, 字节集
.局部变量 字体指针, 整数型
.局部变量 空字节集, 字节集

.如果真 (表项高度 ＝ 0)
    表项高度 ＝ 35
.如果真结束
.如果真 (字体名 ＝ "")
    字体名 ＝ "Microsoft YaHei UI"
.如果真结束
.如果真 (字体大小 ＝ 0)
    字体大小 ＝ 14
.如果真结束

字体字节集 ＝ 编码_Ansi到Utf8 (字体名)
字体指针 ＝ 取字节集数据地址 (字体字节集)

返回 (创建组合框 (父窗口, x, y, 宽, 高, 只读模式, 前景色, 背景色, 表项高度, 字体指针, 取字节集长度 (字体字节集), 字体大小, 粗体, 斜体, 下划线))

.子程序 添加组合框项_辅助, 整数型, , 添加组合框项的辅助函数
.参数 组合框句柄, 整数型
.参数 文本, 文本型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取字节集数据地址 (文本字节集)

返回 (添加组合框项 (组合框句柄, 文本指针, 取字节集长度 (文本字节集)))

.子程序 添加组合框项_字节集, 整数型, 公开, 使用UTF-8字节集添加组合框项（支持emoji）
.参数 组合框句柄, 整数型
.参数 文本字节集, 字节集, , UTF-8编码的文本
.局部变量 文本指针, 整数型

文本指针 ＝ 取字节集数据地址 (文本字节集)

返回 (添加组合框项 (组合框句柄, 文本指针, 取字节集长度 (文本字节集)))

.子程序 获取组合框项文本_辅助, 文本型, , 获取组合框项文本的辅助函数
.参数 组合框句柄, 整数型
.参数 索引, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型

缓冲区 ＝ 取空白字节集 (1024)
实际长度 ＝ 获取组合框项文本 (组合框句柄, 索引, 取变量数据地址 (缓冲区), 1024)
.如果真 (实际长度 ＞ 0)
    返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度)))
.如果真结束
返回 ("")

.子程序 获取组合框文本_辅助, 文本型, , 获取组合框编辑框文本的辅助函数
.参数 组合框句柄, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型

缓冲区 ＝ 取空白字节集 (1024)
实际长度 ＝ 获取组合框文本 (组合框句柄, 取变量数据地址 (缓冲区), 1024)
.如果真 (实际长度 ＞ 0)
    返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度)))
.如果真结束
返回 ("")

.子程序 设置组合框文本_辅助, , , 设置组合框编辑框文本的辅助函数
.参数 组合框句柄, 整数型
.参数 文本, 文本型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取字节集数据地址 (文本字节集)

设置组合框文本 (组合框句柄, 文本指针, 取字节集长度 (文本字节集))

.子程序 创建热键控件_辅助, 整数型, , 创建热键控件的辅助函数
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 虚拟键码, 整数型, , 初始热键虚拟键码（0=不设置）
.参数 Ctrl, 逻辑型, , 是否按下Ctrl
.参数 Shift, 逻辑型, , 是否按下Shift
.参数 Alt, 逻辑型, , 是否按下Alt
.参数 前景色, 整数型
.参数 背景色, 整数型
.局部变量 句柄, 整数型

句柄 ＝ 创建热键控件 (父窗口, x, y, 宽, 高, 前景色, 背景色)
.如果真 (句柄 ≠ 0 且 虚拟键码 ≠ 0)
    .局部变量 修饰键, 整数型
    修饰键 ＝ 0
    .如果真 (Ctrl)
        修饰键 ＝ 修饰键 ＋ 1
    .如果真结束
    .如果真 (Shift)
        修饰键 ＝ 修饰键 ＋ 2
    .如果真结束
    .如果真 (Alt)
        修饰键 ＝ 修饰键 ＋ 4
    .如果真结束
    设置热键值 (句柄, 虚拟键码, 修饰键)
.如果真结束
返回 (句柄)

.子程序 信息框_辅助, , 公开, 显示支持Unicode emoji的信息框（直接使用UTF-8字节集）
.参数 父窗口, 整数型
.参数 标题字节集, 字节集, , UTF-8编码的标题
.参数 消息字节集, 字节集, , UTF-8编码的消息
.参数 图标字节集, 字节集, 可空, UTF-8编码的emoji图标（如"ℹ️"、"✅"、"⚠️"、"❌"）

信息提示框_ (父窗口, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (消息字节集), 取字节集长度 (消息字节集), 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))

.子程序 确认框_辅助, , 公开, 显示支持Unicode emoji的确认框（直接使用UTF-8字节集）
.参数 父窗口, 整数型
.参数 标题字节集, 字节集, , UTF-8编码的标题
.参数 消息字节集, 字节集, , UTF-8编码的消息
.参数 图标字节集, 字节集, 可空, UTF-8编码的emoji图标（如"❓"、"⚠️"）
.参数 回调函数, 子程序指针, , 确认回调函数

确认框_ (父窗口, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (消息字节集), 取字节集长度 (消息字节集), 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集), 回调函数)


.子程序 创建D2D彩色Emoji编辑框_辅助, 整数型, 公开, 创建支持彩色Emoji的D2D自定义编辑框（完美支持彩色emoji）
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 文本, 文本型, 可空
.参数 前景色, 整数型
.参数 背景色, 整数型
.参数 字体名, 文本型, 可空, 留空则是 Microsoft YaHei UI
.参数 字体大小, 整数型, 可空, 留空则是12
.参数 粗体, 逻辑型, 可空
.参数 斜体, 逻辑型, 可空
.参数 下划线, 逻辑型, 可空
.参数 对齐, 整数型, 可空, 0=左 1=中 2=右
.参数 多行, 逻辑型, 可空
.参数 只读, 逻辑型, 可空
.参数 密码, 逻辑型, 可空
.参数 边框, 逻辑型, 可空
.参数 垂直居中, 逻辑型, 可空, 仅单行有效
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型
.局部变量 字体字节集, 字节集
.局部变量 字体指针, 整数型

.如果真 (字体名 ＝ "")
    字体名 ＝ "Microsoft YaHei UI"
.如果真结束
.如果真 (字体大小 ＝ 0)
    字体大小 ＝ 12
.如果真结束

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取变量数据地址 (文本字节集)

字体字节集 ＝ 编码_Ansi到Utf8 (字体名)
字体指针 ＝ 取变量数据地址 (字体字节集)

返回 (创建D2D彩色Emoji编辑框 (父窗口, x, y, 宽, 高, 文本指针, 取字节集长度 (文本字节集), 前景色, 背景色, 字体指针, 取字节集长度 (字体字节集), 字体大小, 粗体, 斜体, 下划线, 对齐, 多行, 只读, 密码, 边框, 垂直居中))

.子程序 获取D2D编辑框文本_辅助, 文本型, 公开, 获取D2D编辑框文本的辅助函数
.参数 编辑框句柄, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型

' 先获取所需缓冲区大小
实际长度 ＝ 获取D2D编辑框文本 (编辑框句柄, 0, 0)
.如果真 (实际长度 ＞ 0)
    缓冲区 ＝ 取空白字节集 (实际长度 ＋ 1)
    实际长度 ＝ 获取D2D编辑框文本 (编辑框句柄, 取变量数据地址 (缓冲区), 实际长度 ＋ 1)
    返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度)))
.如果真结束
返回 ("")

.子程序 设置D2D编辑框文本_辅助, , 公开, 设置D2D编辑框文本的辅助函数
.参数 编辑框句柄, 整数型
.参数 文本, 文本型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取字节集数据地址 (文本字节集)

设置D2D编辑框文本 (编辑框句柄, 文本指针, 取字节集长度 (文本字节集))

.子程序 获取D2D编辑框文本_字节集, 字节集, 公开, 获取D2D编辑框文本（返回UTF-8字节集，避免emoji变问号）
.参数 编辑框句柄, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型

' 先获取所需缓冲区大小
实际长度 ＝ 获取D2D编辑框文本 (编辑框句柄, 0, 0)
.如果真 (实际长度 ＞ 0)
    缓冲区 ＝ 取空白字节集 (实际长度 ＋ 1)
    实际长度 ＝ 获取D2D编辑框文本 (编辑框句柄, 取变量数据地址 (缓冲区), 实际长度 ＋ 1)
    返回 (取字节集左边 (缓冲区, 实际长度))
.如果真结束
返回 ({ })

.子程序 设置标签文本_辅助_字节集, , 公开, 使用UTF-8字节集设置标签文本（支持emoji）
.参数 标签句柄, 整数型
.参数 文本字节集, 字节集, , UTF-8编码的文本
.局部变量 文本指针, 整数型

文本指针 ＝ 取字节集数据地址 (文本字节集)

设置标签文本 (标签句柄, 文本指针, 取字节集长度 (文本字节集))


.子程序 创建D2D组合框_辅助, 整数型, 公开, 创建D2D组合框的辅助函数（支持彩色emoji）
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 只读模式, 逻辑型
.参数 前景色, 整数型
.参数 背景色, 整数型
.参数 表项高度, 整数型, 可空, 默认35
.参数 字体名, 文本型, 可空, 默认Microsoft YaHei UI
.参数 字体大小, 整数型, 可空, 默认14
.参数 粗体, 逻辑型, 可空
.参数 斜体, 逻辑型, 可空
.参数 下划线, 逻辑型, 可空
.局部变量 字体字节集, 字节集
.局部变量 字体指针, 整数型

.如果真 (表项高度 ＝ 0)
    表项高度 ＝ 35
.如果真结束
.如果真 (字体名 ＝ "")
    字体名 ＝ "Microsoft YaHei UI"
.如果真结束
.如果真 (字体大小 ＝ 0)
    字体大小 ＝ 14
.如果真结束

字体字节集 ＝ 编码_Ansi到Utf8 (字体名)
字体指针 ＝ 取字节集数据地址 (字体字节集)

返回 (创建D2D组合框 (父窗口, x, y, 宽, 高, 只读模式, 前景色, 背景色, 表项高度, 字体指针, 取字节集长度 (字体字节集), 字体大小, 粗体, 斜体, 下划线))

.子程序 添加D2D组合框项_辅助, 整数型, 公开, 添加D2D组合框项的辅助函数
.参数 组合框句柄, 整数型
.参数 文本, 文本型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取字节集数据地址 (文本字节集)

返回 (添加D2D组合框项 (组合框句柄, 文本指针, 取字节集长度 (文本字节集)))

.子程序 获取D2D组合框项文本_辅助, 文本型, 公开, 获取D2D组合框项文本的辅助函数
.参数 组合框句柄, 整数型
.参数 索引, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型

缓冲区 ＝ 取空白字节集 (1024)
实际长度 ＝ 获取D2D组合框项文本 (组合框句柄, 索引, 取变量数据地址 (缓冲区), 1024)
.如果真 (实际长度 ＞ 0)
    返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度)))
.如果真结束
返回 ("")

.子程序 获取D2D组合框文本_辅助, 文本型, 公开, 获取D2D组合框编辑框文本的辅助函数
.参数 组合框句柄, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型

缓冲区 ＝ 取空白字节集 (1024)
实际长度 ＝ 获取D2D组合框文本 (组合框句柄, 取变量数据地址 (缓冲区), 1024)
.如果真 (实际长度 ＞ 0)
    返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度)))
.如果真结束
返回 ("")

.子程序 设置D2D组合框文本_辅助, , 公开, 设置D2D组合框编辑框文本的辅助函数
.参数 组合框句柄, 整数型
.参数 文本, 文本型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取字节集数据地址 (文本字节集)

设置D2D组合框文本 (组合框句柄, 文本指针, 取字节集长度 (文本字节集))

' ========== DataGridView 辅助函数 ==========

.子程序 表格_添加文本列_辅助, 整数型, 公开, 添加文本列（文本型参数）
.参数 表格句柄, 整数型
.参数 列头文本, 文本型
.参数 列宽, 整数型
.局部变量 字节集, 字节集
.局部变量 指针, 整数型

字节集 ＝ 编码_Ansi到Utf8 (列头文本)
指针 ＝ 取变量数据地址 (字节集)

返回 (表格_添加文本列 (表格句柄, 指针, 取字节集长度 (字节集), 列宽))

.子程序 表格_添加复选框列_辅助, 整数型, 公开, 添加复选框列（文本型参数）
.参数 表格句柄, 整数型
.参数 列头文本, 文本型
.参数 列宽, 整数型
.局部变量 字节集, 字节集
.局部变量 指针, 整数型

字节集 ＝ 编码_Ansi到Utf8 (列头文本)
指针 ＝ 取变量数据地址 (字节集)

返回 (表格_添加复选框列 (表格句柄, 指针, 取字节集长度 (字节集), 列宽))

.子程序 表格_添加按钮列_辅助, 整数型, 公开, 添加按钮列（文本型参数）
.参数 表格句柄, 整数型
.参数 列头文本, 文本型
.参数 列宽, 整数型
.局部变量 字节集, 字节集
.局部变量 指针, 整数型

字节集 ＝ 编码_Ansi到Utf8 (列头文本)
指针 ＝ 取变量数据地址 (字节集)

返回 (表格_添加按钮列 (表格句柄, 指针, 取字节集长度 (字节集), 列宽))

.子程序 表格_添加链接列_辅助, 整数型, 公开, 添加链接列（文本型参数）
.参数 表格句柄, 整数型
.参数 列头文本, 文本型
.参数 列宽, 整数型
.局部变量 字节集, 字节集
.局部变量 指针, 整数型

字节集 ＝ 编码_Ansi到Utf8 (列头文本)
指针 ＝ 取变量数据地址 (字节集)

返回 (表格_添加链接列 (表格句柄, 指针, 取字节集长度 (字节集), 列宽))

.子程序 表格_设置单元格文本_辅助, , 公开, 设置单元格文本（文本型参数）
.参数 表格句柄, 整数型
.参数 行索引, 整数型
.参数 列索引, 整数型
.参数 文本, 文本型
.局部变量 字节集, 字节集
.局部变量 指针, 整数型

字节集 ＝ 编码_Ansi到Utf8 (文本)
指针 ＝ 取变量数据地址 (字节集)

表格_设置单元格文本 (表格句柄, 行索引, 列索引, 指针, 取字节集长度 (字节集))

.子程序 表格_设置单元格文本_字节集, , 公开, 设置单元格文本（UTF-8字节集，支持emoji）
.参数 表格句柄, 整数型
.参数 行索引, 整数型
.参数 列索引, 整数型
.参数 文本字节集, 字节集
.局部变量 指针, 整数型

指针 ＝ 取变量数据地址 (文本字节集)

表格_设置单元格文本 (表格句柄, 行索引, 列索引, 指针, 取字节集长度 (文本字节集))

.子程序 表格_获取单元格文本_辅助, 文本型, 公开, 获取单元格文本（返回文本型）
.参数 表格句柄, 整数型
.参数 行索引, 整数型
.参数 列索引, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型

' 先获取所需缓冲区大小
实际长度 ＝ 表格_获取单元格文本 (表格句柄, 行索引, 列索引, 0, 0)
.如果真 (实际长度 ＞ 0)
    缓冲区 ＝ 取空白字节集 (实际长度 ＋ 1)
    实际长度 ＝ 表格_获取单元格文本 (表格句柄, 行索引, 列索引, 取变量数据地址 (缓冲区), 实际长度 ＋ 1)
    返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度)))
.如果真结束
返回 ("")

.子程序 表格_导出CSV_辅助, 逻辑型, 公开, 导出CSV文件（文本型路径）
.参数 表格句柄, 整数型
.参数 文件路径, 文本型
.局部变量 路径字节集, 字节集
.局部变量 路径指针, 整数型

路径字节集 ＝ 编码_Ansi到Utf8 (文件路径)
路径指针 ＝ 取变量数据地址 (路径字节集)

返回 (表格_导出CSV (表格句柄, 路径指针, 取字节集长度 (路径字节集)))

.子程序 添加D2D组合框项_字节集, 整数型, 公开, 使用UTF-8字节集添加D2D组合框项（支持emoji）
.参数 组合框句柄, 整数型
.参数 文本字节集, 字节集, , UTF-8编码的文本
.局部变量 文本指针, 整数型

文本指针 ＝ 取字节集数据地址 (文本字节集)

返回 (添加D2D组合框项 (组合框句柄, 文本指针, 取字节集长度 (文本字节集)))

.子程序 创建标签_辅助_字节集, 整数型, 公开, 使用UTF-8字节集创建标签（支持emoji）
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 文本字节集, 字节集, , UTF-8编码的文本
.参数 前景色, 整数型
.参数 背景色, 整数型
.参数 字体名, 文本型, 可空, 留空则是 Microsoft YaHei UI
.参数 字体大小, 整数型, 可空, 留空则是12
.参数 粗体, 逻辑型, 可空
.参数 斜体, 逻辑型, 可空
.参数 下划线, 逻辑型, 可空
.参数 对齐, 整数型, 可空, 0=左 1=中 2=右
.参数 是否换行, 逻辑型, 可空
.局部变量 文本指针, 整数型
.局部变量 字体字节集, 字节集
.局部变量 字体指针, 整数型

.如果真 (字体名 ＝ "")
    字体名 ＝ "Microsoft YaHei UI"
.如果真结束
.如果真 (字体大小 ＝ 0)
    字体大小 ＝ 12
.如果真结束

文本指针 ＝ 取字节集数据地址 (文本字节集)

字体字节集 ＝ 编码_Ansi到Utf8 (字体名)
字体指针 ＝ 取变量数据地址 (字体字节集)

返回 (创建标签 (父窗口, x, y, 宽, 高, 文本指针, 取字节集长度 (文本字节集), 前景色, 背景色, 字体指针, 取字节集长度 (字体字节集), 字体大小, 粗体, 斜体, 下划线, 对齐, 是否换行))

.子程序 数组到文本, 文本型, 公开, 将文本型数组转换为文本，用指定分隔符连接
.参数 文本数组, 文本型, 数组
.参数 分隔符, 文本型, 可空, 默认为逗号
.局部变量 结果, 文本型
.局部变量 i, 整数型
.局部变量 数组长度, 整数型

.如果真 (分隔符 ＝ "")
    分隔符 ＝ ","
.如果真结束

数组长度 ＝ 取数组成员数 (文本数组)

.如果真 (数组长度 ＝ 0)
    返回 ("")
.如果真结束

结果 ＝ 文本数组 [1]

.计次循环首 (数组长度 － 1, i)
    结果 ＝ 结果 ＋ 分隔符 ＋ 文本数组 [i ＋ 1]
.计次循环尾 ()

返回 (结果)

.子程序 创建进度条_辅助, 整数型, 公开, 创建进度条的辅助函数
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 初始值, 整数型
.参数 前景色, 整数型
.参数 背景色, 整数型
.参数 显示文本, 逻辑型, 可空, 是否显示百分比文本，默认假
.参数 文本颜色, 整数型, 可空, ARGB颜色，默认白色

返回 (创建进度条 (父窗口, x, y, 宽, 高, 初始值, 前景色, 背景色, 显示文本, 文本颜色))

.子程序 创建列表框_辅助, 整数型, 公开, 创建列表框的辅助函数
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 前景色, 整数型
.参数 背景色, 整数型

返回 (创建列表框 (父窗口, x, y, 宽, 高, 假, 前景色, 背景色))

.子程序 添加列表框项目_辅助, 整数型, 公开, 添加列表框项目的辅助函数
.参数 列表框句柄, 整数型
.参数 文本, 文本型
.局部变量 文本字节集, 字节集
.局部变量 文本指针, 整数型

文本字节集 ＝ 编码_Ansi到Utf8 (文本)
文本指针 ＝ 取变量数据地址 (文本字节集)

返回 (添加列表框项 (列表框句柄, 文本指针, 取字节集长度 (文本字节集)))

.子程序 创建图片框_辅助, 整数型, 公开, 创建图片框的辅助函数
.参数 父窗口, 整数型
.参数 x, 整数型
.参数 y, 整数型
.参数 宽, 整数型
.参数 高, 整数型
.参数 缩放模式, 整数型
.参数 背景色, 整数型

返回 (创建图片框 (父窗口, x, y, 宽, 高, 缩放模式, 背景色))

.子程序 从文件加载图片, 逻辑型, 公开, 从文件加载图片到图片框
.参数 图片框句柄, 整数型
.参数 文件路径, 文本型
.局部变量 路径字节集, 字节集
.局部变量 路径指针, 整数型

路径字节集 ＝ 编码_Ansi到Utf8 (文件路径)
路径指针 ＝ 取变量数据地址 (路径字节集)

返回 (从文件加载图片_辅助 (图片框句柄, 路径指针, 取字节集长度 (路径字节集)))

.子程序 获取D2D组合框选中文本_辅助, 文本型, 公开, 获取D2D组合框选中文本
.参数 组合框句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 文本长度, 整数型

文本长度 ＝ 获取D2D组合框选中文本 (组合框句柄, 0, 0)
.如果真 (文本长度 ≤ 0)
    返回 ("")
.如果真结束

文本字节集 ＝ 取空白字节集 (文本长度)
获取D2D组合框选中文本 (组合框句柄, 取变量数据地址 (文本字节集), 文本长度)

返回 (Utf8到文本 (文本字节集))

.子程序 获取组合框选中文本_辅助, 文本型, 公开, 获取普通组合框选中文本
.参数 组合框句柄, 整数型

返回 (获取组合框文本_辅助 (组合框句柄))

.子程序 获取编辑框文本_辅助, 文本型, 公开, 获取编辑框文本
.参数 编辑框句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 文本长度, 整数型

文本长度 ＝ 获取编辑框文本 (编辑框句柄, 0, 0)
.如果真 (文本长度 ≤ 0)
    返回 ("")
.如果真结束

文本字节集 ＝ 取空白字节集 (文本长度)
获取编辑框文本 (编辑框句柄, 取变量数据地址 (文本字节集), 文本长度)

返回 (Utf8到文本 (文本字节集))

.子程序 添加组合框项目_辅助, 整数型, 公开, 添加普通组合框项目
.参数 组合框句柄, 整数型
.参数 文本, 文本型

返回 (添加组合框项_辅助 (组合框句柄, 文本))

